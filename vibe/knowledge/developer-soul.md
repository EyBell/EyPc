# EyPc Developer Soul

Tool: codex

## Purpose

EyPc is a high-frequency uTools workbench. Its interaction taste follows the global [Developer Soul Rules](../../../../../czz/CzzProj/CodeNote/AiRef/VibePractice/Vibe_Rules/developer-soul.md#L1), but applies them to local PC operations: dense lists, explicit commands, recoverable focus, and visible risk.

## Product Taste

- Build the usable workbench first. The first screen should be the actual operational surface, not a landing page.
- Keep repeated workflows compact, quiet, and keyboard-first. Prefer scannable rows, stable focus, and direct command surfaces over large cards or explanatory prose.
- Treat a command as the capability owner. Buttons, shortcuts, drawers, and settings rows must dispatch the same action id.
- Put target-specific menus on the target element first. Row/object actions should appear on the row through hover, focus, or context entry; global drawers stay as keyboard/deep-action surfaces instead of always-visible primary menus. While a right-click/action panel stays open, focusing another row with the mouse must retarget that panel immediately and must not keep the previous frozen item.
- High-frequency list previews must be command-owned, recoverable, and non-disruptive. Hover or an explicit `c-*` preview command may reveal readonly previews only through runtime state, must never mutate the target or reflow the list, and `Escape` closes the preview before lower-priority recovery steps.
- Do not nest a second editor for fields already present in the active editor. Keep nested data inline in the current draft unless it needs a separate workbench context; otherwise cache source, save boundary, and focus recovery become ambiguous.
- Search, focus, selection, drawer, and Esc recovery are one contract. If a user enters a transient state, they must be able to recover without leaving the plugin.
- Host-shell transient layers must be owned by runtime state before DOM focus is trusted. Popovers, previews, and editors opened by command should keep their shortcut layer even when the browser active element is stale.
- Global target-jump overlays are command-owned transient layers. They open only from non-editing context, use stable non-trigger markers, activate only the chosen existing target, and recover through layered `Escape`: clear an in-progress marker/search query first, then close the overlay, then yield to lower-priority recovery (drawer → multi-select → search/focus). Plain `Escape` ownership must run on `window` capture and block the event before mutate so uTools host Esc cannot exit over an open layer; only `Shift+Escape` exits outward.
- Show risk where the action happens. Normal termination requires confirmation; force kill can be direct only when the runtime still verifies PID and port ownership.
- Command editing must be consistent inside each module: `Ctrl+S` and `Ctrl+Enter` save the active editing layer, `Escape` cancels it, and the module spec owns whether `F2` is alias or full edit.
- `Tab` / `Shift+Tab` inside an editing layer are isolated field-cycle commands. They must not switch app tabs, port panes, drawers, or list focus while the editor is active.
- Editing fields own native text editing before workbench commands. Plain `Delete` / `Backspace`, arrows, copy/paste, undo/redo, and `Ctrl` / `Cmd` / `Alt` text-navigation chords must not open drawers/popovers, delete records, or reset the caret unless the exact editor contract documents and tests the exception.
- Design feedback must be extracted as project soul while the task is still active. Record selected style, avoided style, affected surface, and source evidence in this file and the active sync/process doc so the next edit inherits the user's design intent.

## Port Page Taste

- `Tab` / `Shift+Tab` belong to the port page pane loop; global feature switching stays on `Ctrl+Shift+Num` so inner `Ctrl+Num` command surfaces stay free.
- Search inputs are not passive fields. In the port page they allow list navigation and action shortcuts while preserving normal text entry for unrelated inputs.
- Search-history suggestions must not steal plain list movement; use `Shift+ArrowUp/Down` for history candidates and keep `ArrowUp/Down` on the active list.
- The right drawer is an action surface, not a decorative panel. It displays icon, title, explanation, and the effective shortcut for the current port, selection, or group.
- Drawer visibility and drawer keyboard focus are different states. Multi-select may show the drawer while list focus remains active; `Ctrl+Right` activates the drawer layer.
- `Escape` must recover inward before it ever exits outward: confirm/editor, active drawer, multi-select, search/filter, then initial result focus.
- Port group editing follows the global editing soul: `F2` edits name/rules/color, `Shift+F2` edits name only, `Ctrl+S` and `Ctrl+Enter` persist the group, and `Escape` cancels the draft.

## File Favorites Taste

- File favorites are a compact, keyboard-first metadata workbench, not a decorative card gallery or a full filesystem manager. Selected style is a quiet two-pane hierarchy/list surface with name-path rhythm, restrained Lucide actions, visible focus/selection/path health, and command-owned responsive side layers; avoided style is repeated toolbars/counts, text abbreviations such as `DIR/FILE/开/定/复`, mouse-only actions, hidden failures, and horizontal overflow at 420px. Evidence label: `user-request`, from the 2026-07-11 file favorites workbench implementation request.
- Favorite search, row focus, multi-select, directory selection, drawer targets, editor/review layers, and `Escape` are one deterministic recovery contract. Quick entry must begin from clean transient state and must never inherit a management-page target.
- `Shift+F2` means inline name editing on a favorite tree/list label; `F2` remains the full correction editor for name/path/type metadata. Dialogs trap focus, restore their trigger, and announce operation results without relying on color or toast-only feedback.

## Workbench List Taste

- Cross-tab list muscle memory is one contract for Ports, Favorites, MQTT workbench lists, and Window Jump. Codex Companion keeps its own selection model and is an explicit exception.
- `↑↓` / `Ctrl+K` / `Ctrl+J` move list focus. While a right action panel/drawer stays open, arrows still move the list and retarget the panel; they must not steal keyboard ownership to the first panel button on every move.
- `Space` toggles multi-select membership of the focused row and advances focus one row down (no wrap past the last row). Clearing selection is layered `Escape`, not a second Space chord.
- Right-click or `Ctrl+ArrowRight` opens the action surface. If the focused row is already in the selection, the surface is multi-target; otherwise it is single-target for that row.
- `Ctrl+Delete` / `Ctrl+Backspace` (`c-del` / `c-bs`) are the cross-system destructive chords: ports force-kill / favorites force-remove / MQTT batch delete / windows close (OS close first, then confirm before process terminate).
- Selection must be structurally visible (leading rail / `aria-selected` / pressed cue), not hue-only. Multi-select cues must not insert a normal-flow top row that shifts list geometry.
- Window Jump: `Space` is selection, never favorite; favorite stays on the action surface / star control. Live enumeration must cover every display and every desktop/Space the host can legally list.
- Evidence label: `user-request`, from “全桌面枚举 + c-del 关窗 + Space 多选下移 + 全插件统一”.

## Cross-Page Interaction Taste

- 2026-07-13 shared-surface feedback: selected style is one quiet product Tooltip for every operation, solid but compact Quick Jump hints, and a consistent left-detail/right-action command architecture across tabs. Avoided style is transparent shortcut letters merged into content, hidden or mouse-only operations, and page-specific shortcut behavior that contradicts global muscle memory. Evidence label: `user-request + user-screenshot`.
- 2026-07-18 Quick Jump placement correction: selected style is a compact framed badge whose center stays exactly aligned with the target center while the fixed pointer-transparent top layer supplies only Z-axis separation; avoided style is title-side, corner, outer-edge, collision or viewport displacement, and also an unframed glyph that can blend into nearby text. Evidence label: `user-request + explicit correction`.
- Side panels belong to the active Tab, not to the whole window. Selected style docks without covering reachable content on wide windows, becomes exclusive inside the Tab on narrow windows, restores focus on close, and always permits vertical access; avoided style is full-window masks for ordinary details/actions, clipped bottoms, fixed viewport-height guesses, or page-level horizontal scrolling.
- Help must follow the actual control. A checkbox inside an actionable row describes the checkbox, not the row context menu; disabled operations explain why they are unavailable; Quick Jump temporarily owns the visual hint layer so normal Tooltip bubbles do not compete with markers.
- 2026-07-30 full-page operation-tip feedback: selected style applies the same quiet, single hover/focus reminder to every named operation button across all main-app Tabs, with about 100ms mouse delay, immediate keyboard focus, one label plus optional shortcut/detail/disabled reason, and no competing page pseudo-bubbles. Dynamic state must refresh the visible hint, and reduced-motion preference must remain respected. The independently rendered Float child is an explicit owner boundary. Avoided style is fixing only one feature page, showing two bubbles for one control, duplicating a title chord in both label and `kbd`, or leaving stale tips after click/blur. Evidence label: `user-request`, from “核验所有的这个按钮悬浮的这种提醒效果优化所有的这个页面。”

## Codex Companion Taste

- 2026-07-18 companion feedback: selected style is a real desktop companion with an independent configuration Tab, a quiet water ball whose center is the 5-hour reading and whose outer ring is Weekly, plus a compact-card alternative. Avoided style is a fake in-main-window bubble, an installer-sized desktop stack, provider sprawl, historical charts and decorative animation. Evidence label: `user-request + A1/B1/C1`.
- 2026-07-18 task feedback, refined through 2026-07-20: selected style separates business buckets `进行中 / 完成未查看 / 已完成` from accurate active/waiting/failed/interrupted/system-error/unknown labels, preserves simultaneous input/approval flags and treats only exact thread `active` as authoritative live work. Three UI Tabs are `进行中 / 已隐藏 / 已完成`; hidden keeps the real bucket, completed-unread leads completed, and all Tabs sort by latest Turn question start. **Its acknowledgement clause — “Opening never advances viewed state; hiding completed-unread does, and there is no separate acknowledgement control” — is historical and superseded by RAW-082/128/138/139 (see [PRODUCT_REQUIREMENTS.md](../specs/PRODUCT_REQUIREMENTS.md#L123) “supersede the preceding … acknowledgement clauses” and [#L174](../specs/PRODUCT_REQUIREMENTS.md#L174) “hide/restore remains unrelated”). Current rule: a confirmed successful open advances viewed state; hiding does not touch unread at all, and the compact completed-unread counter deliberately still counts hidden cards ([#L122](../specs/PRODUCT_REQUIREMENTS.md#L122)). Do not cite the superseded clause as authority — a 2026-08-06 review did exactly that and reached two false conclusions.** The rest of this entry remains current. Avoided style is a single ambiguous clock group, recency-as-question/state inference, prompt/response excerpts, calling persisted failure/interruption completion, or leaving real completion permanently unknown. Evidence label: `user-screenshot + D1/E1/F1 + explicit state corrections`.
- 2026-07-18 visual correction, current portion only: selected style retains the deep-ocean water ball, true horizontal card, adaptive height, gentle low-amplitude wave and stable `水球 / 卡片` switch. Its former paired-color validation preference is superseded by the 2026-07-24 direct-token workbench below. Avoided style is a circularly clipped fake card, duplicated Weekly readings, fixed empty height, forcing paper-white over an explicit user color, or moving the style switch. Evidence label: `user-screenshot + explicit corrections`.
- 2026-07-18 readiness/shortcut feedback: selected behavior is an enabled-feature first-launch self-check with separate system/runtime/process/config/App Server text rows, precise remediation, one stable show/hide action and a visible route to the uTools-owned global-hotkey setting. Diagnostics disclose only safe categories and treat “process not found” as nonblocking when the CLI is usable. Avoided behavior is a generic “server inaccessible” badge, color-only state, hidden or page-local shortcut ownership, silently enabling a disabled feature, direct-spawning a Windows command shim, or exposing paths/PIDs/raw process output. Evidence label: `user-request + focused UI gate`.
- 2026-07-19 diagnostic consistency correction: selected behavior ranks an actual successful App Server round-trip above a missing additive preload capability, while the pre-success legacy state says “等待连接验证” instead of claiming the CLI is available or unavailable. Avoided behavior is a red “unsupported” banner beside visible quota, loaded config or a connected server. Evidence label: `user-screenshot + verified compatibility regression`.
- 2026-07-19 sizing and layered-water correction, refined 2026-07-20: selected behavior keeps compact water/card bounds stable, lets any expanded surface save an adjusted width/height per display without Pin, and resets position separately from size. The shared ball has three colorful horizontal pure-wave layers, refractive highlights, a Weekly-only progress ring and one ongoing badge; single quota never repeats progress, reduced motion stays static. Avoided behavior is a hidden fixed expanded ceiling, native transparent-window resize, moving the snapped edge while resizing, duplicate preview/runtime markup, translucent low-contrast water, multiple overlapping task badges or settings with no consumer. Evidence label: `user-plan + visual implementation review`.
- 2026-07-19 task-density and timing correction: selected behavior renders every row in the bounded source from the same arrays used for counts, discloses partial sources, offers local manual completion without modifying Codex, and shows creation/first/latest/run/completion time without using time to guess status. Avoided behavior is “10 counted / 5 visible”, fixed per-group truncation, timed clearing, a manual-complete action that stops upstream work, or persisting raw IDs/cursors/content. Evidence label: `user-correction + verified regression`.
- 2026-07-20 companion interaction correction: selected behavior uses non-rotating colorful waves, about `220ms` automatic collapse after pointer/focus departure, no Pin/manual-collapse controls (**its “no acknowledgement controls” clause is superseded with the 2026-07-18 entry above — RAW-082/128/138/139 introduced confirmed-open acknowledgement; see [PRODUCT_REQUIREMENTS.md#L123](../specs/PRODUCT_REQUIREMENTS.md#L123)**), the configuration page's first top visibility action, direct three-Tab navigation, opaque Tooltip and strong row hover, recoverable all-row local hide, and true archive for every non-active row with warning plus second fail-closed validation. On macOS the ball stays on every ordinary/full-screen Space of its fixed display. Avoided behavior is hover-sticky expansion, pale transparent overlays, hidden content appended below the visible list, archived-task recovery, pending-only archive, or archiving on incomplete/stale evidence. Evidence label: `user-request + focused review + automated regression`.
- 2026-07-21 companion operation correction selected always-visible fixed short-character buttons, same-slot `确`, and a top/bottom avoiding batch bar whose appearance never moves list rows. Colored dot rails, hover-expanded controls, width animation, quota bubbles and moving list elements remain avoided. Its earlier no-detail/Space-in-place clauses were refined by the later live-input interaction requirement. Evidence label: `user-request + screenshot + browser geometry verification`.
- 2026-07-21 live-input interaction refinement: selected behavior uses six top Tabs ordered `全部 / 待输入 / 动态 / 已完成 / 已隐藏 / 项目`, with the dynamic view prioritizing input, current activity and completed-unread. Compact counters put red input text at upper-left and active/unread at upper-right. Row details are opaque after 500ms and status/short-button explanations after 200ms, but quota hover stays bubble-free. Row click opens; only the selector or Space selects, and Space advances after adding. One mouse/keyboard-arbitrated highlight drives distinct single/batch drawers, right-click, Ctrl+number actions and F Quick Jump; a global activation shortcut enters the card directly. Evidence label: `user-request + interaction correction`.
- 2026-07-21 quota/composer/Shift refinement supersedes that line's ordinary hover, row-click-open and short-button details while retaining six Tabs, activity counters, highlight ownership and drawers. Selected behavior uses ordinary→Spark quota priority with an `S`, quota-bound default model, an always-open native-text composer, single-click selection/double-click or Enter opening, project-only `＋`, synchronized right-click drawers, pure Shift privacy preview, local Escape LIFO and visibility-filtered Quick Jump. Avoided behavior is persistent task action rails, ordinary hover cards, payload/body preview, prompt persistence, main-window Tooltip/Confirm ownership, or shortcuts that bypass Tab/input/layer context. Evidence label: `user-plan + local App Server schema/quota proof + browser verification`.
- 2026-07-21 compact hit-zone refinement: selected behavior reserves the water ball's upper half for directly clickable numeric counters with inert hover, and expands only when the pointer enters the lower half; explicit click/keyboard activation remains available and touch does not emulate hover. Avoided behavior is whole-ball hover expansion, delayed counter hover actions, or overlay hit areas that obscure counters. Evidence label: `user-correction + geometric component regression`.
- 2026-07-22 paired-color correction is historical and superseded by the 2026-07-24 appearance workbench; its linked boards, invalid gamut, partner adjustment, transient real-float preview and rollback must not be recalled as current behavior. The same entry's session-panel rule remains current: unwind `详情 → 更多操作 → 会话行`, preserve the original trigger across Ctrl-arrow switches and cancel confirmation before closing. The float remains display-only with no color editor. Evidence label: `historical user-correction + current supersession audit`.
- 2026-07-24 appearance-workbench correction supersedes the preceding paired-color behavior for current appearance configuration: selected behavior is one full-page workbench divided into water ball, card, and status-signal zones, with each control naming and previewing its visible target. Water base, liquid A/B, Weekly ring/track and counters remain together; card surface/foreground never share that zone. Changes persist and render directly—no format, contrast, coupled-gamut, automatic adjustment or Controller rollback. Avoided behavior is a mixed manual-color grid, hidden quota-mode indirection, or an old paired-card modal that makes a water-color change look as if it was rejected. Evidence label: `user-correction + explicit full-page refactor request`.
- 2026-07-24 water-preview correction refines that workbench: selected behavior makes the configuration preview and desktop float reuse the same water-ball component and real data projection, while preserving the established three SVG wave layers, refraction, high-light and motion timings. Ball background opacity is independent so transparency does not hide liquid, Weekly ring, reading or counters; controls map plainly to background, liquid A/B, amplitude/speed, ring/track and counters. Avoided behavior is a hand-drawn preview that drifts from runtime, simplifying the ball into a static/ordinary liquid circle, or introducing a flat lower rectangular liquid layer. Evidence label: `user-screenshot + explicit correction`.
- 2026-07-24 liquid-palette distinction correction: selected behavior gives each liquid palette a visible, stable material identity while preserving the existing wave paths and timing—pure color is one liquid tone, gradient is A→B, and advanced aurora adds a third/fourth color flow, refractive color pools and a distinct crest. Avoided behavior is a palette selector whose modes all collapse to the same two-color liquid, or a color upgrade that rewrites water motion. Evidence label: `user-correction`.
- 2026-07-24 expanded-card configuration clarification, refined the same day: selected behavior makes the card configuration visibly represent the floating panel after it expands—tabs, search, quota and task surfaces—with a complete independent theme: main/raised panel, border, primary/secondary text, selected, focus, running and completed-unread. Each visible control names the exact region it changes; built-in and saved themes preserve the whole set; preview and real expanded panel use one resolver even when the compact companion remains a water ball. Avoided behavior is treating a compact horizontal card illustration as the expanded-card configuration target, exposing only a surface/foreground pair for a complex panel, routing expanded rendering through the compact skin, or leaving users to infer which floating surface a color will change. Evidence label: `user-correction`.
- 2026-07-26 built-in theme catalog expansion, refined the same day: selected behavior keeps twelve hue variants that all copy the default sea-salt material—deep opaque water base, gradient liquid, solid Weekly ring, soft glow—and never ship segmented clock rings or transparent ball bases. Avoided behavior is aurora/segmented/strong-glow presets that dilute the default look, or lightening the water base enough to lose percentage contrast. Evidence label: `user-request + default-theme mimic`.
- 2026-07-22 constant-action/native-removal correction supersedes the 2026-07-21 quota/composer line's project-only `＋`, absent task rail, row-selection click and pastel Quick Jump styling. Selected behavior uses four fixed `30px` character slots—task `顶 / 隐（显）/ 归（确）/ +`, project `顶 / 移（确）/ 隐（显）/ +`—with direct-open task rows, selection only from the left status control or highlighted-row Space, 500ms opaque privacy-safe task detail and 200ms opaque status/action hints. Project `隐/显` affects project grouping only; project `移/确` performs a fail-closed Codex-native sidebar removal only after Codex exits, with alias/fingerprint/schema checks, atomic main/backup replacement, rollback and verification, and never deletes files or conversations. Quick Jump uses dark/white normal markers and yellow/dark active markers. Avoided behavior is local fake removal, silent fallback while Codex runs, desktop UI automation, repeated confirmation of unspecified style defaults, pastel/alternating F badges or moving/hidden action slots. Development validation and real project removal are user-owned; Agent delivery stays reported and unverified until user acceptance. Evidence label: `user-plan + explicit project rules`.
- 2026-07-22 label/density/selection correction supersedes that line's `30px`/tall-row density and direct-open behavior once selection mode is active. Selected style uses one primary label (alias when present, otherwise original), readable `12/10/9px` information hierarchy, `24px` four-slot actions, `40px` rows, and a two-state click model: ordinary task core opens while the left control starts selection; with any selection active, both regions toggle membership and removing the last item exits. Selection is intentionally vivid but compact—accent gradient, leading rail, focus ring, glow and pressed inset—with matching `aria-pressed` and keyboard feedback. Avoided style is placeholder names despite a valid original, duplicated inline original names, 8px detail text, oversized action rails, 52px rows, invisible multi-select state or opening a task when the user is already modifying a selection. Evidence label: `user-correction`.
- 2026-07-22 explicit-selection correction supersedes relying on gradient/glow alone. Selected behavior adds a persistent mode/count/Esc bar, visibly de-emphasizes unselected rows, uses a `2px` selected border plus `5px` leading rail, and replaces the status glyph with a solid checked control. Avoided behavior is any multi-select state distinguishable only through slight same-hue background, border or shadow changes. Evidence label: `user-acceptance-correction`.
- 2026-07-22 pin-feedback correction: selected behavior makes every pin action immediately legible in both state and placement. Task pins move ahead of unpinned rows within the current task Tab or dynamic-status segment while each partition remains stable; project pins enter `Pinned`; local pins show `本地顶` and a pressed control state. A broken child action bridge reports explicitly. Avoided behavior is persistence-only pinning that leaves the current list visually unchanged, inconsistent pin state on hidden/recovery cards, or a silent no-op. Evidence label: `user-correction`.
- 2026-07-22 selection/pin/counter fusion supersedes the checked-control and row-tail `本地顶` details above. Selected behavior uses a 38px full-height left rectangle that always retains the task-state icon, an accent/running/pending/surface gradient for selected rows, and separate row/button Space/Enter ownership; Ctrl/Cmd+core selects without opening. Pin source belongs to the fixed `顶` control and its 200ms focus/hover help: local is warning-colored, native/Chats are focusable read-only controls whose click, Quick Jump and shortcuts share one gate. Compact input/active/unread counters use the same opaque 200ms layer without expanding or switching tabs. Avoided behavior is a narrow selector, replacing status identity with a check, repeating source as row-tail text, or making counter purpose mouse-only. Evidence label: `user-correction + approved RAW-058 plan`.
- 2026-07-22 float focus-return correction: selected behavior keeps ordinary `Escape` as local LIFO recovery, while `Shift+Escape` immediately hides the transient Codex float and hands focus back to the window/application active before the card was invoked. It does not disable the companion, mutate settings, or close the persistent child; the next global activation reuses and reopens it. Evidence label: `user-request`.
- 2026-07-22 float convergence correction supersedes the earlier visible six-Tab and Weekly-ring details. Selected behavior exposes only `动态 / 已完成 / 已隐藏 / 项目`; `all/inputRequired` remain compatibility data for counters, registration text and compact direct-open targeting, while legacy all/input navigation resolves to dynamic before render. RAW-067 separately refines the former single-input-only click detail. Dynamic is a short, useful working set: non-hidden tasks whose evidence-backed latest Turn activity is within six hours, ordered input → active (including failed/interrupted/system-error with exact local meaning) → host-unknown → completed-unread → completed. A title opens directly, Ctrl/Cmd title click selects, and project/status/minutes metadata merely focuses the row so Ctrl+T inherits its project. Four fixed action slots are dense but stable at `24px / 2px / 102px`; the status text is only “最近 N 天的 M 条”. The water ball keeps liquid, percentage and counters but has no external Weekly SVG ring or ring-controls. Avoided behavior is transient reappearance of hidden all/input tabs, using `updatedAt` or cross-process `notLoaded` as activity/state, discarding recently completed work from the dynamic stream, a metadata click that opens unexpectedly, and cosmetic ring controls with no remaining effect. Evidence label: `user-plan + explicit correction`.
- 2026-07-22 selection-layout correction: selected behavior keeps the mode/count/Esc cue as a pointer-transparent absolute overlay at the bottom of the list stage, with bottom scroll reserve and batch-toolbar avoidance. A transient interaction cue must never add a normal-flow top row or alter the dense list’s top edge, visible height, row coordinates, or learned spatial rhythm. Avoided behavior is any mode label that appears by inserting/removing layout height, even if its text and visual contrast are otherwise correct. Evidence label: `user-correction`.
- 2026-07-22 Weekly-ring and status-projection correction supersedes only the earlier “no Weekly ring/ring controls” choice and the requirement to expose interrupted exactly in task rows. Selected behavior keeps one data-bearing Weekly SVG ring—solid or 20 segments, configurable thickness/color/track/glow—whenever that reading exists. The water component root stays transparent, the surface has no inset, static border, inset outline, decorative shell or same-size outer glow, and keyboard focus is shown on the center reading rather than as another outer circle; without Weekly there is no outer circle. Raw provider `interrupted` remains available for diagnostics and timing but becomes visible product `ongoing` at the domain card projection, so badges, cards, details and hidden views consistently say “进行中” with running icon/color. Failed, system-error and host-unknown retain their distinct meanings. Avoided behavior is deleting the data ring while leaving a cosmetic rim or ancestor focus circle, exposing a shell-opacity control with no product meaning, or leaking raw provider vocabulary directly into user-facing state. Evidence label: `user-correction + approved RAW-065/066 plan + follow-up screenshot`.
- 2026-07-22 compact-counter first-task correction is historical for the dedicated waiting-input/completed-unread actions and is superseded there by RAW-149 below; its complete-set/count consistency remains current. It formerly opened the pinned-first/stable first task and described “打开第一条”. Do not recall that ordering or wording as the current attention-shortcut contract. Evidence label: `user-request + approved RAW-067 plan + RAW-149 supersession`.
- 2026-07-29 Environment Action shortcut slots, refined same day for optional default project: selected behavior places five fixed Action slots under expanded-float quota Time, reads project `.codex/environments/*.toml` in Preload only, and runs EyPc-equivalent commands in the resolved project cwd—never Codex App native top-bar Actions. Target priority is selected/focused task or project, then optional persisted `actionDefaultProjectKey`, then the float Projects-tab focus, then pinned/recent candidates; multi-environment files default to the first and allow keyboard pick; Setup stays display-only; Serve is a managed long-running session; Git Push requires a second confirmation. Paths, commands and PIDs stay host-only. Avoided behavior is fabricating `action/list`/`action/run` RPCs, silently re-running Setup, or exposing raw cwd in the float. Evidence label: `user-plan + official-docs boundary + RAW-1435 + default-project refinement`.
- 2026-07-24 completed-unread command correction refines the prior counter rule. Selected behavior gives the compact completed-unread badge and its uTools global feature one shared command: choose the same pinned-first/stable first counted task, locally acknowledge only its current completion revision in EyPc, then open it and immediately refresh EyPc status surfaces to completed/read. A later completion revision becomes unread again. Waiting-input remains a separate open-only route because input may not have happened yet; generic task opening also stays open-only. Avoided behavior is treating every open as acknowledgement, changing waiting-input early, writing Codex Desktop unread, or resolving the global command from a different/truncated list. Evidence label: `user-correction + RAW-082`.
- 2026-07-22 ongoing archive-stability correction: selected behavior makes product state and action capability converge at the same domain projection. Raw interrupted and desktop-live active may remain distinct provider evidence, but both present as ongoing with a fixed archive slot that stays visible and disabled; drawers, Shift preview, confirmation, batch candidates, Controller evidence and Host revalidation all preserve that block. Avoided behavior is label-only normalization that lets an archive action flash, enable or execute when the provider source alternates between active and interrupted. Evidence label: `user-runtime-correction + RAW-068`.
- 2026-07-22 completion-transition stability correction, refined 2026-07-24 by RAW-077/078/079/080, supersedes the earlier independent active-counter delay. Selected behavior immediately publishes completed-read tasks that return as completed-unread or desktop-live ongoing, while preserving completed-unread rather than relabeling it ongoing. It keeps “进行中” stable across cards, sections, details, Shift preview, counters and archive actions for one persisted, per-task, interruptible ongoing-exit window after authoritative completion or exception first appears; the configuration default is 1500ms and allowed values are `0/500/1000/1500/2000/3000ms`. Returning to running cancels the transition, while continuous completion/exception publishes every terminal surface together once at expiry. Initial terminal inventory does not wait, repeated terminal snapshots do not extend the window, other non-input activity retains its separate 2-second debounce, and the delay never becomes completion evidence. Avoided behavior is separate timers per surface, visible ongoing/terminal oscillation, an archive button that enables before the card settles, or stacked delays that make the counter trail. Evidence label: `user-runtime-correction + interaction-continuity + RAW-069 + RAW-077 + RAW-078 + RAW-079 + RAW-080`.
- 2026-07-24 water percentage-reading correction: selected behavior treats the percent readout as an independently configured water-ball part rather than an implicit fixed label. Position, size, font style and color are persisted with water appearance and rendered through the same shared component in settings preview and the desktop float; defaults are center, 22px, bold and white. Avoided behavior is a preview-only readout, a float-only CSS override, or changing a numeric style through unrelated liquid/ring controls. Evidence label: `user-request + RAW-079`.
- 2026-07-24 compact hit-zone correction (RAW-083) supersedes the earlier upper/lower-half expansion rule. Selected behavior places waiting-input at lower-left, completed-unread at the extreme lower-right and ongoing directly above it; pointer hover/click expands only from the compact surface's upper third, its lower half is the only compact drag-start region, and the middle third is inert. Counter controls retain their independent 200ms opaque help, click, keyboard and touch contracts. Avoided behavior is whole-surface expansion, a lower-half click that opens the card, dragging from the trigger zone, or a hit overlay that obscures a counter. Evidence label: `user-request`.
- 2026-07-24 global task-cycle correction (RAW-084): selected behavior gives “上一个 Codex 任务” and “下一个 Codex 任务” separate uTools global bindings that cycle, with wraparound, through deduplicated waiting-input → completed-unread → ongoing work in the existing pinned-first/stable order. The cursor is transient and each command only opens its target. Avoided behavior is automatic completed-unread acknowledgement, changing a task’s hidden/tab state, persisting cycle history, or assigning an unconfigurable system shortcut. Evidence label: `user-request`.
- 2026-07-28 task-cycle eligibility correction (RAW-109), refined by RAW-134, supersedes RAW-084's completed-unread/full-ongoing ordinary sequence. Selected behavior makes previous/next consume complete waiting-input followed by the same configurable recent-hours non-hidden active group used by cards and the ongoing badge; the task setting defaults to 24 hours and changes the shared Controller projection immediately. Completed-unread retains its explicit open-only action; an old or hidden task can re-enter task cycling only as the empty-pool fallback after the user explicitly pins it in EyPc and only while it is not stopped. Avoided behavior is treating the 30-day conservative ongoing inventory as an action cache, letting old unpinned uncertainty occupy previous/next, or adding a Renderer/task-cycle time filter or debounce. Evidence label: `user-runtime-correction + read-only-task/source-trace + RAW-109 + user-request/RAW-134 / runtime-unverified`.
- 2026-07-31 task-cycle priority correction (RAW-136) refines RAW-109/134 without changing shortcut bindings. Selected behavior makes previous/next choose one exclusive tier at a time: ordinary pending input/approval first, exact completed-Plan implementation confirmations only after ordinary waits clear, and recent active work only after Plans clear; every trigger advances or reverses within the selected tier and wraps. The exact Plan method is reduced to a session-only boolean, with mixed/old/unknown waits conservatively treated as ordinary. Avoided behavior is interleaving Plan confirmations with urgent user input, entering active work while a Plan remains, exposing request methods/bodies, persisting a Plan cursor/classification, or changing unread/hidden/Tab/native state. Evidence label: `user-request + source-trace + RAW-136 / automated-verified-host-pending`.
- 2026-07-30 native unread authority correction (RAW-128), refined by RAW-138, supersedes RAW-082/109's persistent local completed-unread acknowledgement. Codex Desktop live/persisted read-state remains the external authority and legacy receipts never change unread; RAW-138 adds only a session-scoped acknowledgement after EyPc's shared Host path confirms that a task Deep Link opened successfully. Avoided behavior is masking a native blue-dot/unread transition with persisted presentation state, acknowledging a failed/unconfirmed open, skipping a cold-start true observation, repeatedly restarting evidence verification on an unchanged unread poll, or letting delta/full-snapshot callers apply different confirmation rules. Evidence label: `user-runtime-correction + full-chain audit + automated state matrix / host reload pending`.
- 2026-08-03 successful-open/read-replay correction (RAW-138), refined by cold-entry recovery (RAW-139), refines the older counter, generic-open and task-cycle clauses above. Every plugin task-open entry converges on one Host Deep-Link operation; only its confirmed success immediately marks the parent and every known Side Chat read for the current bridge session. `mainHide` alone owns global-entry visibility; a cold command waits for tasks-only inventory, and an invalidated card alias may be rebuilt/retried only for the same anonymous task key. A failed open or the unconfirmed uTools-only dispatch path changes nothing, no completion receipt or Codex native state is written, and a later completion clears the older false so it can become unread again. After a disconnected Codex read, an exact event still wins; otherwise a current refollow snapshot false may repair stale native membership, while current native nonmembership still defeats snapshot true. Avoided behavior is requiring App Server to replay reads, trusting a stale persisted true over current read state, assuming an installed ASAR is already the active child preload, hiding a `mainHide` route twice, consuming a cold shortcut against empty cache, retrying a stale alias against another task, acknowledging before navigation succeeds, or fabricating an entirely unknown transient Side Chat. Evidence label: `user-runtime-report + real uTools ASAR/route evidence + local Codex/extension timeline + Bridge/Controller regression / rebuilt host reload pending`.
- 2026-08-03 mainHide acknowledgement continuity correction (RAW-140) refines RAW-138/139: “session-only” means the current preload process, not a single replaceable Desktop Bridge connection. A confirmed open must survive ordinary plugin hiding, connection close/reset/resubscribe and refollow for the same completion, even while the native set remains stale true; only a new Turn/revision, explicit removal or process end releases it. Avoided behavior is placing a user-confirmed product fact inside a transport object whose routine lifecycle reset silently deletes it. Evidence label: `user-shortcut-reproduction + failing-then-passing bridge lifecycle test / rebuilt host reload pending`.
- 2026-08-08 attention-order and open-progress correction (RAW-149) supersedes pinned-first/Provider-order/always-first behavior only for `待输入` and `已完成未读`; generic previous/next cycling and ordinary project/pin presentation keep their existing contracts. Selected behavior merges ordinary input, Plan and command/file/permissions/MCP approval waits into one attention group with distinct input/approval labels, sorts both attention groups globally by `statusEnteredAt DESC` across providers, and persists at most 200 anonymous opened state instances per group. A new state instance preempts, then traversal resumes older unseen instances; after every current instance is opened the group resets and starts again from newest. Only confirmed Host open advances progress, including manual row opens; failure does not. Request payload, command, path, permission detail and raw request ID never cross the Bridge. Evidence label: `user-plan + official request lifecycle + automated focused regression / real host approval pending`.
- 2026-08-08 waiting-edge and waiting-to-continue correction (RAW-150/151) supersedes RAW-091/131 only for visible stopped vocabulary and task-level archive capability. Selected behavior keeps internal `stopped` but displays “待继续” inside the card/dynamic section without a new Tab, counter or shortcut; explicit stopped may be task-archived after provider-specific write-time revalidation, while project batch stays Codex completed-only. Waiting-input enter and exit use one feature-lifetime hot lane independent of `taskRefreshSeconds`, Tab and Float visibility, with normal event-to-Controller P95 ≤250ms and a one-second phase-only candidate watchdog recovering a dropped callback within 1.25 seconds. Avoided behavior is polling all sessions, exposing a user hot-frequency control, guessing that waiting ended on timeout, directly writing Claude metadata, or presenting old-host v5 observations as v7 acceptance. Evidence label: `user-approved-plan + focused automated regression / rebuilt-host acceptance pending`.
- 2026-08-09 unified companion task-kernel correction (RAW-154, interruption/action ownership refined by RAW-160) established Provider-neutral intent and Provider-owned effects。Current behavior makes Kernel V4 own mutually exclusive state、Plan lifecycle、selectors and capability，Actions v2 own dispatch/confirmation/single-flight，and Controller consume verified packages only。An ordinary `interrupted/user-stopped` now requires branch idle confirmation；an unexecuted Plan stop additionally requires targeted proof of no newer Turn/activity/pending。Claude D′ remains the version-gated single-target `isArchived=true` exception with metadata+active-inventory confirmation and no Deep Link/AX/LevelDB/non-target write。Avoided behavior is Provider-specific Controller branching、global action latches、archive-as-open、optimistic removal、broad interruption stops or reopening an archived Claude session。Evidence label: `user-locked architecture + RAW-160 full automated verification / current host and separately authorized real actions pending`。
- 2026-08-09 waiting-clear causality correction (RAW-153) supersedes RAW-151 only where v7 treated `desktop-live + active` as reusable even when its shadow still carried an older waiting flag. Selected behavior gives each private request/runtime waiting observation a monotonic sequence and lets exact removal/resolved or newer active/Turn-started/continuation evidence establish a clear barrier; stale snapshot, read-state, unrelated patch, refollow and rollout replay cannot revive an older wait, while a genuinely new correlation can. Unmatched resolved preserves concurrent approvals and only schedules bounded task verification. Avoided behavior is waiting for resubscribe before clearing, clearing every approval on an unmatched notification, adding a Renderer timer or `backgroundThrottling` guess, exposing request identity/sequence, or treating the real v7 reproduction as v8 acceptance. Evidence label: `user-runtime-correction + real-v7-provider/product mismatch + focused v8 regressions / v8-host-rework`.
- 2026-07-24 no-readback and configuration-density correction (RAW-087), refined 2026-08-25 by RAW-180: selected behavior never reads or displays current uTools bindings; every shortcut row only opens the official configuration surface. The Codex page defaults to a compact shortcut tab and still separates task/water/card/runtime configuration. Runtime diagnostic title and detail stay visible and wrap; automatic refresh holds the last successful connection copy instead of flashing “正在读取 / 正在核查”. Remaining section help stays behind focusable `i` controls, now using the opaque main-app tooltip layer. Avoided behavior is any private synchronous host IPC, manual-refresh readback, a long all-settings scroll, transparent overlapping pseudo-tips, or announcing routine refresh as a status change. Evidence label: `user-confirmed-root-cause + user-request + preference-gated-distill + 2026-08-25 silent-runtime-correction`.
- 2026-07-26 real-time task-status correction (RAW-089), refined by RAW-091, supersedes the earlier visible failed/system-error/host-unknown distinctions, the separate 2-second Activity Delta debounce and the 60-second interrupted completion marker. Selected behavior keeps exact desktop-live waiting-input/approval/active and treats every unconfirmed or transport-abnormal state as product-visible “进行中” with the same running icon/count and blocked archive action. Active exit is push-driven and followed by one bounded single-task latest-Turn confirmation; the 15-second task setting is labeled as full reconciliation rather than real-time refresh, while the configured completion presentation window starts at the real exit event. Avoided behavior is displaying uncertainty, failure or system error as a separate task state, waiting for the full inventory scan during a normal completion, stacking a fixed debounce after targeted evidence, or converting interrupted by elapsed time. Evidence label: `user-correction + real-local-steady-state + preference-gated-clarify + RAW-089`.
- 2026-07-26 inventory-stability correction (RAW-090) establishes that a transport snapshot is evidence, not an immediate deletion command. Selected behavior holds the last stable in-memory task/project/count projection when one complete snapshot drops prior anonymous keys, performs an immediate full recheck, and accepts removal only when the identical missing set survives at least one complete reconciliation interval. Latest-Turn and update timestamps are monotonic; verified plugin archive/project-removal actions remain immediate. Avoided behavior is replacing the list on every successful response, dropping task counts during a transient omission, persisting task inventories to obtain stability, or adding a generic delay to live active/input/completion feedback. Evidence label: `user-correction + source-contract + RAW-090 / runtime-unverified`.
- 2026-07-27 explicit-stop correction (RAW-091) distinguishes uncertainty from a stopped session. Selected behavior gives exact desktop-live active first priority, projects latest Turn failed/interrupted plus exact live idle or Desktop not-running as a neutral “已停止”, and leaves bridge failure, missing idle/Turn evidence, notLoaded/systemError/inProgress and other transport uncertainty as “进行中”. A stopped task remains in Dynamic/Projects/Hidden but leaves ongoing counters and cyclic navigation and cannot be archived as completed. The first active-exit delta cannot publish an old terminal outcome until targeted or full reconciliation, so active stop/GPT crash/app close does not create a flash. Avoided behavior is counting idle interrupted sessions as active work, treating connector failure as proof that Codex exited, adding a fifth tab, hiding stopped tasks, or allowing a stopped-but-incomplete archive. Evidence label: `user-count-correction + local-anonymous-authority-read + preference-gated-clarify + RAW-091`.
- 2026-07-27 status-timeliness correction (RAW-092) makes feedback intentionally asymmetric. Selected behavior publishes additions, exact waiting-input and targeted post-exit completion through a short event lane: a 50ms coalescing window, dirty/new-task latest-Turn reread, in-flight replay and preload-only pending Desktop shadow. Ordinary full reconciliation remains 15 seconds and complete, while missing tasks, regressive evidence and unconfirmed terminal states retain the longer stability/ongoing rules. Avoided behavior is one global two-second cache, shortening every all-thread scan, delaying a new waiting-input card for negative-jitter protection, letting an in-flight refresh swallow a newer event, or applying the presentation hold after already-targeted completion evidence. Evidence label: `user-latency-correction + source-trace + RAW-092 / runtime-unverified`.
- 2026-07-27 Plan-confirmation correction (RAW-093) treats a completed Plan awaiting implementation confirmation as input, not completion. Selected behavior recognizes the current Desktop's finite unresolved `item/plan/requestImplementation`, lets that request override same-batch idle runtime, and publishes waiting-input immediately for a known task without inventory polling or presentation delay. Removing the request restores normal evidence priority. Avoided behavior is trusting idle/completed over an explicit pending user decision, matching arbitrary plan wording, exposing plan content, or widening unknown requests into input. Evidence label: `user-clarification + current-Desktop-source-verification + RAW-093 / runtime-unverified`.
- 2026-07-27 live-shadow continuity correction (RAW-094) keeps status observation narrow without treating every unknown private field as a broken protocol. Selected behavior consumes well-formed unobserved Desktop patches as revision progress, retains the current shadow, and strictly validates only the finite runtime/request/read-state subset; malformed observed state still fails closed. Avoided behavior is resubscribing for every Turn/tool/body patch, letting an obsolete active snapshot revive after a real exit, or compensating with a longer cache or time-based completion guess. Evidence label: `user-runtime-correction + three-minute local reproduction + current-source zero-resubscribe canary + RAW-094 / uTools-reload-pending`.
- 2026-07-29 task-state ownership correction (RAW-111 refined by RAW-113, ownership and mixed-version behavior superseded by RAW-160) established the historical need for one atomic package. Current behavior places canonical phase、Plan lifecycle、visibility、counts、cycle and capabilities exclusively in `companion-task-kernel-v4`; Controller、Main、Float、Navigation and Actions only consume the latest package/selector cache. Missing V4 Kernel or Runtime Identity mismatch is `reload-required` and never falls back to the former Controller projection, while ordinary hide/close does not erase the Kernel hot cache. Avoided behavior is trusting an unmarked old snapshot, independently rebuilding or clearing state in Controller/Renderer, scattering filters/timers across consumers, or automatically restarting the user's uTools process. Evidence label: `user-screenshot + user-runtime-correction + RAW-160 architecture rework + full automated verification / host-acceptance-pending`.
- 2026-07-29 initial active-snapshot correction (RAW-112) distinguishes a subscription replay from an observed transition. Selected behavior keeps a conflicting active snapshot conservatively visible during the existing bounded latest-Turn reads, then releases only that unchanged snapshot when the final successful read remains terminal; real activity patches, waiting requests and fresh inProgress Turns immediately preserve or restore active, while read/bridge failure stays ongoing. Avoided behavior is minting a new active interval for every follow snapshot, trusting one terminal read immediately, adding a Renderer timer, or converting transport failure into completion. Evidence label: `user-runtime-correction + authorized-read-only-live-debug + anonymous-activity/Turn-correlation + RAW-112 / reload-acceptance-pending`.
- 2026-07-30 cross-source live-ordering correction (RAW-130) refines RAW-124's `app-server-live` authority. Selected behavior orders true Desktop activity patches and exact App Server active/Turn-started evidence on one private process-local sequence, so an older idle event cannot return a resumed task to “已停止” through read-state, refollow, Side Chat aggregation or inventory replay; only a genuinely later Desktop non-active event or explicit terminal evidence revokes it. Avoided behavior is treating the mere presence of an old `activity-event` shape as later evidence, exposing the sequence outside preload, or using wall-clock timestamps across providers. Evidence label: `user-screenshots + source-trace + RAW-130 / implementation-written-host-acceptance-pending`.
- 2026-07-30 closed-state-machine review correction (RAW-131) supersedes scenario-by-scenario claims of a “global audit.” Selected behavior requires one explicit invariant set and a closed source×current-state×event×revision×replay×consumer matrix before task-state acceptance; every positive epoch, terminal path, Side Chat aggregation, inventory dropout, generation barrier, compact count and archive capability must be checked together. Avoided behavior is declaring completeness from a growing test count, writing a regression only for the latest screenshot, or keeping a test whose expected synthetic idle/stopped archive contradicts canonical uncertainty and blocked-stopped rules. Evidence label: `user-process-correction + full-write/clear/replay/consume-source-audit + RAW-131 / seven-P1-implemented-unverified`.
- 2026-07-30 regression-safe optimization (RAW-132) freezes RAW-131 before refactoring. Selected behavior uses one pure parent resolver, treats each Side Chat terminal as branch-scoped evidence, keeps the parent live while any other branch is exact active, and surfaces only anonymous generation-ordered decision totals. Avoided behavior is restoring an older scenario branch during cleanup, letting one child terminal own the aggregate, or adding identity-bearing diagnostics. Evidence label: `user-regression-gate + parent-reducer/branch-guard/anonymous-counters + RAW-132 / implemented-unverified`.
- 2026-07-30 diagnostic-unification correction (RAW-133): selected behavior has one Domain-owned diagnostic schema, one post-generation Controller comparison/notification path, a compact always-visible protection/epoch summary, focus-equivalent native-button details, and a live region limited to meaningful connection status. Avoided behavior is duplicated field lists, refreshes on unchanged/stale counters, long permanent telemetry copy, mouse-only pseudo-buttons or screen-reader announcements for every internal counter increment. Evidence label: `user-optimization-approval + design-preference-gate + better-accessibility + RAW-133 / implemented-unverified`.
- 2026-07-29 expanded-float and registration correction (RAW-114/115) supersedes the visible Float Action slots while retaining Controller/Host global commands. Selected behavior shows quota then source/tasks with no Actions/Environment block, collapses about 220ms after pointer/focus/window departure, and resolves native-active/plugin-zero gaps before the atomic package through exact dirty-task registration plus full privacy gates. Avoided behavior is a Float-owned picker/session/confirmation state, invisible collapse blockers, placeholder cards, count-only active patches, or deleting a never-registered live shadow on the first lagging list. Evidence label: `user-screenshot + user-correction + authorized-read-only-native/plugin-aggregate + source-trace + RAW-114/115 / reload-acceptance-pending`.
- 2026-08-13 悬浮卡片键盘可达性修正（RAW-167）：细化上文所有提到 `Ctrl+number actions` 与 `F Quick Jump` 的条目，并取代「`Ctrl+F` 也开 Quick Jump」的归属。Selected behavior 是——`Ctrl+F` 在每个 Tab 都是搜索，Codex 不做例外；Quick Jump 只保留 `F` / `Shift+F`；`Ctrl+数字` 允许在同一 Tab 内有多重释义，但每种释义都必须有互斥守卫（筛选模式 vs 抽屉打开），不能靠"反正另一种是空操作"蒙混；搜索框是可导航的，打字与 `↑↓` / `Shift` 预览 / `Enter` / `Ctrl+数字` 同时成立；键盘游标只有一个真相，界面高亮不得由一个和游标不同的隐式回退产生；跨进程展开后的焦点落点用有界重试保证，而不是赌一个 `nextTick`。Avoided behavior 是让某个 Tab 单独劫持全插件通用键、把"无游标"和"游标在第 0 项"折叠成同一个下标、让命令派发挂在一个焦点没落进去就收不到事件的节点上、以及用只在抽屉打开时才有意义的动作长期占着 `Ctrl+数字`。Evidence label: `user-correction`，来自「CTRL-F 触发搜索时会被 F 的功能取代…焦点的关注也有问题，上下键的移动也有问题」。
- 2026-08-13 修饰键语义分族与入口可达性（RAW-167 同轮追加）：Selected behavior 是给修饰键一条可学习的统一含义——本域里 **`Alt` = 直接打开**（`Alt+数字` 开第 N 条、`Alt+F` 用标记开任意一条），`Ctrl` 留给搜索与模式内动作；一个 chord 常驻可用，它的视觉线索（编号徽标）就必须常驻，不能只在某个模式里出现；同一个跳转层可以有"转移高亮"和"直接打开"两种专项，但必须是两个显式入口，而不是让同一个键在不同行为之间猜；**注册了动作却没有任何控件派发它，等于这条快捷键在配置页不存在**，而"去设置"传的标签必须是宿主真实能定位到的命令文案，否则按钮点了没反应也不会报错。Avoided behavior 是让修饰键含义按功能点各自为政、把常驻能力藏进某个模式才显形、在用户说明里承诺一个页面上并不存在的入口、以及让一个永远匹配不上的标签静默失败。Evidence label: `user-correction`，来自「配置页面没有直接一键快速配置的」「alt+数字的点击打开效果也需要补全」「给 F 快捷键增加一个快速跳转专项的匹配」。
- The water motion should communicate quota level without becoming the work surface. Reduced-motion keeps the level and percentage readable while removing continuous rotation; missing quota buckets collapse cleanly instead of leaving empty gauges.

## Window Jump Taste

- Inventory and slots: the Window page scans only on explicit load/refresh; explicit refresh clears the page query, while silent cache-miss/lifecycle refresh preserves it. Ten `mainHide` slots use a session cache and one bounded rescan, hide the transit surface only after success, and open the workbench with a sanitized blocking diagnostic on failure. Favorites and slot-bound roots remain visible when live inventory is unavailable. Evidence label: `user-request + runtime failure audit`.
- Main/child tree: one independently operable real root is the stable favorite/pin/alias/slot identity. Ordinary apps render real root → bridge-proven real children; same-app independent roots remain siblings. Finder/Explorer is the only virtual parent and stays virtual parent → real roots with no third level. Browser Tab/editor state stays inside the root; root activation preserves its current internal surface, while exact-child activation is session-only and never falls back. Evidence label: `explicit user correction + first-principles interaction goal + WJ-22`.
- Admission and identity: only user-visible, actionable native windows with positive owner/AX relationship evidence become rows. Windows uses actionable top-level/owned popup HWNDs and `GA_ROOTOWNER`; macOS starts from admitted AX windows and uses positive CG identity as corroboration. CG-only/system/helper/control/no-activate/cloaked/transparent surfaces are omitted. Titles, Tabs, display/Space, app names, ordinals, position, size and candidate count never establish identity or relations; projection absence never proves death. Evidence label: `user-noise-report + exact-focus correction + WJ-19/WJ-22`.
- Interaction: one domain tree owns row identity, search/manual expansion, horizontal navigation and focus return. `↑↓` keeps list ownership even while the right panel is open; `ArrowRight/ArrowLeft` navigate the tree and `Ctrl+ArrowRight/Ctrl+ArrowLeft` navigate the action layer. Child rows expose only exact activate, optional exact close, readonly detail and Windows HWND copy. Root-only selection uses `Space`; force termination always requires confirmation. Evidence label: `user-report + build-primitive ARIA receipt`.
- Presentation and actions: local pin, favorite, slot assignment and OS page-topmost are distinct. Windows may use real topmost; macOS must not pretend permanent third-party topmost is supported. Primary text ellipsizes the display name, secondary text is the app name, and full native details remain in Tooltip/action detail rather than always-visible noise. Development traces are bounded and sanitized; installed builds render no trace module. Evidence label: `user-request + platform API verification`.
- Replacement: a pure session state machine owns root-rebind candidates, complete/partial evidence, focus effects and explicit termination. A missing root is never title- or sole-candidate-rebound; every candidate requires confirmation and native success. Runtime applies effects, the page consumes one projection, and native identity proof remains in the platform bridge. Evidence label: `user-first-principles refactor + WJ-19`.

## MQTT Workbench Taste

- Messages, publish templates, and publish history are equal record lists. Each list must support `↑↓` focus movement, direct detail, action drawer, and readonly preview from the same runtime target model.
- Publish draft history is different from publish history. It belongs beside the send editor as a recovery/reuse popover, records overwritten/manual drafts only, and must not duplicate real outgoing message records.
- Publish topic/payload focus must be mutually exclusive with information-list focus; `Space` in publish editing must never keep toggling message selection. Draft-history popover focus owns its own `Space` multi-select.
- MQTT draft-history editing uses `F2` for title/note alias editing and `Shift+F2` for topic/payload detail editing.
- `Ctrl+ArrowLeft` is the left detail drawer. `Ctrl+ArrowRight` is the right action drawer. Do not merge detail fields and action menus into the same visual layer.
- Item rows should carry identity and a compact payload signal only. Avoid repeating topic in multiple columns, and push secondary operations into row-local preview/detail/more entry points plus the command drawer.
- MQTT connection and subscription rails are first-class row lists. They must show active, selected, focused, and hover states distinctly, and row-local buttons, context menus, `Ctrl+ArrowLeft`, and `Ctrl+ArrowRight` must all enter the same runtime target model.
- Workbench-local MQTT popovers should be highest only inside the workbench, not globally highest. Topic filter, publish options, and draft-history popovers must outrank rows, resizers, and panel contents in stack/split layouts, while drawer masks, previews, modals, and shortcut top layers remain above them.
- Hover, Shift hold, and `Ctrl+I` previews must be non-mutating overlays. They should not reflow rows, steal list focus, or close just because the pointer crosses from the row into the preview.
- MQTT receive/send layout is part of user muscle memory. Stack/split mode and dragged ratios should persist through normalized app state instead of resetting on reload.
- MQTT publish topic/payload editing treats `Ctrl+ArrowLeft` / `Ctrl+ArrowRight` as host text navigation. Publish options open by their button or explicit option-layer commands, and transient publish popovers close through `Escape` or outside click without disturbing editor caret state.
- Current MQTT design direction: selected style is compact command-owned workbench behavior with visible focus/selection, row-local actions, deterministic `Tab`, and layer priority owned by runtime state; avoided style is generic decorative panels, hidden row state, off-screen or clipped drawers/popovers, and delayed documentation of corrected design intent.
- MQTT row time is a dense diagnostic signal. Selected style: show readable `HH:MM:SS`, omit the date only for same-day records, include date for older records, and use distinct date/clock chip treatments; avoided style is compressed undifferentiated digits such as `0626 143436` and sub-second noise in dense rows.
- MQTT detail and preview surfaces are diagnostic contexts, so they should always show full date plus seconds, such as `YYYY-MM-DD HH:MM:SS`, while still avoiding millisecond noise unless a future debugging mode explicitly needs it.
- 2026-06-26 MQTT visual-density feedback: selected style is tighter component rhythm, single-row rail items, smaller icon controls, lighter borders, and restrained focus rings for the workbench in [app.css](../../src/styles/app.css#L1); avoided style is loose card spacing, heavy colored outlines, and large gutters that reduce record density. Evidence label: `user-screenshot`, from the request “各组件的间隔缩小, 样式更精简美化一下”.
- 2026-06-26 MQTT config-editor label feedback: short option labels in the connection editor should keep checkbox and text visually attached and wrap only as a whole option; selected style is a separate compact checkbox row group in [MqttPage.vue](../../src/pages/MqttPage.vue#L1), flex-wrap outer alignment, nowrap label styling, and fixed 16px checkbox inputs in [app.css](../../src/styles/app.css#L1), while avoided style is splitting labels like “重连后重订阅” / “本地归档”, inherited full-width checkbox inputs, or `space-between` layouts that pull checkbox and text apart. Evidence label: `user-screenshot`, from the requests “连接到编辑此处, 无需换行吧” and the follow-up screenshots showing over-spread checkbox labels.
- 2026-06-27 MQTT connection hierarchy feedback: selected style is an EyTodo-like compact tree in [MqttPage.vue](../../src/pages/MqttPage.vue#L1) with nested groups, stable indentation, visible chevrons, row-local group actions, and clear drag/drop target states; avoided style is a flat connection list, large card grouping, hidden hierarchy state, or drag behavior without visible insertion/inside feedback. Evidence label: `user-request`, from “优化mqtt的连接展示, 允许增加分组, 有层级, 可拖拽, 参照EyTodo项目”.
- 2026-06-27 MQTT connection shortcut feedback: connection-tree features must integrate with the project command model, not stop at visual rows. Selected style is default shortcuts plus visible `c-` hints, global `F` Quick Jump targets, right-click action drawers, `Ctrl+ArrowLeft` detail, `Ctrl+ArrowRight` actions, and edit semantics matching existing group modules (`F2`, `Shift+F2`, `Ctrl+F2`); avoided style is unbound mouse-only group controls, hidden shortcut defaults, stealing `Ctrl+T` from subscriptions, or treating cross-tab shortcut reuse as invalid. Evidence label: `user-request`, from “还要提供合理的快捷键： 默认值，c- 提示，f 触发提示，编辑逻辑，右键菜单，c-←→的快捷操作等所有合理的功能，要和整个项目进行融合匹配”, “c-g not valid”, and “还是要c-g作为组合新增的按钮, 你需要区分不同的功能tab是可以复用快捷键的”.
- 2026-06-28 MQTT connection inline-rename feedback: selected style is `Shift+F2` editing the group name directly inside the tree label with the same save/cancel action layer; avoided style is opening the full group editor overlay for a name-only rename. Evidence label: `user-request`, from “s-f2 no need show-up window just edit in label itself”.
- 2026-06-28 MQTT connection group row density feedback: selected style is a one-line group row visually distinct from normal connection rows, with only three visible actions and the rightmost `Ctrl+ArrowRight` / `c-→` more-action entry; avoided style is a two-line group row or exposing every secondary group command as row buttons. Evidence label: `user-request`, from “分组的样式要进行优化, 区分于普通连接, 并且只需要一行, 右侧普通按钮只展示3个(最右侧为更多选项)” and “还要支持 c-→的更多快捷操作”.
- 2026-06-28 MQTT connection group icon-density feedback: selected style is keeping only the left disclosure control in group rows, using indentation, count, and row treatment for group identity; avoided style is an extra folder/logo glyph next to the disclosure or excessive icon gaps. Evidence label: `user-screenshot`, from “这些logo间隙可以减少, 甚至精简” and “只留最左的”.
- 2026-06-28 MQTT connection create-focus feedback: selected style is `Ctrl+G` / `Ctrl+N` deriving parentage from the current connection-focus scope, not stale selection. Row-focused groups create children, row-focused configs create siblings, connection search/blank rail and other non-edit MQTT panes create root targets while the rail is expanded, and ordinary editors keep native ownership. Evidence label: `user-request`, from the numbered focus matrix for `c-g` and `c-n`.
- 2026-07-30 MQTT tooltip/shortcut identifiability: selected style keeps rename vs full-edit icons distinct (`TextCursorInput` vs `Pencil`), message preview vs detail icons distinct (`Eye` vs `Info`), publish-options as `SlidersHorizontal` not the same `…` as the action drawer, drawer `kbd` primary as index chords `c-1…9` (never bare drawer-owned `←`/`→` or misleading「未绑定」 for clickable items), and safe defaults `Ctrl+Shift+Enter` (repeat/template send by target), `Ctrl+Shift+L` (log), `Ctrl+Shift+O` (publish options); MQTT icon buttons must use the shared product Tooltip via `data-operation-tooltip` + always-on `data-operation-shortcut`, not browser-only `title` that the layer strips without a replacement shortcut; avoided style is identical adjacent pencils, Info-as-preview/Logs-as-detail swap, drawer labels that only show unbound action chords, or tips that vanish unless Ctrl-hint mode is active. Evidence label: `user-screenshot + user-plan option 2 + follow-up “还是没有悬浮的提示信息”`.
- 2026-07-30 MQTT right-action float: selected style opens right-click / `Ctrl+→` 快捷操作 as a right-side floating overlay (`mqtt-action-drawer-overlay`) that covers the workbench without `padding-right` reserve or mid-width rail collapse; avoided style is docking the action menu by squeezing connection/subscription/message columns. Evidence label: `user-request`.

- 2026-07-26 Quick Jump Escape host-capture correction: selected behavior owns plain `Escape` on window capture, blocks before mutate, pops query → overlay → prior drawer/multi-select one layer at a time; avoided behavior is bubble-only listeners that let uTools Esc exit the plugin over an open F layer. Evidence label: `user-request + host Esc interaction`.

## Review Gate

- 2026-08-13 companion judgement correction: selected architecture keeps one structured Kernel decision path，uses bidirectional Provider event-time admission，merges phase/unread/Goal as independent evidence lanes，and records Adapter proposals separately from canonical outcomes。Error resolution uses one current contract and one unique Primary memory route；obsolete or repeated routes are logically archived rather than left as competing repair recipes。When two plausible current product rules conflict without an existing user decision，the conflict must be surfaced for user choice instead of silently selecting one。Avoided behavior is transport-time freshness，whole-branch replacement，Renderer patches，flat duplicate error indexes or diagnostics that call a sent proposal accepted。Evidence label: `explicit user requirement + RAW-166 regression review`。
- 2026-08-17 confirmation-hint popover: selected behavior puts archive/remove confirmation in the existing child-owned speech-bubble (arrow, above or below, centered or shifted, clamped to the expanded card) and never inserts an in-card status row that compresses the list. Hover/focus stay 200ms; confirmation is immediate and sticky until confirm, cancel or expire. Avoided behavior is a dedicated confirm row, native `title`, or mounting the main `OperationTooltipLayer`. Evidence label: `user-request`.

Before accepting an EyPc interaction change, check:

- Is the capability named as a runtime action and visible in settings?
- Can the same behavior be reached by keyboard, button, and drawer without duplicate logic?
- Does every edit-like command respect its module's `F2`/`Shift+F2` contract, `Ctrl+S` save, `Escape` cancel, and isolated `Tab` field cycling?
- Does the UI reveal active pane, focused row, selected row, and destructive risk distinctly?
- Does the implementation preserve the safety invariants in [ARCHITECTURE.md](ARCHITECTURE.md#L1)?
- Is there automated coverage for keybinding resolution, runtime state transitions, and Esc recovery?
