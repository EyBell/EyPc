# EyPc Architecture

## Product Scope

EyPc is a keyboard-first uTools plugin for local PC capability calls. Current feature areas are port process management, file/folder favorites, window jump/favorites, MQTT over WebSocket, and a Codex/Claude/Cursor multi-provider quota/task companion.

## Layer Model

```text
uTools feature entry / keyboard input
  -> Interaction Runtime
  -> Keybinding Runtime
  -> Action Runtime
  -> Domain services
  -> Platform bridge / preload
  -> UI projections
  -> uTools storage / local archive
```

## Core Invariants

- Action Runtime is the only user-visible mutation entry.
- Domain functions are pure and covered by tests where behavior is non-trivial.
- Platform functions isolate uTools, shell, process, file-system, clipboard, and local storage APIs.
- Reusable uTools host contracts (packaging, child-window HMR, redirect-only hotkeys, Esc capture, `mainHide` diagnostics) are owned by the CodeNote [uTools module](../../../../../czz/CzzProj/CodeNote/DevelopRef/Multi-System-Use/uTools/README.md#L1); this file keeps EyPc feature implementation only.
- UI renders projections and dispatches intents; it does not call shell, storage, MQTT, or filesystem side effects directly.
- Project interaction taste is recorded in [developer-soul.md](developer-soul.md#L1).
- Process status and task routing live in [../specs/PROJECT_STATUS.md](../specs/PROJECT_STATUS.md#L1), not in architecture memory.
- The unique global current product truth lives in [../specs/PRODUCT_REQUIREMENTS.md](../specs/PRODUCT_REQUIREMENTS.md#L1). This architecture is a fingerprinted implementation input to that truth and never a second product-contract owner.
- Current MQTT implementation mapping lives in [../specs/2606231645-eypc-mqtt-websocket-tab/06-sync-doc.md](../specs/2606231645-eypc-mqtt-websocket-tab/06-sync-doc.md#L1).
- Verification is impact-driven. Root [package.json](../../package.json#L1) exposes a non-running `verify` lane that synchronizes canonical uTools preload mirrors, runs the complete test suite, then performs semantic typecheck, production build, runtime preparation and package validation. [scripts/utools-preload-assets.mjs](../../scripts/utools-preload-assets.mjs#L1) is the shared manifest consumed by sync, prepare and validation so main/float/action preload paths are not re-declared across scripts.

## Ports

- Port scan results are normalized and deduped in [src/domain/ports.ts](../../src/domain/ports.ts#L1).
- Port groups/folders are plugin metadata stored in normalized state, not filesystem or OS process objects.
- Process kill actions must stay behind runtime/platform bridges and revalidate selected PID/port ownership before side effects.
- Force kill is allowed only for explicit selected PID plus verified port match.

## File Favorites

- Favorites are plugin metadata for real file/folder paths and virtual groups.
- Removing a favorite never deletes real files from disk.
- Persistent favorites pass through deterministic graph normalization before tree or ancestor traversal. Duplicate IDs are rebuilt, invalid parent chains return to root, and traversal keeps independent visited defenses in [src/domain/favorites.ts](../../src/domain/favorites.ts#L1).
- Display paths remain user/host values. A separate identity key makes Windows drive and UNC separator/case variants equivalent while preserving POSIX case and legal backslashes.
- Favorites open/reveal/copy stays behind [src/platform/eypcPlatform.ts](../../src/platform/eypcPlatform.ts#L1) and [preload/index.js](../../preload/index.js#L1). Outcomes distinguish confirmed success, host dispatch, reveal-as-open fallback, and failure; capability and inspection data are Runtime-only.
- [src/domain/favoriteLaunch.ts](../../src/domain/favoriteLaunch.ts#L1) owns ten platform-isolated slot defaults, runner normalization/trust fingerprints, placeholder resolution, absolute/PATH executable rules, target-directory resolution and bounded 30-day-half-life search affinity. State migration in [src/domain/state.ts](../../src/domain/state.ts#L1) supplies empty slots/learning to old data while preserving malformed runner metadata only for repair; malformed or fingerprint-mismatched runners cannot execute.
- Runner execution is a structured platform port: Runtime resolves and rechecks trust, then [preload/index.js](../../preload/index.js#L1) validates target/cwd/executable and starts only argument arrays with `shell:false`. Background children detach with ignored output; terminal mode uses static macOS AppleScript, Windows PowerShell payload or known Linux terminal adapters and never falls back to background. PowerShell launch does not alter machine execution policy.
- Runtime owns `containers | items | directory`, explicit/drawer/focus/visible-selection target priority, request generations, batch constraints and removal confirmation in [src/runtime/appRuntime.ts](../../src/runtime/appRuntime.ts#L1). One-step metadata undo restores original node order and removed collapse state; it restores pane/focus/selection only while the user has not navigated away from the post-removal context.
- Runtime also owns one launch path shared by double-click/Enter/quick-number/slot, successful-use recording, slot repair routing and deletion cleanup. Only accepted default open or runner start updates global/query learning; locate/copy/failure do not.
- Quick favorite entry atomically clears management transients, requests search-input focus and highlights its first cross-group result. [src/domain/favorites.ts](../../src/domain/favorites.ts#L1) sorts by text tier, query affinity, global frecency and manual order; [src/runtime/keybinding/keybindingRuntime.ts](../../src/runtime/keybinding/keybindingRuntime.ts#L1) gives an open drawer priority over the page-level `Ctrl+1…0` result actions. Management writes stay in the full favorites page.
- Stable routes `eypc-favorite-slot-1`…`10` are `mainHide` features in [public/plugin.json](../../public/plugin.json#L1). Bindings persist by platform; an unsuccessful slot shows the full favorites slot manager, while a successful launch does not show the uTools main window. Fixed-corner launch is explicitly outside this architecture increment and would use a separate controlled window.
- One-level directory reads do not recurse or navigate. Resolvable file/folder symbolic links retain target metadata, while sockets/devices/FIFOs and unresolved links are omitted instead of being misclassified as actionable files. No bridge method creates, moves, renames, or deletes a real file.
- Pane cycling produces an explicit DOM-focus request and keeps only the active pane's row focus. The narrow container layer becomes visible immediately on open and hides after its close animation so focus handoff never targets a hidden tree. Dialog restoration uses ordered visible fallbacks and a bounded render-frame retry when the original trigger disappears.

## Window Jump Workbench

- [src/domain/windows.ts](../../src/domain/windows.ts#L1) separates raw `NativeWindowObservation` from admitted `LiveWindow`, owns title-blind `WindowFamily { root, children }` generation, relation/capability evidence, explicit `root-current`/`member-exact` requests, persisted discriminated `WindowTarget`, target resolution and ten-slot lookup. App/PID/title/position/size/list order never prove a relationship. [src/domain/windowTree.ts](../../src/domain/windowTree.ts#L1) is the only Window product/workbench-tree framework: ordinary real root/child rows, Finder/Explorer virtual-parent/real-root exception, retained target/slot projection, tree sorting, search flattening, action contexts, root-only selection, navigation and focus recovery. It reuses [src/domain/listSelection.ts](../../src/domain/listSelection.ts#L1) internally.
- [src/domain/windowRebind.ts](../../src/domain/windowRebind.ts#L1) is the framework boundary for session-only manual replacement. One discriminated state owns target, candidates, originating slot and focus return; inventory absence can request an exact probe but can never clear persistence. Slot confirmation activates first and then reuses/creates an exact target while remapping only that slot.
- [src/domain/state.ts](../../src/domain/state.ts#L1) normalizes user-created target metadata (`lastKnownTitle`, bridge-verified `lastInstanceId`, `lastNativeRef`, local pin) plus platform-separated slot IDs. Legacy `titleLocator` migrates only into display metadata and title history is discarded. Live rows, foreground and minimized state remain Runtime session memory only (`windowListLoaded` / `windowCacheUpdatedAt`).
- [src/platform/eypcPlatform.ts](../../src/platform/eypcPlatform.ts#L1) presents capability/list/`probeInstance`/activation/topmost/close/terminate with stale-preload and browser fallbacks. Activation is `root-current` or `member-exact`; liveness is `live | gone | indeterminate`, and only matching `verified-gone` evidence may clear a locator.
- [preload/windows/index.cjs](../../preload/windows/index.cjs#L1) composes the lazy WJ-22 window facade and shared bounded result/trace protocol. [session-cache.cjs](../../preload/windows/session-cache.cjs#L1) owns per-instance liveness and Space bindings plus per-display current Space; [native-command.cjs](../../preload/windows/native-command.cjs#L1) owns bounded privacy-safe native execution; [macos-space.cjs](../../preload/windows/macos-space.cjs#L1) owns direct/reverse SkyLight resolution and target-display-only switching. [macos.cjs](../../preload/windows/macos.cjs#L1) exclusively owns AX/CG list, exact probe/activation/close and permission routing; [win32.cjs](../../preload/windows/win32.cjs#L1) exclusively owns EnumWindows, PID+HWND probe, foreground protection, topmost, close and terminate. Canonical modules mirror byte-for-byte to public/dist and module failure degrades only windows.
- [preload/index.js](../../preload/index.js#L1) now contains no AX/CG/SkyLight/User32 script or window-operation implementation; it only performs guarded CommonJS loading, dependency injection, isolated unavailable fallback and `window.eypcPlatform.windows` mounting. macOS inventory is always a partial current projection; `kCGWindowIsOnscreen` is not liveness. A warm Space binding refreshes only managed-display state, a cold miss resolves only the target, and stale cache eviction retries that target once before failure. Exact AX/HWND final focus remains mandatory. Bridge revision is `wj22-native-instance-space-cache`.
- [src/runtime/window/windowInventoryRuntime.ts](../../src/runtime/window/windowInventoryRuntime.ts#L1) owns complete/partial family merging and freshness, while [windowActivationRuntime.ts](../../src/runtime/window/windowActivationRuntime.ts#L1) owns native request construction and Space failure classification. [src/runtime/appRuntime.ts](../../src/runtime/appRuntime.ts#L1) orchestrates the exact liveness clear gate, diagnostics, persistence and UI effects. No inventory—complete or partial—proves death by itself.
- [src/runtime/appRuntime.ts](../../src/runtime/appRuntime.ts#L1) does not auto-scan on Tab entry. It calls one `buildWindowTreeRows` projection and retains native/persistence/message/focus side effects. Root rows and slots dispatch `root-current`; child rows dispatch `member-exact`; file-manager virtual parent landing remains `focused → persisted last → session recent → visible order`. Only root/group native success may commit target metadata.
- [src/pages/WindowsPage.vue](../../src/pages/WindowsPage.vue#L1), [src/runtime/keybinding/keybindingRuntime.ts](../../src/runtime/keybinding/keybindingRuntime.ts#L1), and [src/runtime/keyboardEvent.ts](../../src/runtime/keyboardEvent.ts#L1) render one Vue-native ARIA tree. Ordinary roots/children are level one/two; Finder/Explorer remains virtual parent/real root at level one/two with no third level. Search temporarily expands matching children without overwriting manual state; hiding, collapsing or losing a child restores root focus. Right-click uses `window / child-window / file-manager-group / selection / slot`; child context is exact activate, optional exact close, readonly details and Windows HWND copy. Children cannot persist, select, favorite, pin, edit, bind slots, topmost, force or join batch actions.
- Static manifest entries `eypc-window-slot-1` … `eypc-window-slot-10` use `mainHide: true` and route through [src/runtime/feature/featureRouting.ts](../../src/runtime/feature/featureRouting.ts#L1) while restoring the previous Tab. Window routes do not schedule a generic hide; only Runtime hides after activation success and explicitly calls the workbench/settings fallback on a global-slot failure. Their labels do not depend on aliases; EyPc can open the official uTools setting for a slot but never reads or copies its host binding.

## Feature Shell And Shortcuts

- Feature visibility/order is normalized in app state and rendered by [src/components/TabShell.vue](../../src/components/TabShell.vue#L1).
- Runtime tab switching uses dynamic visible-feature order; Settings has a fixed shortcut entry.
- Shortcut resolution is layered by context in [src/runtime/keybinding/keybindingRuntime.ts](../../src/runtime/keybinding/keybindingRuntime.ts#L1).
- Input roles are extracted centrally in [src/runtime/keyboardEvent.ts](../../src/runtime/keyboardEvent.ts#L1), then used by keybinding/runtime ownership decisions.
- `Escape` is modeled as layered recovery; `Shift+Escape` hides the uTools window through the platform bridge.

## Shared Help And Command Panels

- [OperationTooltipLayer.vue](../../src/components/OperationTooltipLayer.vue#L1) is the single delegated main-app product-help owner. It resolves the nearest button, actionable row/tree, draggable or form operation control; normalizes explicit metadata, native title fallbacks and legacy `data-tip` detail; separates only shortcut-shaped title suffixes; refreshes an active tip when control metadata changes; suppresses duplicate native titles; attaches `aria-describedby`; captures pointer movement for disabled controls; and clears/suspends on operation, blur or Quick Jump. [app.css](../../src/styles/app.css#L1) disables the Codex main-page pseudo-tip inside `.app-shell`, while the separately rendered Float child remains outside this owner and keeps child-owned hints.
- Runtime panel actions resolve targets as `explicit args → open frozen target → current pane focus → visible selection`. An explicit but invalid ID fails; it never falls through to an unrelated frozen or focused target. Ports, Favorites and MQTT store panel target state in Runtime; Settings keeps command-row detail/action state locally because shortcut drafts are component-owned.
- `Ctrl/Cmd+ArrowLeft/Right`, row-local buttons and context-menu entries converge on the same detail/action action ids. Text editing roles retain native arrow ownership.
- Panel presentation is inside the active Tab: docked at wide widths, reduced-secondary-navigation from `721–1100px`, and exclusive content at `<=720px`. Only confirmation, destructive decisions and atomic editors remain modal.
- Each panel transition captures the trigger, focuses the newly rendered side even when the panel was already open, traps its local Tab cycle where applicable, and restores a stable owner on close.
- [quickJumpLayout.ts](../../src/domain/quickJumpLayout.ts#L1) is a pure target-center projection: each rendered badge keeps its target rectangle's exact center regardless of label width, nearby markers or viewport edges. The overlay owns only Z-axis presentation and never changes source layout or pointer ownership.

## MQTT State And Storage

- MQTT is a default-visible top-level feature declared in feature routing and lazy-loaded from [src/App.vue](../../src/App.vue#L1).
- MQTT domain contracts live in [src/domain/types.ts](../../src/domain/types.ts#L1), normalization helpers live in [src/domain/mqtt.ts](../../src/domain/mqtt.ts#L1), connection tree projection/move helpers live in [src/domain/mqttConnectionTree.ts](../../src/domain/mqttConnectionTree.ts#L1), and lossless versioned multi-record JSON projection lives in [src/domain/mqttExport.ts](../../src/domain/mqttExport.ts#L1).
- Connection configs persist endpoint, client id, username, subscriptions, aliases, colors, publish defaults, reconnect flags, `groupId`, `syncRecords`, layout prefs, and view prefs.
- MQTT connection groups are plugin metadata stored in `MqttState.connectionGroups`; they define connection hierarchy only and must not imply MQTT broker-side resources.
- Collapsed MQTT connection groups persist in `MqttLayoutPrefs.collapsedConnectionGroupIds`.
- Password/token values are excluded from persisted domain models, archives, SQLite mirrors, and publish templates; [preload/index.js](../../preload/index.js#L1) persists them only as an encrypted local userData envelope in `mqtt-secrets-local.json`, using Electron safeStorage when available and AES-256-GCM with the local-only key file `mqtt-secrets-local.key` as fallback.
- MQTT archive durability is SQLite-first through [preload/index.js](../../preload/index.js#L1), with legacy archive fallback surfaced by [src/platform/eypcPlatform.ts](../../src/platform/eypcPlatform.ts#L1).
- `syncRecords=false` is a user-facing sync/privacy preference, not a local durability kill switch.

## MQTT Runtime

- MQTT runtime state and action registration live in [src/runtime/appRuntime.ts](../../src/runtime/appRuntime.ts#L1).
- Runtime owns connection lifecycle, archive loading, record projections, focus targets, drawers, preview state, draft-history state, layout prefs, and view prefs.
- Runtime owns MQTT connection tree projection, group focus/edit/rename/move-parent/delete/collapse/expand, and drag/drop moves. Group focus must not switch the active MQTT connection config.
- MQTT shortcut context carries `mqttTargetKind` so connection-pane edit shortcuts can route config rows and group rows through distinct action ids without duplicate ambiguous bindings.
- `MqttState.viewPrefs` persists the last `全/收/发/藏` filter and valid per-connection topic filters.
- `MqttArchiveState.publishDraftHistory` stores overwritten/manual publish drafts only and never duplicates real outgoing send records.
- `MqttPublishTemplate.operatedAt` is the publish-template operation-order field with legacy fallback to `updatedAt` / `createdAt`.
- Runtime-owned transient layers, such as draft-history popover/editor, must be represented in shortcut context before DOM focus is trusted.
- Runtime owns MQTT merged-export target resolution and accepts only explicit selected IDs from the requested current list. It preserves visible row order and selection, then routes copy/save through named actions; the save action crosses the platform boundary only after the user chooses a host path.

## MQTT UI

- The MQTT page is implemented in [src/pages/MqttPage.vue](../../src/pages/MqttPage.vue#L1), shared publish record rows in [src/components/MqttPublishRecordList.vue](../../src/components/MqttPublishRecordList.vue#L1), and workbench styling in [src/styles/app.css](../../src/styles/app.css#L1).
- The workbench uses compact connection tree, subscription, and message/publish surfaces suitable for uTools default windows.
- The connection rail is a hierarchy surface with group rows, config rows, row-local actions, visible collapse state, Quick Jump row targets, `c-` shortcut hints, right-click/action drawers, and native drag/drop target feedback.
- The top command bar owns connection status, topic filter, record search, `全/收/发/藏`, layout controls, and a count/copy/save export cluster that exists only while the active record list has selected rows.
- Config editing is a right-side drawer with compact endpoint, subscription, publish-topic, and options sections.
- Send-area draft history is a recovery/reuse popover beside the publish editor, not an outgoing-history list.
- Preview surfaces are readonly overlays. Hover/Shift/`Ctrl+I` preview must not mutate records, steal list focus, or reflow rows.

## V7 Feature、Interaction And Presentation Kernel

- [featureModules.ts](../../src/runtime/feature/featureModules.ts#L1) is the FeatureModule registry for Ports、MQTT、Favorites、Windows、Codex and Settings。Each declaration owns lifecycle/background policy、narrow selector、commands、layers、menu kinds、help and diagnostics。[featureRuntimeSlices.ts](../../src/runtime/feature/featureRuntimeSlices.ts#L1) creates immutable feature state views；pages never receive `AppRuntimeSnapshot`。The App shell owns only active Tab、feature configuration、global modal/focus facts and command labels。
- [runtimeSlice.ts](../../src/runtime/runtimeSlice.ts#L1) gives every feature an independently startable/stoppable revision subscription。[App.vue](../../src/App.vue#L1) keeps only the active slice plus policies that require background retention (`entry-enabled` Companion and connected MQTT) subscribed；hidden visible-only/on-demand slices catch up once when restarted。[appRuntime.ts](../../src/runtime/appRuntime.ts#L1) notifies by domain/revision；a hidden feature does not invalidate Shell or another feature，and the full App snapshot cache is rebuilt only for an active consumer.
- [commandCatalog.ts](../../src/runtime/command/commandCatalog.ts#L1)、[keybindingIndex.ts](../../src/runtime/keybinding/keybindingIndex.ts#L1) and [layerStack.ts](../../src/runtime/command/layerStack.ts#L1) are the sole command、shortcut and focus-layer owners。Surface execution owner is filtered before keybinding winner selection，so an Action-local chord cannot shadow a Main command。Nested modal editors retain their explicit ancestor transaction while blocking unrelated Tab/global layers。
- [actionMenu.ts](../../src/runtime/action/actionMenu.ts#L1)、[navigationIntent.ts](../../src/runtime/navigation/navigationIntent.ts#L1) and [quickJumpRegistry.ts](../../src/ui/quickJumpRegistry.ts#L1) give button、context menu、More、drawer、shortcut and QuickJump one exact command/target/intent。Ports、Favorites、Windows and MQTT use domain-specific single/multi target keys；private aliases stay Host-only。Main、Float and Action consume the same Command Catalog but execute only the owner declared for their surface。
- [design-system-v7.css](../../src/styles/design-system-v7.css#L1) defines primitives、semantic colors、density、focus、motion and z-index tokens。Main、Float、Action and Codex settings import it before their surface styles；Codex retains its teal identity while base canvas/surface/text/border/focus/elevation use project tokens。The compact/default/comfortable control targets are 24/28/32px；forced colors、system dark、reduced motion and container-query media fallbacks are shared requirements。

## Companion Providers

### V7 Current Runtime

- [provider-manifest.json](../../preload/companion/provider-manifest.json#L1) is the only Provider ID/order/capability/relation/version manifest shared by CJS and TypeScript；[provider-registry.cjs](../../preload/companion/provider-registry.cjs#L1) loads it and the Host Registry only binds source adapters。Codex、Claude and Cursor are all first-class providers；there is no Auxiliary candidate channel or Cursor-only controller entry.
- [task-topology.cjs](../../preload/companion/task-topology.cjs#L1) and [companionTaskTopology.ts](../../src/domain/companionTaskTopology.ts#L1) define Topology V2 as a membership-only graph。A relation is admitted only when it is exact、same-Provider、same-family、non-self、parent-present、acyclic and non-regressive by topology generation；nested children resolve to one root。It never aggregates phase、unread、Plan、counts or cycle。Invalid relations remain standalone or quarantined，never title/path/time/model/UI inferred。Codex uses App Server lineage，Cursor combines Hook hot lineage with inventory cold reconciliation，and Claude uses the allowlisted `session_id + agent_id` Subagent lifecycle；ChatGPT conversations and Claude Agent Teams currently emit no relation.
- [companion-v7.schema.json](../../contracts/companion-v7.schema.json#L1) is the single revision/evidence/interaction/artifact/envelope contract source；[generate-companion-contracts.mjs](../../scripts/generate-companion-contracts.mjs#L1) produces the TS and CJS mirrors plus runtime validators。[task-kernel.cjs](../../preload/companion/task-kernel.cjs#L1) is `companion-task-kernel-v7`。Provider adapters submit raw membership、activity、interaction、unread、plan-artifact、metadata and topology lanes in `CompanionProviderEvidenceBatchV3`；this sole process owner reduces them into immutable `companion-task-snapshot-v7` and one presentation projection。One semantic transaction advances one Snapshot revision，and a semantic no-op advances none；same-revision disagreement is quarantined instead of last-arrival overwrite。
- Interaction and Plan Artifact are independent Kernel-private state machines。[contracts-v7.cjs](../../preload/companion/contracts-v7.cjs#L1) validates their finite evidence shapes；[task-kernel.cjs](../../preload/companion/task-kernel.cjs#L1) owns opened/resolved/cancelled/execution-started instances、complete request-set revision and monotonic tombstones，while [preload/index.js](../../preload/index.js#L1) owns the anonymous persisted-clear boundary and raw Provider projection。The artifact lane owns unknown/available/executing/consumed/cancelled/removed。Resolve/cancel/start updates the Shadow barrier and tombstone in the same Kernel transaction，so stale replay cannot reopen it。A bare Desktop request-array disappearance does not close a still-current Plan choice/implementation interaction or advance its authoritative request-set watermark；matching terminal evidence、a newer Turn or explicit plain-active runtime does。Artifact-only available projects as stopped/待继续，not waiting-input。
- Public ordering is excluded → causally current running → exact current approval/input/Plan interaction → completed-unread → artifact-only stopped → completed-read → unknown；one root belongs to exactly one group。Every exact current interaction keeps unread private and latent, so resolving it can reveal completed-unread again；completed-unread is never a running↔waiting transition frame。Children never enter the public task list、global count or cycle queue。Unread is true if any member is true，false only when every member is known false，otherwise unknown.
- [task-actions.cjs](../../preload/companion/task-actions.cjs#L1) and [navigation.cjs](../../preload/companion/navigation.cjs#L1) expose one `companion-task-command-v1` gateway。Card click、Enter、compact badge、global shortcut、attention and previous/next submit the same Provider-free command shape；the Kernel resolves the root selector against the same Snapshot and then invokes the Provider Adapter。`operationId` deduplicates，commands serialize per task，revision changes revalidate the original key without substituting a neighbor，and Adapter exceptions degrade only that Provider。[open-handoff.cjs](../../preload/companion/open-handoff.cjs#L1) then validates the monotonic `requested → dispatched → native-confirmed → applied` receipt；an unverified `opened` is downgraded to `dispatched`。Open changes selection only，never phase/unread；archive rejects any root with a live member.
- [preload/index.js](../../preload/index.js#L1) owns all dynamic task evidence for the Host process。Cold startup rebuilds from source；Main hide、Float close and Renderer replacement do not clear the latest Snapshot。Exact supplementary/new-Turn/thinking evidence closes the matching interaction and becomes running synchronously；only a new instance may wait again。Only alias、pin、visibility and collapse preferences persist as user settings；live activity/unread stays process-only，while V7 interaction clear/artifact tombstone persistence contains no raw ID or content。RAW-181 carves the single topology exception：bounded Desktop Side→parent recovery hints（threadId/parentThreadId/observedAt only，cap 200、TTL 48h，[side-relation-hints.cjs](../../preload/codex/side-relation-hints.cjs#L1)）persist so a fresh preload re-follows the child and lets the existing targeted latest-Turn verification decide state；a restored hint asserts nothing on its own，and forget/retire/archive deletes the persisted entry in the same debounced batch。
- [companionTaskPackage.ts](../../src/domain/companionTaskPackage.ts#L1) projects the public Snapshot once；[codexController.ts](../../src/runtime/codexController.ts#L1)、[CodexPage.vue](../../src/pages/CodexPage.vue#L1)、[FloatApp.vue](../../src/FloatApp.vue#L1)、badges、attention and navigation consume that revision without a second reducer、Provider watcher or post-package fold。A public `unknown` is alias-free、neutral and non-actionable；inventory may hydrate metadata but cannot restore phase、unread、bucket or capability。Consumers ACK applied revision；a gap/reconnect replays the latest complete Snapshot，not intermediate deltas。
- [utools-runtime-identity.mjs](../../scripts/utools-runtime-identity.mjs#L1) and the production facade validate `task-state-v12 / companion-provider-registry-v1 / companion-task-topology-v2 / companion-task-kernel-v7 / companion-task-snapshot-v7 / companion-task-command-v1 / companion-task-subscribe-v1 / companion-task-ack-v2` together with Host/Renderer assets。The Host artifact additionally owns non-private `builtAt/builtAtLocal/packageVersion` metadata，which Main/Float Preload project without inference and Codex「运行」renders as the current loaded artifact time。An older or partially exposed Host returns `reload-required`；optional capability checks cannot silently erase a Provider。Build output is only `artifact-ready`，and no source/build check establishes `host-loaded`.
- EyPc alias is the Main/Float display authority；Provider metadata updates `originalTitle` only，and clearing the alias reveals the latest original title。Aliases are never pushed back to Providers，and V1 has no child rename/open operation.
- Pin is a local preference that also **reads** Provider pins (RAW-205). Provider pins enter the metadata lane as `providerPin / providerPinOrder / providerPinAuthority`（`null` = no pin lane）and the Kernel's single `taskPinned` predicate（`localPin || providerPin`）drives the pin group、window exemption、counts and fallback entry。Read sources：Codex `thread/list` `section.id === 01984de2-8f74-7c91-a3b2-5c5e937cf318`（global-state mirror only when the row has no `section`），CodexHost `thread list` `pinned`，Claude `local_*.json` `isStarred`，Cursor `workspaceStorage/*/state.vscdb` `cursor/pinnedComposers`。Write is Codex / CodexHost only：`set-provider-pin` → [task-actions.cjs](../../preload/companion/task-actions.cjs#L1) `setPin` → [pin-bridge.cjs](../../preload/codex/pin-bridge.cjs#L1)（`thread/section/move`，method-not-found fallback `thread/metadata/update`，`thread/read` verify；CodexHost rows use Host CLI `thread pin|unpin`）。Pin policy is declared once in [provider-manifest.json](../../preload/companion/provider-manifest.json#L1) `pin { inbound, outbound, appLabel, pinNoun }`：[provider-registry.cjs](../../preload/companion/provider-registry.cjs#L1) validates it and rejects a `setPin` adapter for an `outbound:false` provider；[companionProvider.ts](../../src/domain/companionProvider.ts#L1) derives `COMPANION_PROVIDER_PIN_POLICY` so no surface branches on provider ids。Producers share `providerPinFields`；Actions share `normalizeActionResult(spec)`；pin-bridge runs one write→verify ladder over two `{ write, verify }` lanes。Cloud Code and Cursor never grant `capabilities.pin`：plugin pin stays `localPin` and may layer on an app-side `providerPin`；native pins still inbound。Inbound is live：a Desktop pin rewrites only the global-state mirror `pinned-thread-ids`，which the unread watcher parses alongside unread ids（`notePinMirror` → `forceTasksOnly` membership rescan）；CodexHost record writes under mapping-store `threads/` invalidate the list TTL via [codexhost-discovery.cjs](../../preload/codex/codexhost-discovery.cjs#L1) `ensureStoreWatcher`。Do not treat Cursor sqlite verify-after-write as a live sidebar pin。Session archive remains the existing Claude `isArchived` / Cursor `composerHeaders` write；phase and unread stay read-only。

### Codex Tab Current Code Overview — 2026-08-24

This is the current source map, not a second requirement owner. Product behavior routes to the unique [PRODUCT_REQUIREMENTS](../specs/PRODUCT_REQUIREMENTS.md#L1), requirement lifecycle routes to [Companion Shared](../specs/requirements/modules/companion-shared.md#L1) and [Engineering Invariants](../specs/requirements/modules/engineering-invariants.md#L1), source addressability routes to the [Source Anchor Catalog](../specs/source-anchors/README.md#L1), and the current architecture delta routes to [RAW-179](../specs/260824/eypc-v7-global-refactor/spec.md#L1)；RAW-177/178 remain native-receipt and sole-truth foundations.

```text
Provider evidence / inventory
  -> preload source adapters
  -> Provider Registry + exact Topology
  -> process-only V7 Kernel sole reducer
  -> one immutable root Snapshot
  -> eypcPlatform identity gate
  -> codexController
  -> CodexPage + Main badges / FloatApp

Tab / card / Enter / badge / hotkey / previous-next intent
  -> codexController
  -> CompanionTaskCommandV1
  -> V7 Kernel target resolution
  -> Provider Adapter effect
  -> requested / dispatched handoff
  -> native-confirmed / applied only with a real target receipt
```

| Boundary | Current owner | What it owns |
| --- | --- | --- |
| Feature entry and Tab | [featureModules.ts](../../src/runtime/feature/featureModules.ts#L1)、[featureRuntimeSlices.ts](../../src/runtime/feature/featureRuntimeSlices.ts#L1)、[featureRouting.ts](../../src/runtime/feature/featureRouting.ts#L1)、[App.vue](../../src/App.vue#L1) | Registry-driven enablement、lifecycle/background policy、narrow RuntimeSlice、static uTools routes、Tab placement、help and diagnostics；no task reduction |
| Public domain contract | [companionProvider.ts](../../src/domain/companionProvider.ts#L1)、[companionTaskTopology.ts](../../src/domain/companionTaskTopology.ts#L1)、[companionTaskPackage.ts](../../src/domain/companionTaskPackage.ts#L1)、[codex.ts](../../src/domain/codex.ts#L1) | Provider identity、node/relation/command types、root Snapshot projection and Codex presentation-compatible task shapes |
| Host evidence adapters | [preload/index.js](../../preload/index.js#L1)、[preload/claude](../../preload/claude/index.cjs#L1)、[preload/cursor](../../preload/cursor/index.cjs#L1) | Private source identity、inventory、phase/unread/topology generations、open/archive effects and privacy reduction |
| State and effects | [provider-registry.cjs](../../preload/companion/provider-registry.cjs#L1)、[task-topology.cjs](../../preload/companion/task-topology.cjs#L1)、[task-kernel.cjs](../../preload/companion/task-kernel.cjs#L1)、[task-actions.cjs](../../preload/companion/task-actions.cjs#L1)、[navigation.cjs](../../preload/companion/navigation.cjs#L1)、[open-handoff.cjs](../../preload/companion/open-handoff.cjs#L1) | Single Registry、exact graph、causal root reducer、Snapshot publication/ACK、Command execution and native handoff receipt validation |
| Platform bridge | [eypcPlatform.ts](../../src/platform/eypcPlatform.ts#L1) | Registry/Topology/Kernel/Snapshot/Command/Subscribe/ACK Runtime Identity gate and the sole V7 `companionKernel` exposure；V4 `companionNavigation` / V2 `companionTasks` facades remain removed |
| Main runtime | [codexController.ts](../../src/runtime/codexController.ts#L1) | Attach/subscribe/monotonic apply/ACK、settings、quota/environment lanes、UI in-flight state and Provider-free command creation |
| Floating child | [preload/float.js](../../preload/float.js#L1)、[FloatApp.vue](../../src/FloatApp.vue#L1)、[float.css](../../src/styles/float.css#L1) | Identity/schema validation、same-revision apply/ACK、root-only rendering、search/quick view/keyboard layers；no second state reducer |
| Environment Action lane | [codexEnvironment.ts](../../src/domain/codexEnvironment.ts#L1)、[codexActionRunner.ts](../../src/domain/codexActionRunner.ts#L1)、[actionRuntime.ts](../../src/runtime/action/actionRuntime.ts#L1)、[ActionApp.vue](../../src/ActionApp.vue#L1) | Separate Host-verified catalog/run/stop child；shares Codex project/task inventory and feature routes but is not part of the task-state reducer or Float UI |
| Help and regression routes | [codex.md](../../src/help/guides/codex.md#L1)、[tests](../../tests/runtime/codexController.test.ts#L1) | User-visible current behavior and focused domain/platform/controller/UI contracts |

The repository now has an EyPc-side `companion-open-handoff-v1` contract but no editable Mirasim source or Codex native receipt channel。Electron/uTools success proves only `dispatched`，with `nativeVisible=false`、`controlOwner=unknown` and `confirmsRead=false`；it does not prove an external Codex Desktop window became visible、that another product transferred control ownership、or that a native applied ACK returned。`native-confirmed/applied` remain real integration gates；source、tests、build and Runtime Identity cannot infer them.

### Retained Provider Details And Historical V4 Foundations

The bullets below retain source-specific evidence、quota、archive and presentation details。Any V4/V5/V6 identity、pre-reduced Provider task、Auxiliary Cursor、Controller direct-dispatch、Renderer/provider task watcher/post-fold/cache or Host-open/read-hint wording is historical and is superseded by the V7 runtime、[RAW-179](../specs/260824/eypc-v7-global-refactor/spec.md#L1) and the retained native-receipt boundary [RAW-177](../specs/260823/codex-tab-boundary-optimization/spec.md#L1).

- The floating companion aggregates more than one agent host. [src/domain/companionProvider.ts](../../src/domain/companionProvider.ts#L1) is the lowest layer and imports nothing: it owns `CompanionProviderId`, task-key namespacing, provider enablement, the shared latest-question comparator, Provider-neutral target/action/result unions and `CompanionProviderAdapter`. `codex.ts` depends on it, never the reverse, so display and cycle order cannot drift apart.
- Task keys are namespaced per provider. Codex keys stay byte-identical to the existing inventory so persisted aliases, pins, hides and receipts migrate at zero cost; every other provider is explicitly prefixed (`claude:<sessionId>`) and therefore cannot collide. `CodexTaskCard.provider` is optional and absent on every legacy card, so readers must go through `companionTaskProvider()` rather than reading the field.
- [codex.ts](../../src/domain/codex.ts#L1)、[companionProvider.ts](../../src/domain/companionProvider.ts#L1) and [companionAggregate.ts](../../src/domain/companionAggregate.ts#L1) share one latest-question comparator. Dynamic display first groups by status, then every group orders `latestQuestionAt DESC → createdAt DESC → anonymous key`; generic cycle selects the first non-empty attention/Plan/active/local-pin tier and uses the same order inside that tier. Provider and pin never override recency. Dedicated attention actions retain their own unseen-instance progress rather than consuming the generic cursor.
- [codexController.ts](../../src/runtime/codexController.ts#L1) owns the two attention queues. Persisted `attentionOpenHistory` stores at most 200 anonymous `task key + statusEnteredAt + openedAt` instances per kind, is pruned when a task leaves the group or enters a new state epoch, and advances only after the Host accepts the exact dispatch—including manual row requests. This is local queue progress, not native opened/read evidence. A new instance preempts, then older unseen entries resume；after all current instances are dispatched, the next successful request wraps from newest。The Renderer never selects `[0]` or stores request content.
- [src/domain/companionAggregate.ts](../../src/domain/companionAggregate.ts#L1) folds a foreign provider's cards into the Codex `ConversationSnapshotV1` additively and in order, then builds a read-only virtual project projection. Exact stable project key wins; normalized name merges only when unique on both sides; ambiguous names stay separate and Claude-only projects are materialized. The Controller strips foreign cards before every replace merge, and project/task counters are recomputed from the final arrays.
- Provider enablement lives in `CodexSettings.providers` and normalizes any absent or malformed value back to Codex-only. Codex-only keeps its data/state semantics; every rendered task now intentionally carries an explicit textual Codex/Claude ownership marker because RAW-022 supersedes the older “marker only when mixed” presentation rule.
- [companionPresentation.ts](../../src/domain/companionPresentation.ts#L1) is the sole task/project ownership-label projection; [FloatApp.vue](../../src/FloatApp.vue#L1) resolves each marker once while constructing its render row and reuses it for visible text, tint and accessibility. [codexEnvironmentPresentation.ts](../../src/domain/codexEnvironmentPresentation.ts#L1) likewise owns the Codex Runtime banner, diagnostic grid, compatibility-pending predicate and launch help; [CodexPage.vue](../../src/pages/CodexPage.vue#L1) only renders the resulting projection. A manual launch-path set/clear already returns the freshly inspected environment, so [codexController.ts](../../src/runtime/codexController.ts#L1) publishes that result directly and must not trigger a second full inspection.
- [src/domain/companionPresentation.ts](../../src/domain/companionPresentation.ts#L1) owns presentation policy: which provider feeds each water-ball channel, when a row marker is meaningful, and the Claude quota section. Renderers consume its result and make no provider decisions of their own.
- [preload/companion/task-actions.cjs](../../preload/companion/task-actions.cjs#L1) is the single `companion-task-actions-v3` implementation behind the V7 Command Gateway. Anonymous key is the public open identity；private alias/revision mapping stays inside Host。If the Host registry already has that key，`resolveOpenTarget` uses the current Host target and never rejects an older UI target；only a missing Host target/private mapping enters provider-scoped exact resolution and a same-key single retry。`open` keeps leading-immediate/final-trailing navigation；`archive` remains uncoalesced；`pause/resume/executePlan` join only the same Provider+task+artifact-revision single-flight。Execute confirmation lasts five seconds and is canceled by phase、artifact revision、activity or pending-selector changes；the first click sends no Provider RPC。An exact available actionable Plan artifact remains pausable/resumable/executable even when no dedicated `Implement Plan` request exists，but it projects `待继续` rather than `待输入`。On confirmation the Host rechecks latest Turn、real activity and other pending，then performs exact open → `thread/resume(excludeTurns=true)` → exactly one `turn/start`；indeterminate timeout uses targeted reread without blind resend.
- [preload/codex/codexhost-discovery.cjs](../../preload/codex/codexhost-discovery.cjs#L48) keeps a persisted per-thread memory (`eypc/codex/codexhost-thread-memory/v1`, injected `storage`, ≤ 300 rows): `statusChangedAt` continuity across rendezvous loss / `codexhostResetDiscovery` / reload, plus the EyPc jump-read bound to that timestamp; `compareHostDesktopUnread` and `sanitizeCodexThreads` consult it, and only a Host status / attention change or a Host unread `false → true` edge supersedes it. Exact Desktop read/unread evidence is one module, [desktop-unread-evidence.cjs](../../preload/codex/desktop-unread-evidence.cjs#L1): the entry honours both polarities for native Codex (reached through the shadow binding), the Host lane honours only `read`. Codex read state is Provider-owned end to end (preload acknowledgements, Desktop read events, thread memory); the Kernel keeps `readAcknowledgements` off for Codex and is the only merger of published root/member unread — the root observation carries the thread-level value only when it has no members.
- [preload/companion/open-readiness.cjs](../../preload/companion/open-readiness.cjs#L1)（`companion-open-readiness-v1`）在 Host Registry 处包装三个 Provider 的 `open`：探测 → 未运行则启动 → 有界轮询 → 软等待 → 原 opener；策略是注入的数据（`label / probe / launch / settle`），模块不按 Provider 分支。Codex 的探测、`open -b` 与 `codexhost launch` 启动、CLI / Host 描述符 / 运行中 Desktop 环境三证据检测和 codexhost 路径偏好在 [preload/codex/desktop-launch.cjs](../../preload/codex/desktop-launch.cjs#L1)。回执可附 `launch { outcome, launcher, waitedMs }`，`companion-open-handoff-v1` 不变。
- Historical V4/V6 foundation only: older Kernel/package identities and pre-reduced membership/state、Renderer post-fold/cache and compatibility facades are historical rather than alternate current owners。Retained evidence/privacy/causality invariants are carried into V7。Current selector readiness is membership-based：a complete V7 Snapshot dispatches previous/next and attention directly even while one task is neutral/verifying；only incomplete membership waits for Provider-scoped preflight，and old/incompatible identities fail closed as `reload-required`（[spec](../specs/260821/2045-shortcut-selector-readiness/spec.md#L1)）。
- [preload/index.js](../../preload/index.js#L1) is the Codex Evidence Adapter, not a second consumer-side reducer. Exact waiting、active、completed and interrupted evidence enters Kernel immediately；a cold/reconnect interrupted/failed conflict triggers one task-scoped single-flight latest-Turn read。An unconfirmed terminal preserves the previous stable phase with `freshness=verifying` instead of forcing running or inventing a terminal。`unknown` is an abstention：existing inventory semantics remain unchanged and a new hydration-only active row starts from unknown rather than a fabricated running baseline；the 250ms window verifies but never grants permission to invent a phase。A process-private Desktop-only Side shadow is structurally retired only after an accepted complete App Server inventory excludes the child and three bounded targeted child reads return exact empty pages；waiting/Plan、confirmed App Server live、newer evidence and incomplete inventory fail closed to retention。The retirement removes the stale relation/shadow and lets the retained parent terminal evidence project again；it never uses age/TTL as terminal authority。Its diagnostic is limited to anonymous topology scope plus finite `inventory=complete / latestTurn=empty` facts。Semantic-only `side-topology-decision` and `parent-state-decision` diagnostics retain only a session-scoped hashed parent ref、finite counts、phase/unread/reason and limited Goal-reason counts；raw identity、title、content、path、Goal content、budget and usage never enter the sink.
- RAW-162 adds a process-private Codex Goal Evidence lane inside [preload/index.js](../../preload/index.js#L1). Cold inventory reads `thread/goal/get` under the existing concurrency bound；steady state consumes `thread/goal/updated` and `thread/goal/cleared`；a terminal candidate with unknown/stale evidence joins one per-thread single-flight reread before completion。The cache stores only finite status、updatedAt、freshness and a live causal sequence。RPC request baselines plus updatedAt reject late reads；transient failure/timeout retains the stable non-terminal phase as verifying，while method-not-found alone establishes process-level compatibility fallback。Goal-only changes force a private-evidence transaction even when the public Activity fingerprint is unchanged，then the matching Host draft and Branch Store still publish at most one task package。The Kernel records whether its retained non-terminal authority came from Goal or Turn，so cleared Goal releases a Goal-owned running phase，and a strictly newer Turn can supersede an old non-active Goal to open a new execution epoch。No Goal objective、raw identity、budget or usage crosses the Bridge、diagnostics or persistence boundary.
- [preload/index.js](../../preload/index.js#L1) 的 `onPluginEnter` 在 Renderer 未挂载时直接让 Kernel 消费静默任务入口；Host 已有精确 key 的热动作立即派发且不重新盘点/分类，只有 Host target/private mapping 缺失的冷/gap 动作才加入 provider-scoped tasks-only 解析，失败拒绝 partial jump。普通隐藏保留动态包，真实退出/功能关闭/Provider 变化才清理；包不落盘。Main Renderer 隐藏时 Preload 仍可直接把同一包推到 Float，quota、环境、完整库存和非任务 unread 沿独立通道运行。[codexController.ts](../../src/runtime/codexController.ts#L1) 的 card/input/cycle/Plan 动作只提交意图，不在派发前主动同步任务包。
- [utools-runtime-identity.mjs](../../scripts/utools-runtime-identity.mjs#L1) 对受管 Host 与 Renderer 输入生成确定性资产 ID，并在每次 production build 写入 ISO/本地打包时间与包版本。Main UI/Main Preload/Float UI/Float Preload 必须完成 Host、Renderer、Registry、Topology、Snapshot、Command、Subscribe 与 ACK revision 的精确握手；任何旧子窗口 ASAR、旧 Preload 或新旧 UI 组合都 fail closed 为 `reload-required`。Main/Float Preload 从自己实际加载的 `runtime-identity.cjs` 投影 `artifactState/builtAt/builtAtLocal/packageVersion`，Codex「运行」页显示「当前宿主产物」及身份状态；磁盘 `dist/runtime-identity.cjs` 只证明打包侧最新产物。Preload 只在身份语义变化时记录匿名 `runtime-identity-handshake`，包含非私密实际/期望 artifact hash、current contract revisions 与 `host-loaded/reload-required`。构建阶段只能形成 `artifact-ready`；uTools 真正加载同一资产并握手后才形成 `host-loaded`，进程启动时间不构成加载证据。这条边界不使用私有 uTools API，也不自动结束后台进程。
- [codexController.ts](../../src/runtime/codexController.ts#L1) accepts the single process package and owns UI in-flight presentation, but a Provider-only Codex `archived` result cannot delete a task. Codex removal requires Kernel `commitArchived` after one write、two persistent server inventory checks and, when Desktop is connected, successful Desktop sync plus matching native `thread/archived` ACK. Until commit, the card、archive button、alias、receipt、cache and shortcut target remain; failure or indeterminate results trigger target reconciliation and user notification. The archive tombstone blocks older inventory from reviving a committed task.

## Claude Code Companion — Current Implementation

- Current product/architecture authority is [the 2026-08-07 reset spec](../specs/260807/claude-code-companion-authority-reset/spec.md#L1); implemented-route evidence and the repeatable strict test route live in its [research record](../specs/260807/claude-code-companion-authority-reset/research.md#L1) and [verification](../specs/260807/claude-code-companion-authority-reset/verify.md#L1).
- [code-sessions.cjs](../../preload/claude/code-sessions.cjs#L1) is the single Code-only reader over `claude-code-sessions/<org>/<user>/local_*.json`. App `localSessionId` is identity；`title` is presentation with `General coding session` as the only blank fallback；`completedTurns`/project/archive/activity are allowlisted history metadata。CLI-only、Cowork、`local-agent-mode-sessions` and transcripts never enter inventory。Duplicate App rows remain distinct and ambiguous CLI relations stay unknown。Each ordinary inventory read additionally builds a Preload-private unique `sessionId → file identity/stat/hash` index；mutation and open preflight use only that index and never rescan or expose a path。
- Phase and unread are orthogonal. [app-state.cjs](../../preload/claude/app-state.cjs#L1) accepts only fixed privacy-safe grammars for the exact supported App versions `1.26832.0 / 1.28929.0 / 1.30096.5 / 1.34493.1 / 1.37937.0 / 1.40609.1` and labels evidence as `live-append` or `cold-replay`；这条日志语法车道是决策产物而非偏好：Claude Desktop 没有受支持的外部状态订阅（对照 Codex IPC 推送、Cursor SQLite 直查），秒级状态只能来自日志，而日志无契约，因此每个版本必须先按隐私边界核验固定行式再进白名单，未列版本 fail closed 到 LevelDB 分钟级镜像（App 约每分钟刷盘一次，实测 2026-08-25）。`1.37937.0` 的精确 `CycleHealth api_error` 额度耗尽行是当前 Turn 的 interrupted 证据，映射为 `stopped/待继续`；焦点行只属于 hot-unread lane，不能续写 phase revision。若未来 Claude Desktop 提供受支持的直接通道（IPC/订阅/结构化状态文件），整条日志语法车道与版本白名单应整体退役，由该通道替代；诊断链见 [error-memory](error-memory/claude-unread-decay-blocked-by-version-gate-and-minute-flush.md#L1)。historical `Sending message...` alone becomes unknown and cannot hold running。 [events.cjs](../../preload/claude/events.cjs#L1) supplies an exact-Hook fallback only after unique correlation；both sources process the first native callback synchronously and use registered-file StatWatchers only for recovery。`completedTurns` restores cold history when no newer active evidence exists。Private IPC injection and Hooks-only state are outside the production boundary.
- [events.cjs](../../preload/claude/events.cjs#L1) now reduces a parent Turn explicitly: only Prompt opens it；successful Stop closes it as completed；StopFailure records a waterline and may tentatively close the Turn, but later same-Turn prompt/tool/permission restore running/waiting。SessionEnd may close only a Turn already observed open by the same reducer。A cold or lifecycle-only SessionEnd records `lastSessionEndAt` but preserves unknown/history phase，so it cannot invent stopped or suppress `completedTurns` recovery。Subagent membership reconciles structurally：a type-less orphan `SubagentStop` after a closed parent Turn closes the earliest pending active subagent one-for-one（`reconciledAt` marker）；direct same-id evidence revives a reconciled child and SessionEnd finalizes the closure，while typed orphans、open-Turn arrivals and duplicate stops keep placeholder-only behaviour and no TTL exists（RAW-181）。[code-sessions.cjs](../../preload/claude/code-sessions.cjs#L1) treats that lifecycle-only Hook as non-authoritative，centralizes App/Hook/history choice，lets App live-append running/waiting defeat Hook stopped unless Hook proves a newer Prompt，and lets an App terminal defeat same-Turn Hook tails；[claudeCode.ts](../../src/domain/claudeCode.ts#L1) compares generation before evidence time and authority.
- Unread has two read-only layers. [unread.cjs](../../preload/claude/unread.cjs#L1) reads exact equality on the App's Chromium-tagged `epitaxy-unread-v1` key from a stable private `0700` LevelDB snapshot；this is the cold-start/recovery baseline and its registered files use a one-second StatWatcher only for dropped-notification recovery. [app-state.cjs](../../preload/claude/app-state.cjs#L1) separately folds exact live completion and `[CCD] LocalSessions.setFocusedSession` into bounded process-private hints：focused completion is read，unfocused completion is unread，focus clears unread，and a new running event clears the prior completion hint。Cold replay establishes focus only and never fabricates unread；same-second events use monotonic hint revision。No native write or persistent receipt exists，and the global focus signal deliberately does not claim multi-pane visible-but-unfocused read intent.
- [index.cjs](../../preload/claude/index.cjs#L1) exposes separate inventory, state, mutation, unread, quota and App-presence ports through subscriber sets, and its shared App watcher wakes both state and unread lanes even when only unread is subscribed。`readCodeUnread()` merges the LevelDB baseline with newer hot hints，caches the last valid baseline and acknowledges a hint only after observing that session's opposite persisted edge followed by a post-event matching snapshot；an equal value left by the previous completion is not catch-up，and delayed true/false cannot roll back the newer live edge。[code-sessions.cjs](../../preload/claude/code-sessions.cjs#L1) installs watchers for directories discovered after the first cold inventory even when subscription started earlier；an exact membership callback is synchronous，partial metadata JSON retains the verified row，and registered files have a one-second StatWatcher recovery without a full inventory scan. [codexController.ts](../../src/runtime/codexController.ts#L1) retains maps for feature lifetime；inventory failure retains membership and quota never blocks phase.
- State and unread reads are joinable Promise singleflights shared by Host/Renderer, watchers, per-task sync and successful-open sync. An asynchronous unread result re-reads the latest task package and lane generation before publishing, preventing an old whole-package capture from deleting a newly added session. Hot unread semantic generation is process-monotonic and does not depend on LevelDB write timing. No manual state/read payload or public preload port exists.
- [archive.cjs](../../preload/claude/archive.cjs#L1) is the only Claude task-archive adapter and exposes `claude-metadata-archive-v2`. It is disabled outside macOS；自 2026-08-21 起归档资格是状态门禁（进行中阻断、待继续/已完成放行），版本白名单不再作为资格硬闸（见 [error-memory](error-memory/provider-version-whitelist-must-not-gate-generic-capability.md#L1)），写入安全由派发时的结构化重验保障：a completed/stopped App-local target must still match the indexed file identity、phase、stat and raw-byte hash。The transaction reads and retains original bytes/mode/identity，changes only parsed `isArchived`，writes a unique `wx` temporary sibling，fsyncs、semantic-compares every other field and atomically renames。Metadata true plus removal from the private active inventory returns `archived` even without an App log；a verified-safe rollback returns `failed`，while a concurrent Claude rewrite or uncertain rollback returns `indeterminate` and is never overwritten with stale bytes。This path bypasses Claude's running session-manager mutation and native `archived` event, so `archived` proves only EyPc removal and never native-sidebar convergence；the user-visible success copy states that boundary. It never uses Deep Link、AX/JXA、LevelDB、directory-wide writes、non-target sessions or archive-log mutation；non-success leaves the Controller card intact and project archive remains Codex-only。A stopped card may invoke this adapter directly after the common two-step confirmation；it never needs a fabricated completed phase。
- [open.cjs](../../preload/claude/open.cjs#L1) caches the positive main-App PID/bundle/start generation and dispatches only an exact Epitaxy target after revalidating a unique nonarchived session. [navigation.cjs](../../preload/companion/navigation.cjs#L1) owns the cross-provider process cursor and leading-immediate/in-flight-final-trailing queue；the Claude module remains only the exact provider-local opener.
- Quota is a dynamic N-window domain gated by `claudeAppQuotaAccess`. On macOS [quota.cjs](../../preload/claude/quota.cjs#L1) reads Claude App's v10 token cache and Claude-specific Safe Storage Keychain item only in memory, arbitrates organization/account/scope fail-closed (`keyIdentity` reads both the legacy `<client>:<org>:…` and the current `acct:<account>|<profile>:<org>:…` key shapes; a several-organization tie is broken only by the organization the App's own `plan-usage-history` meters), and calls the endpoint through Node-16 HTTPS. Current `kind/percent/scope.model.display_name` limits map to session/weekly_all/Fable through the generated vocabulary mirrors of [contracts/claude-quota-vocabulary.json](../../contracts/claude-quota-vocabulary.json#L1) (`preload/claude/quota-vocabulary.cjs`, `src/domain/generated/claudeQuotaVocabulary.ts`: base keys, aliases, scoped-key pattern, window minutes and `5h/周` labels authored once); statusline/history are partial patches only. 401/403 wait for credential fingerprint change, 429 follows Retry-After, reset+1s can wake earlier than cadence, and failures retain stale-marked success. A manual refresh from a float quota chip passes `force`, which skips only the fallback's own interval/backoff clock, never Retry-After or a credential lock; every Claude quota read then records one bounded `quota / claude-quota-read` line through the renderer diagnostics port (trigger reason, per-lane reading ages, usage-api outcome and block reason, counts and primary source; no percentages or reset moments). Successful usage-API calls do not follow a configured cadence below 60s; a manual refresh still bypasses that floor. A manual refresh awaits the Claude read and publishes a bounded `companion.quotaRefreshReceipt` (lane enums, counts, waits) that the float overlays on the same quota row for 8s; an authorized but blocked App lane is a "!" marker whose reason lives in the 200ms hint and accessible name.
- Registration remains the only write into Claude **settings**. It is idempotent, marked, preserves existing Hooks/statusline and uninstalls cleanly；queue payloads keep only identity、normalized event/reason and timing。The separate RAW-154 D′ exception may write only one already-indexed App-local session metadata file under the transaction above；it never changes settings、LevelDB or another session。
- [scripts/utools-preload-assets.mjs](../../scripts/utools-preload-assets.mjs#L1) is the single preload manifest and removes stale unlisted Claude/companion modules from public/dist；[scripts/validate-utools-runtime.mjs](../../scripts/validate-utools-runtime.mjs#L1) requires exact module parity、the versioned facades、the D′ transaction markers and absence of Claude archive Deep Link/AX/exec/log-hard-gate behavior。
- [claudeCode.ts](../../src/domain/claudeCode.ts#L1) owns observation/evidence normalization and phase/unread projection; [claude.ts](../../src/domain/claude.ts#L1) owns quota/readiness only. The former mixed desktop/CLI transcript, persisted unread/receipt, all-watcher full refresh, per-open enumeration and process-lifetime three-attempt paths are removed or superseded, not dormant fallbacks.
- [FloatApp.vue](../../src/FloatApp.vue#L1) keeps a session-only all/Codex/Claude project filter with native Tab semantics, rejects regressing companion revisions, and disables unsupported Claude project/task actions. [float.css](../../src/styles/float.css#L1) uses explicit ownership text plus 8%/12% provider tints and forced-colors fallbacks.
- [preload/float.js](../../preload/float.js#L1) carries the V6 complete root Snapshot on a lane independent of quota/settings and returns `received / applied / rejected` ACK with sent/current revision. Host resends only the latest Snapshot once after 500ms without applied ACK；a further miss on a healthy window records diagnostics but does not recreate it。The Renderer keeps the same task-cache object for an equal revision and does not rerun Vue projection。A Host send event is diagnostic transport progress，not proof that the UI applied state or that the window failed.
- Verification is `implementation-landed / automated-verified / quota-host-verified / interactive-host-partial`: affected tests, typecheck/build/uTools validation, actual uTools unread reads and live App quota are green. Native unread enter/remove, controlled waiting transitions, real mixed-project filter and final rendered/high-contrast observations remain pending.

## Codex Companion

- Codex domain and migration contracts live in [src/domain/codex.ts](../../src/domain/codex.ts#L1); polling, verified/stale cache behavior, local metadata and the atomic task-state package live in [src/runtime/codexController.ts](../../src/runtime/codexController.ts#L1). Appearance persistence has three independent areas—water ball, expanded-card theme, and status signals. The expanded-card object persists nine direct tokens (main/raised panel, border, primary/secondary text, accent, focus, running and completed-unread), travels in the float snapshot, and is selected only after the float expands; twelve sea-salt-material built-in themes and saved themes include the entire object. Non-empty stored color strings are retained as given, and the Controller directly persists each appearance patch without a color format, contrast, paired-gamut or rollback gate.
- The Runtime depends on the identity-gated `companionKernel.getLatest/subscribe/dispatchCommand/ack` seam for every task open/archive/attention/cycle effect, plus Codex-specific inventory/environment/project methods、optional app/hotkey ports and `float.sync/close/onAction/diagnostics` in [src/platform/eypcPlatform.ts](../../src/platform/eypcPlatform.ts#L1). V4 `companionNavigation` / V2 `companionTasks` facades and provider-specific forwarding fallbacks are absent。A Provider open returns a validated `companion-open-handoff-v1` receipt；the current Codex path stops at `dispatched` and cannot publish a read hint without future native confirmation。
- [preload/index.js](../../preload/index.js#L1) owns two interim provider sessions before Easy Agent: the local App Server for quota/models/inventory/create/persistent actions, and the macOS Codex Desktop private-IPC companion for live/read authority plus archive notification. Ordinary inventory reads only allowlisted fields from `.codex-global-state.json` (falling back to `.bak` only when necessary), completes every `archived=false` page, resolves assignment → Chats → deepest valid cwd, reads every candidate latest Turn and compares a source fingerprint before publishing Host Snapshot V2. Exact read events and revisioned v11 stream events are the current live path; bounded v1 read/v6 unrevisioned stream adapters cover only the verified older payload shapes. Because App Server latest/full Turn omits finite decisions, an interrupted/failed/inProgress row may read at most the final 4 MiB of its real rollout under `CODEX_HOME/sessions` and project only an unmatched exact `request_user_input`; a completed row may progressively inspect `256 KiB / 1 MiB / 4 MiB` for exact latest-Turn `item_completed + Plan` artifact evidence. Rollout Evidence V3 preserves that artifact across task-started、AgentMessage、reasoning and supplementary default Turns, but an updating default Turn's structural `fileChange/patch_apply` emits a monotonic `execution-start` clear, so its later completion cannot revive the old Plan as stopped。Both structural fallbacks cross Host V2 only as anonymous finite evidence; plain connector hints never gain this authority. No content/path/identity crosses Host V2. Codex 原生状态的唯一写路径仍是下述项目移除事务；Claude 另有 RAW-154 限定的唯一目标 `isArchived` D′ 事务，不扩大 Codex 写边界。
- Exact App Server completion notifications stay entity-scoped even when their payload omits the Turn object. Preload runs one immediate latest-Turn read plus bounded retries；newer positive activity cancels it. The result confirms only that Turn's branch evidence；the RAW-162 private Goal lane still decides whether the parent task may become completed. Only exhaustion, read failure, an unmapped thread or explicit membership gap requests urgent Provider-only inventory, so live Turn settlement has no periodic/full-scan dependency. [public/preload.js](../../public/preload.js#L1) is a synchronized mirror, not a second scheduler.
- Host V2 fails closed on native-state, pagination, cursor or Turn-time ambiguity. A source change retries the whole scan once; Controller keeps only the previous verified snapshot as stale. Raw thread/Turn IDs, roots/paths, cursor, native state and Desktop full-snapshot content stay transiently in preload memory; Renderer receives anonymous keys, authority enums, short-lived aliases, anonymous project order and a source fingerprint.
- Controller keeps a row-scoped dropout boundary. If a complete inventory omits an anonymous key, present rows still publish, the missing row is retained, and one Provider-only recheck runs. The same sorted set must be absent in at least two complete snapshots across a fixed three-second hold before removal. Reappearance, changed keys, failure, disablement or disposal resets the candidate；verified archive/project removal bypasses it. There is no periodic task-refresh setting or persisted task list.
- RAW-161 adds a process-Host membership recovery lane for external Codex Desktop archive/unarchive changes. [preload/index.js](../../preload/index.js#L1) watches only the exact `sessions` and `archived_sessions` roots；directory rename and one-second StatWatchers are wake mechanisms, while full-cursor `thread/list archived:false/true` comparison is the authority. A currently published key that is absent from unarchived and present in archived inventory is privately cleaned and emitted as urgent anonymous `archivedKeys`, bypassing the generic dropout hold. Other membership differences queue Codex-only preflight. Plugin enter、Desktop IPC reconnect and watcher rebuild force the same tasks-only lane；dirty recovery subtracts archived identities before exact read。The lane retains no periodic Controller inventory scan and is suppressed for an in-flight EyPc-owned archive until its stricter native postcondition transaction resolves.
- Historical Conversation projection V4 exposed only `动态 / 已完成 / 已隐藏 / 项目` and treated `all / inputRequired` as compatibility selectors。V7 always reduces every confirmed main/Side branch and then aggregates one parent through independent activity、interaction、unread、artifact and Goal lanes；accepting one lane cannot erase another。Causally current running outranks every terminal projection；on a terminal task any exact current ordinary input、approval、Plan choice or Plan implementation interaction outranks unread while retaining the unread lane，so running↔waiting does not publish an intermediate completed-unread frame。Artifact-only available projects as stopped/待继续 and enters the actionable Plan cycle without entering the input badge；a newer default Turn with structural file mutation consumes the older artifact through `execution-start`, while a non-mutating supplementary Turn retains it。The generic navigation tier is current input/approval interaction → artifact-only actionable Plan → windowed active → local pin；the dedicated input action never falls through to artifact or pin。`readStateOnly` changes unread only，elapsed time invents no state，and public unknown is neutral/alias-free。EyPc-owned hide、pin、alias and artifact preferences remain Provider-independent。
- The dedicated completed-unread attention entry is a Kernel-owned exclusive selector, not a concatenated queue: any openable real unread completion suppresses the pinned group entirely; only a zero-unread Snapshot enables pinned fallback. Kernel also owns a process-only instance walk for each `input/completedUnread` queue: new lifecycle instances prepend in current latest-first order and surviving instances keep their relative order for the active round. Metadata-only re-sorts affect ordinary input/unread presentation without moving the next jump；the pinned public group and fallback are stricter and always reuse persisted local pin order, so background metadata changes neither presentation nor navigation order。Exhausting a round adopts the latest published order; candidate removal prunes progress, so an unread interruption discards the pin walk and a later zero-unread fallback restarts at the current head. Command shape and Controller/Renderer routing do not branch on this policy.
- [src/domain/codexPresentation.ts](../../src/domain/codexPresentation.ts#L1) creates mutually exclusive input、active、stopped、completed-unread and completed groups, names stopped “待继续”, and orders sections `待输入 → 进行中 → 待继续 → 已完成未读 → 已完成`. State groups use latest-question descending order after Provider aggregation；the pinned group alone uses the Adapter-projected persisted local `displayOrder`。Search stays downstream and cannot alter counts. [src/FloatApp.vue](../../src/FloatApp.vue#L1) only renders the package and dispatches intents；its pinned section collapse is session-local。Quick numbers derive from visible executable RenderRows：expanded pins are numbered tasks, while a collapsed pinned section hides every child and contributes one expand-only header number；expansion recomputes task numbers and `Alt+F` remains task-only。Expanded-card inventory chrome is presentation-owned: [companionPresentation.ts](../../src/domain/companionPresentation.ts#L485) splits the old status row into a left search glyph alert, a left placeholder and a right-aligned `最近 N 天的 M 条`; Controller stale/error semantics are unchanged.
- RAW-083 gives the compact surface a geometric input contract: its upper third alone expands on pointer hover/click, the lower half alone begins a drag, and the middle third is inert. Waiting-input is lower-left; completed-unread owns the extreme lower-right and ongoing sits directly above it. Keyboard activation and the independent counter controls preserve their existing actions, while touch still does not synthesize hover.
- Generic previous/next uses attention → Plan → active → local-pin tiers, each latest-question-first. [navigation.cjs](../../preload/companion/navigation.cjs#L1) retains the non-persisted cursor across Renderer remounts, dispatches the first press immediately, keeps only a final trailing target while in flight, lets manual/attention supersede it, and serializes all Provider opens at concurrency one. [preload/index.js](../../preload/index.js#L1) consumes ready hot entries without forwarding duplicates；cold/reconnect/gap state alone enters tasks-only preflight.
- RAW-087 removes host shortcut readback entirely after the user-confirmed uTools entry freeze (CodeNote [host-hotkey-redirect](../../../../../czz/CzzProj/CodeNote/DevelopRef/Multi-System-Use/uTools/host-hotkey-redirect.md#L1)): [preload/index.js](../../preload/index.js#L1) exposes only the official settings redirect, while [eypcPlatform.ts](../../src/platform/eypcPlatform.ts#L1), [codexController.ts](../../src/runtime/codexController.ts#L1) and [appRuntime.ts](../../src/runtime/appRuntime.ts#L1) contain no readback contract, snapshot or action. [CodexPage.vue](../../src/pages/CodexPage.vue#L1) and [WindowsPage.vue](../../src/pages/WindowsPage.vue#L1) never display current host bindings. The Codex configuration owner uses five sticky top tabs and renders one panel at a time. A silent re-inspect holds the last stable connection copy (RAW-180). Runtime chrome is a compact title row plus fact chips (RAW-196); ready/checking detail stays behind `i`, while warning/error inline remediation and the full itemized set. Remaining section guidance is focusable progressive help through the opaque main-app tooltip layer rather than transparent page-owned bubbles.
- [CodexWaterBall.vue](../../src/components/CodexWaterBall.vue#L1) receives the quota projection from [codexPresentation.ts](../../src/domain/codexPresentation.ts#L1): ordinary 5-hour positive quota, ordinary weekly positive quota, then the highest positive Spark pool. A primary or secondary Weekly reading drives a solid or 20-segment SVG ring for that pool's remaining percentage; without Weekly the component renders no outer ring. The component root is transparent and the water surface has no inset, static border, inset outline, decorative shell or same-size outer glow, so the only explicit ring is data-bearing. The shared water appearance persists the percentage reading's position, size, font style and color (default center, 22px, bold, white), so preview and float render exactly the same readout. [FloatApp.vue](../../src/FloatApp.vue#L1) owns water-mode compact hit routing, first-task counter activation and shared 200ms counter help; when it expands, it switches to the dedicated expanded-card resolver rather than retaining the compact skin. [float.css](../../src/styles/float.css#L1) overrides the generic button focus circle for this water surface and places the keyboard focus cue on the percentage reading instead. [companionPresentation.ts](../../src/domain/companionPresentation.ts#L1) additionally owns the expanded card's whole quota row as one ordered strip: which provider owns a reading, whether a provider caption is meaningful (never in Codex-only compatibility mode), the dense `5h / 周 / S5h / S周` captions and the hint/accessible-name text. It takes the Codex windows as plain input rather than importing `codex.ts`, so it stays provider-shaped. [codexAppearance.ts](../../src/domain/codexAppearance.ts#L57) ships twelve complete built-in themes that all follow the default sea-salt material (gradient liquid, solid Weekly ring, opaque base, soft glow), emits water base/liquid/ring tokens without color correction and resolves the one expanded-card token object for both preview and runtime; quota ring progress is status-derived unless its dedicated custom mode is selected. It also owns `defaultCompanionQuotaTones`, the derived per-provider quota reading colors (`--codex-quota-codex` / `--codex-quota-claude`): both follow the theme's own signal hue, are held apart by a fixed hue rotation rather than by pairing `healthy` with `warning`/`critical` (several presets place those within a few degrees of each other), and are pulled to 4.5:1 against the card surface. `codex.ts` value-imports only this helper; because `codexAppearance.ts` type-imports `codex.ts` and nothing else, the emitted module graph has no cycle. Both tones are persisted expanded-card tokens, so a stored theme predating the row simply normalizes into the derived default. [CodexPage.vue](../../src/pages/CodexPage.vue#L1) places water/status and expanded-card targets on separate tabs while retaining the same shared renderers and theme object. The old card-color preview/commit/cancel Runtime actions and Controller transient state are absent; `CodexCardColorDialog.vue` remains historical, unmounted compatibility code rather than an active route.
- [codexNewThread.ts](../../src/domain/codexNewThread.ts#L1) owns `quota-auto`, catalog normalization and one-off model selection. [preload/index.js](../../preload/index.js#L1) owns the transient execution boundary: text-only Renderer submissions supply only hashed project context/model ID/prompt, while preload resolves cwd/raw thread ID, performs exact no-fallback `thread/start`, validates top-level actual model/cwd, optionally starts the first turn and returns only safe outcomes or short-lived reopen aliases. An attached image never starts an App Server thread; on the explicit fallback action, the constrained float bridge copies only the user's text and dispatches a blank Codex request for manual image paste/model selection, keeping the image and preview URL in memory only；the Renderer stays pending until native visibility is independently confirmed。
- Persisted Codex state contains settings, last verified quota/config, scan time, geometry/appearance, last tab, project collapse, aliases, local pin order, visibility and bounded anonymous attention progress. `quotaRefreshSeconds` defaults to 300 and normalizes to `1–86400`; legacy zero becomes 300 and legacy minutes migrate once. No task-refresh period or dynamic task package is persisted. Stable hashed task/project keys are the only stored identities.
- Disabling the feature stops polling/watchdogs and closes the child through the host path. Controller first advances a runtime generation, detaches old in-flight quota/activity/inventory operations, and clears every Codex-derived task/project inventory, source fingerprint, Activity generation, active-exit baseline, missing-key timer/candidate and task-loop cursor. Re-enabling immediately bootstraps quota, config, complete inventory and latest Turns under a fresh generation; asynchronous results from the old generation are discarded. EyPc-owned settings, aliases, local pins, hidden groups/tasks and collapse state remain intact. The companion has no native close control; its explicit hide action updates persisted visibility first. Explicit Controller close/feature disable, `onPluginOut(true)` and process death close App Server pipes; feature disable/kill also close the Desktop socket. Ordinary `onPluginOut(false)` is only a visibility transition and preserves both App Server aliases/latest-Turn cache and the Desktop observer; ordinary active-surface deactivation may still clear Controller-derived public state through its explicit close path. No path introduces process-kill behavior.
- Feature enablement performs one read-only environment inspection on first launch even while the Codex Tab and float are inactive. The Controller's verified task/project view and Activity subscription stay hot for the entire enabled-feature lifetime and accept off-tab deltas; only quota/config polling remains active-surface-owned. Feature/inbox disable, Provider configuration change, kill and process exit are reset boundaries; ordinary Renderer disposal stops local subscriptions and detaches its navigation lease without closing the process-owned Host cache. The static `eypc-codex-toggle` feature and app-level shortcut route through `codex.float.toggle`; `eypc-codex-activate` routes through `codex.float.activate` to show, expand and focus the child; `eypc-codex-completed-unread` routes through `codex.completed-unread.openFirst`. These and the input/previous/next entries already declare `plugin.json.mainHide`; [featureRouting.ts](../../src/runtime/feature/featureRouting.ts#L1) preserves the current Tab and assigns visibility to `mainHide`, while [App.vue](../../src/App.vue#L1) neither schedules a second hide nor shows the main window for an enabled route. uTools owns system-global binding.
- Electron Codex Deep Link success and accepted uTools fallback dispatch now create only an opaque `companion-open-handoff-v1` receipt in [preload/index.js](../../preload/index.js#L1)：`stage=dispatched`、`nativeVisible=false`、`controlOwner=unknown`、`confirmsRead=false`。No opened-read acknowledgement is created，so `mainHide`、App Server/Bridge rebuild、IPC reset、refollow and inventory replay leave native/persisted unread unchanged。Separately, input/approval/Plan shadows already observed from exact Desktop state remain sticky across owner/transport loss, while ordinary active shadows are dropped；a new snapshot、exact Turn/activity or newer inventory revision supersedes them。Native unread remains read-only.
- The Renderer platform adapter tolerates additive preload version skew: if an older desktop preload exposes `codex.readSnapshot` but not `inspectEnvironment`, it infers only macOS/Windows from browser host metadata and keeps CLI readiness unverified until the App Server round-trip. A successful round-trip updates only connector/config evidence; it must not overwrite preload-owned runtime, related-process or Desktop bridge classifications. Request generations reject results that outlive disable/re-enable or disposal. The no-host browser path remains unsupported.
- The expanded host defaults to content sizing and sends explicit unpinned targets rather than a blind toggle. Pointer/focus departure and window blur share an approximately 220ms automatic-collapse schedule; blur clears float-local confirmation/hints, while a real composer, panel, alias editor, Quick Jump, Shift preview or resize interaction owns temporary deferral. Any expanded surface accepts allowlisted resize start/move/end/cancel coordinates; resize keeps the snapped edge, clamps to work area, persists once on successful end, restores on cancel and never changes compact bounds.
- Codex task-level archive accepts only a short-lived alias plus the Kernel-approved terminal epoch/capability. Preload performs exact preflight, sends one `thread/archive`, verifies unarchived absence plus archived presence twice at least 300ms apart, sends Desktop sync when connected, and waits up to two seconds for the matching native archive ACK. Any resumed active、write/read error、sync failure、ACK timeout or contradictory second inventory returns failed/indeterminate and retains local state; only complete postconditions publish Kernel archived membership and run one task-scoped reconciliation. Claude completed/stopped remains under [archive.cjs](../../preload/claude/archive.cjs#L1)'s version-gated D′ transaction；success confirms EyPc metadata+active-inventory convergence only and reports native-sidebar sync as unconfirmed/unsupported。Project archive remains Codex-only.
- Native project removal is a Host-only high-risk transaction. Renderer sends only a short-lived project action alias and expected `sourceFingerprint`. Preload refuses while the Codex Desktop process is running, refuses backup fallback and unknown schemas, revalidates the alias/target/fingerprint against the primary state, and modifies only `local-projects`, `project-order`, `pinned-project-ids` and a matching `selected-project`. It writes synced same-directory temp files, atomically replaces main and `.bak`, restores their prior bytes on failure, then rereads both before returning `verified`. Thread assignments, conversations, project directories and unknown fields are untouched. Successful removal clears only that project's EyPc hide/collapse/local-pin/alias metadata. Native order otherwise remains read-only.
- The floating child is host-owned and auto-collapses about `220ms` after pointer/focus departure or child-window blur unless focus, resize, composer, panel, alias editing, Quick Jump or Shift preview owns interaction. Blur clears float-local confirmation and hint state before scheduling the same collapse; removed Environment UI cannot retain an invisible blocker. It has no Pin/manual-collapse control, does not mount the main app's `OperationTooltipLayer`, and never uses native `title`. Task detail is an opaque privacy-safe card after 500ms; status and fixed short-action explanations are opaque child-owned speech-bubble hints after 200ms. Archive/remove confirmation uses that same layer immediately and never inserts a `float-source-status` row. RAW 2026-08-05 (float-quota-single-line) supersedes the former bubble-free quota surface: the quota row is one line of short-caption readings and its full window title plus reset time move into that same child-owned 200ms hint, with the complete reading also present as each chip's screen-reader text so the information is never hover-only. Quota chips deliberately take no focus stop. On macOS the parent reapplies always-on-top and all ordinary/full-screen Space visibility after creation, display migration and geometry rebuild; only privacy-safe capability booleans cross to settings diagnostics.
- Float liveness is process-owned: [preload/float.js](../../preload/float.js#L1) sends a two-second heartbeat and interaction health, while [preload/index.js](../../preload/index.js#L1) treats more than six seconds as stalled and recreates only after a sixty-second cooldown plus a ten-second recovery observation. Anonymous interaction IDs expire after ten seconds idle and clear on blur/hide/unmount/exit, so lost pointer events cannot retain a permanent drag/expanded lock.
- [diagnostics.cjs](../../preload/diagnostics.cjs#L1) is the sole installed-runtime log sink and owns `eypc-runtime-diagnostics-v3`. Preload reads `{enabled, level, userConfigured, defaultsRevision:3}` before its start event；unconfigured installation verification defaults to enabled debug and every explicit choice remains authoritative. Every call supplies an explicit error/info/debug level. Runtime Action、Renderer/plugin lifecycle、Evidence normalization、Kernel lane/revision decisions、package/no-op publication、navigation/focus、Float health and all Codex archive stages converge on one operationId/traceId timeline. The sink persists only a session-scoped `h:<hex>` taskRef、state/watermarks、cache、duration and errorCode；it excludes raw task IDs/paths、prompt/Plan/conversation/execute text、commands/argv/tool arguments、stdout/stderr、credentials/tokens、stacks and hidden reasoning. Plain JSONL rotates at 8 MB/file、64 MB total、14 days. [probe-eypc-diagnostics-runtime.mjs](../../scripts/probe-eypc-diagnostics-runtime.mjs#L1) reads v2/v3, filters by session/operation/trace/provider/taskRef/scope/event/level/since/tail and aggregates state changes、no-ops、shortcuts、navigation、archive stages and errors.
- The floating child owns a narrow `return-focus` bridge: `Shift+Escape` hides the existing BrowserWindow without changing `floatEnabled`, allowing the host to restore the prior application/window focus. A later global activation shows and focuses the same child again; ordinary `Escape` remains the child-local LIFO recovery path.
- [FloatApp.vue](../../src/FloatApp.vue#L1) keeps four task-row slots：ordinary `顶 / 隐 / 归 / +`、Plan-ready `顶 / 暂 / 归 / 执`、paused Plan `顶 / 恢 / 归 / 执`；new-session remains in the full drawer。Project rows retain `顶 / 移（确）/ 隐（显）/ +`。Its presentation adapter may apply search and stable pin display only；it never mutates V4 task arrays、state membership、counts or task identity。Project pins remain structural entries in `Pinned`；Chats children append directly below their Chats title。The `顶` control exposes `data-pin-source`、warning-colored local state、stateful `aria-pressed`、focusable native/Chats `aria-disabled` and 200ms source help without extra row-tail labels。`F` / `Shift+F` work from any non-editing Codex content focus。**其中「`Ctrl+F` 也开 Quick Jump、`Ctrl+Shift+F` reserved for search」已被 RAW-167 取代：`Ctrl+F` 现在是聚焦会话搜索，`Ctrl+Shift+F` 降为搜索别名，Quick Jump 只保留 `F` / `Shift+F`。**该卡片的命令派发同时收敛为单一 `window` capture 入口（事件目标不在根内时补派发），`focusedKey` 是唯一键盘游标（`focusedItem` 无隐式回退、`moveFocus` 区分「无游标」与「游标在第 0 项」），激活焦点使用有界渲染帧重试而非单个 `nextTick`。会话搜索框声明 `codex-search` 输入角色并拥有导航白名单；`codex-composer` 保持完全隔离。快速筛选模式（`eypc-companion-quick` / `Ctrl+Alt+K`）在动态列表上给前十条任务编号，`Ctrl+1…9` / `Ctrl+0` 直接打开并退出模式。`Alt` 是该域的「直接打开」族：`Alt+数字` 不依赖筛选模式，`Alt+F` 是只针对会话行的专项 Quick Jump（`quickJump.mode='tasks'`，激活等同点击标题打开），普通 `F` / `Shift+F` 仍只转移高亮；因为 `Alt+数字` 常驻可用，编号徽标也常驻。`Ctrl+数字` 因此有筛选模式与抽屉两种互斥释义，与 `Alt` 族三者守卫两两互斥。配置页必须为每个已注册的 `*.hotkey.configure` 提供派发控件，且 `configureHotkey` 标签必须是 `plugin.json` 真实存在的 `cmds` 文案。详见 [RAW-167](../specs/260813/1455-companion-quick-task-view/spec.md#L1)。Project hide removes only its project-page group；task projections and counts are unchanged。Two or more visible selections create an absolutely positioned batch toolbar inside the list stage；placement follows the focused/last-selected row without reordering tasks，and batch hide converts Plan-ready targets to pause。
- Compact Float counters and the settings water preview share one geometry contract：`20px` height、`20px` single-digit minimum width、`0 5px` padding and fully rounded corners。Two digits and `99+` expand naturally；monospace/tabular numerals are not part of the product contract。Color、border、corner placement and actions remain independent tokens/behavior。
- [preload/index.js](../../preload/index.js#L1) resolves Codex startup through controlled macOS/Windows candidates. An optional manual executable location is validated by that same native/Node-wrapper/shim launch plan and stored in a separate local-plugin record; the Renderer receives only `manual/automatic` mode, validation outcome and safe source labels, never a path. With no manual location it uses automatic discovery plus the existing App Server connector; that fallback is explicit and does not create live-state authority. Windows discovery is supported, while the current private Desktop bridge remains a macOS canary.
- [preload/index.js](../../preload/index.js#L1) owns the privacy-safe `task-state-v12` Provider evidence lanes and App Server transport；it reads `sessionId/forkedFromId` only inside complete inventory/targeted reads，resolves valid nested forks to roots and leaves invalid relations standalone。Roots alone enter public inventory；Side relations、interaction instances、tombstones and branch evidence remain process-private。Branch references are process-local hashes derived from stable identity，never transport lane，and never enter Renderer、logs or persistence。Inventory success supplies an observation but never mints terminal causal sequence。Claude membership mutation advances only membership/metadata；activity、interaction、unread and topology are reread/published by their separate state/read owners, so a high-frequency metadata callback cannot make the next real state/unread generation stale。Adapter diagnostics remain proposals until the committed canonical Snapshot matches，without raw identity/content。The Renderer owns no phase interval、Provider task watcher/sync action or configured task period.
- [events.cjs](../../preload/claude/events.cjs#L1)、[app-state.cjs](../../preload/claude/app-state.cjs#L1) and the process subscription in [preload/index.js](../../preload/index.js#L1) form Claude's hidden-Host state lane。Directory `fs.watch` invokes the first drain/read synchronously；no real transition waits behind the former 50ms timer。A native `fs.watchFile` StatWatcher supplies the one-second missed-notification recovery while Main is `background-hidden`，and the Renderer Controller has no compensating interval。Reduced-state and Kernel fingerprints suppress duplicate tails without revision/push。The measured automation endpoint is Host package → Float `applied` ACK (`≤250ms` normal，`≤1.25s` recovery)，not watcher callback。App-log live evidence is admitted only for the exact version set owned by [app-state.cjs](../../preload/claude/app-state.cjs#L1)；cold running/waiting replay still abstains。Exact completion/focus additionally creates the process-private hot-unread edge；exact usage-limit interruption creates stopped only；LevelDB remains recovery/cold authority rather than the realtime completion trigger。
- [task-kernel.cjs](../../preload/companion/task-kernel.cjs#L1) owns the private Branch Evidence Store、Interaction Store、Plan Artifact lifecycle and parent reducer；Topology V2 supplies membership only。Complete evidence batches merge comparable Turn epochs/event sequences and independent lanes。An older terminal cannot replace newer activity/interaction，an older live row cannot cross a newer interaction tombstone or reopen terminal，and a genuine new Turn advances immediately。All confirmed root/Side branches are selected；public order after excluded membership is causally current running → exact current approval/input/Plan interaction → completed-unread → artifact-only stopped → completed → unknown。Domain and Controller may project or dispatch but cannot override Kernel causality。One synchronous semantic transaction advances Snapshot revision at most once。
- Exact Codex open treats the public task key as identity and `actionAlias` as a short-lived version hint。A missing target、invalid capability or alias mismatch triggers one shared tasks-only preflight even when the package is otherwise fresh；Host renews from the exact private mapping or full trusted inventory and retries that same key once。Manual rows、titles、Enter、compact attention and global feature routing converge on this owner；accepted dispatch advances only attention request progress，while read progress remains native-authority-only。
- RAW-124 gives exact positive App Server activity its own `app-server-live` authority. `thread/status/changed=active` and complete fresh `turn/started=inProgress` override an older Desktop initial/refollow idle snapshot and are preserved in private inventory reconstruction state. RAW-130 orders true Desktop activity patches and exact App Server live events on one process-local monotonic sequence: read-state-only patches, initial/refollow snapshots and inventory reconstruction preserve the App Server waterline；when App Server wins, [desktop-activity-aggregation.cjs](../../preload/codex/desktop-activity-aggregation.cjs#L1) derives waiting only from that authority's connector flags and ignores older Desktop waiting flags/`desktopActiveSince`。Only a strictly later Desktop non-active activity patch、genuinely newer interaction、exact terminal/non-active notification or targeted terminal evidence may replace it。The sequence never enters the public Activity payload or persistence, connector inventory active remains non-authoritative, and no raw identity crosses preload.
- RAW-131 binds every bounded latest-Turn request to both its anonymous parent and raw query task plus the parent's current positive-evidence sequence. A result is discarded when a newer exact positive event has advanced that sequence, so an older terminal reread cannot close a resumed task. Side Chat verification targets each child and then re-aggregates the parent; inventory reconstruction schedules/replays every known child rather than treating the parent latest Turn as the child result. Initial active replay that conflicts with failed/interrupted evidence may suppress only that uncorroborated snapshot to unavailable/not-loaded authority, never to exact idle. Every exact activity patch that remains active opens a fresh epoch even across active→active transitions; waiting-request patches additionally outrank older completion. Missing inventory rows retain their existing anonymous activity mapping inside preload for at most 120 seconds under the same source fingerprint, matching the longer Controller quarantine; the mapping never creates a Host inventory row and explicit archive removes it immediately.
- RAW-132 historically centralized a transient parent projection in `codexResolveParentActivity`, but that guard was not a real per-branch ledger and allowed old parent idle to coexist with newer active evidence。RAW-160 rework keeps the resolver as a raw-evidence adapter only and moves durable session-level branch arbitration into the Kernel-private Branch Evidence Store described above。
- RAW-133 keeps that resolver as the only parent-priority implementation and makes tests invoke it directly through a test-only VM hook. The diagnostic boundary is similarly single-owned: [codex.ts](../../src/domain/codex.ts#L1) defines the five keys, normalization and equality; [codexController.ts](../../src/runtime/codexController.ts#L1) performs one post-generation package comparison and emits one notification only when accepted diagnostics change. Renderer code never re-normalizes the package.
- RAW-125 makes live/terminal arbitration provenance-based rather than shape-based. Plain inventory completed cannot close live activity; exact/targeted/corroborated completion can. Same-revision exact started or targeted inProgress reopens the activity epoch, and exact completed can publish with startedAt alone. The remaining bounded snapshot/exit/inventory guards are kept at their existing owners and do not stack a presentation delay.
- RAW-126 removes the split ownership inside Controller: delta application and verified inventory call the same pure active-exit transition. RAW-127 removes the remaining local terminal blockers: exact/corroborated completion may omit `completedAt`, incompatible latest-Turn modes replace instead of suppressing each other, ambiguous stale-active unread may request verification, confirmed provenance persists in the session inventory, and missing Turn outcome remains ongoing. RAW-128 removes the remaining cross-layer batch blockers: native read-state replaces persistent EyPc receipts as external authority, every delta advances a snapshot-barrier generation, full inventory preserves stronger Turn evidence, unknown delta keys do not suppress known rows, missing-key quarantine is row-scoped, identical active snapshots reuse one bounded cycle, initially true unread still performs one verification, and the reducer—not an individual caller—recognizes confirmed terminal provenance. RAW-138 below adds only the success-gated session/open and directional reconnect refinement. None of these paths lets unread itself cross Activity/Turn fields.
- RAW-138 historically treated successful Host-open as session evidence；RAW-177#3 supersedes that edge。Current refollow/native read snapshots may still repair a missed disconnected read event，but Deep Link or shell dispatch never creates false/read evidence。Snapshot true cannot invent unread against native nonmembership，and unread never alters Activity/Turn ordering。
- Controller applies RAW-131's Activity generation barrier to the whole same-source event in both directions: a lower delta cannot update task fields or Desktop bridge state; after a positive live waterline exists, a lower or generationless V2 full snapshot is rejected and triggers urgent structural reconciliation. Accepted snapshots only raise the waterline. Domain waiting flags remain stronger than confirmed completion. Update introduction (RAW-150): task-level archive capability now allows completed and explicit stopped, with Host contracts `evidence=completed | stopped`; the old `blocked-stopped` statement remains historical only. Project batch still accepts completed alone.
- RAW-132 decision diagnostics are session-only aggregate counters in Activity Delta V2. Controller accepts them only after the same source-fingerprint and generation checks as task/bridge state, exposes them read-only in the Runtime view, and the settings page renders counts only. They are neither persisted nor copied into the Float snapshot and contain no task key, raw ID or content.
- RAW-133 limits permanent diagnostic copy to one protection total plus the live-epoch total. The five counters remain available through a focusable native help button, outside the connection-status live region, so ordinary polling does not repeatedly announce internal telemetry. `.codex-tip` has one native-button interaction shape across the page; no ARIA pseudo-button branch remains.
- RAW-092/110/115 split positive acceleration again without weakening the inventory boundary. `thread/started` resolves its nested `params.thread.id` only inside preload and marks that new row dirty; this avoids the former empty dirty-set fallback that reread every latest Turn. If an event scan's complete `thread/list` still lacks a dirty identity, preload may exact-read that ID once with the bounded thread-read timeout, but admits it only after exact identity/status, native project, latest Turn, anonymous alias and fingerprint validation. Complete fresh started/completed notifications for an already inventoried task use immediate sanitized paths; other unknown/incomplete events and an unregistered Desktop main-task shadow request `inventoryRefreshPriority=urgent`. The Controller coalesces these for 50ms and retains one follow-up if a full scan is in flight. An event scan reads latest Turn only for dirty or uncached rows and reuses session-verified cache for unchanged rows; an ordinary periodic scan has no dirty set and still rereads every eligible Turn. A never-registered main shadow remains preload-only across a lagging scan while exact live active, so it can supply exact waiting-input immediately after safe registration; identity mismatch/failure publishes nothing, and terminal/archive/disconnect or a later disappearance after prior registration still clears through ordinary gates. Cache, dirty generations and shadows clear with the App Server session and never persist.
- RAW-120/121 remove completion smoothing entirely and close each accepted terminal epoch. Exact notification, cached latest Turn, targeted reread and complete snapshot publish accepted completion immediately; the settings control, field, per-task hold, expiry timer and held-card rewrite are absent from the current runtime shape. RAW-090 missing-key quarantine, RAW-112 conflicting-active corroboration, structural coalescing and RAW-091 stopped/ongoing evidence remain because they protect missing evidence or inventory rather than delaying accepted completion.
- RAW-179 refines the earlier Plan evidence lineage：an unresolved current Plan choice/implementation request is an Interaction instance；an exact completed Plan is an independently available Artifact。RAW-189 extends the same terminal priority to every exact current ordinary input、approval and Plan interaction：it becomes waiting-input/approval while unread remains latent；after the interaction closes, unread can reappear as completed-unread, while an already-read artifact-only task becomes stopped/待继续。RAW-207 extends that same interaction ownership onto a still-running Turn：Codex Questions、Cursor AskQuestion、Claude AskUserQuestion and CodexHost `attention=input` project waiting-input/approval even while activity remains `turn-running`；Kernel member aggregation is `waiting-approval > waiting-input > running`。Resolve/cancel/execution-start commits a same-transaction interaction tombstone；a newer Turn or explicit plain-active runtime clears the old wait immediately。Artifact cancel/remove/consume remains explicit, and a bare request-array disappearance invents neither Plan interaction close nor artifact cancellation。Old snapshots/refollow/replay cannot cross the tombstone；a new instance can wait immediately。
- RAW-094 separates stream-envelope continuity from field projection. Desktop broadcasts the complete private conversation patch stream, but `codexApplyDesktopShadowPatch` observes only runtime/request/read-state roots. A well-formed unobserved root is consumed as revision progress without storing its value or rebuilding the subscription; an invalid observed root, owner change, revision gap or version error still forces recovery. This prevents unrelated Turn/tool/body churn from temporarily dropping live authority and later restoring an obsolete active snapshot. It introduces no freshness timeout or terminal inference.
- RAW-111/113 historically introduced end-to-end semantic revision；RAW-160/176 consolidated V4/V6，and RAW-179 now makes [task-kernel.cjs](../../preload/companion/task-kernel.cjs#L1) the sole V7 reducer/latest-Snapshot owner while Topology V2 owns membership only。[eypcPlatform.ts](../../src/platform/eypcPlatform.ts#L1) turns a missing/incompatible V7 Kernel or Runtime Identity into `reload-required`；Controller/Main/Float have no phase/group/count/cycle fallback or Provider task-sync action。The last complete V7 Snapshot may remain visible during ordinary hide/detach，but incompatible actions stop until one identity chain is re-established。
- `task-state-v12` adds raw seven-lane evidence、Interaction/Artifact separation、atomic tombstones and generated contracts to the v11 causal foundation。[preload/index.js](../../preload/index.js#L1) keeps request/Plan content、raw identity、instances、sequences、barriers、terminal watermarks、private action aliases and execution prompt private。Only anonymous finite evidence crosses the bridge；pause/tombstone persistence contains hashed references and finite revisions。No Renderer timer、Provider task watcher/sync action、second reducer、raw request cache or prompt persistence is allowed。
- RAW-142 stabilizes unread at the provider boundary rather than adding a Renderer debounce: while the native atom file is transiently unavailable, the current Desktop Bridge reuses its last successfully parsed membership/nonmembership even if a full inventory rebuild replaced the public activity object. Exact native read-state, refollow false and the next successful native read still supersede it immediately；Deep Link dispatch does not。A weak snapshot true cannot create one incorrect intermediate frame.
- RAW-114/143/144 remove the expanded float's Environment Action Renderer/orchestrator entirely. [FloatApp.vue](../../src/FloatApp.vue#L1), [preload/float.js](../../preload/float.js#L1) and [float.css](../../src/styles/float.css#L1) have no slot/picker/session/confirmation/log state or IPC. The independent [ActionApp.vue](../../src/ActionApp.vue#L1) child consumes only Host-owned snapshots/cursor log deltas and emits whitelisted Runtime Actions plus sender-validated hide/geometry requests. Controller owns priority/catalog/intent, Action-only preflight, one-shot alias recovery, Host-persisted Lane selection and reload-required revision gating; Runner/slot feature routes keep the current main Tab and let `mainHide` own entry visibility. The first Runner entry without verified task inventory performs one tasks-only preflight. Environment catalogs are cached per project key/current alias with per-project single-flight: inventory additions load only the new project, alias changes invalidate only that project, removals delete it, and unchanged projects remain hot. Main Preload separates the unique Environment `configRoot` from the exact task/worktree `executionCwd`; only strict raw `version = 1` and complete package-manager Build/Serve, Vite Build/Serve or plain Git Push argv enter the catalog/launch planner. The cache never replaces execution safety: every run rereads and validates current target/TOML/command fingerprints. Project `targetId=projectKey` preserves existing Lane/SQLite identity, while task `targetId` hashes canonical cwd and isolates vault/session/confirmation/run/stop. Ordinary `onPluginOut(false)` retains live Actions and does not request alias-clearing App Server close; replaceable Renderer Controller disposal only releases local listeners and detaches its Host lease. Feature disable or process-ending `onPluginOut(true)` requests semantic teardown, with Runner/preflight/run ownership still allowed to defer it; the process-ending path frames/flushes logs, persists interrupted, clears runtime authority and sends non-force termination. POSIX uses process-group SIGTERM, Windows uses `/T` without `/F`, and no Action path auto-SIGKILLs. Cross-project lifecycle authority is the CodeNote [uTools plugin-out record](../../../../../czz/CzzProj/CodeNote/DevelopRef/Multi-System-Use/uTools/error-memory/utools-onpluginout-hidden-vs-process-exit.md#L1).
- RAW-115 closes the pre-package registration gap rather than adding a Renderer badge correction。Dirty raw identity and exact `thread/read` remain preload-private；only a row that completes the same verified inventory sanitization joins Provider evidence，where an already retained live shadow may override older connector state。RAW-176 V6 publishes the resulting anonymous task through the single Kernel Snapshot；there is no Controller state package、anonymous-less active aggregate、placeholder card or second count lane。
- RAW-116–119 treat task-selection follow churn as one owner-continuity edge, not client loss. When the previous owner announces `following=false`, [preload/index.js](../../preload/index.js#L1) retains an inventoried main/Side shadow and targeted-refollows the same owner without publishing connector fallback. Exact idle therefore remains stopped; an unwaiting active shadow triggers the existing single-flight, three-second latest-Turn reader so a raced/missed completion can publish anonymous `targeted-after-exit` evidence even if the replacement snapshot replays active. A complete exact notification takes the shorter path and is ordered by Turn revision, never by provider-time versus local-observation-time comparison. If a resumed interrupted/failed Turn keeps the same startedAt and its intermediate inProgress event was missed, the current completed latest-Turn still advances that revision; terminal snapshot settlement always emits targeted completion after idle so Controller cannot re-guard it as ongoing. InProgress, waiting, read failure or real activity remains active/ongoing. Independent disconnect, inventory removal, archive and bridge reset/close still revoke authority; no Renderer badge lane, timer, persisted shadow or terminal inference is added.
- RAW-095 keeps explicit archive evidence separate from ordinary inventory disappearance. A compatible Desktop/App Server archive event resolves only to a current anonymous key inside preload; Activity Delta V2 then removes that exact Controller projection and schedules a 50ms urgent verified scan. Unknown, unarchive, delete and malformed events do not remove anything, while snapshot-only missing keys still use RAW-090 quarantine. Raw identities and private payloads remain preload-only.
- RAW-096 historically introduced an anonymous local observation time for each Desktop live active interval. RAW-118/120/121/125 supersede its former cross-clock ordering: [preload/index.js](../../preload/index.js#L1) may still carry `desktopActiveSince` as v2 compatibility metadata, but [codex.ts](../../src/domain/codex.ts#L1) and [codexController.ts](../../src/runtime/codexController.ts#L1) never compare it with provider `completedAt`. Current ordering uses Turn revision/status plus explicit evidence provenance; missing, older or unconfirmed terminal evidence preserves active/ongoing. Side Chat aggregation and privacy boundaries remain unchanged.
- RAW-097/122/137/138 separate read state from activity authority and distinguish snapshot unread from exact read-state events；RAW-140/144 Host-open acknowledgement is superseded by RAW-177#3。[preload/index.js](../../preload/index.js#L1) turns native `thread-read-state-changed`、a patch containing only `hasUnreadTurn` and a changed Codex-persisted unread set into anonymous V2 `readStateOnly` entries carrying only key/unread state/authority；this includes Side Chat aggregation。Exact native event false/true stays highest priority；Deep Link dispatch emits no read delta。Otherwise current refollow snapshot false may repair stale persisted true，while a successfully parsed native set remains authoritative for snapshot true in both directions：membership keeps unread and nonmembership clears it。Initial/refollow snapshot is the full fallback only when the native source is unavailable。[codexController.ts](../../src/runtime/codexController.ts#L1) applies only those unread fields，and its Activity generation barrier rejects an older full snapshot that would restore completed-unread after a newer read delta。Runtime/request changes remain full activity deltas，so read evidence can only move a completed task between completed-unread and completed rather than resurrecting an ongoing task。
- The Desktop bridge also keeps confirmed Side Chat streams as preload-only live shadows, using `forkedFromId` first and the parent navigation path as fallback；App Server inventory contributes the same process-private topology only after same-session parent validation。Side shadows never enter Renderer inventory or persistence；their private phase/unread evidence reaches the Kernel together with a finite `main/side` role。Every confirmed bead participates immediately in the anonymous parent reduction：a causally newer approval/input interaction wins older running，a causally newer true running clears older interaction，and either suppresses latent completed-unread until no current activity/interaction remains。RAW-137 retains at most 1,000 observed child→parent topology hints in preload memory across a bridge close/reopen or a same-socket App Server inventory reset, without retaining status or exposing the hint to Renderer. A recovered bridge—or a parent returning after the App Server inventory was cleared—refollows only children whose parent is again inventoried and gives each recovered non-active child one existing bounded latest-Turn check；confirmed child/parent archive or parent inventory removal drops the hint。A Side Chat created and ended wholly while every authority is unavailable has no persistence authority and is deliberately not fabricated。Every parent open dispatches only `codex://threads/<parent>`；Side Chat identity is never selected as a Deep-Link target，and raw child/parent IDs remain outside Renderer、logs and error copy。
- Recurring failures are routed through seven responsibility modules in the [error-memory index](error-memory/README.md#L1)；every leaf has exactly one Primary owner，Related links are evidence-only，and `pnpm run validate:error-memory` rejects duplicate identity、broken ownership、oversized indexes and invalid lifecycle metadata。Superseded records remain logical history rather than parallel current architectures or default recall routes。Additive diagnostic-port skew remains separately owned by [codex-preload-capability-version-skew.md](error-memory/codex-preload-capability-version-skew.md#L1), while runtime lookup and PAC startup failures remain in [codex-gui-nvm-launcher-path.md](error-memory/codex-gui-nvm-launcher-path.md#L1) and [codex-gui-pac-proxy-timeout.md](error-memory/codex-gui-pac-proxy-timeout.md#L1). The former coupled-card transaction record is explicitly [superseded historical evidence](error-memory/codex-coupled-color-editor-atomicity.md#L1), not an active architecture rule.

## Verification

- Project gates are listed in [../specs/PROJECT_STATUS.md](../specs/PROJECT_STATUS.md#L1).
- MQTT verification evidence is indexed in [../specs/2606231645-eypc-mqtt-websocket-tab/06-sync-doc.md](../specs/2606231645-eypc-mqtt-websocket-tab/06-sync-doc.md#L1).
- Current cross-source Codex companion V6 verification is recorded in [../specs/260823/companion-task-topology-v5/verify.md](../specs/260823/companion-task-topology-v5/verify.md#L1)；its [post-V5 assessments](../specs/260823/companion-task-topology-v5/assessment/README.md#L1) and the older [codex-quota-float verification](../specs/260718/1148-codex-quota-float/verify.md#L1) remain historical evidence.
- Detailed maintenance facts and update triggers are in [technical-details.md](technical-details.md#L1).
