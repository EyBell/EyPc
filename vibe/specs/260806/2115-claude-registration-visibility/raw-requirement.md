# RAW：Claude 注册状态可见 + 一致性偏差收口

Date: 2026-08-06 · Tool: claude (Cowork) · Level: Standard（一个 owner）

## 用户原话（2026-08-06 核验轮之后）

> 1. 不一致的地方，你按你的建议进行优化。
> 2. 实际校验的话，我一会儿进行。
> 3. 注册钩子的进程，可以实际放到配置页或者 Codex 的功能 Tab 列里面，实现可看、可查状态，并可以重新注册这个钩子。

## 上文语境

本轮之前是一次只读核验，对 [260805/1150](../../260805/1150-claude-companion-provider/raw-requirement.md#L1) 的 10 条原始要求逐条比对，结论是 10 条全部匹配，并列出三处偏差：

1. 桌面端卡片不进「已完成未读」角标——对「角标一律以状态为准」的收窄，只写在帮助文档里，没进任何合同文档。
2. 未注册钩子 → 无实时推送（与 Codex 的毫秒级事件流不对等）——已知边界，已有文档。
3. 宿主验收矩阵一次都没执行。

另有两项更早的遗留：`companionResetDetailText` 的过期窗口分支零测试覆盖；[1046 verify.md](../1046-claude-quota-status-visibility/verify.md#L1) 的用例计数与实际漂移。

## 本轮范围

- 第 1 条 + 两项遗留：按上一轮给出的建议收口（第 1 条上升为产品合同，其余补测试与更正记录）。
- 第 2 条：不动。它是已记录的已知边界，用户第 3 点要的是让这个边界**在设置里看得见**，而不是消除它。
- 第 3 条：**归用户**（用户明确"一会儿进行"），本轮不代跑、不代判。
- 用户第 3 点：设置页「运行 → 接入来源」列出逐项注册状态，并让「重新注册」在钩子过期时可达。

## 归属决策

放**配置页的既有「接入来源」面板**，不新开功能 Tab：该面板已经承载 Claude 开关、额度兜底开关与注册/移除按钮，注册状态是同一件事的读数；另立 Tab 会把一件事拆到两处。用户给的是两个可选位置，此处取已存在的那个。
