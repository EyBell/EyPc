# Companion Task Topology — V6 Handoff

status: `v6-implementation-landed / automated-verified-with-known-mqtt-timeout / artifact-ready / host-excluded-by-user`
updated: `2026-08-24`

## Resume Point

- Current authority: [spec.md](spec.md#L1)
- Current work list: [plan.md](plan.md#L1)
- Attempt ledger: [tasks.md](tasks.md#L1)
- Verification truth: [verify.md](verify.md#L1)
- Changed inventory: [changes.md](changes.md#L1)
- Post-V5 audit: [assessment](assessment/README.md#L1)
- RAW-177/native handoff authority: [boundary optimization](../codex-tab-boundary-optimization/spec.md#L1)

## Fixed Boundaries

- Task title remains `260823-Cursor任务统一架构`; no title mutation without a measured Codex title API.
- No Safari、uTools、real-plugin or real-host testing unless separately requested.
- Dynamic task state is process-only and rebuilt from Provider evidence; local preference persistence remains separate.
- V1 exposes root summaries only; expandable child UI and native child rename/open remain future capability audit work.
- Deep Link/shell dispatch remains `confirmsRead=false`; only a matching native receipt may establish opened/read.

## Delivered State

- One manifest/Registry owns Provider order、capability and relation mode；Codex、Claude、Cursor are first-class Kernel adapters。
- Topology V2 owns exact root/member identity only；Kernel V6 alone owns member causality、Plan/unread/root phase、groups、counts and cycle；one Snapshot V6 revision feeds Main、Float、badges、attention and cycle。
- One Command Gateway owns manual/keyboard/global/cycle effects，with operation dedup、per-task serialization、same-key revision revalidation and Provider failure isolation。
- Runtime Identity chain is `task-state-v11 / registry-v1 / topology-v2 / kernel-v6 / snapshot-v6 / command-v1 / subscribe-v1 / ack-v2`；older or partial Hosts return `reload-required`。
- Waiting-input clears immediately on exact supplementary/new-Turn/thinking evidence；Plan uses monotonic `unknown/ready/cleared` and the completed-unread → read waiting-input → cancel completed / execute running truth table。
- Public consumers receive no private action alias and expose no Provider-specific task-sync route；all task actions resolve the anonymous key inside Kernel。
- Public `unknown` is neutral and non-actionable；inventory may hydrate names/projects/times but cannot revive an older bucket、unread or capability。Verified archive removal is committed once by Kernel，not repeated by Provider-specific Controller cache mutation。
- Float missing applied ACK causes one latest resend and diagnostics only；a healthy window is not recreated, avoiding the former previous/next crash path。
- Current automated evidence、stable two-build artifact `host-ebb1e6b699892efb8151 / renderer-6e9dbf12ac1479057e23` and the one known unrelated MQTT timeout are recorded in [verify.md](verify.md#L1)。Local batch commits were completed；no push was requested。
- Codex Tab requirement/source re-verification recovered RAW-167 and three already-numbered additions，synchronized the V5 current code map，and found no unresolved repository semantic conflict。The focused current-worktree slice passed `231/231`；requirements now pass at 298 leaves with zero `conflicted`。
- 102 no-parent ordered clauses now have stable `SA-*` source identities but remain outside the requirement registry until semantic classification。Legacy platform facades remain compatibility-only，and no repository contract proves Mirasim/native window visibility or external control ownership。

## Future Capability Audit

Only a Provider that later proves exact identity、safe child title、independent open and reliable lifecycle may upgrade from V1 `aggregate` to expandable `group`。Ordinary ChatGPT and Claude Agent Teams remain independent until an EyPc-consumable exact topology source exists。
