# EyPc Product Requirements

Tool: codex

Requirement version: `2026-08-10.2`

## Purpose

This file is the current product requirement index for EyPc. Task-level history remains in dated folders under [PROJECT_STATUS.md](PROJECT_STATUS.md#L1); MQTT implementation mapping is maintained in [2606231645-eypc-mqtt-websocket-tab/06-sync-doc.md](2606231645-eypc-mqtt-websocket-tab/06-sync-doc.md#L1).

## Global

- EyPc is a keyboard-first uTools plugin for local PC operations.
- Runtime actions are the only user-visible mutation boundary; UI components emit commands and never perform shell, storage, MQTT, or filesystem writes directly.
- Top-level feature switching uses configurable `Ctrl+Shift+数字`; Settings opens with `Ctrl+Alt+S`; `Shift+Escape` hides the plugin window.
- Shortcut resolution is layered by command context. Editor, drawer, preview, dropdown, and popover layers take priority over page-level shortcuts.
- Host-shell transient command layers must be represented in runtime state before DOM focus is trusted, especially when a host reserves browser-like shortcuts.

## Shared Interaction Surfaces

Current increment authorities: [260713/0834-cross-tab-responsive-command-panels/spec.md](260713/0834-cross-tab-responsive-command-panels/spec.md#L1), [260718/0947-quick-jump-center-overlay/spec.md](260718/0947-quick-jump-center-overlay/spec.md#L1), and [260730/2050-global-operation-tooltip-polish/spec.md](260730/2050-global-operation-tooltip-polish/spec.md#L1).

- Main-app operation controls use one product Tooltip owner with 100ms hover/immediate focus activation, accessible name, current shortcut, description or disabled reason, viewport clamping and `aria-describedby`; native `title` is not the only help surface. Buttons, menu/tab/option/treeitem roles, draggable rows, checkbox/radio/number/range controls and selects are covered. Shortcut-shaped title suffixes are split into a `kbd` hint without duplicating the label, ordinary parenthetical text stays intact, legacy `data-tip` details are absorbed by the shared layer, and active tips refresh when control metadata changes. Pointer action, window blur and Quick Jump clear or suspend the layer; page-owned pseudo-tooltips must not compete with it. The independently rendered Codex Float child retains its child-owned hints and does not mount this owner.
- `Ctrl/Cmd+ArrowLeft` opens or switches to target detail and `Ctrl/Cmd+ArrowRight` opens or switches to target actions across Ports, full/Quick Favorites, MQTT row targets and Settings command rows. Explicit action arguments outrank an already-open frozen target; invalid explicit targets fail instead of silently falling back.
- A focused Port outside the current visible selection becomes the single target; a focus that remains inside the selected batch preserves the batch action target.
- Ordinary text editors keep native arrow/navigation ownership. Settings only claims left/right panel commands from non-editing command rows, and Quick Favorites exposes safe read-only details/actions only.
- Opening a context panel moves focus into it; switching sides refocuses the new panel; closing restores the trigger or the stable list/grid owner. Buttons, context menus and shortcuts dispatch the same action id.
- Tab content owns page height. Wide pages dock side panels; `721–1100px` temporarily reduce secondary rails; `<=720px` panels become exclusive inside the active Tab. Every main page supports vertical scrolling and must not require page-level horizontal scrolling at 420px.
- Global Quick Jump renders each compact solid badge at the exact center of its target rectangle in a fixed pointer-transparent top layer. Marker width may grow for multi-character labels, but its center never shifts for title anchors, collisions or viewport edges; overlap and partial edge clipping are accepted so target correspondence remains deterministic.

## Ports

- The port page scans local TCP listeners, dedupes by `pid:port:protocol`, supports search, user-defined groups/folders, focus movement, group filtering, detail drawers, action drawers, and safe kill/force-kill commands.
- Metadata changes to port groups/folders are plugin-state changes only. Process kill actions must revalidate current PID/port ownership.
- Port group editing follows command-soul semantics: `F2` edit, `Shift+F2` rename, `Ctrl+F2` move folder, `Ctrl+S` / `Ctrl+Enter` save, `Escape` cancel, and editor-local `Tab` field cycling.

## File Favorites

Current increment authorities: [260711/1452-file-favorites-workbench/spec.md](260711/1452-file-favorites-workbench/spec.md#L1) and [260806/1120-favorite-quick-open-runners-slots/spec.md](260806/1120-favorite-quick-open-runners-slots/spec.md#L1); original workbench implementation traceability: [260711/1452-file-favorites-workbench/requirements-traceability.md](260711/1452-file-favorites-workbench/requirements-traceability.md#L1). Approved-but-unimplemented next increment: [260807/1740-favorite-runner-console-sync-and-args/spec.md](260807/1740-favorite-runner-console-sync-and-args/spec.md#L1) — its bullets are collected at the end of this section and are not shipped behavior.

- Favorites are plugin metadata for file/folder paths and virtual groups. Removing a favorite never deletes the disk file or folder.
- Quick favorite mode is a readonly, cross-group search/open-or-run/reveal/copy surface; management mode supports add, edit, move, duplicate focus, directory listing, runner trust, slot maintenance and learning reset.
- Default open/reveal/copy and structured runner execution stay behind the platform bridge in [src/platform/eypcPlatform.ts](../../src/platform/eypcPlatform.ts#L1) and [preload/index.js](../../preload/index.js#L1).
- Favorite metadata normalizes duplicate ids, orphan/self/cyclic parents, and equivalent paths without rewriting the displayed path. Windows drive and UNC paths compare case-insensitively with normalized separators; POSIX paths remain case-sensitive.
- File action results distinguish confirmed success, host dispatch, reveal-as-open fallback, and failure. Missing, offline, or denied paths remain saved and surface runtime health instead of being silently removed.
- Full favorites use command-owned `containers`, `items`, and one-level `directory` targets with deterministic focus, selection, drawer targeting, `Escape` recovery, inline rename, batch metadata actions, and one-step metadata-removal undo.
- Each file/folder may store one custom runner per macOS/Windows/Linux platform. Runners use an executable plus argument array and `{path}` / `{dir}` / `{name}` placeholders; raw shell strings, custom environment variables, secrets and PowerShell policy bypasses are unsupported. Any platform, target identity/path/name or runner-config drift invalidates trust until the user confirms the fully resolved preview again.
- Runners and slots are edited on the platform in use: the editor and the slot manager read and write only the current platform's entry and never create, overwrite or clear another platform's saved binding. Configuring all three platforms means configuring each one on that system.
- Ten stable `mainHide` uTools entries (`eypc-favorite-slot-1`…`10`) bind favorites independently per platform. A favorite occupies at most one slot per platform: assigning it to another slot releases its previous slot on that platform, leaving other platforms untouched. Full favorites expose one compact slot manager for assign/replace/clear/test/hotkey settings; failed or untrusted slots open the manager with a repair reason, while successful background/default opens keep the uTools main window hidden.
- Quick entry autofocuses one global search and highlights the first result. Text exact/prefix/contains tiers precede query-specific successful-use affinity, global 30-day-half-life frecency and manual order; learning is bounded to 50 LRU queries and 10 favorites per query and increments only after an open or runner start the host accepts — a confirmed start or an accepted dispatch. Locate, copy and rejected launches never count. Full favorites can reset item/global learning.
- Quick result rows 1–10 show `1…9,0`; with the drawer closed, `Ctrl+1…Ctrl+9` / `Ctrl+0` execute those results. An open action drawer has higher shortcut priority and reuses the same keys for its first ten actions.
- Full favorites keep a compact two-pane layout, collapse containers into a command-controlled side layer below 720px, and provide keyboard, focus, ARIA, loading, empty, error, unsupported, and 420px overflow-safe states. Quick favorites remain readonly and never inherit management-page selection or drawers.
- File favorites never create, move, rename, or delete real files; directory reads are non-recursive and do not follow symbolic links.
- A fixed-corner launcher is a separate second-phase window proposal; the product does not monitor, move or pin the uTools main search window.

Next increment — **approved requirement, not implemented**: [260807/1740-favorite-runner-console-sync-and-args/spec.md](260807/1740-favorite-runner-console-sync-and-args/spec.md#L1). Nothing below is shipped behavior yet.

- Every favorite carries a sync choice (`synced` by default, or local-only). Synced data stays in the uTools plugin database and follows the user's own account sync switch; local-only data lives in a channel that never enters that database. uTools sync is account-level and whole-database with no per-document opt-out, so the granularity is the plugin's own responsibility.
- Runner trust never syncs. A synced favorite carries its runner configuration to another machine but not its trusted state, because trust is a human confirmation of one executable path on one machine.
- **Shipped 2026-08-07** — Trust survives a rename: the fingerprint includes the favorite name only when a runner argument or working directory actually references `{name}`. Existing trusted configurations upgrade in place; a fingerprint matching neither algorithm is kept as-is and stays non-executable, so migration never invents trust.
- **Shipped 2026-08-07** — There is no in-plugin terminal or console UI. A background run leaves one local run record — resolved command line, working directory, status and exit code — shown on the favorite row, with open / reveal / copy actions that hand the log to the system's default viewer.
- **Shipped 2026-08-07** — Two definite log sources exist: the plugin's own redirected run log, and an optional absolute log path the runner declares (placeholder-expanded, never created and never executed; relative or malformed values are dropped). Run logs and records are local-only, bounded in size and count, and a log directory that cannot be opened degrades the launch to "no log", never to a silent success.
- **Shipped 2026-08-07** — Background runs surface an exit code, and a non-zero exit is never presented with success wording. Exit codes are observable only while the plugin process lives; the child deliberately survives a plugin restart, so a historical exit code is not backfilled. System-terminal mode states that its exit code is unavailable rather than fabricating one, and remains the escape hatch for interactive prompts and full-screen programs.
- Not yet implemented: a third, best-effort log source that scans the working/script directories for files modified during the run. It must be labelled a guess and kept distinguishable from the two definite sources. System log stores and privileged process-write tracing stay out of scope.
- **Shipped 2026-08-08** — Runner arguments may declare dynamic slots as `{ask:name}` or `{ask:name=default}`, collected from the user right before launch with a live preview of the resolved command line. Declarations are allowed in arguments only: the executable and working directory stay static, so a prompt can never redirect what runs or where. The entered value is passed as one argv element and is never word-split, shell-parsed or placeholder-expanded — and favorite data that happens to contain `{ask:…}` never becomes a prompt either. Trust covers the declaration, not the entered value. Cancelling runs nothing and records nothing; a slot or number-key launch shows the window for the prompt rather than failing silently.

## Window Jump Workbench

Current increment authority: [1527-window-jump-workbench/spec.md](260724/1527-window-jump-workbench/spec.md#L1).

- Window Jump is a default-disabled feature. Each independently operable real main/root window is the stable identity; browser Tabs and editor pages remain internal mutable state, while bridge-proven real native children are transient exact destinations. Stable slots use exact `PID+CGWindowID/HWND`, may switch only the target display to a session-cached Space, and verify the final root/member. Titles, Tab, position, size, order, display and Space never define identity; there is no title/ordinal/unique-candidate fallback, environment snapshot or background poller.
- `EyPc 窗口槽 1` through `EyPc 窗口槽 10` are `mainHide` uTools features. Successful jumps activate the target without showing the plugin transit window; the slot route must not add a second generic hide. Missing, ambiguous, unauthorized, or otherwise failed activation opens the windows workbench/settings fallback with an explicit diagnostic.
- `AppState` persists EyPc-local root/group targets and ten `1–10` platform-separated slot mappings. `WindowTarget.id` is the stable logical target; `scope='instance'` stores only the last verified real root locator, while Space/display/liveness remain preload-session cache. Child windows are session-only. Projection never merges targets or remaps slots: one unambiguous legacy member may adopt its proven root, while same-root multi-target conflicts require explicit per-slot recovery. That recovery changes one slot, reuses or creates one precise target, and never copies unrelated aliases/favorite/pin state.
- Windows uses fixed PowerShell/User32 calls and `EnumWindows` only; it never uses `EnumChildWindows` to enumerate control HWNDs. Admission requires a visible, non-cloaked, activatable top-level/owned popup with valid bounds; `WS_CHILD`, no-activate, transparent, host, system and helper surfaces are omitted. Same-app `GA_ROOTOWNER` proves root/member relationships. `root-current` resolves the current/last member at call time and verifies final foreground ownership; `member-exact` additionally requires final focus on the requested member. `win32:PID:HWND` remains a current verified locator, not a promise against later handle reuse. Focus refusal is reported without simulated input or foreground-protection bypass.
- macOS starts from Accessibility windows of regular foreground applications, admits valid `AXWindow`/`AXSheet`/`AXDialog` roles, maps each through `_AXUIElementGetWindow`, and requires a positive Core Graphics window ID as identity corroboration. `AXParent`/`AXTopLevelUIElement`/`AXWindow` prove root/member relationships. Core Graphics alone never creates a product row; CG-only, system-layer, helper and non-actionable surfaces are omitted. Root and exact-member activation/close revalidate application, root, member and relation, then require final `AXFocusedWindow` to map to the requested root and exact member when supplied. Bridge revision mismatch blocks activation, and macOS cannot claim permanent topmost for arbitrary third-party windows.
- Alias changes leave the uTools feature binding intact. EyPc may open the official uTools binding screen, but never reads or mirrors the current host binding.
- Root activation always uses `root-current`, so Tab/editor/dialog changes do not invalidate favorites or slots. Selecting a real native child uses fail-closed `member-exact`. Inventory completeness governs projection merging only: missing rows become temporarily unobserved, and neither complete nor partial absence can prove death. `windows.probeInstance()` alone returns live/gone/indeterminate; only matching verified-gone evidence clears the native locator before explicit same-app candidate recovery.
- The replacement flow has one root-only session state machine as its source of truth. Complete/partial inventory handling, candidate membership, success/cancel termination, stale-binding effect and focus-return effect are not reimplemented by the page or individual actions. One Window domain projection owns family rows, target/slot retention, tree/search, `window / child-window / file-manager-group / selection / slot` action contexts, navigation and root-only selection; Runtime applies side effects and the page consumes rows. An active editor draft and replacement confirmation must never coexist.
- Window activation diagnostics are session-only Runtime state, capped at 50 and cleared through a named action. They carry only opaque timing/entry/platform/stage/code/level and sanitized explanations; no title, app, PID, handle, native ref, raw command/error or Space/display identifier is exposed. Development may request bounded enum/count traces including a `space` stage; production requests no trace. The internal Space cache is not a diagnostic snapshot and never persists.
- The compact workbench uses a toolbar, persistent slot strip, status band, ARIA tree and existing action/editor layers. Ordinary real roots are level one and verified real children are level two; same-app independent roots remain siblings and no application virtual parent is created. Finder/Explorer alone use one virtual level-one parent plus independent real roots at level two, never a third level. Search temporarily expands a matching child while preserving manual expansion state. Right-click first focuses then opens one contextual panel; child context allows exact activate, capability-gated exact close, readonly details and Windows HWND copy only. Children and virtual parents never enter multi-selection; children additionally cannot favorite, pin, rename, bind slots, topmost, force terminate or join batch actions. `ArrowRight/ArrowLeft` own tree navigation, and Escape closes the right panel before search/editor/rebind/selection while restoring hidden/stale-child focus to its real root.

## MQTT

- MQTT over WebSocket is a first-class feature with lazy page/runtime loading, dynamic MQTT client import, SQLite-first local archive, legacy archive fallback, and local-only persistent secret storage.
- Connection configs store endpoint, Client ID, username, subscriptions, aliases, colors, publish defaults, reconnect options, `syncRecords`, and layout preferences. Password/token values persist only in the preload local userData encrypted secret envelope `mqtt-secrets-local.json`; they never enter app state, archive, templates, SQLite mirrors, or synced storage.
- `MqttConnectionConfig.publishTopics` stores multiple default publish topic candidates. The first normalized value is mirrored to `publishTopic` for compatibility with the existing send editor.
- Subscription topics are configured per connection. Aliases/colors are pruned with topic removal, and topic colors normalize to five defaults or valid hex colors.
- Runtime owns message sessions, publish templates, outgoing history, publish draft history, record-list focus/selection, drawers, preview, search, layout, view preferences, and focus targets.
- `MqttState.viewPrefs` persists the last `全/收/发/藏` information filter and valid topic filters per connection config.
- MQTT ordinary message rows and outgoing history rows sort newest first by message `timestamp`. Publish draft history sorts newest first by `updatedAt`. Publish favorites/templates sort newest first by `MqttPublishTemplate.operatedAt`, with `updatedAt` / `createdAt` fallback for older archives.
- Message, outgoing-history, and publish-favorite/template lists expose a fixed-toolbar quick export whenever the current list has explicit multi-selection. Copy and Save export only those selected rows in current visible order, keep selection unchanged, and emit one `eypc-mqtt-merged-export/v1` JSON envelope. Every item retains its raw payload; valid JSON payloads additionally expose `payloadJson`. File export writes only after the user chooses a path in the host save dialog.
- `MqttArchiveState.publishDraftHistory` stores overwritten drafts and manual saves only. Real sends remain ordinary outgoing message records and are not duplicated into draft history.
- MQTT focus targets include `records`, `topic-filter`, `publish-topic`, `publish-payload`, `publish-options`, `publish-history`, draft-history editor fields, `connections`, and `subscriptions`.
- Connection rail rows support focus highlight, hover highlight, multi-select, `ArrowUp` / `ArrowDown` movement, `Space` selection, `Ctrl+C` endpoint copy, delete shortcuts, row-local edit/detail/more buttons, and right-click action menus.
- Subscription rail rows support focus highlight, hover highlight, multi-select, `ArrowUp` / `ArrowDown` movement, `Space` selection, `Enter` topic filter, `Ctrl+C` topic copy, `Ctrl+Enter` use-as-publish-topic, delete shortcuts, row-local edit/detail/more buttons, and right-click action menus.
- `Ctrl+ArrowLeft` opens the detail drawer and `Ctrl+ArrowRight` opens the action drawer for MQTT record, connection, subscription, and draft-history row targets.
- `Ctrl+S` favorites/unfavorites the current record/template in record focus and saves the current publish topic/payload as a template in publish editor focus. Edit layers keep `Ctrl+S` as save.
- `Ctrl+1/2/3` select `全/收/发`; `Ctrl+M` selects `藏`; `Ctrl+H` opens/closes the send-area draft-history popover and focuses it; `Ctrl+Shift+H` manually saves the current draft. `Ctrl+L`, `Ctrl+Shift+L`, and `Ctrl+Shift+M` have no default MQTT binding.
- `Ctrl+Shift+S` toggles layout, `Ctrl+Shift+F` opens the topic dropdown, and `Ctrl+P` enters the publish topic field. Publish topic/payload editors keep native `Ctrl/Cmd+ArrowLeft/Right`; QoS/retain options open from their explicit button.
- Draft-history rows apply only from the row apply button or `Enter`; plain row click focuses the row. `Ctrl+Enter` applies and sends the focused draft while keeping the popover open.
- Draft-history focus owns `Space` multi-select, `Ctrl+Left` detail, `Ctrl+Right` actions, `F2` title/note edit, `Shift+F2` topic/payload edit, `Ctrl+Delete` / `Ctrl+Backspace` delete, and `Escape` close/cancel.
- Draft-history editor modes use `Ctrl+S` / `Ctrl+Enter` save, `Tab` / `Shift+Tab` field cycling, and `Escape` cancel.
- Publish topic/payload focus clears information-list focus so `Space` cannot keep toggling message selection while the user edits publish content.
- Topic dropdown lists only current connection subscriptions, supports alias/topic search, is single-select, and filters only ordinary message rows under `全/收/发`; template and history rows ignore topic filtering.
- Subscription modal rows, connection config subscription rows, and connection config publish-topic rows use `ArrowUp` / `ArrowDown` for same-field row movement and `Ctrl+Delete` / `Ctrl+Backspace` for current-row deletion. Plain `Delete` / `Backspace` inside text inputs remains native text editing.
- Connection config editor `Tab` / `Shift+Tab` cycles through connection fields, each subscription alias/topic/color row, each publish-topic row, connection option fields, and storage options inside the editor layer.
- Pure Shift preview can open a readonly overlay for valid MQTT message/template rows and draft-history rows while the draft popover is open. `Ctrl/Command+Shift` suppresses Shift preview so `c-s-*` commands remain shortcut-owned.
- MQTT UI uses compact rails, red/green/gray status rectangles, connection-title `host:port` hover text, a top topic dropdown without a `topic:` prefix, unified record/favorite/history rows, a send-area draft-history popover, a draft editor modal, and a publish options popover.
- MQTT local popovers for topic filtering, publish options, and draft history must be topmost inside the workbench in both stack and split layouts, but must stay below global detail/action drawer masks, previews, modals, and shortcut top-layer hints.

## Companion 多来源汇总

- Codex 与 Claude Code 是两个彼此独立的来源，可各自开关，也可同时开启共享同一个水球。默认只开启 Codex，此时插件完全不读取任何 Claude 数据，数据、状态、额度、空态与角标语义保持 Codex-only 兼容；唯一有意的展示差异是 RAW-022 要求任务/项目行即使在单来源模式也保留一个文本化、可访问的 Codex 归属标记。
- 任务排布与角标以**状态**为准而非来源：待输入、进行中、已完成未读的角标数字是启用来源的合计；任务列表显示顺序不按来源分组。每条任务仍必须以文字、图标和可访问名称显示“归属 Codex/Claude”，并使用来源背景与状态标记正交区分。
- Claude 当前需求权威为 [Claude Code Companion 权威重置](260807/claude-code-companion-authority-reset/spec.md#L1)，技术选择与严格测试门禁见其 [research](260807/claude-code-companion-authority-reset/research.md#L1)，实现/未验收边界见 [verify](260807/claude-code-companion-authority-reset/verify.md#L1)。代码已按 revision 5 路线落地，真实 Claude App quota 数据权威已通过；完整 uTools 状态/未读/项目筛选矩阵与最终 Fable/reset 渲染同屏仍待通过，因此不得宣称产品验收完成。
- Claude 库存只镜像 Claude App **Code 模式**的 `claude-code-sessions/<org>/<user>/local_<uuid>.json`；CLI-only、Cowork、`local-agent-mode-sessions` 和云端索引均不进入卡片。卡片身份使用 App local id，标题使用 App `title`，空标题固定为 `General coding session`；UUID 不得成为可见标题。
- App 已有的重复 Code 行严格保留，不由 EyPc 自动隐藏、合并、删除或修复。多个本地行共享一个 `cliSessionId` 时，Hook 状态必须经唯一映射或定向元数据脉冲关联；不能唯一归属则状态未知，不得一对多扇出。
- Claude phase 与 unread 是两个正交维度。phase 为 `running / waiting-approval / waiting-input / completed / stopped / unknown`；待审批与待回答进入「待输入」，运行进入「进行中」，stopped 在卡片内显示「待继续」，完成且 App 原生未读进入「已完成未读」，其余完成进入「已完成」。一张卡只能进入一个可见状态分组；待继续不新增顶层 Tab、角标或快捷入口。
- Claude phase 的生产路线是**版本门禁 App 私有日志 + 可唯一映射的官方 Hooks + Code 元数据历史恢复**：App local id 精确事件优先，唯一 Hook 次之，`completedTurns > 0` 且无更新 active 证据时恢复历史 completed，歧义或冲突保持 unknown。日志只接受已门禁版本的发送、权限/提问、request-id 响应、完成、停止/失败固定模板；失配 fail closed，原始行/正文/工具参数不跨 Bridge。Hooks-only 和私有 IPC 注入均为已拒绝路线。
- 只有 `UserPromptSubmit` 开启 Claude 父 Turn；Stop/StopFailure/SessionEnd 关闭后，SubagentStop、工具和 lifecycle 尾事件不得把旧任务恢复为 running。通用 `Stopping session` 只标记 session-end：同一 Turn 已有成功 Stop/Result 时保持 completed，只有无成功结果的 session-end 或显式 failed/interrupted 才进入 stopped。App 明确终态优先同 Turn Hook 尾事件，只有严格更新的新 Prompt 可重新激活；source generation 优先于事件时间参与版本纠错。
- Claude 未读持久权威是 App Local Storage 中包含 Chromium string tag 的 `epitaxy-unread-v1` 精确键。EyPc 只在权限 `0700` 的完整 LevelDB 临时快照上用真实 reader 读取，复制前后源指纹一致才发布 V2 generation 并立即清理；失败返回 unknown，不复用旧集合。LevelDB 目录首个不同指纹由进程 Node 原生回调立即通知，同指纹不通知；已登记目录/文件由一秒 StatWatcher 补漏。精确 live running/waiting 优先；否则 native unread membership 可把非 live 历史任务确认为 completed-unread。成功精确跳转完成态后，只允许同 `sessionId + completionEpoch` 的进程内可撤销提示和 0/100/300/1000ms 原生复读；同轮迟到 true 不回跳，新轮次可再次未读。禁止持久回执、WAL/`.ldb` 字节扫描或写 App 未读。
- Claude 功能启用期间由进程生命周期 Host 维护 `inventory / phase / unread / quota / appPresence` 物化视图；切页、Main/Float 显隐和快捷键复用同一缓存，重启后从真实来源冷启动且不持久化 live phase。五条 authority 独立增量更新；Hook/App state、已登记任务成员文件和 unread LevelDB 的首个完整文件事件在 Node 原生回调中立即 drain/read，不进入可被 `background-hidden` 节流的 JavaScript timer；部分任务元数据 JSON 保留最后可信成员关系，目录 `fs.watch` 为快路，已登记文件的 1 秒 `fs.watchFile` StatWatcher 只作漏通知恢复，Renderer Controller 不另设 phase 轮询。等价 reduced-state/package 指纹完整 no-op，不增加 revision 或推送；source generation、Kernel revision、Float applied revision 全链拒绝倒退。额度网络不得阻塞任务状态，watcher callback 延迟不得冒充最终 Float applied 延迟。
- RAW-160 requires the current Claude `session.phase` evidence to outrank `previous.phase` whenever its causal event is newer；a delayed older inventory generation cannot regress a newer watcher/open-refresh event。App state 固定语法当前仅门禁已核验的 Claude App `1.26832.0 / 1.28929.0`，相邻未知版本 fail closed；日志冷重放的普通 running/waiting 不得推导 live activity。phase、phaseRevision、statusEnteredAt、unread and capabilities are accepted atomically through the same State Store。D′ archive success text is exactly split into EyPc convergence and native-sidebar capability：EyPc 已归档并移除；Claude 原生侧栏同步未确认，当前不受支持。Occasional native sidebar refresh is observation only，never a supported postcondition。
- Claude 任务更多操作提供“同步 Claude 状态”，并在成功打开后执行一次静默同步；两者都加入现有 state/unread singleflight，合并后最多发布一次并可反馈部分失败。它们只读真实来源，不允许人工指定完成或已读，不新增持久回执或 Claude App 写入。
- 「上一个/下一个」通用任务循环先选择 attention → Plan → active → local pin 的首个非空层，层内跨来源按最近提问时间倒序，创建时间与匿名 key 只用于稳定并列；Provider 与置顶不改变顺序。候选来自进程原子任务包；热且可信时直接派发，只有冷启动、重连或明确成员缺口才等待所需 Provider 的 tasks-only 盘点，不得从部分集合跳转。待输入与已完成未读专用入口保留独立未打开进度。
- 每个任务行和项目行固定显示一个明显的文本化来源标记；即使只启用单一来源也不得省略，且图标与可访问名称必须同步表达来源。
- 跳转由各来源自身能力执行：Codex 沿用自身深链；Claude 在执行前用私有唯一文件索引确认会话仍存在且未归档，再派发 Epitaxy deep link。全局游标与跨来源派发由 Preload 进程拥有：第一下按键立即派发；仅当打开仍在执行时，后续按键合并成一个最终 trailing 目标；卡片/manual/attention 取消未派发 trailing，全部 Provider 最大并发为 1。普通 mainHide/Renderer 重建保留，来源变化、功能停用、kill/进程退出失效。禁止 import、CLI/终端、自动启动、App 未读写入或会话复制。
- RAW-160 upgrades dispatch to `companion-task-actions-v2`: Renderer submits only Provider-neutral intent；the process Dispatcher owns Provider routing、five-second confirmations and action/revision single-flight；Provider Adapters own exact transport effects；Controller consumes only verified package/mutation results。Open remains leading-immediate + in-flight final-trailing，archive never merges or removes optimistically，and Plan pause/resume/execute use the current Plan lifecycle revision。
- RAW-160 makes `task-state-v10 / companion-task-kernel-v4 / companion-task-package-v4` the only current authority. The Kernel alone reduces branch/parent phase、tri-state unread、freshness、Plan lifecycle、visibility、dynamic groups/counts、badges、attention/cycle/focus keys and capabilities；Host、Controller、Renderer and Float consume selectors without reconstructing them。Provider generations reject ordering only；equivalent evidence is a complete no-op across Kernel、Main、Float、Navigation and Actions。Kernel/facade absence or Runtime Identity mismatch is `reload-required` with no V3 fallback。RAW-159 remains the historical pagination、archive、diagnostics and hot-cache foundation；its conflicting V3 ownership/publication rules are superseded。
- RAW-162 makes Codex Goal status the completion boundary whenever a long-running Goal exists. An active Goal may span any number of automatic Turns；`turn/completed` closes only one round and must not publish task completed。Current input/approval evidence remains higher priority；paused、blocked、usageLimited and budgetLimited reuse stopped/“待继续”；only Goal complete may finish。Goal unknown、malformed、timeout or transient read failure preserves the last stable non-terminal phase as verifying；only explicit Goal cleared/no Goal or App Server method-not-found falls back to the existing Turn lifecycle。Goal Evidence is process-private and contains only finite status、updatedAt、freshness and causal sequence；Goal-only changes join the same atomic Kernel package without a public field、phase、Tab or badge。
- RAW-163 makes the main task the default presentation and navigation authority. Main phase/unread wins whenever it is running、waiting、stopped、unknown/verifying or completed-unread；Side Chat phase/unread joins the parent projection only after the main task is exact completed with known-read evidence。At that gate，a running Side Chat makes the parent running and a completed-unread Side Chat makes the parent completed-unread；without it，a child cannot overwrite the main task。Every card、badge、attention action、cycle and shortcut opens only the parent task Deep Link，never a Side Chat Deep Link。Branch role and unread evidence remain process-private and the Kernel commits the selected phase/unread atomically。
- Codex 原生 completed-unread 变化属于进程 Host authority：目录 `fs.watch` 首回调立即读取，已登记状态文件和 rollout decision 文件由 1 秒 `fs.watchFile` StatWatcher 补漏，watcher error 自动重建；Renderer 不持有 phase-only 周期恢复。persisted unread false→true 且任务仍 active 时，对同一匿名 key 定向复核 latest Turn，即使旧证据为 exact active/turn-started 也不能跳过；未读不直接虚构 terminal，更新正向序号仍拒绝迟到终态。私有 Branch Evidence 与同源公开 draft 在 Kernel 一次语义事务中提交，一次 source 变化最多一次 package revision，同值恢复信号零推送。
- 静默 uTools 入口必须由 Preload 的 `onPluginEnter` 直接消费，不依赖 Renderer 挂载或后续 Alt+Tab。新鲜进程包直接切换；冷/过期时只进行一次全启用 Provider 的 tasks-only 预检，等待全部来源，600ms 后提示、5 秒超时，任一来源失败保留旧包且不以部分集合跳转。普通 `mainHide` 保留内存包；功能停用、Provider 变化、真实进程退出清理；动态任务状态不持久化。
- 明确 waiting/active/completed/interrupted 不得被 debounce、quota、环境、完整库存或非任务 unread 阻塞；同 tick 只做 microtask 合并并在下一帧原子展示，可信 observedAt 到 package 发布 P95 ≤100ms。unknown 最多核验 250ms；无法确认的 interrupted/failed 保留最后稳定状态并标记 verifying，不强制 running。Codex 冷启动/重连冲突只对该任务 single-flight 精读一次 latest Turn。phase 与 unread 独立；同一 completion epoch 的 unread 保留到明确 read receipt 或新 Turn。Main 与 Float 必须应用同一 package revision。
- 任务库存与所有消费者不得使用产品级固定条数上限：Claude inventory、Kernel、操作、导航、mutation、批量归档均处理完整已接纳集合；Codex `thread/list limit=100` 仅为协议页大小，必须遍历至 cursor 结束并拒绝 cursor loop。数量增长只能影响分页和性能，不得改变卡片、Tab、角标或动作资格。
- 每次生产/uTools 构建生成确定性 `hostAssetId / rendererAssetId`，并携带 Kernel/Package revision。Main UI、Main Preload、Float UI、Float Preload 四端握手缺失或不一致时必须显示 `reload-required` 并停止任务动作，不得表现为无响应或继续运行旧逻辑。构建只表示 `artifact-ready`；只有真实 uTools 重新接入/安装产物并完成四端握手才表示 `host-loaded`。产品不调用私有 uTools API，也不自动结束插件后台进程。
- 水球额度映射随启用组合变化：仅 Codex 时与旧版完全一致；仅 Claude 时 Claude 独占整个水球；两者同时启用时外圈进度表示 Codex、球心百分比表示 Claude 并标注来源，Claude 未连接或无读数时百分比回退为 Codex 原样。
- 展开卡额度区按来源分区展示；Claude 分区在授权关闭、凭据不可用、Retry-After 或其它失败时显示对应安全原因，已有值保留但标为可能过期。
- 新设置 `claudeAppQuotaAccess` 默认关闭，旧已授权 quota fallback 配置迁移为开启。授权后 macOS 只读 Claude App `oauth:tokenCacheV2` 并以内存方式使用 Claude 专属 Safe Storage Keychain 项；账号/组织无法唯一仲裁时失败关闭，密钥、令牌和缓存明文不进入诊断/Renderer/持久化。
- 额度窗口由实际动态 limits 声明：`session`、`weekly_all`、`weekly_scoped` 映射为 5h、总周与稳定 scoped key，名称来自上游 Fable/Fable 5，`spend` 等非额度元数据不得显示。Node 16 通过显式 HTTPS 读取主权威，App history/statusline 只逐窗补充，不能抹掉 scoped 窗口、source、freshness 或 reset。启动/启用/恢复/网络、普通 cadence 与最早 reset+1s 唤醒；401/403 等凭据变化，429 遵循 Retry-After，其它失败按 1m/5m/15m/每小时退避。每个周限额显示剩余百分比，200ms 提示包含绝对/相对 reset 与 freshness，20%/10% 为警告/危险。
- 项目区只生成 EyPc 虚拟项目，不写两端原生项目。合并先按规范绝对路径对应的稳定 key，双方名称都唯一时才以名称兜底；重名歧义保持分离，Claude-only 项目批量加入，共享项目只显示一次。Projects 子页签提供会话级 `全部 / 只显示 Codex / 只显示 Claude`，同步过滤子任务并重算计数。Claude 任务支持打开、本地置顶、本地隐藏，以及仅在已明确核验的 macOS Claude `1.26832.0 / 1.28929.0` 门禁下的 completed/stopped 静默归档；可见“待继续”行直接提供任务级归档入口，但仍保留既有五秒二次确认。普通库存读取只在 Preload 内建立 `sessionId → 唯一 local_*.json` 私有索引；写前重新核验平台、版本、精确身份、phase 与文件 stat/hash，事务只把单一目标对象的 `isArchived` 改为 `true`，同目录临时文件核验后原子替换，禁止 Deep Link、AX/JXA、LevelDB、扫改目录和非目标会话。若过期索引仍可精确解析到当前唯一目标，普通 title/focus/activity 元数据 churn 可安全 rebase；只对写前 `source-changed` 在重新读取 phase 后重试一次，写后并发绝不重试。元数据为 true 且私有活动库存移除即为 `archived`，插件任务包立即精确移除并自动刷新；该结果只确认 EyPc 归档/移除，成功提示必须同时声明 Claude 原生侧栏可能仍待刷新且尚未确认同步。当前没有受支持的本地 Code 原生归档入口，因而不得用元数据/LevelDB、私有 IPC、AX/JXA/UI 自动化、重启或事后视觉结果宣称原生侧栏已及时收敛。App 日志仅作增强证据；失败安全恢复且返回 `failed`，检测到 Claude 并发修改或恢复不能确定时返回 `indeterminate`，两者均保留卡片。项目级归档、移除、移动等未支持能力继续禁用并解释，不能误调 Codex 动作。
- Claude 文件变化走独立 `CompanionTaskMutationDelta`：精确文件事件只重读已登记目标并立即向 Controller 发布 `archived/upsert/remove`；丢 callback 时，一秒 watchdog 也只核验私有索引中的登记文件。该通路不等待 quota、state、unread 或完整 inventory Promise，正常事件至最终原子任务包 P95 ≤250ms，漏通知恢复 ≤1.25s；手动在 Claude App 归档和 EyPc 静默归档共用同一 Controller mutation reducer。
- 来源视觉使用现有 Codex/Claude token：普通背景约 8%，悬停/选中约 12%；所有任务和项目显示文本化归属，不能只靠颜色。项目筛选使用原生 Tab/按钮键盘、焦点、`aria-selected` 和高对比度语义，并保持紧凑卡片高度。
- 事件钩子注册是插件唯一一次写入用户的 Claude 安装，需用户在设置中显式操作，保留用户已有钩子与状态栏并可干净卸载。对话正文、工具参数与凭证值永不进入观测、诊断或落盘文件。

## Codex Companion

Current increment authority and detailed requirement/implementation map: [260718/1148-codex-quota-float/spec.md](260718/1148-codex-quota-float/spec.md#current-requirement-and-implementation-map).

- Codex is an independently configurable feature Tab. General Settings controls feature enablement; the Codex page owns the 1–365 day inventory window, the `动态` Tab's 1–8760 hour activity filter and quota auto-refresh (`quotaRefreshSeconds`, default 300, integer 1–86400; legacy zero migrates to 300). There is no user-configurable full-inventory period, broad manual refresh or Ctrl+R task refresh. Trusted Provider push is primary; full inventory runs only for cold start, reconnect or an explicit membership gap. Targeted environment detection and Claude task sync remain. The page also owns companion visibility, appearance, readiness, diagnostics and shortcuts; the product title is “额度任务悬浮球” with eyebrow “CODEX · CLAUDE COMPANION”.
- Runtime diagnostics use the independent `eypc-runtime-diagnostics-v3` plain JSONL Host sink. Installation verification defaults to `{enabled:true, level:debug, userConfigured:false}`；manual error/info/debug/off choices persist. Every call supplies an explicit level. One session/seq/operation/trace timeline records Provider、session-only `h:<hex>` taskRef、before/after state、Turn/watermark/revision、cache、navigation/focus、duration/errorCode and action stages. Files rotate at 8 MB/file、64 MB total、14 days；the sink excludes raw task IDs/paths、prompt/Plan/execute instruction/conversation bodies、commands/argv/tool arguments、stdout/stderr、credentials/tokens、stacks and hidden reasoning. The read-only v2/v3 probe filters session/operation/trace/provider/taskRef/scope/event/level/since/tail and aggregates state changes、no-ops、shortcuts、navigation、archive stages and errors.
- Codex archive is commit-on-native-postcondition. One operationId covers intent、confirmation、preflight、one provider write、server verify-1、Desktop sync、matching native ACK when connected、server verify-2 after at least 300ms、Kernel commit and reconciliation/UI removal. RPC success、one transient list result or dispatched Desktop message cannot hide a card. Failure/indeterminate retains the card、button、alias/cache/shortcut target and notifies all surfaces with the short operationId. Claude/Cloud archive behavior is unchanged in RAW-159.
- External Codex archive membership is recovered from authoritative App Server inventories, not from archive broadcasts alone. The process Host watches exact `CODEX_HOME/sessions` and `archived_sessions` membership roots; a native rename triggers immediate full-cursor `thread/list archived:false/true` comparison, and one-second Node StatWatchers recover a dropped directory event within 1.25 seconds. A current anonymous key found only in archived inventory emits urgent `archivedKeys` and bypasses ordinary missing-row quarantine；ambiguous/missing membership only requests Codex tasks-only reconciliation. Plugin enter、Desktop IPC reconnect and watcher rebuild force one such tasks-only check。Dirty-thread `thread/read` recovery excludes archived inventory, and this external path cannot bypass the stricter EyPc-owned archive transaction above.
- Float liveness uses a two-second heartbeat, a six-second stall threshold, a sixty-second recreate cooldown and a ten-second recovery observation. Interactions use anonymous IDs and expire after ten seconds idle or on blur/lifecycle teardown, so a lost pointer/focus event cannot indefinitely hold drag/expanded state.
- The companion uses a real uTools child window and retains water-ball/compact-card skins, host-owned resize, automatic collapse and macOS all-Spaces behavior. In the expanded surface the four task Tabs (`动态 / 已完成 / 已隐藏 / 项目`) are the first content; the old water/card, hidden, refresh, settings and close toolbar does not render there, and quota is followed directly by source/task content rather than an Actions/Environment block. Pointer/focus departure or clicking elsewhere and blurring the child schedules collapse after about 220ms; only an actual composer, panel, alias editor, Quick Jump, Shift preview or resize interaction may defer it. Card appearance stores an explicit surface/foreground pair rather than forcing paper-white.
- Five Environment Action command IDs and one “打开 Action 执行工作台” entry are exposed as uTools global features; every slot synchronously opens/locates the independent Action Runner before its tasks-only preflight, while Codex Float has no Action UI or Action IPC. Runner is a frameless, explicitly draggable/resizable/optionally pinned child whose close control and Command/Ctrl+W only hide it. Its project-rooted tree omits a single Environment level; selected-Action history expands each new live run once, compresses older records to one line, supports reversible ended-record archive and exposes visible enabled controls through F/Shift+F Quick Jump. Action entries keep the main window's current Tab and rely on `plugin.json.mainHide` rather than scheduling a Renderer hide.
- Controller owns target priority, Runtime Actions, one-shot stale-alias recovery and the catalog's safe loading/error message. Five-slot Environment authority comes from the Host-persisted selected Lane: only a project with no selection defaults to its first Environment; a cross-project, stale or missing-slot selection blocks and asks the user to repair it. A dedicated Action Host runtime revision makes old/mixed preloads reload-required and execution-disabled. A tasks-only preflight may bypass Codex Tab/Float visibility when the feature is enabled, but does not read quota or start polling.
- Host separately owns the unique Environment `configRoot`, exact task/worktree `executionCwd`, per-run TOML/runtime reread, target identity, shell:false absolute launch, SIGTERM-only lifecycle, cross-chunk sanitized cursor output and cleanup-before-load SQLite history. Project `targetId` remains `projectKey` for Lane/SQLite compatibility; a task `targetId` includes a Host digest of its canonical worktree cwd, and vault/session/confirmation/run/stop all verify that identity. TOML accepts only the raw integer token `version = 1`. Only complete argv arrays for `pnpm|npm|yarn|bun run build|serve`, `vite build|serve` and `git push` enter the catalog; flags, extra tokens and shell syntax never reach the launch planner. Commands, cwd, PID, Node paths, environment values and unsanitized output stay Host-only.
- Ordinary `onPluginOut(false)` background hiding preserves live Actions and defers alias-clearing server close while the Runner or an Action is active. Only `onPluginOut(true)` performs process-exit cleanup: cancel pending restarts, frame/flush logs, persist live runs as interrupted, clear runtime authority, then send a non-force stop. POSIX stops the process group with SIGTERM; Windows uses `taskkill /PID … /T` without `/F` and falls back only to direct-child SIGTERM. Neither normal stop nor shutdown auto-escalates to SIGKILL.
- On macOS, Node-backed Environment Actions default to project `.nvmrc`, then `.node-version`, NVM default, another locally installed NVM version and finally a verified controlled system Node. A project may persist one Host-verified candidate override; Renderer receives only opaque candidate IDs, labels, versions and sources. An invalid or missing explicit project version fails closed unless the user selects an override. EyPc never sources `nvm.sh`, runs a login shell, accepts an arbitrary Node path, installs a runtime/package manager or treats the uTools/Electron executable as Node; npm/pnpm/yarn/vite launch through the selected absolute Node plus a verified JavaScript entry.
- Appearance configuration is one workbench with independent water-ball, expanded-card and status-signal targets. Every labeled token persists and renders directly; EyPc does not apply format, contrast, coupled-gamut, automatic adjustment, transient Controller preview or rollback validation. Built-in and saved themes still carry the complete token set, while the floating child remains display-only and contains no color editor.
- The compact water ball has no intermediate mini-detail or decorative surface rim. Its component root is transparent, its surface keeps only inset depth shading, and neither a same-size outer glow nor the host button's circular focus outline may draw a second full circle; keyboard focus is indicated on the center reading instead. Its upper half is a counter-safe zone and does not expand on hover; the three counters stay directly clickable and show child-owned opaque purpose help after 200ms hover/focus without expanding or switching tabs. Waiting-input includes both input and approval states; completed-unread uses its complete counted set, including hidden tasks. Ongoing strictly equals the unsearched, non-hidden `active / ongoing` cards inside the configured recent-hours filter, so approval is not double-counted. The Controller publishes one atomic task-state package containing stabilized conversations, mutually exclusive dynamic groups, all three counts, the next time boundary and compatibility diagnostics; Renderers never independently filter, count, clear or time task state. Waiting-input and completed-unread call the Controller attention action, whose hint says “最新优先，连续触发依次打开”; ongoing only expands. Successful Deep Link opening still acknowledges the current completion for this session; failure changes nothing. Zero counters hide and values above 99 display `99+`. The lower half, center quota choice, Spark mark, Weekly data ring and direct token rendering retain the existing geometry and appearance contracts.
- RAW-083/084/087/105/106/107/108/109/128/134/136/138/139/152/155 keep the compact geometry, Host-open acknowledgement and generic navigation contracts. “上一个/下一个 Codex 任务” selects the first non-empty tier—ordinary input/approval, exact Plan implementation, recent non-hidden active, then non-stopped local pin—and wraps only inside it. Every tier uses latest-question descending order; Provider/pin do not override. The process-only cursor advances per key; first dispatch is immediate and only in-flight presses collapse to the final trailing target. These commands never alter hidden/Tab/native state. A ready Host consumes a hot entry once; cold/reconnect/gap state uses tasks-only preflight and old Host revisions fail closed.
- RAW-140/144 refine that session acknowledgement boundary: the confirmed-open false belongs to the current preload process and a concrete Turn, not to one Desktop Bridge instance. Normal `mainHide/onPluginOut(false)`, IPC reset, resubscribe and refollow retain it even when Codex native unread remains stale true. Electron success and an explicitly accepted uTools fallback dispatch share this boundary; failure/rejection does not. The hint prefers an internal Turn ID, so a late `completedAt` correction for the same Turn cannot release it; a different/new Turn, exact active/inProgress, explicit task removal or process termination can. The bounded hint is private preload memory only and does not change Renderer logic, local storage or Codex native files.
- RAW-143 supersedes the ordinary-hide cold-cache portion without changing recovery authority: `mainHide/onPluginOut(false)` now preserves the App Server session, task/project aliases and latest-Turn cache, so a verified hot task alias opens with no inventory RPC. Cold inventory still runs after real teardown; concurrent stale aliases share one successfully published threads preflight. Action Runner/slots reuse verified project inventory and rebuild only after first use or explicit Host stale-alias rejection; every execution still revalidates target, TOML and command fingerprints.
- RAW-144 makes the Controller materialized task view feature-lifetime and incrementally live: changing the main Tab or Float visibility does not clear inventory, aliases, Activity generation or subscriptions, while quota/config polling remains surface-owned. RAW-152 narrows replaceable Renderer disposal to local timer/subscription cleanup plus Host-lease detach; feature/inbox disable, Provider changes, kill and process exit remain the semantic reset boundaries. Action Runner keeps a per-project catalog and reloads only added or re-aliased projects; unchanged projects remain hot, while Host execution continues exact safety revalidation.
- RAW-141 closes the equivalent pending-request owner-loss boundary. Exact Desktop request state remains strongest, but a new follower must not assume `following=true` includes a current snapshot after the original stream owner disappears. For interrupted/failed/inProgress inventory only, preload may recover an unmatched exact `request_user_input` from at most the last 4 MiB of a real file under `CODEX_HOME/sessions`; matching output or a later user message clears it. Previously observed input/approval/Plan shadows remain session-only across owner/transport loss and non-kill plugin hiding, ordinary active does not; a new snapshot, exact active/Turn/completion, newer inventory revision or removal supersedes the hint. No prompt, path, call identity, new public field, Renderer branch, native write or persisted task status is allowed.
- RAW-142 treats an exact Plan item in the latest completed Turn as an unimplemented user decision: it stays `waiting-input + planImplementationOnly` and outranks completed/completed-unread regardless of read state until a newer Turn starts. The fallback reads only bounded rollout event metadata under `CODEX_HOME/sessions`, while exact App Server Plan item/completion notifications use the same anonymous projection. During a transient native unread atom failure or full inventory replacement, the current Bridge retains its last successfully parsed native membership/nonmembership ahead of weaker snapshot true, so no incorrect unread frame is rendered before correction. No Renderer delay, public field, content disclosure or native write is added.
- RAW-145 established persisted-decision provenance for structurally verified finite input/Plan evidence；plain connector/Access hints remain non-authoritative。RAW-149 introduced anonymous state-entry time，RAW-151 bidirectional waiting，RAW-153 waiting-clear causality，RAW-154 the V9 action/mutation boundary，and RAW-160 advances the current bridge to `task-state-v10` with exact Turn mode、idle-confirmed interruption and revisioned Plan lifecycle。New exact active clears old waiting，but only exact default execution/abandonment/completion/archive/removal clears Plan readiness；Plan-edit Turns retain it。Older adapter revisions may contribute compatible evidence but cannot activate an older Kernel reducer；V4 identity mismatch is reload-required。
- RAW-149 extends the exact Desktop request set to command-execution, file-change and permissions approvals, MCP elicitation, ordinary user input and Plan implementation requests. A request-provided time is preferred; otherwise its first private observation time is stable across repeated snapshots. Removal/resolution immediately recomputes the latest unresolved time, and Side Chat aggregation takes the latest child/main request. The public boundary contains only `waitingSince/statusEnteredAt`, never request body, command, path, permission detail or raw identity. Access mode alone never creates waiting state.
- Waiting-input and waiting-approval both enter the input group/count, while approval is excluded from ongoing. Every visible status group is sorted by latest question time descending, then creation time and anonymous key; pins and Provider grouping do not override. Dedicated attention actions still persist at most 200 anonymous opened state instances and advance only after confirmed Host success. EyPc only opens the source task and never approves, rejects or submits the request.
- Easy Agent is not implemented yet. The interim adapter keeps two host channels: the local Codex App Server supplies quota, models, inventory, creation and verified persistent actions; a macOS Codex Desktop private-IPC companion bridge supplies live task/read authority and archive-refresh notification. The App Server connector is therefore still required in the current phase.
- CLI startup is independently diagnosable: EyPc auto-discovers only controlled macOS/Windows candidates and may store one user-selected, validated executable location in local plugin storage. The UI exposes only safe source/availability labels, never the actual path. Without a manual location it falls back to automatic discovery plus the existing App Server connector and explicitly warns about connection delay; this fallback never turns local cache or App Server activity into Input, ongoing or completed-unread authority. Windows supports CLI discovery/validated shim handling, while the private Desktop live bridge remains a macOS canary.
- Host Snapshot V2 reads the local Codex App Server plus an allowlisted native project registry. Ordinary inventory remains strictly read-only: the main `.codex-global-state.json` is authoritative and `.bak` is used only when the main file is missing/invalid. The sole write exception is an explicitly confirmed native project removal transaction; EyPc never scans Codex SQLite or LevelDB. A Codex Desktop full snapshot may be consumed transiently inside preload for live-state projection, but its bodies/items/raw identities never cross the bridge or persist.
- Every `archived=false` page is read to completion. Project ownership uses native thread assignment first, native projectless IDs as `Chats` second, deepest active project-root cwd match third; unmatched tasks are treated as removed/unregistered and excluded. Native state is fingerprinted before and after scanning and the entire scan retries once on change.
- Each candidate must have a valid newest Turn `startedAt`; there is no `updatedAt`/recency fallback. A malformed cursor, page, project state or existing Turn without `startedAt` fails the whole scan. Controller retains an older verified snapshot as explicitly stale, or shows an error empty state when no verified snapshot exists.
- A single otherwise-complete inventory that omits a published anonymous key is a transport-dropout candidate, not immediate deletion. Controller retains the previous projection and requests one Provider-only recheck. Reappearance, changed keys, failure, disablement or disposal resets the candidate. Codex explicit archive events bypass the ordinary hold only by starting/advancing task-scoped verification；local removal still requires native postconditions and Kernel commit. Verified project removal and existing Claude membership mutation retain their Provider-specific contracts. No task list or raw identity is persisted.
- Conversation projection V3 exposes four visible tabs: `动态 / 已完成 / 已隐藏 / 项目`. The `all` and `inputRequired` projections remain compatibility data for the registration text and compact counter first-task activation, but cannot render or route as separate tabs. The rolling-day boundary is inclusive and uses the latest supported Turn activity `max(startedAt, completedAt)` while still requiring `startedAt`; `updatedAt` never substitutes for Turn evidence. Source task arrays sort strictly by newest Turn `startedAt` descending then anonymous key. Each task Tab and each dynamic status segment visually places pinned tasks before unpinned tasks while preserving stable order inside both partitions; search only filters the active visible tab.
- `statusAuthority=desktop-live` is the strongest authority for `waiting-input / waiting-approval`; ordinary active also accepts the exact App Server authority defined below. A real runtime/request patch or fresh inProgress Turn establishes an active interval carrying only an anonymous local observation time; a repeated full-inventory projection must not refresh it. A follow subscription's first active snapshot is replay evidence, not by itself an observed active transition: when it has no waiting request but conflicts with a known terminal Turn, EyPc keeps conservative ongoing during the existing `[0,300,1000]` targeted reads and suppresses only that snapshot if the same shadow/activity revision remains unchanged and the final successful read is still terminal. Any activity patch, waiting request, new inProgress, mapping/revision change or read failure preserves/restores active. Desktop task selection may make the previous stream owner announce `following=false`; while that main task remains inventoried, or a Side Chat's parent remains inventoried, EyPc must retain the last exact owner shadow and request a replacement snapshot from that same owner instead of publishing connector fallback. If that retained shadow is active without an input/approval wait, the same bounded single-task latest-Turn reader must also reconcile a completion notification that raced with the selection change; only confirmed `turn-completed / targeted-after-exit / snapshot-corroborated` evidence may move it to completion, while plain inventory terminal, inProgress or read failure preserves active/ongoing. Only independent client-disconnect, bridge failure, archive or inventory removal revokes live authority. Provider `completedAt` is never ordered against the local active interval. RAW-160 supersedes the broad RAW-154 interruption edge：unresolved input/approval or a causally newer active Turn wins；active/terminal conflict preserves the non-terminal stable phase as `verifying`；an ordinary `interrupted/user-stopped` becomes stopped only after branch idle confirmation；an unexecuted Plan interruption requires targeted reread proving no newer Turn、activity or pending request and retains `planReady`。Missing outcome, raw transport states, elapsed time and recency never create completion or stop.
- RAW-124 refines the preceding active-authority sentence: waiting-input/approval still require exact Desktop request evidence, while ordinary active also accepts exact App Server `thread/status/changed=active` or fresh `turn/started=inProgress` as `statusAuthority=app-server-live`. This positive event overrides an older `interrupted + initial/refollow idle` projection and survives replayed snapshots plus inventory reconstruction; only a later exact terminal/non-active event or Desktop non-active activity patch revokes it. Connector inventory active alone remains non-authoritative.
- RAW-125 removes redundant state blockers around that authority. A live active event is not closed by a plain inventory completed shape; only `turn-completed`, `targeted-after-exit` or `snapshot-corroborated` completion provenance may beat a draining active projection. Exact App Server started and targeted latest-Turn inProgress may advance a same-`startedAt` completed/interrupted/failed revision, while strictly older startedAt remains rejected. An exact completed event requires completed plus startedAt but may omit completedAt, in which case startedAt is the completion revision. Initial/refollow conflict reads, active-exit baseline, structural coalescing and missing-key quarantine remain narrowly scoped debounce/inventory guards and never delay exact events.
- RAW-126 makes active-exit terminal arbitration single-owned. Activity deltas and verified full snapshots pass through the same Controller transition reducer, so an unchanged pre-active completed/interrupted/failed result cannot clear the open exit baseline or publish stopped; it remains conservative ongoing until a newer Turn outcome, confirmed `turn-completed / targeted-after-exit / snapshot-corroborated` evidence, or explicit Desktop not-running closes the epoch. A later Codex-native unread true for a non-active task whose latest Turn is not yet completed starts one bounded latest-Turn reread, but unread never directly creates activity or completion. A completed reread publishes with the current unread evidence in one anonymous delta; a later unread write upgrades it only through `readStateOnly`.
- RAW-127 removes the remaining terminal blockers without weakening evidence ordering. An exact same-revision completed notification may close an unresolved live-active epoch without `completedAt`; bounded active-snapshot corroboration likewise requires terminal plus `startedAt`, not a completion timestamp. Targeted/corroborated provenance is retained in the session inventory. Latest-Turn single-flight merges only compatible modes: active-exit, stale-active and snapshot-corroboration mode changes cancel the older request cycle and let the current state own the result. Persisted/live unread true may verify an ambiguous active row only when no exact `turn-started` or waiting request already proves current activity, and unread still never creates completion. Missing latest-Turn outcome remains ongoing even under idle/not-running; stopped requires explicit failed/interrupted plus exact idle/not-running.
- RAW-128 completes the cross-layer audit, with its opening edge refined by RAW-138/139. Codex live/persisted read-state remains the external unread authority and legacy persistent EyPc completion acknowledgements are ignored. Every task-open command uses one Host Deep-Link path; cold commands restore tasks first and stale card aliases stay bound to the same anonymous key. Only confirmed success adds a session-only exact false, without writing Codex native state, and a later completion removes the older false before reconciling its new unread epoch. Every Activity Delta emit advances one generation and verified full snapshots carry the assembled generation barrier. Full inventory rebuild preserves exact live inProgress and confirmed terminal provenance. A mixed delta applies known entries while unknown keys request urgent structural reconciliation. Missing-key quarantine retains only absent rows, so present rows publish completion/unread immediately. Repeated identical active snapshots reuse one bounded corroboration cycle. An initially true or newly true native unread observation may start one cycle; unchanged polling does not restart it. Task-switch ambiguity, activity/mapping change or mode change may replace an incompatible cycle. The shared active-exit reducer reads confirmed provenance directly for both delta and full-snapshot candidates; ordinary completion advances only from a newer cache result or already confirmed provenance. These guards debounce evidence acquisition and inventory membership only; none delays accepted state.
- RAW-130 closes the remaining cross-source ordering gap inside preload. Every true Desktop activity patch and every accepted exact App Server active/Turn-started event receives one process-local monotonic live-evidence sequence. A Desktop non-active event may revoke `app-server-live` only when its sequence is strictly later than the App Server waterline; an older activity shape replayed by read-state publication, Side Chat aggregation, refollow or full inventory reconstruction cannot. Exact App Server terminal/non-active evidence and targeted terminal evidence still revoke directly. Full inventory preserves the private waterline, while initial/refollow snapshots and read-state-only patches mint no sequence. Neither sequence nor raw identity enters the Renderer payload, persistence or public API.
- RAW-131 closes the remaining false-stop and blocking edges across preload, Controller, projection and archive. A bounded latest-Turn reader records the parent live-evidence sequence and may publish stale-active terminal evidence only while that sequence is unchanged and no exact positive activity exists; Side Chat readers query the child raw identity while updating the anonymous parent, and inventory reconstruction re-aggregates every known child. An initial active snapshot that still conflicts with failed/interrupted evidence projects unavailable live authority instead of synthetic idle, so uncertainty stays ongoing rather than stopped. Every exact activity patch whose resulting state is active opens a new live epoch even across active→active; a finite waiting request such as Plan confirmation additionally outranks older completed evidence. Preload retains a previously published missing row's anonymous activity mapping for up to 120 seconds only while the source fingerprint is unchanged, covering Controller's missing-key quarantine without exposing a placeholder task; explicit archive still removes it immediately. Controller treats Activity generation as a bidirectional monotonic barrier: an older same-source delta cannot update even Desktop bridge state, and after a live generation is accepted a lower or generationless V2 full snapshot cannot lower the waterline or overwrite newer state. 更新引入：RAW-150 只取代 RAW-131 的历史 `blocked-stopped` 归档结论；显式 stopped 现允许任务级归档，但 Host 必须重读精确任务/latest Turn，边界消失或恢复运行时返回 `state-changed`。
- RAW-132 requires regression-safe parent aggregation. One pure resolver owns the priority of main activity, Side Chat activity, waiting requests, system error and exact App Server live. A terminal latest-Turn result from one child is branch-scoped: while the main task, another child, or an unattributed exact App Server event remains active, the anonymous parent must remain `active/inProgress` and that child terminal is deferred. Session diagnostics may expose only aggregate non-negative counters for live epochs, discarded stale reads, deferred branch terminals, suppressed conflicting snapshots and retained missing mappings; the counters obey the same source-fingerprint/generation barrier and contain no task identity or content. This optimization must not weaken any RAW-131 positive-sequence, synthetic-idle, missing-row or generation invariant；stopped 归档能力由 RAW-150 的写前复核单独约束。
- RAW-133 makes the diagnostic package a single Domain-owned contract: one key list drives non-negative safe-integer normalization and equality, while Controller accepts it only after the existing source/generation barrier. A changed diagnostics-only delta produces exactly one view notification; unchanged, mismatched-source and stale-generation packages produce none. The Runtime view keeps only a compact protection/epoch summary visible, exposes the five totals through focusable native help, and excludes internal counters from the status live region. Tests must invoke the production parent resolver rather than maintain a second aggregation algorithm.
- `动态` is the stable persisted `ongoing` tab. The task package normally filters non-hidden/non-paused tasks inside `dynamicTaskWindowHours`, creates mutually exclusive `待输入 → 正在进行中 → 待继续 → 已完成未读 → 已完成` segments and supplies counts. The only hour-window exception is `stopped + planReady + !paused`, which remains in 待继续 and its segment count while still respecting the wider inventory retention window；ordinary stopped expires normally and there is no compact stopped badge。Every segment orders rows by latest question time descending, then creation time and anonymous key; pins/provider grouping never override. Search filters rendered rows only and never changes package counts.
- Known App Server Turn notifications may take the shortest positive path. `thread/started` reads its raw identity from `params.thread.id` only inside preload and marks exactly that new task dirty. If that dirty identity is not yet present in a lagging `thread/list`, the same verified scan may perform one bounded exact `thread/read(includeTurns=false)` and accept it only when identity/status are valid and all ordinary project ownership, newest-Turn, anonymous-key/action-alias and fingerprint checks pass. Thus registration reuses other tasks' session latest-Turn cache without creating a placeholder or count-only active row. For an already inventoried task, complete fresh `turn/started` inProgress evidence updates the cache and anonymous activity immediately; the same revision may recover interrupted/failed to inProgress but must not regress completed. Preload allowlists only `status / startedAt / completedAt`; `turn/completed` immediately emits anonymous confirmed Turn evidence for a newer revision, a same-revision non-completed → completed transition, a later completion timestamp, or a same-revision completed result that closes a still-unresolved live epoch. This settles the branch's Turn cache only：RAW-162 Goal evidence still owns the parent task completion boundary and can suppress that terminal from the public task package. The Turn result may omit `completedAt`; a duplicate after confirmed completion is not treated as a new unread epoch. Provider `completedAt` must not be compared with the local `desktopActiveSince` clock, whose precision and replay order differ; malformed, strictly older, unknown or non-target notifications retain the existing urgent dirty-task reconciliation and bounded targeted retry. Raw thread/Turn IDs, items, bodies and errors never cross preload.
- Desktop active exit and exact completion use cancellable task-scoped latest-Turn confirmation; newer active/waiting cancels older reads. Exact completed/interrupted evidence closes a stale active shell only when no causally newer activity exists. There is no presentation delay or task refresh period. Structural coalescing, push watchdog and missing-key quarantine are recovery protections; trusted push never waits for or triggers full inventory unless it exposes a membership gap.
- Waiting edges keep their bidirectional task-scoped reducer and one-second phase-only recovery watchdog. It excludes unread, quota, inventory and full latest-Turn I/O, remains active across Tabs/hidden Float, and has no user-adjustable frequency. Failures preserve conservative state; normal event publication targets P95 ≤250ms and a dropped callback recovers within 1.25 seconds without duplicate grouping or broad scans.
- RAW-153 adds the missing active-vs-active causal rule to that lane. Every private main/Side Chat request instance and runtime waiting flag receives a monotonic observation sequence; request removal, a matching `serverRequest/resolved`, newer `thread/status/changed active` or `turn/started`, matching output, user continuation and new `task_started` establish a waiting-clear barrier. An older full snapshot, read-state, unrelated patch, refollow, sticky shadow or rollout resume cannot cross the barrier and revive the cleared wait. A truly new correlation observed after it enters waiting immediately. Runtime waiting-flag removal uses the same rule. `desktop-live + active` is reusable only when no visible waiting flag remains; a current-owner old wait must yield to newer exact running evidence. An unmatched resolved event starts only the existing target-task bounded resubscribe and never clears another concurrent approval. The barrier, request identity/hash and observation sequence remain preload-private; no public field or persistence is added.
- Positive and negative task changes use different timing. Complete fresh started/completed notifications for an inventoried task use the direct paths above; `thread/started`, unknown-task or incomplete/stale Turn notifications, unknown-task status and an unregistered Desktop main-task snapshot request an urgent structural refresh coalesced for 50ms. An event that arrives during a scan schedules one follow-up instead of waiting for the normal period. Preload marks only the raw thread dirty, reuses its session-only verified latest-Turn cache for unchanged tasks, and rereads dirty/new tasks for that event scan. With no dirty event, the configured full-inventory cycle still rereads every eligible latest Turn and catches missed events. It is structural reconciliation, not the waiting-edge lane.
- An unregistered Desktop shadow may remain transiently inside preload so a brand-new task already waiting for input can publish that exact live state as soon as verified inventory or the exact dirty-registration path establishes its anonymous key, project and short-lived action alias. A single empty/lagging inventory cannot delete a never-registered shadow while it remains exact live active; exact-read failure/identity mismatch publishes nothing and the shadow may wait for a later real list, while a terminal/archived/disconnected shadow and any task that disappears after prior registration still clear through the existing gates. It cannot create a placeholder or cross raw identity/content into Renderer. Accepted exact, targeted or snapshot-corroborated completion publishes immediately with no presentation hold; plain inventory terminal remains unconfirmed state evidence and cannot close a live epoch. Missing old keys, regressive evidence and unconfirmed terminal states keep the RAW-090/091 stability rules.
- A completed Plan awaiting user confirmation or implementation is waiting-input, not completed. Current Desktop expresses this as an unresolved `item/plan/requestImplementation` or an exact latest completed Plan. `planReady` and `planLifecycleRevision` survive Plan edits、refresh、owner changes、refollow and restart；only an exact default/non-Plan execution Turn、explicit abandonment、completion、archive or removal clears them。An unexecuted exact interruption becomes stopped only after targeted no-newer-activity/pending verification. Waiting Plan may age out of the expanded dynamic list but remains in the input badge and shared Plan cycle；stopped Plan-ready alone bypasses the dynamic-hour window。Plan/request content and raw identities never cross the preload boundary.
- Desktop live stream continuity must not depend on understanding the full private conversation schema. Preload strictly parses only `threadRuntimeStatus`, `requests`, `hasUnreadTurn` and `resumeState`; any other well-formed patch root is ignored while the same stream revision advances. Only malformed observed-state patches, owner/revision mismatch or incompatible frames may trigger resubscription. This prevents private Turn/tool/body churn from deleting a valid shadow and reviving an old active snapshot, while preserving conservative ongoing, task-count stability and privacy rules.
- `task-state-v10` identifies the current evidence and Plan-lifecycle contract. It retains v9 request/waiting privacy and causal barriers but adds `idleConfirmed`、`turnMode`、`planReady`、`planLifecycleRevision` and local `paused` evidence。Provider adapters never output final groups/counts；`companion-task-kernel-v4` alone reduces them。Missing、older or future Kernel/package identities do not invoke a degraded state reducer：the existing last complete package is retained only for display and task actions fail closed with `reload-required`。Pause receipts contain only a hashed task reference、Plan revision、paused and time；request bodies、Plan content、raw IDs/paths、correlations and execute prompt remain private。
- Execute Plan is a Codex-only Actions v2 capability. An exact completed actionable Plan remains pause/resume/execute-capable even if Codex did not expose a dedicated `Implement Plan` request；`planImplementation` controls cycle priority only。The first click creates a five-second in-place confirmation and sends no RPC；the second establishes a Plan-revision single-flight, revalidates the same anonymous key and Plan revision, latest Turn, real work and other pending requests, renews an expired alias only for that key, opens the original task and calls `thread/resume({excludeTurns:true})`。Default collaboration mode and current-model discovery only select the optimized native path；they are not menu gates。It then calls `turn/start` exactly once：prefer a full default CollaborationMode object preserving model/reasoning effort when confirmed，otherwise send the private fixed implementation instruction to the same task without the optional mode object。Success is projected only from the exact new Turn；timeout is indeterminate and triggers targeted reread without blind resend。Clipboard、keyboard、UI automation and replacement sessions are forbidden fallbacks.
- The Kernel exposes one process `getLatest/subscribe(afterRevision)` cache. Package revision/publishedAt advances only for consumer-visible membership、phase/freshness/unread、Plan/pause、visibility/capability/action token、group/count/cycle/attention/order or required metadata changes。Main、Float、Navigation and Actions each reject old/same revisions and equal selector fingerprints；equivalent polling/refollow/ACK only advances private watermarks。Float task packages use an independent received/applied/rejected ACK lane：500ms permits one latest-package resend，and only two missed applications totaling 1s with a healthy heartbeat permit controlled recreate。
- A compatible Desktop `thread-archived` broadcast or App Server `thread/archived` notification can cross preload only as an already-published anonymous key. The Controller immediately removes that exact key and queues an urgent verified inventory read; unmapped, unarchive, delete and malformed events cannot remove a task, and normal inventory omissions continue through the dropout quarantine.
- When the conversation snapshot is verified, its registration text only says `最近 N 天的 M 条`; raw-source and registered-source counts remain diagnostics, not user-facing float text.
- The project Tab mirrors native `Pinned / Projects / Chats`: native pinned conversations/projects first, then clearly marked EyPc local pins; remaining native projects follow project order and include empty projects; Chats contains only native projectless tasks. No task appears twice. A project-name search match shows all its in-window tasks, otherwise only matching tasks remain.
- Task and project aliases are display/search metadata only; a list row shows the alias when present and otherwise the original name, never a placeholder caused by a missing display field. The original name remains searchable and appears in detail/Shift preview when an alias exists. Original identity still governs ownership and archive. Native order is read-only. EyPc local pins can be reordered with `Alt+↑/↓`; task pins immediately move the row into the pinned partition of the current task/status view, while project pins enter `Pinned`. Pin source is expressed by the fixed `顶` control and its 200ms help, never by row-tail “本地顶” text: local uses warning color, native/Chats remain focusable with `aria-disabled` and every input route shares the read-only gate. Project-page `隐/显` is reversible local grouping visibility and never removes its tasks from other Tabs or counts. Project `移` is a fail-closed native Codex sidebar-removal transaction and never deletes the directory, assignments or conversations.
- First use defaults to `动态` while retaining `ongoing` as its stable persisted ID. Last Tab, project collapse, aliases, local pin order, local project-hide set, full-inventory day window, dynamic activity hours and display geometry persist. Legacy local-remove sets are discarded on migration without changing Codex. Search, selection, focus and confirmation do not persist. Storage uses hashed task keys and stable project fingerprints; raw IDs, paths, action aliases and task lists remain memory-only.
- Task rows keep four fixed slots: ordinary tasks `顶 / 隐 / 归 / +`；Plan-ready `顶 / 暂 / 归 / 执`；paused Plan `顶 / 恢 / 归 / 执`。Project rows keep `顶 / 移（确）/ 隐（显）/ +`。Every action owns an opaque `24px` slot，disabled actions retain the slot and expose reason/ARIA/focus restoration。Plan new-session stays in the full drawer；batch hide converts Plan-ready targets to pause and batch pause/resume share package capabilities。Projected ongoing tasks keep archive disabled；stopped archive retains Provider-specific preflight。Only `archived` removes；failed/indeterminate retain the row and target reconciliation。The 已隐藏 tab renders 已暂停 before ordinary hidden，and migration from hidden Plan commits pause before clearing hidden。
- The Codex floating child does not mount the main product Tooltip layer and never uses native `title`. The compact quota surface has no explanatory bubble. Privacy-safe task detail appears in an opaque card after 500ms, while task status and short-character action explanations use a child-owned opaque hint after 200ms. Full action access remains available through the drawer, pure-Shift preview, ARIA and Quick Jump; other main-app surfaces continue using the shared Tooltip owner.
- Destructive operations require same-position confirmation within five seconds. With no active selection, a task title opens Codex while Ctrl/Cmd+title selects without opening, and the 38px full-height left rectangle starts selection. The project/status/minutes metadata row only focuses and highlights the task so profile shortcuts such as Ctrl+T inherit its project. Once any task is selected, both core and left clicks toggle membership; removing the last member exits selection mode. Its `选择模式 / 已选 N 项 / Esc 退出` cue is an absolute bottom overlay inside the task-list stage, never a normal-flow top row, so entry/exit cannot shift the list top or visible height. The scroll area reserves its bottom-safe space, de-emphasizes unselected rows and gives selected rows an accent/running/pending theme gradient with stronger hover/focus/active. The left rectangle always retains the task-state icon and synchronizes `aria-pressed`. Task rows, left buttons and right action buttons own Space/Enter separately so a child action runs once without changing selection. Search/Tab/refresh removes invisible selections.
- Two or more visible selections automatically show an absolute `已选 N / 归 / 操 / 清` toolbar. It anchors at the opposite top/bottom edge from the focused or last-selected row and recomputes on selection, focus, scroll and resize without changing task DOM order, row coordinates or list height. When the toolbar is at the bottom during selection mode, it lifts above the bottom mode cue and the scroll-safe area covers both overlays.
- One item is highlighted per Tab. Arrow movement temporarily owns highlight until the pointer actually moves again. Right-clicking an unselected task first makes it the sole selection; right-clicking a selected task preserves multi-selection, while a project target clears task selection. Right-click and Ctrl+right then open the same full action drawer; its arrows move among actions and Enter/Ctrl+1…9 execute. Batch actions never expose operations that lack batch semantics.
- The isolated Codex shortcut domain adds settings-visible, rebindable `codex.thread.createFocused` on `Ctrl+T` (displayed as `c-t`) and continues to own arrows, Space, Enter, Ctrl+left/right, Delete, F2, Ctrl+P, Alt+up/down, Ctrl+F/R, Ctrl+1…9, F/Shift+F Quick Jump and Escape. Conflicts are reported only when Tab, layer and `when` can be true together. The child resolves the effective bindings with the same `when`/layer precedence while locally owning DOM focus, `codex-composer` input role and transient layers.
- Pure Shift opens a non-focus-stealing task preview: hovered task wins, otherwise the keyboard-highlighted task is used. Shift+up/down moves highlight and preview without changing selection; actual mouse movement restores hover ownership. Ctrl/Cmd/Alt+Shift, editable focus, composer, confirmation, drawer/detail, Quick Jump and model layers suppress it. The preview contains only name/original name, project, state/active flags, creation/first/latest/completion times, duration, source, hidden/pinned and archive capability; it never reads body, summary, raw ID, cwd or path.
- Escape unwinds confirmation → composer/model → Quick Jump → Shift preview → inline edit → detail → more actions → multi-selection → search → collapse. A single-item detail always returns to the same target's more-actions drawer and focuses “查看详情”; the next Escape closes to the original row. Ctrl+left/right layer switches retain that original trigger. Batch drawers have no detail child and close in one step. Missing targets/triggers fall back to a visible task or the list container. Shift preview closes on key release, window blur, collapse, Tab switch or another layer; an Escape-closed preview stays suppressed until Shift is released.
- Quick Jump excludes targets covered by composer/drawer/preview/masks, clipped by an ancestor, outside the viewport, `pointer-events:none`, or absent from the hit stack. Opening it closes Shift preview. Row markers transfer the single highlight; fixed action markers execute the corresponding row action through the same confirmation and availability gates. Normal badges use dark opaque backgrounds with bold white text and white outlines; the active target uses yellow with dark text and outline.
- Every new-thread entry opens a composer showing target project, frozen current model name/ID, selection reason and corresponding quota. The native multiline field autofocuses and supports system dictation; Enter inserts a newline, Ctrl/Cmd+Enter submits, Tab/Shift+Tab stay inside, and Escape cancels/clears/restores focus. Users may send-and-open or create an empty thread; a changed quota/catalog/project fingerprint refreshes the explanation and requires a second confirmation.
- The default model policy is `quota-auto`: ordinary quota uses configured `newThreadPreferredModel`, otherwise the catalog default/first non-Spark model; any actually returned ordinary window at zero selects the highest available Spark pool/model, while a missing window is not zero. If Spark should be used but its model/quota is unavailable, manual selection is required. A composer override affects only that thread.
- The dedicated transient bridge calls `thread/start` with exact cwd/model and `allowProviderModelFallback=false`, validates the response's top-level actual model/cwd, then optionally calls `turn/start` and opens a thread-only Deep Link. First-turn failure cleans the zero-turn thread and retains only the in-memory draft; uncertain cleanup disables automatic retry. A started turn survives Deep Link failure and receives only a short-lived reopen alias. App Server unavailability never copies or encodes the prompt and only offers an explicit blank-Codex action.
- The composer accepts one PNG/JPEG/WebP reference image by paste, file selection or drag-and-drop, retaining the attachment and preview URL only in memory. Because the current App Server catalog is text-only, image mode never calls `thread/start` or `turn/start`; the explicit fallback action copies only the user's text through the constrained float bridge and opens a blank Codex session for manual image paste and model selection.
- The Desktop bridge consumes versioned initialize, thread snapshot/patch/follow, request and read-state events through a secure loopback Unix socket. Socket owner/mode and protocol versions fail closed. Its watchdog runs every 5s and temporarily uses 1s after three consecutive failures; Activity Delta V1 remains connector-only compatibility. Active exit additionally triggers the bounded single-task Turn confirmation above, without exposing raw identity or content.
- Completed-unread requires both a completed newest Turn and current unread evidence. Exact live read-state events and a confirmed successful Host-open acknowledgement win. Otherwise a current initial/refollow snapshot `false` may repair stale native-set membership after EyPc missed a disconnected read event；a parseable native set's membership/nonmembership still outranks snapshot `true`，and snapshots are the full fallback only when that native source is unavailable。Preload watches the native state directory read-only and publishes file changes as anonymous V2 `readStateOnly` entries；the private Kernel branch ledger keeps each main/Side unread observation separate until final projection。A non-completed-read main task keeps its own phase/unread；only a completed-read main permits a Side Chat's running、waiting or completed-unread state to project onto the parent。Every plugin card/counter/shortcut task-open path converges on the parent-only Host Deep Link：a cold shortcut first restores tasks-only inventory，and a stale card alias is rebuilt only for its same anonymous parent key；only confirmed success marks the parent and known Side Chats read for the current session，while failure or unconfirmed dispatch does nothing。It never writes Codex native unread or a persistent local receipt；hide/restore remains unrelated，and a later completion clears pre-completion false evidence。The compact surface uses the atomic package's complete waiting-input count、configured recent-hours non-hidden active-group count and complete completed-unread count，capped at `99+`；stopped tasks do not enter any compact counter。
- Single archive is exposed only for a completed newest Turn and accepts a short-lived action alias plus expected completed revision/latest-Turn/source fingerprint. It rereads identity/state/recency/Turn/native fingerprint, requires the same exact completed revision, archives, then verifies absence from `archived=false` and presence in `archived=true`. After verification it dispatches versioned `thread-archived` to the connected Codex Desktop; dispatch failure is reported but does not roll back verified persistence. Failure before verification never optimistically removes a row.
- Project “归档已完成任务” ignores the display window and rescans all unarchived history for that project. It skips every non-completed, missing or malformed Turn as ongoing work and processes only completed candidates 20 per batch at concurrency two with per-task dual verification; partial failures remain visible with individual results. Real project-batch mutation requires separate temporary-project authorization.
- Twelve built-in appearance presets plus custom theme/water settings remain available. Water ball, expanded-card and status-signal colors persist and render directly through the appearance workbench; reduced motion removes wave movement.
- Renderer-facing snapshots/actions never include raw thread/Turn IDs, paths, cursors, Turn items, account/session IDs, tokens, native state objects or transcript content. Desktop full snapshots are transient preload input only and are reduced to anonymous live/unread authority before crossing the port. New-thread requests carry only a hashed project key, short-lived project alias, project/context fingerprints, model ID and transient prompt. The text-only prompt never enters general actions, snapshots, logs, storage, process documents, error memory or Deep Links and is cleared after success/cancel/close; clipboard use is limited to the explicit user-triggered image fallback, while raw identity/path mapping exists only in preload memory.
- Codex startup must not depend on a GUI host inheriting shell/NVM PATH. Controlled JavaScript CLI wrappers use their verified sibling Node runtime; startup failures expose only a bounded error code/message and never raw stderr, command paths or environment values.
- When the Codex feature is enabled, first app launch performs a privacy-minimized macOS/Windows readiness inspection even when its Tab and float are inactive. It reports platform support, controlled CLI/runtime discovery, observational related-process state and controlled config-file metadata; if the feature starts disabled, inspection waits until enable. The configuration Tab keeps separate text guidance for runtime, process, config and App Server failures and provides a fresh reinspection action.
- Readiness diagnostics tolerate additive uTools preload version skew. If a legacy desktop preload has the Codex snapshot bridge but lacks the newer inspection method, EyPc infers only the host platform category and waits for an App Server round-trip before treating the CLI as verified. A successful read becomes authoritative availability evidence; the page must never display system/CLI “unsupported” beside loaded configuration or a connected App Server.
- Windows discovery covers controlled npm-global, local, Volta, NVM and inherited-Path candidates. A `.cmd`/`.bat` shim is never direct-spawned through a shell: it must resolve to a known Codex JavaScript entry plus a verified absolute Node runtime, or to a verified bundled native Codex binary. Environment snapshots never expose paths, PIDs, command lines, raw process output, config contents, credentials or proxy material.
- On macOS, Codex startup must also support an active local static PAC when the GUI host has no explicit proxy environment. Discovery is bounded, loopback-only and child-only: PAC code is never evaluated, proxy material never crosses into UI/storage/logs, and EyPc does not change system network settings.
- On macOS, the companion is always-on-top and visible on every ordinary and full-screen Space of its current display. The host reapplies this after creation, display migration and geometry rebuild; the Codex page shows only privacy-safe supported/enabled diagnostics. Real multi-Space interaction remains a host acceptance gate.
- One `codex.float.toggle` action owns companion visibility. Inside EyPc it defaults to `Command+Option+Q` on macOS or `Ctrl+Alt+Q` on Windows and remains available across Tabs and ordinary editors. A static `eypc-codex-toggle` uTools feature exposes the same action for system-global binding. A separate `codex.float.activate` action and `eypc-codex-activate` entry default to `Ctrl+Alt+Enter` and directly show, expand and focus the internal card for keyboard selection.
- Renderer contracts remain behind Host Snapshot V2, Conversation projection V3, Activity Delta V2 and platform-owned read/open/archive/float ports. A future Easy Agent adapter may replace both interim App Server and Desktop companion channels while keeping anonymous identities, four visible Tabs, compatibility projections, local metadata and confirmation behavior stable.

## Settings

- Settings owns feature visibility/order, shortcut profiles, shortcut conflict/reservation visibility, storage status, and shared tool preview preferences.
- Shortcut edits are Settings-local drafts until saved as runtime shortcut profile updates.
- Non-editing shortcut command rows support left detail, right actions and right-click actions locally; input/select controls retain native editing behavior.

## Documentation

- Process hub: [PROJECT_STATUS.md](PROJECT_STATUS.md#L1).
- MQTT implementation sync: [2606231645-eypc-mqtt-websocket-tab/06-sync-doc.md](2606231645-eypc-mqtt-websocket-tab/06-sync-doc.md#L1).
- Architecture memory: [../knowledge/ARCHITECTURE.md](../knowledge/ARCHITECTURE.md#L1).
- Technical memory: [../knowledge/technical-details.md](../knowledge/technical-details.md#L1).
- Error memory: [../knowledge/error-memory.md](../knowledge/error-memory.md#L1).
- Project soul: [../knowledge/developer-soul.md](../knowledge/developer-soul.md#L1).
