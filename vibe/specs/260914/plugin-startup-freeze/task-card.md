# 插件启动卡死排查与悬浮窗竞态修复

## 目标与状态

- 目标：定位加载后失去响应的阻塞链，修复已证实的启动缺陷。
- 当前：启动竞态聚焦测试通过，但 2026-09-15 真实宿主复验失败，`focused-automated-verified / host-reproduced-failure`；主要卡顿未解决。
- 用户授权结束已核实的 EyPc 渲染进程后，已确认该进程退出，uTools 主进程保留。未扩大进程处理范围。

## 证据与边界

- 活进程的连续采样显示渲染主线程从 JavaScript 进入原生等待，未能恢复具体 JavaScript 调用名。
- 该进程打开的插件 asar 与当前主入口构建身份一致。两次启动诊断均在悬浮窗身份不匹配和受控重建之后停止继续更新。
- 确定的源码缺陷：Float 在首个主插件快照尚未到达时，把缺失快照判为身份不匹配并请求重建。异步快照先后顺序因此影响窗口生命周期。
- 另一个生命周期缺陷：窗口加载完成及故障回调引用可变的当前窗口，旧回调可能操作或重建后来创建的窗口。
- 上述证据可确认启动竞态，不能单独证明用户此次原生阻塞的全部根因。未记录私有任务内容或原始采样文本。

## 实现

- [FloatApp](../../../../src/FloatApp.vue#L196)：连接等待和已确认身份不匹配分别判断；等待时继续阻止任务动作，真实不匹配仍请求恢复。
- [Float bridge](../../../../preload/codex/float-bridge.cjs#L556)：创建代际隔离加载和故障回调，关闭前失效旧回调。
- [使用说明](../../../../src/help/guides/codex.md#L7)：说明「正在连接」与「需要重载」。

## 验证选择

- VerificationImpactTrace：Float 挂载 → 首快照与身份判断 → 重建请求；窗口创建/关闭 → 异步回调 → 当前窗口。仅选择这些传播边界。
- 聚焦新增回归：首快照延迟时不重建、正确快照到达后解除等待、确实不同版本时仍重建；旧窗口回调在重建和关闭后均无效。2/2 通过。
- 相关套件：codexCompanion UI 与 codexFloatWindowBridge，90/90 通过。夹具反复加载产生 StatWatcher 监听数警告，不将通过结果解释为原生宿主验收。
- 构建升级依据：修改 Renderer 与 Preload 的构建身份输入，必须同步实际插件产物和版本真值。
- 构建：合同校验、类型检查、生产构建、uTools 产物校验及 92 对镜像校验通过；需求真值已同步。
- 产物：EyPc V7；`host-bbd433e1d90d4b1cc58b / renderer-5c78ebd1efc8929bada4`；北京时间 `2026/09/14 21:41:54`。入口 [dist/plugin.json](../../../../dist/plugin.json#L1)。
- 未运行：全仓测试、真实 uTools 重载和交互验收。已有 Claude 未提交修改保留并随当前工作区构建；不将其归入本任务修复。未提交、推送或替换已安装 asar。

## 下一步

按用户要求保持插件屏蔽，只做代码和离线验证；真实宿主复验留待用户恢复测试时进行。不能以旧包、缓存或聚焦回归通过解释现场卡顿。

## 2026-09-15 真实宿主复验

- 用户重启后再次请求现场验证。进程与主入口身份握手确认已加载上述 `host-bbd433e1d90d4b1cc58b / renderer-5c78ebd1efc8929bada4`，不是旧版。
- 新进程启动诊断仅 24 条，首末事件相隔 1,296 ms，末尾为任务包 applied ACK。未出现上次的 `float-identity-mismatch`，仍出现 `plugin-enter` 触发的受控重建。
- 原生采样再次显示 EyPc 渲染主线程从 JavaScript native accessor 进入 `kevent64` 等待；仍无法从符号化结果识别具体 JavaScript 行或断定是哪一个 SDK 方法。
- uTools Float 的实时 AX 显示「需要重载」「Float 与主插件运行版本不一致」。主入口握手匹配与 Float 的异常提示必须分开看，后者不能反推安装包过旧。
- 后续复采样时目标渲染进程已退出，uTools 主程序仍在；本轮未发送结束进程命令，退出原因未确定。
- 本轮只更新复验证据，未继续做猜测性代码修补、重新构建或替换已安装包。上一节未运行项为 09-14 构建时的历史边界；本节更新真实宿主验收为失败。

## 最近变更的离线排查

- 用户将自行屏蔽插件以恢复正常使用；后续限制为代码和离线回放，不再启动、重载或操作真实宿主。
- 最近三次代码变更为 `2d23da7`（Orca 未读和原生标签读取）、`1e74f4b`（置顶及证据接入）、`418d094`（打开选定与分组切换）。
- 确定的性能缺陷位于 `2d23da7` 新增的 [unread-bridge.cjs](../../../../preload/orca/unread-bridge.cjs#L124)：observe 逐行调用 remember，每次都序列化并写入整份账本。done 记录未发生任何变化时仍写入。默认 store 使用同步 uTools dbStorage；poll 定时为 1,000ms，并有 pollInFlight 防重入，不能据此声称多轮并发。
- 纯 Node 注入内存 store，200 条匿名模拟任务：首轮 working 写 200 次、序列化 2,642,700 字节；第二轮 working 写 200 次、5,249,600 字节；首次 done 写 200 次、5,289,800 字节；未变化的 done 轮仍写 200 次、5,329,600 字节。计量的是每轮累计序列化量，不是单次文件大小或实机耗时。
- 同次提交的 [native-state.cjs](../../../../preload/orca/native-state.cjs#L77) 在文件修改时同步读取和解析整个 orca-data.json，无尺寸上限；这是第二个阻塞风险，当前未测实际文件或耗时。
- 最后一次焦点变更路径有 key 相等短路，focus 更新只 syncConsumers 而不 emitPackage；目前未发现可证明的无限发布循环。
- 结论：已证明新增轮询路径存在同步写放大，足以形成可修复的性能缺陷；现场无限等待的具体 SDK 调用仍未捕获，不能宣称唯一根因已确认。
- 修复方向：每轮合并写、无变化不写，并将持久化移出同步轮询路径；原生文件读取需异步且限制解析开销。原有未读与已查看语义必须通过离线回放保留。本阶段未执行上述新修复或启用插件。

## 2026-09-15 未读存储链修复（用户选择 3）

- 移除 Orca 未读链的同步 `dbStorage.getItem/setItem`，主入口显式注入宿主 API，通过公开 `db.promises.get/put` 读取同一 `_id/value/_rev` 文档，无需迁移已有回执。
- 每轮观测先更新内存，再异步合并提交；完成状态未变化不写。进行中的 `seenWorkingAt` 仍按原行为更新，因此每个变化扫描最多提交一个快照。
- 单写者串行提交，挂起时只保留最新内存状态；失败保留脏标记，下次观测重试。初读最多异步等待 1 秒，失败或超时后只维护会话账本，禁止覆盖未知持久记录；迟到的旧读取也不能覆盖新内存状态。异常会话的新回执不保证跨重启保存。
- VerificationImpactTrace：宿主注入 → 异步加载 → inventory 首次投影 → 批量观测/点击回执 → 串行持久化。7 个 Orca 套件 49/49 通过，覆盖旧格式读取和 revision、200 行单次提交、无变化零写入、挂起写者合并、读取超时、失败重试、首次投影等待和已有未读行为。
- 合同、类型检查、生产构建、uTools 产物校验、92 对镜像及需求登记校验通过。入口预算检查失败：HEAD 已有 14,721 行，比预算 14,679 多 42 行；本次 API 注入增加 1 行至 14,722。未扩大本次修复去整理已有入口或放宽预算。
- 当前产物 EyPc V7：`host-2b5607cca0aed647e385 / renderer-92ebffb17fd1fd6e3764`，builtAt `2026-09-15T06:23:45.000Z`（北京时间 `2026/09/15 14:23:45`）。只生成工作区产物，未启动/启用/重载插件或替换安装包。已有 Claude 修改保留并随当前工作区构建。
- 状态：`focused-automated-verified / artifact-ready / host-not-retested`。已消除可复现的同步写放大缺陷；不能认定它是现场卡顿的唯一根因，也不能将本轮离线通过改写为真实宿主验收通过。未提交、推送。

## 2026-09-15 完整复核与独立日志

- 用户要求先重新核实昨天的进度、改动和关联内容，再增加测试/本地日志，以确认插件归因。完整范围为昨天 10 个提交、已有未提交 Claude 改动、两轮 Float/未读修复及真实验收记录，见 [完整复核](yesterday-audit.md#L1)。
- 新证据：Orca 引入前 `b874d84^` 的两项 App Server 测试通过，昨天最终 `418d094` 两项因额外 Orca CLI 调用失败。无条件启动 watcher 是插件新增行为，仍不能单凭它认定原生卡死的具体原因。未把原断言改松，也未在这一轮扩展业务修补。
- 新增 [独立线程追踪](../../../../preload/freeze-trace.cjs#L1)、[只读汇总脚本](../../../../scripts/summarize-freeze-trace.mjs#L1) 和 [使用/归因说明](trace-guide.md#L1)。固定 span begin/end、时长/计数/字节数与独立心跳，覆盖初始化、状态存储、Orca、Float/SDK 创建、Kernel、窗口列表和原日志 drain。10 分钟、256 条待确认、单文件 4 MiB/8 份保留上限；不含业务内容，错误不阻断业务。
- VerificationImpactTrace：新增 Worker 文件线程 → 主线程 span/心跳投递 → 超时/限流/写失败/到期 → 汇总归因；源代码触点 → Orca/窗口/日志/Float → 昨天 Provider/Kernel/导航/Claude 既有路径。构建身份输入改变，升级到类型检查、生产构建、产物与镜像校验。
- 新追踪/Orca/窗口/原日志 11 套件 101/101；最后补精确 Float SDK span 后 Float+追踪 34/34。昨天关联范围 13 套件 645 项：643 通过，2 项保留历史失败，隔离历史导出复现相同差异。重复 VM preload 夹具有 StatWatcher 监听数警告，未据此宣告真实宿主内存泄漏。
- 合同、类型检查、生产构建、uTools 校验、92 对已跟踪镜像校验、新 freeze-trace 源码/public/dist 逐字节比对、需求与来源锚点检查通过。入口预算仍失败：14,761 行相对 14,679 多 82 行；昨天 HEAD 已多 42 行，上轮异步存储注入 +1，本轮临时插桩 +39；函数数/可变绑定数未超限。未放宽门禁。
- 最新工作区诊断产物 EyPc V7：`host-b657e94d7d43b7ccbc56 / renderer-c712a58d9da0cd70f9fa`；builtAt `2026-09-15T06:37:00.863Z`（北京时间 `2026/09/15 14:37:00`）。原有 Claude 改动仍保留并参与构建；未提交或推送。
- 状态 `diagnostic-artifact-ready / known-regression-present / host-not-retested`。用户正常使用优先，插件保持屏蔽。尚未验证真实 uTools Worker 支持、实际日志生成、屏蔽/启用对照、单路径隔离和卡顿消除；缺少这些证据时不得称「唯一根因已确认」。
