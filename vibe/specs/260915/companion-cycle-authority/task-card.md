# 快捷键循环被已完成分组接管

## 目标与结论

1. 进行中展示正确，但上一／下一集中跳往 Orca 已完成任务。
2. 当前导航诊断显示：前一次直接打开 Codex 后，连续向前切换使用 22 项环，索引从首项回绕到 21，再递减至 14，目标连续为 Orca。该数量与截图中的已完成组一致。
3. 根因是 `ringFromSelection` 用当前任务的展示分组替换 Kernel 状态候选；组内超过一项便封闭循环。这绕过了既有 [RAW-182 循环候选权威](../../requirements/shared-raw-182.md#L1)。属于已实现契约回归，不新增需求身份。

## 实现

1. [导航模块](../../../../preload/companion/navigation.cjs#L1) 删除分组接管，首次及冻结期间补入候选均读取 Kernel `cycleKeys`。
2. 环外卡片正常派发打开结果并更新 Kernel `focusedKey`，但不改循环游标；环内直接打开仍接管游标。保留连按合并、单次派发、冻结次序及游标恢复。
3. [使用说明](../../../../src/help/guides/codex.md#L1) 与[架构](../../../knowledge/ARCHITECTURE.md#L1)同步；保留原 main 上的 Orca、状态、冻结追踪等并行改动。没有提交或推送。

## VerificationImpactTrace

| 序号 | 边界 | 证据与结果 |
| --- | --- | --- |
| 1 | Navigation → Kernel → Provider open | 两项导航回归先失败，修复后导航与 Kernel 共 140 项通过。覆盖已完成卡片不接管、跨状态组跨来源循环、冻结期间刷新，以及 2 项进行中＋22 项 Orca 已完成的正反向连续跳转与高亮。 |
| 2 | 生产镜像／内嵌帮助／构建身份 | 类型检查、合同校验、生产构建与 uTools 产物校验通过；导航镜像与产物逐字节一致。版本真值随产物同步。 |
| 3 | 宿主调用 | 导航模块未引入同步 IPC 或直接 uTools 调用；主入口无本任务改动。 |
| 4 | 未运行门禁 | 未跑全仓测试；影响限于导航派发与内嵌帮助。真实插件／uTools／原生应用未启动、重载或操作，运行效果待用户验收。 |
| 5 | 扩大验证 | 无全仓测试升级理由；构建由生产预加载与内嵌帮助变更触发。 |

## 当前状态

1. `focused-automated-verified / artifact-ready / host-not-retested`。产物 EyPc V7，`host-0f2f731bf53e95875d86 / renderer-e8d47a2a95408bba843c`，北京时间 `2026/09/15 15:35:21`。
2. 本修复不接受其他任务的宿主卡顿或 Orca 原生功能交付；本地构建已完成，下一步由用户在下次加载产物时验证快捷键。

## 全局规则增量与本地保护基线（2026-09-15）

1. 中央父规则：`260710-1605-codex-bidirectional-requirement-governance`；[业务变更保护增量](../../../../../../../czz/CzzProj/CodeNote/vibe/specs/2609/260915/1549-business-change-protection/spec.md#L1)统一拥有前置审查与测试预期完整性要求。本项目规则只反链，不复制全局正文。
2. 已确认基线：RAW-182 的候选层序、环外打开不接管、稳定 walk；RAW-183/188 的置顶与未读入口边界；RAW-215 的状态与展示分组分离。RAW-221 是本地未读投影的已确认局部精化，保留其并行实现和独立测试。
3. 历史对照：从不可变提交 418d094 载入导航模块，在模拟 Provider 下点击已完成项并连续向前四次，全部进入已完成组；当前同场景四次均在两条进行中任务间往返。未调用真实原生应用。
4. 当前受影响三套件 149 项通过，包括 Navigation、Kernel 和 shortcut-read。规则链另有 8 项负向合同检查、15 项紧凑加载及 13 项入口回归通过；这些通过不等于其他项目完整实现或新会话行为已验收。
5. 本轮只加来源标注与治理映射，没有修改产品测试预期、没有新增运行时代码，也没有重载或重新验收插件。
