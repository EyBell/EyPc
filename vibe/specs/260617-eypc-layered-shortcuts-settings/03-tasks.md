# EyPc Layered Shortcuts Settings Tasks

Tool: codex

## Tasks

- [x] 添加 layered shortcut 单测：layer priority、Esc stack、旧状态迁移、when overlap、reservation rules。
- [x] 实现 typed layer model、command profiles、when parser、conflict/reservation helpers。
- [x] 更新 app runtime 的 Esc command stack 和 command-level override persistence。
- [x] 重做设置页为分区总控、命令表、解析预览、录制弹窗、when 编辑器和层级规则页。
- [x] 更新项目过程文档和架构记忆。
- [x] 完整运行 `pnpm run test`、`pnpm run typecheck`、`pnpm run build`、`pnpm run validate:utools`。
