# Codex 可空回合开始时间兼容修复

状态：实现与针对性自动验证完成；真实 Provider 预检通过；新产物宿主身份已核对，用户反馈恢复。

## 问题与边界

当前 CLI 0.153.4 的真实回合记录可同时具有 completed 状态、有效完成时间和空开始时间。旧适配器在单条记录上拒绝整个批次，界面统一显示版本不兼容。之前的 active 空回合页修正与本问题独立，均须保留。

本次用户授权兼容修复、相关问题修复、构建与插件复检。已按本轮授权分批本地提交：识别修复 a04652d，空时间与预检修复 a30a7ff；未推送。工作区已有并行改动保留。没有修改原生任务、已读、归档或凭据。

## 修改

- [读取和公开投影](../../../../preload/index.js#L9625)：合法状态允许缺少开始时间；非法状态单条跳过并输出匿名计数诊断，不记作空会话。RPC 故障和分页结构错误仍拒绝不可信扫描。
- [库存状态合并](../../../../preload/codex/inventory-turn-fields.cjs#L37)：保留真实完成时间，缺失开始时间不合成；保留直接运行证据与较新回合的防倒退保护。
- [产品投影](../../../../src/domain/codex.ts#L1912)：完成时间可以支持任务进入列表；不伪造问题时间、开始时间或持续时长。
- [真实预检](../../../../scripts/codex-real-preflight.mjs#L10)：preload 的相对依赖按 preload 路径解析；JSON 直接加载并启用默认导入兼容；时间窗口按实际回合开始/完成时间判断。

## VerificationImpactTrace

| 项目 | 结论 |
| --- | --- |
| 影响链 | App Server Turn → preload 库存与活动合并 → Domain 列表/完成状态 → 产物与宿主 |
| 聚焦回归 | Bridge 与 Codex Domain 共 217 项通过；另新增合并防倒退回归 1 项通过 |
| 覆盖场景 | 缺失/显式 null 开始时间、真实完成时间保留、无假时长、非法状态单条隔离、240 条全分页、游标循环拒绝、直接运行证据不被冷库存覆盖 |
| 构建 | contracts、类型检查、生产构建、uTools 校验通过；83 对镜像、入口预算通过 |
| 真实预检 | ok=true、completeness=verified、desktopBridgeState=connected；瞬时 raw=39、registered=39、zeroTurn=1。采样数不是固定期望值 |
| 未运行 | 全仓测试未升级：修改可由直接消费者聚焦测试覆盖；宿主于 12:43:26 加载匹配身份，12:43:52 Codex 库存 accepted=23，浮窗显示 4 个进行中且无原协议错误提示 |
| 帮助与需求 | 修复既有识别行为，无新增操作流程；无需改帮助或创建新的产品需求编号 |

## 构建与恢复

产物：EyPc V7，host-ebeee3b6262b75611535 / renderer-f4e2c63ebc6edd4fbff7；北京时间 2026-09-09 12:40:39。当前完整目录为 [dist](../../../../dist/plugin.json#L1)。安装后必须从宿主运行身份读回同一组身份，不能用 dist 存在代替加载验收。

只读核验期间观察到用户将旧安装替换为 9 月 8 日构建；该变化不是本任务自动安装的结果。新产物发布前仍需保留现有可恢复安装。

最终宿主核验：用户自行更新后，安装包变为 unsafe-9dbfe797e10ffb344cc24c90d501ac1e.asar；Host/Renderer 身份与上述构建完全相同。此为当前加载与识别恢复证据，不包含归档、发送、已读操作验收。
