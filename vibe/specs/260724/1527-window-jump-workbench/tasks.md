# Window Jump Workbench — Tasks and Execution Journal

## Work Items

| ID | Work | Status |
| --- | --- | --- |
| WJ-01 | Create controlled task authority and preflight | completed |
| WJ-02 | Add state/domain/platform contracts | completed |
| WJ-03 | Add native adapters, manifest, routes, runtime and keyboard ownership | completed |
| WJ-04 | Add the accessible dense window page and styles | completed |
| WJ-05 | Synchronize documentation and conduct permitted static review | completed |
| WJ-06 | Session cache, manual Tab load, silent mainHide slots, layout refactor | completed |
| WJ-07 | Repair native enumeration, filter invalid handles, add local pin and application-first ordering | completed |
| WJ-08 | Repair slot visibility races; add bounded activation diagnostics and close-only acceptance | source-verified / host-validation-pending |
| WJ-09 | Repair macOS CG/AX activation-identity mismatch found by host diagnostic | source-verified / host-validation-pending |
| WJ-10 | Add development-only operation tracing, platform-accurate page topmost, and non-favorite slot assignment | source-verified / host-validation-pending |

## Execution Journal

- 2026-07-24 15:27 CST — Classified this as controlled: it crosses persisted product state, preload-native capability, manifest routing, keyboard behavior, and a new UI surface. Preserved the pre-existing dirty worktree; no external or destructive operation was authorized.
- 2026-07-24 15:27 CST — UI preference lookup passed for task-only full-ui work with global stable authorities and no conflicts. Selected a narrowly applicable accessibility/keyboard primitive reference; implementation stays Vue-native.
- 2026-07-24 — Added normalized `WindowTarget`/`WindowSlot` persistence and transient `LiveWindow` contracts, then extended the platform adapter with a safe stale-preload fallback. No live window row is saved during enumeration.
- 2026-07-24 — Added the fixed Windows User32/PowerShell and macOS System Events adapters, ten stable manifest features, enabled-route protection, candidate-safe Runtime resolution, official shortcut-setting redirects, and layered keyboard ownership. The adapters contain no simulated input, shell-interpolated title, privilege grant, title write, or foreground-protection bypass.
- 2026-07-24 — RAW-087 removed the transient host-binding readback shared with Codex because its private synchronous IPC blocked uTools entry loading. Window slots retain assignment and official setting redirects, but no longer display current bindings.
- 2026-07-24 — Added the `WindowsPage` workbench, responsive styles, and source-level coverage for contracts, routing, keyboard ownership, stale bridge fallback, ambiguity, focus denial, macOS permission guarding, and alias/slot stability. Automated and host validation remains intentionally unrun.
- 2026-07-24 — Synchronized product requirement, architecture, project status, controlled specification, plan, verification, and handoff. `git diff --check`, canonical/public preload and runtime-validator syntax parsing, preload byte comparison, manifest JSON/slot-count inspection, and the Markdown code-link audit are the only executed validation; no test/build/runtime/native call was made.
- 2026-07-24 — Corrected the reported `WindowsPage` Vue emit-name mismatch from `cancel-draft` to the declared `cancelDraft`; `pnpm run typecheck` passed. Test, build, uTools-host, browser, and native-window validation remain unrun.
- 2026-07-26 — Added session live-window cache and manual Tab load; rewrote slot activation as cache-first with one miss rescan; marked slots `mainHide` and restored the previous Tab so successful global jumps skip the plugin transit window; kept slot-bound non-favorites in the list; refactored the page into toolbar/slot-strip/status/list. Source tests and docs updated; no test/build/host run (`未校验，待用户验收`).
- 2026-07-26 — User requested explicit manual load/refresh to clear a stale window query. The named `windows.refresh` action now clears and persists only that query before enumeration; cache-miss and lifecycle refreshes retain it. Specification, verification, and stable interaction memory were synchronized; source diff whitespace check passed, while tests/build/uTools host validation remain user-owned.
- 2026-07-26 — User confirmed host configuration was unchanged and explicitly authorized a real read-only local enumeration. The exact current CG path returned zero because a CoreFoundation array proxy was passed directly to `ObjC.deepUnwrap`; the array contained hundreds of native records but failed `Array.isArray`. An in-memory `ObjC.castRefToObject` correction produced 53 valid rows across 16 applications in 123ms, while the AX all-process fallback exceeded its bounded budget. No titles, application names, PIDs, prompts, commands, stdout or stderr were persisted.
- 2026-07-26 — Reused this Controlled task for the net requirement delta: local pin independent from favorite/slot, pinned-first then global application ordering, and actionable-native-window filtering. The task-only `interaction-flow` preference receipt is ready, adopting the project Workbench List authority; missing motion/accessibility/content preferences were acknowledged under current project defaults. Selected only the `build-primitive` ARIA/keyboard reference; implementation remains Vue-native.
- 2026-07-26 — Implemented the WJ-07 delta in both canonical preload mirrors, domain/state, Runtime/keybinding projection, and the Window Jump page. Windows now validates live Alt-Tab/root-owner surfaces and excludes tool/no-activate/cloaked/host handles; macOS casts the CF window-list reference, validates regular applications, and bounds AX fallback to foreground processes. Pin persists independently, supports single/batch toggle with `aria-pressed`, and is the only prefix before global application sorting.
- 2026-07-26 — Permitted checks passed: both preloads parse and are byte-identical; nine changed TypeScript files and `WindowsPage.vue` pass syntax/SFC compilation; scoped diff whitespace passes. Exact aggregate-only local enumeration completed through current source (CG 22 rows/14 applications/159 ms; AX 13 rows/10 applications/2432 ms). Source tests were updated but not run; no activation, close, terminate, title/PID/handle disclosure, test, typecheck, build, browser, screenshot, or uTools-host validation occurred.
- 2026-07-26 — Natural-design closeout replayed the complete interaction-flow preference receipt after Root acceptance; the production closeout returned no W29 candidate/reminder and performed no write.
- 2026-07-27 — WJ-08 traced two independent visibility races: the slot route's generic `hideAfterAction` and Runtime's pre-resolution hide could conceal a later workbench fallback. Removed both only for Window Jump slots; Codex routes, the ten `mainHide` manifest entries, preload mirrors, official shortcut-setting redirect, and no-readback boundary remain unchanged.
- 2026-07-27 — Unified global-slot and manual activation around capability read, retained-reference retry, one healthy real-time rescan, and one retry. A successful rescan with no match is the sole accepted non-activation (`target-closed`); it clears only the stale native reference. Every other opening fault records a sanitized blocking Runtime diagnostic and global-slot failures request a visible workbench/settings fallback.
- 2026-07-27 — Added bounded (50) non-persistent activation diagnostics plus an accessible compact panel and clear action. The record contains no target title, application name, PID, handle, native reference, or raw host output. Authorized targeted evidence passes: entry/routing 2 files / 11 tests, named runtime diagnostic group 7 tests, and diagnostics UI 3 tests. Full action aggregation, typecheck, build, browser/screenshot, and real uTools host acceptance were not run.
- 2026-07-27 — User host evidence reported manual `activation-not-found` after repeated refresh. A read-only aggregate probe confirmed that Core Graphics still yielded 477 references while System Events saw 17 processes / 15 windows / 14 titled windows and exposed zero `AXWindowNumber` values. WJ-09 removes that unsupported identity assumption: activation now resolves a normalized AX title within the owning process, uses the fresh AX ordinal only for equal-title disambiguation, returns `ambiguous` rather than choosing arbitrarily, and passes the transient title through a bounded child environment rather than shell/JXA interpolation. The generated JXA probe parsed without action; the read-only resolver matched 14/14 titled AX windows. Host activation remains user-owned and unverified.
- 2026-07-27 — WJ-10 followed official Apple/Microsoft window API research before changing the bridge. Windows now restores first, optionally calls `SetWindowPos(HWND_TOPMOST)`, then retains the existing one-shot foreground attempt; macOS restores where readable, requests foreground/focus, calls `AXRaise`, and verifies readable state, while permanent third-party topmost remains explicitly unsupported. No foreground-protection bypass, permission mutation, or live host-window action was executed by Codex.
- 2026-07-27 — Added a Runtime-only, development-build-only operation trace (50 records, separate clear action). It accepts only fixed stage/outcome pairs from the native bridge and contains no title, application, PID, handle, native reference, host output, or exception text. Production builds neither request nor render this trace. “页面置顶” is separate from EyPc “列表置顶”; assigning a live row to a stable slot now creates a non-favorite target. Authorized targeted evidence passes: platform bridge 16 tests, named runtime diagnostic group 11 tests, diagnostics/UI 6 tests. Full aggregation, typecheck, build, browser/screenshot, and real uTools host acceptance remain unrun.
- 2026-07-27 — The explicit `vue-tsc` gate caught a compile-only mismatch: diagnostic stage `activate` was not a development trace stage. WJ-10 now maps it to trace stage `native`; `pnpm run typecheck`, the named Runtime diagnostics group (11 passed, 133 filtered), and whitespace validation pass. No behavior, native call, persisted state, or production trace condition changed.
