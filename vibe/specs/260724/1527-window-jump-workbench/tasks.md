# Window Jump Workbench — Tasks and Execution Journal

## Work Items

| ID | Work | Status |
| --- | --- | --- |
| WJ-01 | Create controlled task authority and preflight | completed |
| WJ-02 | Add state/domain/platform contracts | completed |
| WJ-03 | Add native adapters, manifest, routes, runtime and keyboard ownership | completed |
| WJ-04 | Add the accessible dense window page and styles | completed |
| WJ-05 | Synchronize documentation and conduct permitted static review | completed |
| WJ-06 | Session cache, manual Tab load, silent mainHide slots, layout refactor | completed |
| WJ-07 | Repair native enumeration, filter invalid handles, add local pin and application-first ordering | completed |
| WJ-08 | Repair slot visibility races; add bounded activation diagnostics and close-only acceptance | source-verified / host-validation-pending |
| WJ-09 | Repair macOS CG/AX activation-identity mismatch found by host diagnostic | source-verified / host-validation-pending |
| WJ-10 | Add development-only operation tracing, platform-accurate page topmost, and non-favorite slot assignment | source-verified / host-validation-pending |
| WJ-11 | Before macOS AX activate, switch to CG-owned Space via SkyLight/`koffi` (windowDerived) | source-verified / host-validation-pending |
| WJ-12 | Add read-only preload-context CG/AX/Space environment snapshots | source-verified / host-validation-pending |
| WJ-13 | Replace nondeterministic cross-Space fallback with exact binding, single-window fallback, bridge revision and rebind confirmation | source-static-verified / host-validation-pending |
| WJ-14 | Add an isolated JXA SkyLight bridge when the Electron preload returns an empty managed-Space result | completed / real-host-verified |
| WJ-15 | Map AX elements to the selected CG ID and verify the exact focused AX window | completed / AiTools-off-Space-host-verified |
| WJ-16 | Add guarded session Space fast path and complete/partial inventory retention | source/type/focused-tests-verified / host-latency-pending |
| WJ-17 | Add restart-safe logical-window recognition and success-only automatic rebind | source/type/focused-tests-verified / restart-host-pending |
| WJ-18 | Stop cross-API CG/AX title equality from falsely reporting a stable window as changed | static-verified / host-acceptance-pending |
| WJ-19 | Make native window-instance identity authoritative and require explicit replacement confirmation | implemented / verification-pending |
| WJ-19.1 | Make candidate recovery focused and title-readable; verify identity before instance-keyed Space-cache use | implemented / verification-pending |
| WJ-19.2 | Keep recovery stable across empty/partial refreshes and expose recognition context without identity inference | implemented / verification-pending |
| WJ-19.3 | Centralize rebind lifecycle, effects and action availability behind one domain state machine | implemented / verification-pending |
| WJ-20 | Collapse native members into independent root-window targets and add the Finder/Explorer virtual tree | implemented / verification-pending |
| WJ-20.1 | Centralize page-facing tree projection, row identity, navigation, selection and action context; remove stale current-contract text | implemented / static-closeout-complete / host-validation-pending |
| WJ-21 | Preserve roots as stable targets, expose verified ordinary-app children as transient exact destinations, and filter unproven native surfaces | implemented / focused-tests-typecheck-build-verified / host-validation-pending |

## Execution Journal

- 2026-07-31 — User replaced the former no-execution rule with impact-based verification. WJ-21 now has executed domain/platform/UI/window-Runtime tests, explicit semantic typecheck, production build and uTools package validation. The required-field/type-mock repair removed all 23 reported TypeScript errors; exact-child disappearance now preserves its specific no-fallback message.
- 2026-07-31 — Exact staged-snapshot verification passes all four changed window files at `219/219`, semantic typecheck, production build and uTools validation. Full-suite A/B improves from parent `9ba8b91` at `661/675` with 14 failures to WJ commit `d834f7b` at `664/676` with 12 failures: the two former window failures close, no new failure appears, and every remainder is confined to three untouched Codex files. No uTools/app/browser/dev server/native window operation ran.

- 2026-07-24 15:27 CST — Classified this as controlled: it crosses persisted product state, preload-native capability, manifest routing, keyboard behavior, and a new UI surface. Preserved the pre-existing dirty worktree; no external or destructive operation was authorized.
- 2026-07-24 15:27 CST — UI preference lookup passed for task-only full-ui work with global stable authorities and no conflicts. Selected a narrowly applicable accessibility/keyboard primitive reference; implementation stays Vue-native.
- 2026-07-24 — Added normalized `WindowTarget`/`WindowSlot` persistence and transient `LiveWindow` contracts, then extended the platform adapter with a safe stale-preload fallback. No live window row is saved during enumeration.
- 2026-07-24 — Added the fixed Windows User32/PowerShell and macOS System Events adapters, ten stable manifest features, enabled-route protection, candidate-safe Runtime resolution, official shortcut-setting redirects, and layered keyboard ownership. The adapters contain no simulated input, shell-interpolated title, privilege grant, title write, or foreground-protection bypass.
- 2026-07-24 — RAW-087 removed the transient host-binding readback shared with Codex because its private synchronous IPC blocked uTools entry loading. Window slots retain assignment and official setting redirects, but no longer display current bindings.
- 2026-07-24 — Added the `WindowsPage` workbench, responsive styles, and source-level coverage for contracts, routing, keyboard ownership, stale bridge fallback, ambiguity, focus denial, macOS permission guarding, and alias/slot stability. Automated and host validation remains intentionally unrun.
- 2026-07-24 — Synchronized product requirement, architecture, project status, controlled specification, plan, verification, and handoff. `git diff --check`, canonical/public preload and runtime-validator syntax parsing, preload byte comparison, manifest JSON/slot-count inspection, and the Markdown code-link audit are the only executed validation; no test/build/runtime/native call was made.
- 2026-07-24 — Corrected the reported `WindowsPage` Vue emit-name mismatch from `cancel-draft` to the declared `cancelDraft`; `pnpm run typecheck` passed. Test, build, uTools-host, browser, and native-window validation remain unrun.
- 2026-07-26 — Added session live-window cache and manual Tab load; rewrote slot activation as cache-first with one miss rescan; marked slots `mainHide` and restored the previous Tab so successful global jumps skip the plugin transit window; kept slot-bound non-favorites in the list; refactored the page into toolbar/slot-strip/status/list. Source tests and docs updated; no test/build/host run (`未校验，待用户验收`).
- 2026-07-26 — User requested explicit manual load/refresh to clear a stale window query. The named `windows.refresh` action now clears and persists only that query before enumeration; cache-miss and lifecycle refreshes retain it. Specification, verification, and stable interaction memory were synchronized; source diff whitespace check passed, while tests/build/uTools host validation remain user-owned.
- 2026-07-26 — User confirmed host configuration was unchanged and explicitly authorized a real read-only local enumeration. The exact current CG path returned zero because a CoreFoundation array proxy was passed directly to `ObjC.deepUnwrap`; the array contained hundreds of native records but failed `Array.isArray`. An in-memory `ObjC.castRefToObject` correction produced 53 valid rows across 16 applications in 123ms, while the AX all-process fallback exceeded its bounded budget. No titles, application names, PIDs, prompts, commands, stdout or stderr were persisted.
- 2026-07-26 — Reused this Controlled task for the net requirement delta: local pin independent from favorite/slot, pinned-first then global application ordering, and actionable-native-window filtering. The task-only `interaction-flow` preference receipt is ready, adopting the project Workbench List authority; missing motion/accessibility/content preferences were acknowledged under current project defaults. Selected only the `build-primitive` ARIA/keyboard reference; implementation remains Vue-native.
- 2026-07-26 — Implemented the WJ-07 delta in both canonical preload mirrors, domain/state, Runtime/keybinding projection, and the Window Jump page. Windows now validates live Alt-Tab/root-owner surfaces and excludes tool/no-activate/cloaked/host handles; macOS casts the CF window-list reference, validates regular applications, and bounds AX fallback to foreground processes. Pin persists independently, supports single/batch toggle with `aria-pressed`, and is the only prefix before global application sorting.
- 2026-07-26 — Permitted checks passed: both preloads parse and are byte-identical; nine changed TypeScript files and `WindowsPage.vue` pass syntax/SFC compilation; scoped diff whitespace passes. Exact aggregate-only local enumeration completed through current source (CG 22 rows/14 applications/159 ms; AX 13 rows/10 applications/2432 ms). Source tests were updated but not run; no activation, close, terminate, title/PID/handle disclosure, test, typecheck, build, browser, screenshot, or uTools-host validation occurred.
- 2026-07-26 — Natural-design closeout replayed the complete interaction-flow preference receipt after Root acceptance; the production closeout returned no W29 candidate/reminder and performed no write.
- 2026-07-27 — WJ-08 traced two independent visibility races: the slot route's generic `hideAfterAction` and Runtime's pre-resolution hide could conceal a later workbench fallback. Removed both only for Window Jump slots; Codex routes, the ten `mainHide` manifest entries, preload mirrors, official shortcut-setting redirect, and no-readback boundary remain unchanged.
- 2026-07-27 — Unified global-slot and manual activation around capability read, retained-reference retry, one healthy real-time rescan, and one retry. A successful rescan with no match is the sole accepted non-activation (`target-closed`); it clears only the stale native reference. Every other opening fault records a sanitized blocking Runtime diagnostic and global-slot failures request a visible workbench/settings fallback.
- 2026-07-27 — Added bounded (50) non-persistent activation diagnostics plus an accessible compact panel and clear action. The record contains no target title, application name, PID, handle, native reference, or raw host output. Authorized targeted evidence passes: entry/routing 2 files / 11 tests, named runtime diagnostic group 7 tests, and diagnostics UI 3 tests. Full action aggregation, typecheck, build, browser/screenshot, and real uTools host acceptance were not run.
- 2026-07-27 — User host evidence reported manual `activation-not-found` after repeated refresh. A read-only aggregate probe confirmed that Core Graphics still yielded 477 references while System Events saw 17 processes / 15 windows / 14 titled windows and exposed zero `AXWindowNumber` values. WJ-09 removes that unsupported identity assumption: activation now resolves a normalized AX title within the owning process, uses the fresh AX ordinal only for equal-title disambiguation, returns `ambiguous` rather than choosing arbitrarily, and passes the transient title through a bounded child environment rather than shell/JXA interpolation. The generated JXA probe parsed without action; the read-only resolver matched 14/14 titled AX windows. Host activation remains user-owned and unverified.
- 2026-07-27 — WJ-10 followed official Apple/Microsoft window API research before changing the bridge. Windows now restores first, optionally calls `SetWindowPos(HWND_TOPMOST)`, then retains the existing one-shot foreground attempt; macOS restores where readable, requests foreground/focus, calls `AXRaise`, and verifies readable state, while permanent third-party topmost remains explicitly unsupported. No foreground-protection bypass, permission mutation, or live host-window action was executed by Codex.
- 2026-07-27 — Added a Runtime-only, development-build-only operation trace (50 records, separate clear action). It accepts only fixed stage/outcome pairs from the native bridge and contains no title, application, PID, handle, native reference, host output, or exception text. Production builds neither request nor render this trace. “页面置顶” is separate from EyPc “列表置顶”; assigning a live row to a stable slot now creates a non-favorite target. Authorized targeted evidence passes: platform bridge 16 tests, named runtime diagnostic group 11 tests, diagnostics/UI 6 tests. Full aggregation, typecheck, build, browser/screenshot, and real uTools host acceptance remain unrun.
- 2026-07-27 — The explicit `vue-tsc` gate caught a compile-only mismatch: diagnostic stage `activate` was not a development trace stage. WJ-10 now maps it to trace stage `native`; `pnpm run typecheck`, the named Runtime diagnostics group (11 passed, 133 filtered), and whitespace validation pass. No behavior, native call, persisted state, or production trace condition changed.
- 2026-07-27 — WJ-11 (B-route): user required opening slot targets on other Spaces/displays (e.g. 小米桌面5). Host still saw `activation-not-found` after WJ-09 because System Events AX cannot raise off-Space windows. Added `koffi`, `trySwitchMacosSpaceByCGS` in both preloads before osascript, and prepare-time `koffi` copy into the plugin tree. Local probe: live CG ref switched (`ok: true`); AX-fallback ref skipped. Real uTools off-Space slot acceptance remains user-owned.
- 2026-07-27 — Host acceptance对照 locked WJ-11 gap: same-Space activate succeeds; off-Space/off-display slot 1 fails with stable `space=failed:empty-spaces` (not `no-api` / `ax-fallback`). Next repair candidate: Space-mask / reverse managed-display-space window scan when `SLSCopySpacesForWindows` returns empty; then re-run only the off-Space slot gate.
- 2026-07-27 — WJ-11 repair applied: `MACOS_CGS_ALL_SPACES_MASK` set to `0x7`; empty primary lookup falls back to managed-display Space IDs + per-Space window scan before `SLSManagedDisplaySetCurrentSpace`. Dual preloads mirrored; platform contract asserts new symbols/mask. Host re-acceptance of off-Space slot 1 still pending.
- 2026-07-27 — Host retest: `empty-spaces` → `no-display`. Switch path now retains `Display Identifier` from `SLSCopyManagedDisplaySpaces` for the resolved Space ID (SkyLight `SLSCopyManagedDisplayForSpace` remains fallback only).
- 2026-07-27 — User: cache / unique CG binding so same-desktop AX path can run after Space switch. Session cache `macosWindowSpaceCache` filled on inventory; display UUID from plist (not CFDictionary); activate resolves cache then `SLSManagedDisplaySetCurrentSpace`.
- 2026-07-27 — User: prior cache only saw each display's current desktop. Full reload now unions `CGWindowList(OptionAll)` with inventory, binds every window with `SLSCopySpacesForWindows` to all managed Spaces/displays; refresh rebuilds the map.
- 2026-07-27 — Host regression: same-display activate also `space=failed:no-display`. Found stale `dist/preload.js` still on `SLSCopyManagedDisplayForSpace`. Fixed parser (Spaces-only id64), same-Space `current` skip, activate-time reverse-scan fallback; `prepare-utools-runtime` re-synced public/dist. Local probe current/off-Space ok; host re-acceptance pending after serve reconnect.
- 2026-07-27 — Host: visible desktop ok; hidden desktop still `empty-spaces`. Cache rebuild now primary-walks managed displays via CFDictionary + `SLSCopyWindowsWithOptionsAndTags` per Space (off-current included); `SLSCopySpacesForWindows` only supplements inventory misses. Local off-current bindings/switch probe ok; host re-acceptance pending.
- 2026-07-27 — Host still `empty-spaces` for hidden-desktop activate. Many CG inventory window IDs are not in tags. Added AX `not-found` fallback: walk non-current managed Spaces, retry activate, restore currents on failure (`space=ok:walked`). Serve/reconnect + host re-acceptance pending.
- 2026-07-27 — Continue SIP-safe route: remember space bindings only after AX `activated`; stale switch/`current` + AX `not-found` forgets learned/session binding then walks (skips already-tried Space); settle 80→120ms. Dist synced. Host re-acceptance pending.
- 2026-07-27 — Compact window-title display: list secondary line keeps application name only; HWND leaves the always-visible row; action-panel titles ellipsize and multi-select collapses to “A、B 等 N 个”; hover Tooltip restores full displayName/title/app/HWND. UI contract added in `tests/ui/windowsDiagnostics.test.ts`. Tests/typecheck/build/uTools visual acceptance remain user-owned.
- 2026-07-27 — Windows Tab UI layout: list-first compact toolbar; left collapsible equal-height slot rail with empty-slot picker; right collapsible log rail for diagnostics/DEV traces; `Ctrl+1`…`Ctrl+0` assign focused row. Spec/verify/handoff/architecture/technical-details synchronized. Tests/typecheck/build/uTools visual acceptance remain user-owned (`未校验，待用户验收`).
- 2026-07-27 — WJ-13 root-cause review compared user traces with aggregate-only local probes. Both targets had one exact CG match and no current System Events AX match; AiTools' Chromium owner had two actionable CG windows while Rider had one. A fresh standalone SkyLight query resolved a Space for both, so `empty-spaces` is not proof that macOS lacks the binding; the unresolved variable is the loaded preload/context revision. No raw identity or window/Space mutation was emitted.
- 2026-07-27 — WJ-13 implementation now revalidates CG ID + PID/app/title, unions two direct Space masks with managed-Space reverse corroboration, switches only a unique remote binding, and confirms the switch before AX. Unbound single-window owners get one explicit process-frontmost retry; unbound multi-window owners, multiple remote bindings and switch timeout fail closed with distinct reason codes. The old all-Space walk and persisted `eypc/macos-window-spaces/v1` learned bindings are removed; only the exact obsolete key receives a best-effort local migration delete.
- 2026-07-27 — Runtime now gates native activation on `wj13-exact-space`, records separate pre-initial/pre-retry aggregate snapshots, preserves native Space reason codes, and turns same-PID title drift into an explicit candidate rebind instead of an automatic substitute. Current source keeps post-Raise `AXFocused=false` non-authoritative. Automated tests, typecheck, build and uTools activation were not authorized for WJ-13; static checks and host acceptance remain to be recorded.
- 2026-07-27 — WJ-13 static closeout passed: canonical/public/dist preload parse and byte equality, TypeScript syntax parse, Vue SFC parse through the installed pnpm compiler path, exact revision presence, removed-symbol scan, scoped `git diff --check`, and Markdown code-link audit. The first top-level `require('@vue/compiler-sfc')` attempt failed because that module is not hoisted; the verified pnpm package path passed without installing dependencies. Automated tests, semantic typecheck, build and uTools/native activation remain unrun.
- 2026-07-28 — The reported AiTools slot-2 trace was reproduced as `space-unbound-multiwindow`: the uTools/Electron preload exposed one exact CG target and two owner windows but no Space binding, while the same SkyLight symbols and target resolved one direct+reverse binding from an independent process. Dependency bytes/version/architecture were equal, so WJ-14 moved the fallback into a fresh JXA process instead of treating the host-context empty result as an OS absence.
- 2026-07-28 — The isolated bridge revalidates the selected PID/app/CG ID/title, merges both direct masks with every managed Space's reverse window scan, switches only one remote binding, and confirms the display's current Space within two seconds. Aggregate probe evidence was `managed=7`, direct=1, reverse=1, unique binding; no raw PID, CG ID, Space ID, display UUID, or unrelated title was persisted.
- 2026-07-28 — uTools retained the old preload while the Codex float kept the plugin session alive. After temporarily toggling the float off, exiting the plugin and reopening it, the host reported `wj14-isolated-space`. This follows the official uTools development rule that preload code does not hot-reload. The float was restored after WJ-15 validation.
- 2026-07-28 — After the Space switch, System Events exposed four Edge AX windows but zero target-title and zero `AXWindowNumber` matches; the ordinal fallback brought the owning process forward but left a sibling window selected. A private read-only probe mapped all four AX elements through `_AXUIElementGetWindow` and found exactly one element for the selected CG ID. WJ-15 now prefers that mapping, writes the exact element to the application's `AXFocusedWindow`/`AXMainWindow`, sets window main/focus, raises, activates, and verifies the focused element maps back to the selected CG ID.
- 2026-07-28 — Real uTools acceptance: with AiTools on a non-current managed Space, global slot 2 completed on bridge `wj15-exact-ax`. The sanitized chain was `isolated-space-bridge → direct-unique → switch-confirmed → ax-cg-id-match → foreground/raise → ax-focused-window → native/visibility ok`; a separate UI read confirmed `Window: "AiTools"`, and a read-only post-check confirmed target Space current, Edge frontmost, and exact AX focus.
- 2026-07-28 — Verification passed: production build/typecheck/uTools runtime validation, platform+window-log UI suites 35/35, and the named Runtime diagnostics group 11/11. The WJ-13-era diagnostics fixtures were updated to share the bridge revision constant and retain the already-authorized selected target title. One malformed test selector unintentionally ran the whole current suite and exposed 76 failures; it is not an acceptance gate. The focused WJ suites are green, while unrelated full-suite baseline failures remain explicit.
- 2026-07-29 — WJ-16 performance/list repair: `macosWindowSpaceCache` is now read before direct/reverse resolution, unique isolated-JXA bindings are imported into it, and cached activation requires app/title plus exact CG→AX and focused-window readback. Cache/native miss evicts both Space and short-lived aggregate identity evidence; no Space/display binding crosses the preload lifetime. Bridge revision is `wj16-session-cache`.
- 2026-07-29 — Cross-Space list loss root cause: Core Graphics `kCGWindowIsOnscreen=false` had been converted to `minimized=true`, then Runtime hid every minimized macOS live row; ordinary windows on another Space therefore disappeared despite successful CG inventory. CG inventory no longer fabricates minimization, minimized rows remain visible, and list results declare complete/partial scope. Partial/current-Space refreshes merge with session cache and cannot clear a target as closed.
- 2026-07-29 — WJ-16 verification: preload syntax/mirror and diff checks pass; the WJ checkpoint semantic typecheck passed; the three focused cache/list/recovery cases pass; platform plus window-diagnostics UI suites pass 35/35. A final current-tree typecheck rerun is now blocked by an unrelated concurrent `FloatApp.vue` environment-picker narrowing error; WJ files did not introduce that diagnostic. A broader Runtime file run is 139/148 and remains non-green in nine cases outside the new WJ-16 cases; no pre-change fresh baseline was captured, so those failures remain explicit and are not claimed as a WJ-16 regression or acceptance gate. Real uTools cache-hit latency, first-hit fallback, stale-cache recovery, and visible cross-Space inventory remain pending.
- 2026-07-29 — WJ-17 requirement supersedes exact-title-only/same-PID-manual recovery for one safe case: after an expired persisted native reference, a complete inventory may automatically choose one exact-platform/app, strongly similar and clearly leading logical-window candidate. Partial inventories, low-confidence candidates, and tied/multiple same-browser siblings remain manual; no background scan or application launch was added.
- 2026-07-29 — WJ-17 implementation adds bounded verified-title history to normalized target state, app-label-stripped stable-segment/token/trigram/edge scoring, stricter multi-window score/margin, and activation-success-only learning. Manual locator edits reset learned history. `msedge` is now recognized as Chromium so its placeholder `Window` shell cannot pollute candidate ranking. Preload protocol and bridge revision remain unchanged.
- 2026-07-29 — WJ-17 verification: `tests/domain/state.test.ts` plus `tests/domain/windows.test.ts` pass 21/21; the named Window activation diagnostics Runtime group passes 17/17, including automatic restart replacement, failed-focus no-learning, manual-locator history reset, ambiguous sibling refusal, and partial-inventory refusal. `pnpm run typecheck` reports only the pre-existing concurrent [src/FloatApp.vue](../../../../src/FloatApp.vue#L1259) narrowing error. Real uTools reboot/reopen, learned-title reuse, latency, build/dist, and production validation remain pending.
- 2026-07-29 — WJ-18 user correction identifies a new false-positive gate introduced after the WJ-15 exact mapping: the selected AX element already mapped to the saved PID/CG window ID, but its `AXTitle` was still required to equal the saved Core Graphics title. Because the APIs may expose different strings for one unchanged window, Runtime surfaced `target-title-changed` even without a real target change.
- 2026-07-29 — WJ-18 now revalidates title only against the current same-PID/same-CG-ID `kCGWindowName`, fixes the aggregate JXA whitespace normalizer, keeps app identity plus exact AX→CG/focused-window verification, and returns a generic failure when same-source title evidence is unavailable. Bridge revision is `wj18-cg-title-source`; no state/UI protocol or persistence shape changed.
- 2026-07-29 — WJ-18 static closeout passes: both preload files and both generated activation scripts parse, the mirrors are byte-identical, the bridge revision appears in the two preloads plus Renderer platform constant, private synchronous IPC search is empty, scoped project/CodeNote diff checks pass, and both repositories' changed Markdown code links audit cleanly. Tests, typecheck, build, dist preparation, uTools reload, screenshots and native activation remain deliberately unrun and user-owned.
- 2026-07-30 — WJ-19 replaces WJ-17 automatic title recovery and WJ-18 title equality with `WindowInstanceId`. State migration keeps only `lastKnownTitle`; Runtime identity paths use `instanceId`; one bounded complete refresh exposes all same-app candidates for manual confirmation, while a partial inventory cannot clear binding.
- 2026-07-30 — Native bridge revision is `wj19-native-instance-id`. Windows now revalidates actionable HWND + owner/app and returns the verified instance ID. macOS list/activate/close require PID + CGWindowID and exact AX mapping/readback; title/ordinal fallbacks and title environment gates are removed. Canonical/public preload files are byte-identical.
- 2026-07-30 — WJ-19 follow-up removes title-content membership filters as well: actionable native rows remain listed across empty/`Window`/host-like title changes, with application-name display fallback. Runtime deduplicates only by `instanceId`; existing tests were updated as unexecuted contracts.
- 2026-07-30 — Existing domain/state, platform, Runtime and diagnostics UI tests were rewritten as WJ-19 contracts without execution. Typecheck, build, uTools reload, real window activation and visual acceptance remain `未校验，待用户验收`.
- 2026-07-30 — WJ-19.1 follows the user's first-principles review that interaction convenience is the core outcome. Candidate rows now show their own live titles and no inherited favorite/pin/slot state; entry clears unrelated selection and requests list focus; candidate mode blocks side actions; Escape exits first and requests original-row focus restoration.
- 2026-07-30 — macOS now force-refreshes PID/application/CG identity before any session-cache Space switch and keys Space hints by full `darwin:PID:CGWindowID`. Windows documentation now treats `PID+HWND` as a current verified locator because the OS can reuse handles; owner/app/actionability validation and manual rebind remain the safety boundary.
- 2026-07-30 — Existing Runtime, platform and diagnostics UI contracts were extended for WJ-19.1 without execution. No test, typecheck, build, preload execution, uTools reload, native window operation or visual acceptance was run.
- 2026-07-30 — Closeout found no new error-memory trigger: the candidate-state leaks and cache-before-identity ordering were corrected before runtime execution, while the Windows handle-reuse limitation is an explicit current platform boundary rather than a reproduced failure. Existing WJ-17/WJ-18 records remain historical links only; no rule, Soul, DB memory or new error archive was added.
- 2026-07-30 — WJ-19.2 keeps `windowCandidateTargetId` authoritative even when a complete refresh yields zero candidates. Complete refreshes replace the same-app candidate set and clear a proven-stale instance binding when empty; partial refreshes retain prior candidates and add newly observed same-app instances. Candidate reappearance/replacement requests list focus, while only successful confirmation or Escape ends the flow.
- 2026-07-30 — Candidate guidance now names both the logical alias and last title, shows current frontmost/minimized or retained-cache state on each candidate, and uses a dedicated zero-candidate message. Titles and live state remain recognition-only. Existing Runtime/UI tests were extended as contracts and not executed.
- 2026-07-30 — The user's explicit no-regression correction is a WJ-19.2 acceptance gate. Scoped static review retained one-way legacy `titleLocator` migration only and found no runtime title history/similarity/equality identity path, sole-candidate auto-bind, macOS title/ordinal fallback, cache-before-identity order, or unguarded candidate mutation action. Review also removed dead `ax-fallback`/desktop-walk/process-frontmost trace vocabulary from native and Renderer allowlists so those retired routes cannot be reintroduced through diagnostics. Runtime/host execution remains pending.
- 2026-07-30 — WJ-19.3 replaces the three parallel Runtime candidate variables and hand-written refresh/focus branches with [windowRebind.ts](../../../../src/domain/windowRebind.ts#L1). The reducer is the sole candidate-membership owner and returns effects for stale-binding clear, candidate focus and cancel focus restoration; Runtime applies effects without duplicating transition policy.
- 2026-07-30 — All window action predicates now route candidate availability through one `always / browse / rebind` policy, and [WindowsPage.vue](../../../../src/pages/WindowsPage.vue#L1) consumes one `windowRebind` projection. Existing domain/Runtime/UI tests were updated as unexecuted contracts. Static searches and diff checks only; no test, typecheck, build, preload execution, uTools reload, native operation or visual acceptance ran.
- 2026-07-30 — 分批提交前从 WJ-19.1 的“一次 Escape、无候选副层”回查异步入口，发现窗口编辑草稿可能与槽位恢复同时抵达换绑分支。Runtime 现保留草稿并用 `editor-active` 阻断候选创建；新增合同确认编辑内容不丢失、`windowRebind` 保持 idle。合同未执行。
- 2026-07-31 — WJ-20 将产品身份统一为独立 OS 根窗口族：桥接只输出 `rootInstanceId/rootNativeRef/rootPid` 与成员观察，唯一 [windows.ts](../../../../src/domain/windows.ts#L1) coalescer 生成根 `LiveWindow`；Runtime、缓存、焦点、槽位和换绑均只消费根 ID。相同应用/标题的独立根不合并，无法证明的成员关系保持独立。
- 2026-07-31 — 新增 [windowTree.ts](../../../../src/domain/windowTree.ts#L1) 作为唯一树领域框架，集中 Finder/Explorer allowlist/groupKey、树排序/搜索/扁平化、收起焦点恢复、父节点激活落点和桥接证据驱动的旧目标无损合并。普通应用始终按独立根平铺；文件管理器始终保留虚拟父子结构。
- 2026-07-31 — Windows 原生观察使用同应用 `GA_ROOTOWNER`，激活可前置最后活动成员但最终核验前台 root owner。macOS 使用 CGWindowID + `AXTopLevelUIElement`/`AXWindow` + `_AXUIElementGetWindow` 建立根关系并核验最终 `AXFocusedWindow` 根。旧环境快照、SkyLight/Space 查找缓存切换、标题/序号/唯一候选门禁已删除；版本升级为 `wj20-root-window-family`。
- 2026-07-31 — 页面升级为 ARIA tree，文件管理器父节点支持展开、收藏、列表置顶、别名和槽位，禁止批量选择/页面置顶/关闭全部；右键统一 `window / file-manager-group / selection / slot` 面板，Escape 先关闭面板，搜索临时展开消失时焦点回父节点。只更新既有测试模块。所有测试、`vue-tsc`、构建、preload 执行、uTools 重载、真实 Windows/macOS 跳转和视觉验收均未执行。
- 2026-07-31 — Closeout 未发现新的可复用失败模式：`releaseOlderSnapshot` 的 TypeScript 异步闭包问题已有 [typescript-async-closure-release-narrows-to-never.md](../../../../vibe/knowledge/error-memory/typescript-async-closure-release-narrows-to-never.md#L1) 并保持“声明即 callable + 直接调用”；本轮只把三个旧 macOS Space 记录标为 WJ-20 历史/局部适用，未新建错误记忆、规则或 AI-DB 记录。
- 2026-07-31 — WJ-20 静态收口通过：双 preload 字节一致、桥接版本一致、生产树仅一个根族 coalescer、旧标题/序号/环境快照/Space 路径零命中、项目与 CodeNote 代码链接审计及 scoped diff 检查通过。部分清单合并额外收紧为保留已证明成员证据但不复活独立成员。测试、`vue-tsc`、构建、preload 执行、uTools 重载、真实跳转与视觉验收仍未执行。
- 2026-07-31 — WJ-20.1 根据用户再次提出的第一性原理/全局精简要求复核需求与实现：修正 raw/spec 中仍把环境快照、Space 切换和裸 `ArrowRight` 操作层写成当前合同的矛盾；把目标解析、槽位映射、行 ID/候选解析、完整树行投影、动作上下文、左右导航和只允许真实根的选择统一收进 [windows.ts](../../../../src/domain/windows.ts#L1) 与 [windowTree.ts](../../../../src/domain/windowTree.ts#L1)。Runtime 只传入状态并执行副作用，页面改为消费领域 `WindowRow` 与一次性槽位映射。既有测试合同同步但未执行。
- 2026-07-31 — WJ-20.1 静态收口通过：选定 TypeScript/测试源码和 `WindowsPage.vue` 语法解析通过，根族 coalescer 与 `buildWindowTreeRows` 各仅一个生产定义，Runtime 旧行构造/候选前缀分支及所有退役原生符号零命中，双 preload 一致，项目/CodeNote 链接审计和 scoped diff 检查通过。未运行测试、语义类型检查、构建、preload、uTools 或真实窗口验收。
- 2026-07-31 — 用户通过截图与第一性原理纠正 WJ-20 的成员展示：普通应用必须是“真实主窗口 → 真实子窗口”，稳定收藏/槽位仍只属于根；根按 `root-current` 打开当前子窗，用户点选子窗时按 `member-exact` 精确打开且失效不回退。Finder/Explorer 继续固定为“虚拟父 → 真实根”两级，WJ-20“成员全部吞并不展示”降为历史。
- 2026-07-31 — WJ-21 领域层新增 `WindowFamily`、显式关系/能力证据与根/成员激活请求；完整清单替换家族，部分清单保留缓存成员。树投影为普通根/子窗、文件管理器虚拟父/根，搜索展开与折叠/消失焦点回根由同一 `windowTree` 处理。子窗不进入持久化、收藏、列表置顶、槽位、多选、编辑、页面置顶、强杀或批量动作。
- 2026-07-31 — Windows 桥仅从 `EnumWindows` 顶层/owned popup 与同应用 `GA_ROOTOWNER` 建关系，过滤 child/no-activate/cloaked/transparent/host/helper；macOS 改为 AX-first admitted window role + 正 CGWindowID 佐证，CG-only/system/helper/non-actionable 表面不生成产品行。根/成员激活和成员关闭均在调用时重验实例、应用与关系，精确成员失败不得打开根或兄弟。
- 2026-07-31 — 当前桥版本升级为 `wj21-main-child-window-tree`。本轮按用户边界只修改生产代码与文档，没有新增或修改测试文件；最终只执行双 preload 镜像、版本/静态符号、语法、代码链接和 scoped diff 检查，不运行测试、类型检查、构建、preload/uTools 或真实窗口操作。真实噪声过滤、根当前子窗、精确子窗、两独立根、Finder/Explorer 固定两级与完整/部分焦点行为仍待用户宿主验收。
- 2026-07-31 — WJ-21 静态收口通过：双 preload JS 语法与字节镜像、五个变更 TypeScript 文件和 `WindowsPage.vue` script-setup 的 TypeScript 语法转译、三处桥版本、`WindowFamily/root-current/member-exact/child-window` 符号、生产源码无旧桥/`EnumChildWindows`/CG-only 清单脚本、未改测试文件、项目/CodeNote 代码链接及两仓 scoped `git diff --check` 均通过。Vue 检查是脚本语法回退，不是 SFC/语义类型检查；测试、typecheck、build、preload/uTools 和真实窗口操作仍未执行。
- 2026-07-31 — WJ-21 第一性原理/回归复核完成：旧 preload 的列表刷新现在与激活共用版本门禁，拒绝在 coalescer 前混用；Runtime 用一个 inventory 函数原子更新 family/root/freshness，主窗口折叠会把已打开的子窗操作上下文收回可见根；macOS 精确激活补齐操作时 CG 有效范围复验。静态调用链确认窗口原生副作用仍只有 Runtime → platform 一条边界，Files/Ports/MQTT/Codex/存储合同未变化。
- 2026-07-31 — 本次 review 无静态 P0，发现的两个 P1 与两个 P2 均已按最小范围修复；没有为了去重重写跨进程 PowerShell/JXA，也没有增加 UI/原生依赖。既有 WJ-20 测试合同仍属历史且按用户边界未修改、未执行，不能作为 WJ-21 回归通过证据；宿主验收状态不变。
