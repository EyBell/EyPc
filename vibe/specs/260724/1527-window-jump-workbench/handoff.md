# Window Jump Workbench — Handoff

## Delivery State

`wj17-auto-rebind / source-verified; restart-host-acceptance-pending` — WJ-15's real AiTools off-Space exact-focus acceptance and WJ-16's guarded session Space cache/list retention remain valid. WJ-17 lets a fixed slot recover across restart from a changed PID/native reference without routine rebinding: exact app identity plus one complete-inventory, high-confidence unique title candidate may replace the target only after successful activation. Status: **源码与定向回归已通过；真实重启换绑及缓存命中速度待验**.

## Delivered Surface

- Domain/state: persistent local aliases/targets with independent favorite/pin, current plus bounded verified titles, pinned-first/global application comparator, ten platform-separated slots, and shared [listSelection.ts](../../../../src/domain/listSelection.ts#L1) helpers in [windows.ts](../../../../src/domain/windows.ts#L1), [state.ts](../../../../src/domain/state.ts#L1), and [types.ts](../../../../src/domain/types.ts#L1). The candidate resolver requires exact app identity, ignores common app-name title suffixes, and auto-selects only one strongly similar, clearly leading result.
- Native bridge: macOS first validates a session binding against the current managed-display map; a unique hit skips full direct/reverse/isolated resolution, while activation still requires matching app/title, exact `_AXUIElementGetWindow` identity and exact `AXFocusedWindow` readback. Native miss evicts the hint and preserves one bounded recovery. Cache misses retain WJ-15's exact isolated bridge. No Space/display binding is persisted. Windows behavior is unchanged. See [preload/index.js](../../../../preload/index.js#L1), [public/preload.js](../../../../public/preload.js#L1), and [eypcPlatform.ts](../../../../src/platform/eypcPlatform.ts#L1).
- Runtime/UI: capability revision remains `wj16-session-cache` because WJ-17 changes no preload protocol. Complete inventories replace the session list and may authorize one high-confidence unique replacement; partial/current-Space inventories retain absent cached rows, disable fuzzy learning, and cannot prove closure. Successful replacement updates the locator/reference/history; failed activation leaves the binding unchanged. Off-Space CG rows remain visible. See [appRuntime.ts](../../../../src/runtime/appRuntime.ts#L1) and [WindowsPage.vue](../../../../src/pages/WindowsPage.vue#L1).
- Verification: WJ-17 domain/state suites pass 21/21 and the Window activation diagnostics group passes 17/17. The final current-tree typecheck is blocked only by unrelated concurrent `FloatApp.vue` environment-picker work. WJ-15 production build/uTools validation and real exact-focus acceptance remain historical evidence; WJ-17 real reboot/rebind and WJ-16 build/dist/latency were not run. The earlier broader Runtime baseline remains non-green; see [verify.md](verify.md#L1).

## User Validation Focus

Reload the non-hot-reloading preload and first confirm `bridge=wj16-session-cache`. Reboot/recreate one fixed Rider or AiTools target so its PID/native reference changes, then invoke the slot without editing it: one high-confidence unique replacement must activate and persist, and the next invocation must use that new reference. With two similarly named browser windows, automatic replacement must stop and show candidates. Also compare cold/repeated latency (`session-cache` should still end at `ax-focused-window`) and confirm a partial/current-Space refresh keeps off-Space rows as “缓存保留” without learning. Other residuals remain unbound/ambiguous/timeout/exact-focus negatives, true close, production-trace absence, and Windows topmost/close.

Durable prevention: [macos-cgwindowlist-cf-proxy-not-js-array.md](../../../../vibe/knowledge/error-memory/macos-cgwindowlist-cf-proxy-not-js-array.md#L1) records the verified CF proxy failure; [macos-cg-ax-window-identity-mismatch.md](../../../../vibe/knowledge/error-memory/macos-cg-ax-window-identity-mismatch.md#L1) records the verified exact AX→CG route; [utools-macos-ax-activation-misses-other-spaces.md](../../../../vibe/knowledge/error-memory/utools-macos-ax-activation-misses-other-spaces.md#L1) covers the isolated off-Space bridge; [utools-window-target-auto-rebind-after-restart.md](../../../../vibe/knowledge/error-memory/utools-window-target-auto-rebind-after-restart.md#L1) records safe logical-target recovery; [utools-mainhide-window-activation-diagnostics.md](../../../../vibe/knowledge/error-memory/utools-mainhide-window-activation-diagnostics.md#L1) retains the debug-trace production gate. The CodeNote canonical files have pre-existing dirty/untracked content and were intentionally not overwritten in this task.

## Safety Reminder

Neither the plugin nor this task should attempt to grant macOS accessibility/screen-recording access, alter an application title, or bypass Windows foreground protection. Force terminate runs only after an explicit user confirm.
