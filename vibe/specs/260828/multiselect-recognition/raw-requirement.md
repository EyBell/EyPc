# 多选识别度仍不达标（暂停，待用户补充）

Tool: claude · Date: 2026-08-28 · Level: Standard（需求）· Status: **paused — awaiting user input**

## 状态

**暂停中。** 用户已确认问题存在但尚未描述具体不满之处，后续会补充完整信息。在补充到位之前**不要动手改实现**——这条历经三轮修正仍未过验收，再凭猜测改一次只会产生第四条失败记录。

## 用户原话（2026-08-28）

> d2 除了多选识别度有问题 其他的可以结案 多选识别度作为一个新的任务进行说明 暂停 后期可以继续 我后面再给你补充完整的这个信息

同轮用户验收通过的另外三项（水球数据环、置顶来源标签、任务名回退）已结案，不属于本任务。

## 已知历史：三轮修正均未过验收

完整根因与检测顺序见错误记忆 [selection-state-needs-structural-contrast](../../../knowledge/error-memory/codex-selection-state-needs-structural-contrast.md#L1)（状态 `candidate`，本任务是它的当前 owner）。三轮尝试摘要：

| 轮次 | 失败路线 | 用户反馈 |
| --- | --- | --- |
| RAW-057 | 同色系渐变 + 辉光 + 窄前导条 | 强化后仍然不明显 |
| RAW-058 | 窄选择区 + 用勾选替换状态图标 | 经常点不中，勾选结果仍不满意 |
| RAW-064 | 顶部常规流模式栏 | 多了一行，进出多选时列表跳动 |

当前实现（[FloatApp.vue](../../../../src/FloatApp.vue#L462)、[float.css](../../../../src/styles/float.css#L1111)）已是第三轮修正后的形态：38px 全高选择区、保留状态图标、非成员降权、模式提示改为底部浮层。

## 待用户补充的信息

补充时请尽量落到以下任一条，避免再次凭猜测修改：

1. **不达标的具体环节**是哪一个——分不清「是否处于多选模式」、分不清「哪些行被选中」，还是「选择区难以点中」？
2. **对照场景**：在什么主题、什么列表密度、鼠标还是键盘操作下最明显？
3. **期望形态**（可选）：是否有你认可的参照实现或期望的视觉强度。

## 恢复条件

用户补充上述信息后解除暂停。届时先读上述错误记忆的 `Detection Order` 与 `Prevention Rule`，再动实现；本任务的任何改动都必须新增一条覆盖对应识别通道的断言，并在错误记忆里追加 Occurrence History。
