# EyPc Command Soul Shortcuts Plan

Tool: codex

## Plan

1. Add behavior tests for command-soul shortcuts, editor layer isolation, and shortcut profile migration.
2. Update keybinding profiles so edit-like commands use `F2`, `Shift+F2`, `Ctrl+S`, `Escape`, and editor-local `Tab`.
3. Extend port group draft state with edit/rename modes and active editor field cycling.
4. Persist shortcut overrides into `global`, `ports`, `favorites`, and `settings` profiles while keeping the legacy aggregate.
5. Update project rules, architecture, task records, and run full verification.
