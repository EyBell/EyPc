# Codex Companion v3 Implementation Plan

Status: `execution-and-automated-verification-complete / host-gate-pending`

1. `complete` — 建立 v3 Evidence、唯一 Kernel Reducer、Canonical Package、语义 revision 与 no-op 合同。
2. `complete` — 删除固定任务上限，完成 Codex cursor 分页、新 membership 最小卡片和定向元数据补读。
3. `complete` — 统一热缓存、focus key、快捷键/手动跳转 operationId 和单并发尾随队列。
4. `complete` — 用两次服务器核验、Desktop sync/native ACK、Kernel commit 重写 Codex 归档事务。
5. `complete` — 升级明文运行诊断 v3、显式等级、归档阶段日志与查询/聚合探针。
6. `complete` — 全量自动化、类型、生产构建、Preload 镜像、Runtime Identity 和 uTools validator。
7. `complete` — 同步任务、产品、架构、帮助与错误记忆，完成实现审查、文档链接审计与同步回执。
8. `pending-host-gate` — 用同一安装包执行真实 uTools 状态、库存、交互、归档和日志矩阵。

验证结果统一写入 [verify.md](verify.md#L1)，宿主操作顺序统一写入 [handoff.md](handoff.md#L1)。
