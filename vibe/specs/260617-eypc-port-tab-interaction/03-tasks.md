# EyPc Port Tab Interaction Tasks

Tool: codex

- [x] 补 `Tab` 页内切栏、`Ctrl+F` 当前栏搜索、`Escape` 层级处理测试。
- [x] 调整默认 keybinding，避免端口页 `Tab` 抢到全局切页。
- [x] 注册 `ports.pane.toggleNext` / `ports.pane.togglePrev` runtime action。
- [x] 暴露 `searchFocusTarget` 并接入 DOM 搜索框聚焦。
- [x] 更新端口页提示和焦点/选择视觉层级。
- [x] 运行自动化验证并记录结果。
