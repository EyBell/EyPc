# RAW: MQTT 悬浮提示与快捷键可辨识性

Tool: codex
Date: 2026-07-30

## Request

MQTT Tab 内菜单与图标按钮悬浮提示不足，多处操作图标重复难以区分；快捷操作抽屉与部分快捷键显示异常，需核查并优化。范围仅 MQTT Tab。

## Confirmed Scope

- 方案 2：可辨识性修复 + 为常用「未绑定」动作补安全默认快捷键。
- 不改连接树 `c-n`/`c-g`、消息/草稿 `F2`/`s-f2` 既有肌肉记忆，不改抽屉索引路由本身。
- 高风险「清空全部」类不给全局默认键。

## Acceptance Intent

- 悬停可区分预览 / 详情 / 快捷操作 / 别名 / 完整编辑 / 发送选项。
- 快捷操作抽屉以 `c-1…9` 为主显示，不再大量「未绑定」或标出抽屉占用的 `←`/`→`。
- 再次发送、日志、发送选项具备默认可用快捷键。
