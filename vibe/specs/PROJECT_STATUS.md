# EyPc Project Status Hub

Tool: codex
Date: 2026-08-03

## Purpose

This hub routes current implementation, verification gates and durable authorities. Product behavior belongs in [PRODUCT_REQUIREMENTS.md](PRODUCT_REQUIREMENTS.md#L1); architecture and implementation facts belong in [ARCHITECTURE.md](../knowledge/ARCHITECTURE.md#L1) and [technical-details.md](../knowledge/technical-details.md#L1).

## Current Snapshot

- Product: keyboard-first uTools plugin for local PC capability calls.
- Active lines: port management, file favorites, cross-platform window jump, MQTT workbench, Quick Jump, Codex Companion and settings feature help.
- Development acceptance is user-owned. Automated/static evidence below does not claim real uTools, cross-platform or production acceptance unless explicitly stated.

## Codex Companion

- Authority: [current spec](260718/1148-codex-quota-float/spec.md#L1), [verification](260718/1148-codex-quota-float/verify.md#L1), [handoff](260718/1148-codex-quota-float/handoff.md#L1).
- Current requirement: RAW-116–146. RAW-131–142 retain the closed activity, terminal, unread, waiting, Side Chat, Plan and inventory-state matrix described by the current spec. RAW-143/144 make shortcut state feature-lifetime and incremental: ordinary hide plus Tab/Float changes preserve the App Server, verified task/project inventory, Activity subscription and aliases; concurrent stale aliases share one published threads preflight; Runner catalogs reload only added/re-aliased projects. Successful open confirmation is Turn-ID-bound and covers accepted uTools fallback dispatch, so same-Turn completion-time enrichment or an older full snapshot cannot recreate unread. RAW-145 advances to `task-state-v5`: exact persisted user decisions carry explicit provenance through Preload and Domain, while ordinary connector waiting remains rejected. RAW-146 makes direct-open ordering and compact action text consume the same pinned-first contract. Privacy/native-write rules remain unchanged. Automated and current-machine source-path evidence are verified; new-build UI acceptance remains separate.
- State invariant: real activity patches open a new epoch; only a Desktop non-active activity patch whose private sequence is strictly later than the current App Server active event may revoke `app-server-live`. Delta and full snapshot share one active-exit reducer that reads confirmed provenance from the candidate itself. Unchanged pre-active terminal stays ongoing and cannot clear the baseline; accepted exact/targeted/corroborated terminal closes the epoch, except an exact latest completed Plan remains Plan-only waiting until a newer Turn begins. Stopped additionally requires explicit failed/interrupted plus exact idle/not-running; missing outcome remains ongoing. Full inventory preserves stronger session evidence, the private live-event waterline and a bidirectional generation barrier; stale delta bridge-state fields and lower/generationless V2 snapshots cannot cross it. Known/present task updates are never batch-blocked by unknown/missing rows. Missing rows perform an immediate structural recheck and schedule the remaining quarantine hold themselves even when periodic inventory is disabled. Compatible Turn verification reuses one bounded cycle; initially/newly true unread or mode/epoch change may start or replace it, while unchanged polling does not. Exact read-state/successful-open session events rank first; otherwise refollow snapshot false may repair stale persisted true, while a parseable native set's membership/nonmembership outranks snapshot true and native-unavailable reuses the Bridge's last successful native observation before weaker snapshot true. Exact Desktop requests rank first for waiting; an ownerless interrupted/failed/inProgress row may use only a safe unresolved `request_user_input` rollout tail marked `persisted-decision`, and a completed row may use only exact latest-Turn Plan item metadata with the same finite-decision provenance, while plain connector waiting stays non-authoritative and previously observed input/approval/Plan shadows survive soft owner loss until newer snapshot/Turn/inventory evidence. Side Chat initial/final-exit and recovered known-child rereads target the causal child; only child/parent topology survives a bridge interruption, and a wholly unknown transient child is not reconstructed. Controller disablement clears derived inventory/source/generation/exit/cursor baselines while preserving EyPc-owned settings and local organization; non-kill surface exit preserves the Desktop observer, explicit disable/kill does not, and a new runtime generation rejects old asynchronous results. Read-state changes unread only; a new completion clears pre-completion false.
- Read acknowledgement invariant: a confirmed task open is held in bounded preload-process memory for a concrete Turn and its known Side Chats. Transport teardown/rebuild, stale native true, same-Turn `completedAt` enrichment and older full snapshots cannot revoke it; exact active/Turn-started, a different Turn ID/startedAt, explicit removal or process end can.
- Shortcut cache invariant: non-kill `mainHide` is a visibility transition and does not close App Server or clear task/project aliases and latest-Turn cache. A concurrent task action may reuse an in-flight refresh only after that refresh publishes threads; explicit Controller close, feature disable, kill and process exit retain teardown authority.
- Removed paths: completion presentation hold/timer/held-card rewrite, its visible setting and normalized `completionPresentationDelayMs`; provider/local cross-clock ordering; historical card-color preview/cancel/commit Runtime actions and Controller transient override. Initial-snapshot corroboration, 50/200ms structural coalescing, watchdog, 15s reconciliation and missing-key quarantine remain.
- Verification: 2026-08-03 RAW-146 聚焦 Controller/UI `90/90`、完整 Vitest `752/752`（`57/57` 文件）、typecheck、main preload 语法/镜像和 uTools runtime validation 通过；当前真实 Provider→production Domain 仍为 `persistedWaitingInput=1 / productWaitingInput=1`。`127.0.0.1:8092` 当前未监听，已安装 ASAR 仍为修复前 v4 哈希；本轮按用户要求不启动服务、不更新或重载实际宿主。源码状态为 `automated-verified / host-pending`。
- Gate: normal uTools reload 与真实 ordinary/recovered active→completed、stopped↔active、task-switch、Side Chat、多分支、badge/list/action atomicity，“普通输入/审批阻断 Plan、Plan 清空后进入近期 active”的快捷键顺序，“关闭插件—在 Codex 侧读取/等待态变更/归档删除或项目移除—重开”的库存与状态恢复矩阵，以及 RAW-139–145 的冷/热全局任务入口、跨显隐同 key alias 恢复、连续快捷键与 Runner 时延、uTools fallback 打开、同 Turn completion 时间补全、ownerless persisted-decision 展示/新 Turn 解除、completed Plan 无视 unread 持续待输入和完成未读无闪跳仍为 host-acceptance blocker。没有归档、项目移除、进程启停或 Codex 原生状态写入。
- Reusable failure routing: [six task-state evidence boundaries](../knowledge/error-memory/README.md#L1); fixture contract: [float-bridge mock drift](../knowledge/error-memory/codex-float-bridge-mock-contract-drift.md#L1).

## Other Active Deliveries

- Window Jump: [spec](260724/1527-window-jump-workbench/spec.md#L1) / [verify](260724/1527-window-jump-workbench/verify.md#L1). WJ-21 keeps a real main/root window as the stable persisted target, displays bridge-proven ordinary-app children as transient exact destinations, and keeps Finder/Explorer fixed at virtual parent → real roots. Root/slot activation uses `root-current`; child activation/close uses fail-closed `member-exact`. Windows filters to actionable top-level/owned popups with `GA_ROOTOWNER`; macOS is AX-first with positive CG identity corroboration, so CG-only/system/helper/unproven surfaces are omitted. Bridge `wj21-main-child-window-tree`, production/docs, exact-snapshot focused tests `219/219`, semantic typecheck, production build and uTools package validation pass. Full-suite A/B improves from parent `661/675` (14 failures) to WJ `664/676` (12 failures), with all remaining failures confined to untouched Codex files; stale preload list ingestion is blocked before coalescing and Runtime inventory updates are centralized. uTools reload, native activation/close and visual acceptance remain pending.
- File Favorites: [traceability](260711/1452-file-favorites-workbench/requirements-traceability.md#L1). Automated acceptance remains `36 files / 332 tests`; real macOS uTools file-action and Windows/Linux checks remain pending.
- Cross-tab command panels: [spec](260713/0834-cross-tab-responsive-command-panels/spec.md#L1) / [verify](260713/0834-cross-tab-responsive-command-panels/verify.md#L1). Shared panels/tooltips and responsive browser matrix are accepted; real host file actions remain pending.
- Global operation tooltip: [spec](260730/2050-global-operation-tooltip-polish/spec.md#L1). All 271 current main-app native buttons have a static name source (including MQTT tooltip helpers); the shared layer now owns title-chord normalization, descriptions, disabled reasons, dynamic refresh and lifecycle cleanup, and suppresses the competing Codex main-page pseudo-tip. Float remains child-owned; development acceptance is user-owned.
- Quick Jump: [spec](260718/0947-quick-jump-center-overlay/spec.md#L1). Target-center overlay automated/browser acceptance is retained; real uTools visual smoke remains pending.
- MQTT: [current sync](2606231645-eypc-mqtt-websocket-tab/06-sync-doc.md#L1); multi-select merged JSON export: [spec](260730/1016-mqtt-multi-export/spec.md#L1); tooltip/shortcut identifiability polish: [spec](260730/1044-mqtt-tooltip-shortcut-polish/spec.md#L1) / [verify](260730/1044-mqtt-tooltip-shortcut-polish/verify.md#L1), now wired through product `data-operation-*` tips; development acceptance user-owned after reload hover check.
- Settings help: [raw requirement](260729/1135-settings-feature-help/raw-requirement.md#L1); `EYPC-FEATURE-HELP-001` coverage remains active.

## Active Process Index

| Concern | Authority | Verification |
| --- | --- | --- |
| Codex quota/task companion | [spec](260718/1148-codex-quota-float/spec.md#L1) | [verify](260718/1148-codex-quota-float/verify.md#L1) |
| Window jump and stable slots | [spec](260724/1527-window-jump-workbench/spec.md#L1) | [verify](260724/1527-window-jump-workbench/verify.md#L1) |
| Settings feature help | [raw](260729/1135-settings-feature-help/raw-requirement.md#L1) | [verify](260729/1135-settings-feature-help/verify.md#L1) |
| Quick Jump overlay | [spec](260718/0947-quick-jump-center-overlay/spec.md#L1) | Spec-owned |
| Cross-tab panels | [spec](260713/0834-cross-tab-responsive-command-panels/spec.md#L1) | [verify](260713/0834-cross-tab-responsive-command-panels/verify.md#L1) |
| Global operation tooltip | [spec](260730/2050-global-operation-tooltip-polish/spec.md#L1) | User-owned acceptance |
| File favorites | [traceability](260711/1452-file-favorites-workbench/requirements-traceability.md#L1) | Task-owned |
| MQTT workbench | [sync](2606231645-eypc-mqtt-websocket-tab/06-sync-doc.md#L1) / [multi-export](260730/1016-mqtt-multi-export/spec.md#L1) / [tooltip polish](260730/1044-mqtt-tooltip-shortcut-polish/spec.md#L1) | User-owned acceptance |
