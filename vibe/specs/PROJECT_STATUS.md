# EyPc Project Status Hub

Tool: codex
Date: 2026-08-09

## Purpose

This hub routes current implementation, verification gates and durable authorities. Product behavior belongs in [PRODUCT_REQUIREMENTS.md](PRODUCT_REQUIREMENTS.md#L1); architecture and implementation facts belong in [ARCHITECTURE.md](../knowledge/ARCHITECTURE.md#L1) and [technical-details.md](../knowledge/technical-details.md#L1).

## Current Snapshot

- Product: keyboard-first uTools plugin for local PC capability calls.
- Active lines: port management, file favorites, cross-platform window jump, MQTT workbench, Quick Jump, Codex Companion and settings feature help.
- Codex Environment Action Runner: [spec](260729/1435-codex-environment-actions/spec.md#L1) / [verify](260729/1435-codex-environment-actions/verify.md#L1). 2026-08-03 接纳复核现为 `automated-verified / host-pending`：exact structured argv、strict TOML version、Action Host revision、worktree-sensitive target identity、tasks-only cold preflight、Host-persisted Environment 槽位、Runner-first message、一次性 stale-alias retry、`onPluginOut(isKill)` 双生命周期与 Windows 绝对 `taskkill /T` 非强制树停止已闭环。聚焦 7 文件 `95/95`；最终 `pnpm run verify` 为 57 文件 `733/733`，随后 typecheck、production build、runtime prepare 与 uTools validation 全部通过。真实 child/hotkey/plugin-out/Windows/长期 Serve/真实 Action 继续由宿主验收。
- RAW-143 performance increment: ordinary non-kill hide now keeps App Server/project aliases hot; Runner/slots reuse verified inventory and rebuild only on first use or Host stale-alias. Related four files are `149/149` with typecheck and preload syntax/mirror verified; full verify/build was intentionally not repeated, and real shortcut latency remains host-pending.
- RAW-144 incremental-cache/read-recurrence increment: task inventory and Activity remain hot across Tab/Float visibility for the enabled feature; Runner catalogs invalidate per project rather than rebuilding all; accepted uTools fallback open and Turn-ID-bound acknowledgement close same-Turn unread recurrence. Related affected suite is `301/301` with type/preload and focused packaging checks; real-host latency and read recurrence remain pending.
- RAW-145 persisted-decision increment: current-machine read-only evidence exposed a Preload→Domain provenance break hidden by a duplicated loose preflight predicate. `task-state-v5` now carries only structurally verified ordinary-input/Plan decisions as `persisted-decision`; plain connector hints remain conservative. Focused `192/192`, typecheck and full build passed. The historical v4 installation gate is closed by the 2026-08-07 active-ASAR readback below; RAW-147 has repaired the [real-preflight script](../../scripts/codex-real-preflight.mjs#L1) so its production-Domain gate is reusable again.
- RAW-146 consumer-consistency closeout is historical for the two dedicated attention actions: it originally unified pinned-first “打开第一条” behavior and passed focused `90/90`, full Vitest `752/752`, typecheck and runtime/static gates. RAW-149 now supersedes only those waiting-input/completed-unread action semantics with global newest-first persistent progress; the ordinary display comparator and generic task cycle retain the RAW-146 foundation.
- RAW-147 active→waiting optimization: active-ASAR/readback and current Codex package inspection proved `following=true` is a peer follower-state announcement, not a request. The old branch now represented by the [no-reply boundary](../../preload/index.js#L3519) echoed it, allowing two EyPc followers to create 32,329 messages in 250ms and about 368,000 in 2.5s. The handler now replies only to explicit following-status requests; the new RED/GREEN contract and Bridge `81/81`, exact-change typecheck/production build, current three-way Preload/runtime gates and repaired real preflight pass. A packaged-source probe sends a bounded 24 initial follows and emits no recursive writes after 48 old-client announcements. Error memory is routed as one stream-follow primary plus one pending-request dependency introduction, preventing a second protocol-loop owner. A later concurrent Claude fixture change blocked the whole-tree typecheck/build rerun at that time; RAW-148 current-tree typecheck now passes. Running uTools still uses the pre-RAW-147 ASAR, so reload, owner snapshot and real active→waiting acceptance remain pending.
- RAW-148 recognition/dedup closeout: [codexEnvironmentPresentation.ts](../../src/domain/codexEnvironmentPresentation.ts#L1) now owns the Runtime banner, grid, compatibility-pending predicate and launch help; the Page only renders it. Task/project ownership is resolved once per Float row from the Domain markers, and launch-path mutations publish the Host-returned snapshot without a second inspection. RAW-022 wins over stale Codex-only wording: data/state/quota/empty-state compatibility remains, while every task/project keeps one visible and accessible ownership marker. Focused `188/188` and current-tree typecheck pass; no Bridge/Preload/build/runtime surface was touched.
- RAW-149 permission-attention/latest-first increment is `automated-verified / host-pending`: the Desktop follower now treats command execution, file change, permissions and MCP elicitation approvals as input attention alongside questions and Plan decisions. `task-state-v6` carries only anonymous state-entry time; input/approval share one count, approval is excluded from ongoing, and input/completed-unread are globally latest-first across providers. Their dedicated actions persist a bounded anonymous unseen-instance progress and advance only after confirmed Host open, including manual row opening. Generic previous/next, projects/pins and local-pin fallback remain unchanged. Affected `8/8` files and `292/292` tests, typecheck, production build, Preload sync/syntax, runtime validation and documentation audit pass. Real Provider→production Domain/Presentation v6 preflight is connected/verified and asserts input/active disjointness; running uTools ASARs remain v5, and the first UI reconnect observation stopped at the locked-Mac gate, so real non-Full-Access approval acceptance remains pending.
- RAW-150 waiting-to-continue/archive increment is `automated-verified / host-pending`: Domain keeps `stopped`, while all card/segment copy shows “待继续” without a fifth top Tab, counter or shortcut. Explicit stopped is task-archiveable；Codex rereads exact thread/latest Turn and returns `state-changed` after resume。更新引入：RAW-154 已以 Claude D′ 单目标静默 `isArchived` 事务取代本条历史 Deep Link+AX 路线；App 日志降为增强证据，元数据+私有活动库存双确认即可成功，非成功仍保留卡片。Project batch remains Codex completed-only.
- RAW-151 bidirectional waiting-edge increment is now `host-exposed-defect / superseded-by-RAW-153` for active-vs-active arbitration: its reducer, bounded resubscribe, rollout watcher, phase-only watchdog and 100-cycle P95 contracts remain valid, but the true v7 host proved that current-owner old waiting could survive a newer active/Turn-started and later reappear after shadow replay. The former “running uTools is still v5 / host-pending” statement is no longer current.
- RAW-152 process-navigation increment is `automated-verified / host-pending`: `companion-navigation-v1` now owns the process-lifetime generic cursor and one Codex/Claude dispatch queue. Every enabled Provider inventory must settle before one ready snapshot is published; each previous/next press advances the cursor, 75ms trailing coalescing opens only the final target, manual/attention opens supersede an undispatched generic target, and Host dispatch concurrency is capped at one. A hot uTools entry is consumed in Preload without a duplicate Renderer payload; ordinary mainHide/Renderer disposal only detaches, while Provider changes, feature disable, kill and process exit invalidate. Exact card targets can still enter that queue while a remounted Renderer temporarily retains the older ready snapshot. Affected `11/11` files and `325/325` tests, typecheck, production/uTools build, Preload sync/syntax and runtime validation pass. Real cross-provider rapid-switch crash acceptance remains pending because the current host was not reloaded.
- RAW-153 waiting-clear causal barrier increment is `automated-verified / host-rework`: `task-state-v8` gives private main/Side Chat request instances and runtime waiting flags monotonic observation sequences. Request removal, matching `serverRequest/resolved`, newer active/Turn-started, matching output, user continuation and new `task_started` establish a clear barrier; older snapshot/read-state/refollow/sticky/rollout evidence cannot revive the cleared instance, while a genuinely newer correlation re-enters immediately. Unmatched resolved only bounds a target-task resubscribe and preserves concurrent approvals. Bridge `94/94`, the affected `9/9` files `379/379`, typecheck, production/uTools build, canonical/public syntax+mirror and synchronous-IPC static gates pass；当前源码的真实 Provider→生产 Domain/Presentation 只读预检也返回 v8、connected、completeness verified，匿名瞬时投影为 `waiting=1 / active=2`。运行中 ASAR 仍是 v7，并曾复现 `waiting=1 / active=4` 而同期旧 Provider→Domain 为 `0 / 5`；正常 v8 reconnect 和 1.25 秒/30 秒 no-rebound 门禁仍未完成，所以不得形成完成提交。
- RAW-154 unified companion task kernel is `automated-verified / host-pending`: current source advances to `task-state-v9` and `companion-task-actions-v1`. Exact `interrupted/user-stopped` now establishes 待继续 immediately after unresolved input/approval and causally newer Turn/active checks, clearing older active/waiting shadows without inventing Desktop idle；ordinary failed keeps the conservative idle/not-running gate。Renderer submits intent only，the Dispatcher owns action/provider arbitration，Provider adapters own side effects，and Controller owns the verified mutation reducer。Claude `claude-metadata-archive-v2` performs one indexed target-only atomic metadata transaction with rollback/concurrency guards，while exact file deltas and a one-second indexed watchdog converge independently of full inventory/quota/state/unread。The process-owned archive shortcut requires the same target identity twice within five seconds and never opens Claude。Affected `20/20` files、`550/550` tests，typecheck、1868-module production/uTools build、runtime validator、canonical/public mirror、syntax and diff gates pass；33 个变更 Markdown 零断链，适配器正文一致，34/49/31 文档同步组私有回执已重签。Real v9 host reconnect and separately authorized disposable-session D′ canary remain pending。
- RAW-154 rework is now `automated-verified / host-pending`, not a new task. Preload owns one `companion-task-kernel-v1` and atomically publishes `companion-task-package-v1`; card/attention/cycle/archive interactions submit one intent and re-resolve the complete target from the latest package. Silent `onPluginEnter` no longer waits for Renderer or replays after Alt+Tab；cold/degraded state uses one all-enabled-Provider tasks-only preflight and never jumps from a partial or non-fresh set。Deterministic Host/Renderer asset IDs plus Main/Float four-end handshakes fail closed as `reload-required`；Renderer identity is seeded by the embedded Host contract, build means only `artifact-ready`，while `host-loaded` requires a real matching uTools handshake。Final review also closed lower/duplicate producer drafts、per-Provider generation regression、the new-Preload/old-Main raw-port bypass and membership-only Codex events that previously skipped Host inventory reconciliation，并以真实 Kernel 验证 Controller 向 Main/Float 发布同一修订且卡片与快捷键命中同一完整目标。New directly affected evidence is `12/12` files、`485/485` tests plus typecheck、production/uTools build、runtime validator、preload mirror/syntax and diff gates；the older `550/550` remains the D′/v9 baseline。The running uTools host was not reconnected or terminated, so silent hotkey, no-Alt+Tab-replay and cross-provider latency acceptance remain host-pending。
- Development acceptance is user-owned. Automated/static evidence below does not claim real uTools, cross-platform or production acceptance unless explicitly stated.

## Codex Companion

- Authority: [current spec](260718/1148-codex-quota-float/spec.md#L1), [verification](260718/1148-codex-quota-float/verify.md#L1), [handoff](260718/1148-codex-quota-float/handoff.md#L1).
- Current requirement: RAW-116–154. RAW-131–145 retain the closed activity, terminal, unread, waiting, Side Chat, Plan, inventory, hot-cache and read-acknowledgement matrix. RAW-146 remains historical for the ordinary pinned-first comparator；RAW-149 owns latest-first attention progress；RAW-150 owns card-local “待继续” plus write-time revalidation and is refined by RAW-154's exact interrupted/D′ clauses；RAW-151 retains the bidirectional waiting hot lane except its v7 active-vs-active arbitration is superseded by RAW-153's v8 waiting-clear barrier；RAW-152 owns process-lifetime generic navigation arbitration；RAW-154 owns v9 exact interrupted terminal priority、the Provider-neutral task-action kernel、Claude indexed metadata mutation delta and process-owned archive confirmation。RAW-147 keeps follower announcements one-way and RAW-148 keeps environment/source recognition single-owner。Sorting、attention progress、generic previous/next eligibility/order、project-batch completed-only and privacy rules remain unchanged；the only new native-write exception is the version-gated single-target Claude D′ transaction。Automated verification and new-build host acceptance are separate gates.
- State invariant: real activity patches open a new epoch; only a Desktop non-active activity patch whose private sequence is strictly later than the current App Server active event may revoke `app-server-live`. Waiting requests/runtime flags share that monotonic sequence and remain visible only when their observation is later than the applicable waiting-clear barrier. Exact request removal/matching resolved or newer active/Turn-started/continuation evidence clears immediately; old snapshot/read-state/refollow/rollout replay cannot cross the barrier, while a newer request correlation can. Delta and full snapshot share one active-exit reducer that reads confirmed provenance from the candidate itself. Unchanged pre-active terminal stays ongoing and cannot clear the baseline; accepted exact/targeted/corroborated terminal closes the epoch, except an exact latest completed Plan remains Plan-only waiting until a newer Turn begins. For exact `interrupted/user-stopped`, unresolved input/approval remains waiting first and a causally newer Turn/active restores ongoing；otherwise the terminal watermark establishes stopped immediately and clears only older active/waiting shadow。Ordinary failed still requires exact idle/not-running；missing outcome remains ongoing。Full inventory preserves stronger session evidence, the private live-event waterline and a bidirectional generation barrier；stale delta bridge-state fields and lower/generationless V2 snapshots cannot cross it。Known/present task updates are never batch-blocked by unknown/missing rows。Missing rows perform an immediate structural recheck and schedule the remaining quarantine hold themselves even when periodic inventory is disabled。Compatible Turn verification reuses one bounded cycle；initially/newly true unread or mode/epoch change may start or replace it，while unchanged polling does not。Exact read-state/successful-open session events rank first；otherwise refollow snapshot false may repair stale persisted true，while a parseable native set's membership/nonmembership outranks snapshot true and native-unavailable reuses the Bridge's last successful native observation before weaker snapshot true。Ownerless rollout/persisted-decision and Side Chat topology keep their existing finite/private boundaries。Feature/inbox disablement clears derived inventory/source/generation/exit baselines while preserving EyPc-owned settings and local organization；ordinary Renderer disposal only detaches from retained Host navigation/action snapshots，explicit disable/Provider change/kill does not preserve them，and a new runtime generation rejects old asynchronous results。Read-state changes unread only；a new completion clears pre-completion false。
- Read acknowledgement invariant: a confirmed task open is held in bounded preload-process memory for a concrete Turn and its known Side Chats. Transport teardown/rebuild, stale native true, same-Turn `completedAt` enrichment and older full snapshots cannot revoke it; exact active/Turn-started, a different Turn ID/startedAt, explicit removal or process end can.
- Shortcut cache invariant: non-kill `mainHide` is a visibility transition and does not close App Server or clear task/project aliases, latest-Turn cache or the ready process navigation snapshot. A concurrent cold action may reuse an in-flight refresh only after every enabled Provider inventory lane settles. Renderer `dispose()` releases only its navigation lease; feature disable, Provider configuration change, kill and process exit retain teardown authority.
- Removed paths: completion presentation hold/timer/held-card rewrite, its visible setting and normalized `completionPresentationDelayMs`; provider/local cross-clock ordering; historical card-color preview/cancel/commit Runtime actions and Controller transient override. Initial-snapshot corroboration, 50/200ms structural coalescing, watchdog, 15s reconciliation and missing-key quarantine remain.
- Verification: historical evidence remains in [verify](260718/1148-codex-quota-float/verify.md#L1). Current source carries `task-state-v9`、`companion-navigation-v1`、`companion-task-actions-v1` and `claude-metadata-archive-v2`；RAW-154 affected `20/20` files、`550/550` tests，typecheck、1868-module production/uTools build、runtime validator、canonical/public mirror、syntax and diff gates pass。The running host has not yet loaded v9；its earlier v7/v8 observations are defect evidence，not v9 acceptance。Current state is `automated-verified / host-pending` until normal v9 reconnect/no-rebound acceptance and separately authorized D′ canary pass.
- Gate: after automated acceptance, normally reload EyPc and first validate one Codex and one Claude card click, then rapid alternating previous/next across both Providers through at least two mainHide cycles; only the final coalesced target may open and the plugin must not crash. Continue with the non-Full-Access approval, multiple input/unread progress, `1→2→3 / new 6→6→4→5`, read acknowledgement and recovery gates. EyPc must not approve/reject/submit requests or write Codex native state.
- Reusable failure routing: [six task-state evidence boundaries](../knowledge/error-memory/README.md#L1); fixture contract: [float-bridge mock drift](../knowledge/error-memory/codex-float-bridge-mock-contract-drift.md#L1).

## Claude Code Companion

- Current authority: [spec](260807/claude-code-companion-authority-reset/spec.md#L1) / [updated plan](260807/claude-code-companion-authority-reset/plan.md#L1) / [research and strict local test route](260807/claude-code-companion-authority-reset/research.md#L1) / [verification](260807/claude-code-companion-authority-reset/verify.md#L1).
- Current state: `revision-6 implementation-landed / RAW-024 focused-verified / RAW-154 D′ automated-verified / interactive-host-partial`. Production adds explicitly authorized Claude App encrypted-cache quota, live limits/reset parsing, parent-Turn terminal reconciliation, source→Controller→Float revisions, joinable per-task state/unread sync, stable unread V2 plus same-completion read hint, virtual project merging/filtering, explicit provider visuals/capabilities and version-gated completed/stopped task archive. RAW-154 replaces the historical open→AX archive with `claude-metadata-archive-v2`: only a unique indexed App-local file may be changed, compatible phase/stat/hash are reread, only `isArchived` changes through a same-directory atomic transaction, metadata+private active-inventory removal confirm success, and rollback/concurrent-write uncertainty is fail-closed. App logs are optional evidence；archive never opens Claude。Mixed inventory/transcript、Hooks-only completion、Stop-tail revival、manual state/read override、import route、byte scan、persistent read receipt、Claude Code quota credential、all-authority refresh、fixed-window and AX archive paths are superseded.
- Verified gates: prior affected Claude regressions (`16/16` files、`343/343` tests)、RAW-024 (`4/4` files、`120/120` tests) and historical RAW-150 automation remain retrieval evidence；RAW-154's exact file-transaction、open preflight、mutation-delta and Controller tests are finalized in the Codex Companion [verify](260718/1148-codex-quota-float/verify.md#L1)。Latest historical local projection has 27 Code rows (0 running / 24 completed / 1 stopped / 2 unknown), 25 from exact App logs；actual uTools LevelDB reads previously passed 30/30 with one native unread membership，and live App quota HTTP 200 yielded exactly 5h + weekly_all + Fable scoped windows with reset。The historical 74-file / 1061-test full ladder remains non-repeatable without a new RAW-018 impact trigger。更新引入（2026-08-10）：真实 D′ canary 已在用户显式授权下执行并通过 —— 生产 Bridge 对真实 App 数据完成单目标 `completed` 会话归档，`outcome=archived`、语义 diff 仅 `isArchived`、权限位与其余 21 键不变、活动库存 14→13、重复调用幂等且文件哈希不变，证据收口于 [reset verify](260807/claude-code-companion-authority-reset/verify.md#L94)。更新引入（2026-08-11）：D-1 成功与幂等提示已明确区分 EyPc 移除和 Claude 原生侧栏未确认，聚焦 `3/3` 文件、`100/100` 测试、Preload 语法/镜像、typecheck、1870-module production build、runtime validator 通过，产物身份为 `host-36616822511986c18f2c / renderer-25da7ef64b81aadc76f8`；D-2 只读核验结论为当前 `unsupported`，因为 D′ 绕过原生 session-manager/`archived` 事件且官方公开入口没有本地 Code 归档。Manual App archive delta、real old-task UI sync、controlled permission/input/response/title/restart、mixed-project filters、final rendered/high-contrast matrix 以及 D′ 在真实 uTools 宿主内经 Float 卡片点击的移除路径仍 pending。
- Decision/archive owner: `DEC-20260807-01..13`, `DEC-20260808-14` and `ARCH-20260807-01..09`/`ARCH-20260808-10` in the current spec. Five directly affected older Claude documents are `archived-linked` at their original paths with this Spec as replacement; no physical migration occurred or is authorized.

## Other Active Deliveries

- Window Jump: [spec](260724/1527-window-jump-workbench/spec.md#L1) / [verify](260724/1527-window-jump-workbench/verify.md#L1). WJ-22 keeps exact root identity as `PID+CGWindowID/HWND`, treats browser Tab/title/display/Space as mutable state, and clears a locator only after `probeInstance()` returns matching verified-gone evidence. A preload-session per-instance/per-display Space cache switches only the target display and retries one stale target once; same-app roots remain isolated, family projection cannot merge targets/slots, and slot recovery updates one slot. Native window scripts and operations now live exclusively in the platform modules; the main preload only guards, injects and mounts. Bridge `wj22-native-instance-space-cache`; full Vitest `58/58` files and `767/767` tests, typecheck, production/uTools build/package validation and post-extraction direct macOS multi-display/multi-Space exact-root smoke pass. Actual uTools reload/global-hotkey visual acceptance and Windows real-host checks remain pending.
- File Favorites: [current increment spec](260806/1120-favorite-quick-open-runners-slots/spec.md#L1) / [original workbench traceability](260711/1452-file-favorites-workbench/requirements-traceability.md#L1). 2026-08-06 状态 `automated-verified / host-pending`：按平台信任的结构化运行器、10 个 `mainHide` 文件槽、跨分组快速搜索、30 天半衰期学习、`Ctrl+1…0` 结果/抽屉分层和完整页槽位/学习管理已完成。收藏聚焦 `91/91`，新增 Runtime `4/4`，typecheck、production build、preload prepare 与 uTools validation 通过；当前并行工作树完整 Vitest 为 `1124/1127`，两项失败来自非收藏 Claude hook 监听，另一项 MQTT Runtime 超时已在干净 `HEAD 28e5db6` 复现。macOS/Windows/Linux 真实默认打开、后台/终端脚本、全局快捷键、重启与平台绑定仍为宿主门禁；固定角独立窗口是第二阶段，未实现。2026-08-07 独立复核 [review](260806/1120-favorite-quick-open-runners-slots/review.md#L1)：需求↔实现逐条取证，无 P0/P1；复跑 8 个 validator 得 `256/257`，唯一失败的 MQTT 巨型用例以 30s timeout 单跑 passed（5.43s，阈值问题）。三处口径差（同平台槽位互斥、只在当前平台配置、学习以宿主受理计数）已回填合同/PRD/用户说明；MQTT 用例阈值明确不归属本增量、暂停并交 MQTT owner。2026-08-07 后继增量 [收藏运行控制台、逐项同步与动态参数](260807/1740-favorite-runner-console-sync-and-args/spec.md#L1) 已批准但**未实现**：插件内终端 UI 已明确否决。**同日落地**：重命名不掉信任（指纹条件纳入 `name` + 旧指纹原地升级）、后台运行 run log 自动采集（fd 重定向、2MB/40 份/60 条上界、目录不可写时只丢日志不丢运行）、运行器选填声明日志路径（占位符展开 + 绝对路径校验，从不创建/执行）、退出码贯通（`已启动 ≠ 成功`，非 0 退出独立措辞与红色态，系统终端明说退出码不可得）。证据：`tests/domain|ui|integration|platform|unit` `66 files / 818 tests`、`action.test.ts` `167/167`、`pnpm run build` 全链通过（typecheck 0 error + uTools validation）、preload 镜像一致；宿主验收未跑。2026-08-08 追加落地：声明式动态参数 `{ask:名称[=默认值]}`（只允许在参数里、取值不二次展开、信任绑声明不绑取值、取消不运行、槽位触发先显示窗口），证据为 `66 files / 822 tests`、`action.test.ts` `168/168` 与 `pnpm run build` 全链通过。**仍未实现**：逐项同步（信任状态永不同步）、L3 自适应日志推测；搜索与使用学习经裁决维持现状不改，系统终端降为逃生舱使 Linux 适配器风险降为常规验收项。该增量未落地前，用户说明与架构文档一律不得按其宣称行为已交付。
- Cross-tab command panels: [spec](260713/0834-cross-tab-responsive-command-panels/spec.md#L1) / [verify](260713/0834-cross-tab-responsive-command-panels/verify.md#L1). Shared panels/tooltips and responsive browser matrix are accepted; real host file actions remain pending.
- Global operation tooltip: [spec](260730/2050-global-operation-tooltip-polish/spec.md#L1). All 271 current main-app native buttons have a static name source (including MQTT tooltip helpers); the shared layer now owns title-chord normalization, descriptions, disabled reasons, dynamic refresh and lifecycle cleanup, and suppresses the competing Codex main-page pseudo-tip. Float remains child-owned; development acceptance is user-owned.
- Quick Jump: [spec](260718/0947-quick-jump-center-overlay/spec.md#L1). Target-center overlay automated/browser acceptance is retained; real uTools visual smoke remains pending.
- MQTT: [current sync](2606231645-eypc-mqtt-websocket-tab/06-sync-doc.md#L1); multi-select merged JSON export: [spec](260730/1016-mqtt-multi-export/spec.md#L1); tooltip/shortcut identifiability polish: [spec](260730/1044-mqtt-tooltip-shortcut-polish/spec.md#L1) / [verify](260730/1044-mqtt-tooltip-shortcut-polish/verify.md#L1), now wired through product `data-operation-*` tips; development acceptance user-owned after reload hover check.
- Settings help: [raw requirement](260729/1135-settings-feature-help/raw-requirement.md#L1); `EYPC-FEATURE-HELP-001` coverage remains active.

### Historical Claude-related delivery evidence

- Retrieval index only: [feasibility](260805/1130-claude-companion-feasibility/spec.md#L1), [provider](260805/1150-claude-companion-provider/spec.md#L1), [quota row](260805/2051-float-quota-single-line/spec.md#L1), [status visibility](260806/1046-claude-quota-status-visibility/spec.md#L1), [desktop provider](260806/1130-claude-desktop-provider/spec.md#L1), [registration](260806/2115-claude-registration-visibility/spec.md#L1), [N-window quota](260806/2210-claude-quota-all-windows/spec.md#L1), [old open](260806/2147-claude-open-in-desktop-app/verify.md#L1) and [old unread](260806/2147-claude-open-in-desktop-app/unread-authority.md#L1).
- Reusable Hook registration, presentation and N-window domain evidence may be carried forward only through the new plan. Mixed session-family, `resume/import`, byte-scan unread and latest-event conclusions are `ARCH-20260807-01..04` and are not current requirements or acceptance evidence.

## Active Process Index

Claude 的唯一当前行是下表首行；其余 Claude 行保留为历史检索入口，不是并行当前合同。

| Concern | Authority | Verification |
| --- | --- | --- |
| Claude Code Companion authority reset | [spec](260807/claude-code-companion-authority-reset/spec.md#L1) / [plan](260807/claude-code-companion-authority-reset/plan.md#L1) / [research](260807/claude-code-companion-authority-reset/research.md#L1) | [Implementation landed; automated + quota host verified; interactive UI matrix partial](260807/claude-code-companion-authority-reset/verify.md#L1) |
| Codex quota/task companion | [spec](260718/1148-codex-quota-float/spec.md#L1) | [verify](260718/1148-codex-quota-float/verify.md#L1) |
| Window jump and stable slots | [spec](260724/1527-window-jump-workbench/spec.md#L1) | [verify](260724/1527-window-jump-workbench/verify.md#L1) |
| Settings feature help | [raw](260729/1135-settings-feature-help/raw-requirement.md#L1) | [verify](260729/1135-settings-feature-help/verify.md#L1) |
| Quick Jump overlay | [spec](260718/0947-quick-jump-center-overlay/spec.md#L1) | Spec-owned |
| Cross-tab panels | [spec](260713/0834-cross-tab-responsive-command-panels/spec.md#L1) | [verify](260713/0834-cross-tab-responsive-command-panels/verify.md#L1) |
| Global operation tooltip | [spec](260730/2050-global-operation-tooltip-polish/spec.md#L1) | User-owned acceptance |
| File favorites | [traceability](260711/1452-file-favorites-workbench/requirements-traceability.md#L1) | Task-owned |
| MQTT workbench | [sync](2606231645-eypc-mqtt-websocket-tab/06-sync-doc.md#L1) / [multi-export](260730/1016-mqtt-multi-export/spec.md#L1) / [tooltip polish](260730/1044-mqtt-tooltip-shortcut-polish/spec.md#L1) | User-owned acceptance |
| Historical Claude deliveries | [retrieval index above](#historical-claude-related-delivery-evidence) | Superseded/partial evidence; not current acceptance |
