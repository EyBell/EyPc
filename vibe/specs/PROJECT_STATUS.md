# EyPc Project Status Hub

Tool: codex
Date: 2026-09-02

## Purpose

This hub routes current implementation, verification gates and durable authorities. The unique global current product truth is [PRODUCT_REQUIREMENTS.md](PRODUCT_REQUIREMENTS.md#L1); architecture and implementation facts belong in [ARCHITECTURE.md](../knowledge/ARCHITECTURE.md#L1) and [technical-details.md](../knowledge/technical-details.md#L1).

## Process Governance Baseline

- Template propagation baseline: project adapters route to the CodeNote process owners and keep only EyPc-specific facts; `v3-route-accepted`.
- Hook Primary Response: `w24-primary-objective-continuity-accepted`; non-blocking evolution advisories cannot replace the task result.
- Documentation Impact: `w28-documentation-impact-accepted`; affected current authorities, verification, memory and user help are synchronized before Root acceptance.
- Standard requirement: `w30-standard-requirement-owner-accepted`; `raw-requirement.md + spec.md` uses the Spec owner, while Controlled work retains its five-file ledger.

## Current Snapshot

- RAW-205 Cursor Plan 模式会话纳入库存与阻塞待决展示为待输入（2026-09-03，claude 会话，worktree `260903-cursor-plan-mode`）is `implementation-landed / focused-automated-verified / host-pending`。根因：进行中的 Cursor 会话是 Plan 模式（`unifiedMode = plan`），[库存白名单](../../preload/cursor/inventory.cjs#L107) 按 260818 裁决只收 `agent`，钩子事件一直到达却无任务可挂；Plan 运行时 Cursor 不写 `unfinishedRunAt`，进行中只能靠钩子 `turnOpen`。改为 `agent | plan`，`hasBlockingPendingActions` 从 `unknown` 改为精确 `user-input` 交互（待输入、可一键跳转、不发明待确认）。聚焦 vitest 7 文件 142 例、typecheck 通过；主检出另有并行会话未提交的 Cursor 置顶工作，本任务隔离在 worktree，合并时 `inventory.cjs` 需人工合一次。Current authority: [spec](260903/cursor-plan-mode-sessions/spec.md#L1)。
- RAW-203 / RAW-204 额外进程已读记忆与 Claude App 额度组织仲裁（2026-09-03，claude 会话）is `implementation-landed / focused-automated-verified / artifact-ready / host-pending`。根因一：额外进程没有真实 Turn 时间，`statusChangedAt` 只活在进程内 roster，会合点丢失 / 会话重置 / 重载后整行重新落座为「现在」，跳转已读确认被当成旧 Turn 清掉，Host 仍报 unread 的行弹回「已完成未读 · 刚刚」；[discovery](../../preload/codex/codexhost-discovery.cjs#L48) 新增持久线程记忆（`eypc/codex/codexhost-thread-memory/v1`）延续时间戳并记住跳转已读。根因二：Claude App `oauth:tokenCacheV2` 的 `acct:<account>|<profile>:<org>` 键把第 1 段当组织，同账号两个 profile 被判成两个组织 → fail-closed，59 条 `claude-quota-read` 全部 `credential-unavailable`，球心失去 Fable 并列读数；[quota.cjs](../../preload/claude/quota.cjs#L82) 改取第 2 段并以 App 计量组织裁决平局。手动刷新新增可见回执与凭据状态行；审计 31 处重复判定五轮全部处置（收敛 19 处：单一「已读覆盖 Turn」规则、机器子跑/已读裁决只在观察层、Desktop 读证据共享模块、Kernel 唯一未读合并、Codex 已读归 Provider 声明、额度词汇表单一来源、claude.ts 合并路径共用 helper；4 处判为有意差异），台账在 [spec](260903/codexhost-read-memory-and-claude-quota-org/spec.md#L1)。聚焦 vitest 43 文件 979 例、typecheck、build、mirrors、entry-budget（14288 / 278 / 155）通过；真机待用户验收。
- RAW-202 跳转前确保目标应用已打开与 CodexHost 启动通路（2026-09-03，claude 会话）is `implementation-landed / focused-automated-verified / artifact-ready / host-pending`。根因：CodexHost 只在 `codexhost launch` 启动时以环境变量接管 Desktop，未运行时 `codex://threads/<id>` 冷启动的是不经 Host 的普通 Desktop，之后 `codexhost launch` 被拒。新增 provider 无关的就绪层 [open-readiness.cjs](../../preload/companion/open-readiness.cjs#L1) 在 Host Registry 处包装三个 `open`（探测 → 启动 → ≤25 秒轮询 → ≤8 秒软等待 → 原 opener），Codex 通路 [desktop-launch.cjs](../../preload/codex/desktop-launch.cjs#L1)（CLI / Host 描述符 / 运行中 Desktop 环境自动检测、`codexhost launch`、codexhost 路径手动 > 观察 > 常见目录 > PATH），Claude 以 `pgrep -x Claude` 为在运行权威。「运行」页新增统一开关（默认开）、CodexHost 三态（默认自动检测）与 codexhost 位置表单。聚焦 vitest：新增 24 例 + 桥/Claude/discovery/domain 全绿（`codexAppServerBridge` 165），typecheck、build、entry budget 14295/278/155 通过。本机 headless 真机核验通过（quit → `codexhost launch` ready 7.1 s → 进程/描述符/IPC 就绪 → 深链派发 → 检测 `managed`）；插件点击 14:56 用户实测经 CodexHost 启动（`launched` 6.1 s，CLI 来自会合点观察路径）；余三项可选。Current authority: [spec](260903/companion-open-launch-first/spec.md#L1)。
- 2026-09-02 CodexHost 已归档额外进程残留行修复 is `implementation-landed / focused-automated-verified / artifact-ready / host-pending`。Codex Desktop 归档 Grok 会话后 EyPc 行以「待继续 · 刚刚」复活并停留到下一次完整 membership 发布：`thread-archived` 已发 tombstone，但归档前缓存的 Host roster 被在途扫描以更新的 membership revision 再发布击穿 tombstone，而增量扫描从不为额外进程 id 发删除。现在归档时同步遗忘 roster 项并给 30 秒抑制窗（只有不再列出它的完整 Host 列表才释放，窗后再现视为 unarchive），完整列表少掉的 id 由下一次扫描 drain 成 `archivedKeys` 删除，发布前再过滤 roster 已不含的行。discovery / Host 归档 / App Server 桥 / level 契约 `4` 文件 `188/188`，入口棚轮 +5 行已登记，production/uTools build 通过，artifact `host-816cac3f4031ea1cca94 / renderer-74b28714faa02d22750f`。真机复验待宿主重载；见 [error-memory](../knowledge/error-memory/codexhost-archived-row-resurrected-by-stale-roster.md#L1)。
- RAW-201 额度读数点击即刷新与 Claude 额度车道诊断 is `implementation-landed / focused-automated-verified / artifact-ready / host-pending`。用户报 Claude 额度刷新「形式与频率都把握不到」：读数由 statusline 缓存（桌面 App 会话永不写入，本机停在 08-17）、App 每 15 分钟一份 plan 样本、开关开启时按「额度刷新（秒）」节奏的 usage API 三条车道合并，此前零诊断。现在每次 Claude 额度读取写一条有界 `quota / claude-quota-read`（trigger `cold/timer/reset/force/manual/lifecycle-*`、三车道读数年龄、usageApi `disabled/accepted/skipped/failed` 与 `blockedBy`、Retry/reset 距离、窗口计数、主读数来源，不含百分比或 reset 时刻）；展开卡额度行任一读数块点击（Claude 块含 Enter/Space，button 角色）派发 `codex.quota.refresh`，Claude usage API 以 `force` 绕过 interval/backoff 但保留 429 Retry-After 与 401/403 凭据锁，提示末尾追加「点击立即刷新」。聚焦 `4` 文件 `190/190`、contracts、typecheck、production/uTools build、requirements 与 source-anchors 均通过，artifact `host-37142fb92108f4649367 / renderer-208ce8973347950289ac`；顺手修掉 `runtimeDiagnosticsLevelContract` 对入口两处 `record(entry)` 透传（09-01 引入）报缺 level：[preload/index.js](../../preload/index.js#L1204) 两个发现车道改走带显式 level 的 `recordCompanionDiagnosticEvent`，契约测试转绿、入口棚轮行数不变。真机点击验收与 manual 事件回读待宿主重载。见 [spec](260902/quota-chip-refresh-and-lane-diagnostics/spec.md#L1)。
- RAW-201 side 子对话计入主任务（2026-09-02，claude 会话）：Host `thread list` 把运行中 / 挂起的 side 子对话汇总到主行，Codex 与 CLI 归档级联到子对话；EyPc 无代码变更，等 Host 重启后实机验收。见 [spec](260901/codexhost-external-completion/spec.md#L95)。
- RAW-199 额外进程归档走 Host 通道（2026-09-02，claude 会话）：EyPc 归档桥对 `codexhostExternal` 行改走 codex-host 委派 CLI `thread archive`（read 预检 / archive 写入 / live+archived 列表双核验），官方 app-server 不再收到这些 id；codex-host 侧新增 `thread archive|unarchive` 动词（其仓库 `vibe/specs/260902/1412-delegation-thread-archive/task-card.md`）。聚焦测试与构建通过（`host-12d0666f1b06419aecb0`）；Host 运行时新路由需 Desktop 正常退出后 `codexhost launch` 重启，uTools 重载与实机归档验收待用户。见 [spec](260901/codexhost-external-completion/spec.md#L94)。
- 2026-09-02 Claude App 版本门扩至 `1.40609.1`（跳转已读延迟核验）is `implementation-landed / focused-automated-verified / artifact-ready / host-reload-pending`。用户报快捷键跳转后「已完成已读」仍延迟；真机诊断两次样本均为 Claude Code 会话而非 CodexHost 额外进程，分别 56.2s / 42.9s 才翻转，都落在 Claude App 每分钟 hh:mm:30 的 LevelDB 刷盘栅格上。根因是 App 08-30 自更新到 `1.40609.1` 后 [app-state.cjs](../../preload/claude/app-state.cjs#L26-L35) 白名单第三次滞后，app-log 热未读车道（focused 即清）fail closed，只剩分钟刷盘兜底；EyPc watcher 本身在每次刷盘后 <1s 发布。新版 `setFocusedSession 214 / Query completed 22 / Sending message 27 / permission 3+3 / Stopping 4` 行式与 v2 语法逐字一致，已加入 canonical+镜像版本门并钉测试；`3` 文件 `95/95`、contracts、typecheck、production/uTools build 与 requirements 均通过，artifact `host-12d0666f1b06419aecb0 / renderer-1bd741805912f6467de8`。CodexHost 额外进程「跳转即已读」（`0768ca4`）已在运行中 asar 内，本窗口无该类样本；运行中的 Host 仍是旧版本门，focused ≤1–2s 快清待插件重载后真机验收。见 [error-memory](../knowledge/error-memory/claude-unread-decay-blocked-by-version-gate-and-minute-flush.md#L1)。
- RAW-167/168/170/171/172 工程不变量 is `user-confirmed / active`（2026-09-01）。五条同源于 [RAW-167 draft](260810/1155-install-runtime-diagnostics/raw-requirement-next.draft.md#L1)，此前因「用户从未确认转述忠实」滞留 `proposed`，`validate:requirements` 每次都为此告警。本轮用户确认转述忠实并转 `active`；草案 §5 的三项待确认项（单一判断点是否为核心理念、Codex 侧拆分范围、零行为 diff 验收口径）同批结清。`authority` 保留 `agent-transcribed`——确认的是忠实度，不是作者身份。登记 `proposed` 由 5 降到 2，剩余两条是本轮新补的 RAW-197/198。
- RAW-197/198 Claude 会话中断相位与 Host 线程权威 is `implementation-landed / focused-automated-verified / pending-user-confirmation`。**事后补登记**：两条行为已随 `8bb0e3e` / `96cf75a` 落地，此前唯一书面依据是入口预算棘轮注释 (q)。RAW-197 用转录尾巴的 `[Request interrupted by user]` 把 Esc 中断的 CLI 轮次折成「待继续」（Esc 不触发任何 Hook）；RAW-198 让绑定 `CODEXHOST_THREAD_ID` 的会话在 Host roster 持有期间退掉原生 claude 行。两条 `authority: agent-transcribed / status: proposed`——非用户当轮请求，**用户确认前不得当作已接纳语义引用**。RAW-198 会改变用户可见行数。聚焦 `claudeInterruptProbe` + `claudeBridge` 通过。Current authority: [spec](260901/claude-host-thread-authority/spec.md#L1)。
- RAW-196 Codex 配置页信息密度收紧 is `implementation-landed / focused-automated-verified / artifact-ready / host-visual-pending`。运行区 ready/checking 改为单行标题 + 小图标 + 事实芯片，健康噪声行与说明性详情默认不常显；warning/error 仍内联详情并展示全部分项。静默刷新与不透明 i 仍属 RAW-180。聚焦 `codexEnvironmentPresentation` + `codexCompanion` + `designSystemV7` `91/91`。当前产物 `host-4f0c0fa37a7a7e0d4822 / renderer-95c8612052694f015d41`，builtAt `2026-09-01T07:14:40.662Z`；真实 uTools 重载仍待用户执行。Current authority: [spec](260901/codex-config-density/spec.md#L1)。
- RAW-190–195 CodexHost 额外进程 is `implementation-landed / focused-automated-verified / artifact-ready / host-pending`。库存能发现已有额外进程，但官方 follow 会种下 `notLoaded` 影子并把 Host running/completed 打成 unknown；本轮不再 follow 这些 id，Desktop notLoaded/idle 回落到 Host connector。2026-09-02 重载复验：影子清除后 `group-counts` 仍逐位相同，第二根因是额外进程 id 无 Goal 缓存时默认 `unknown/verifying`，`goal-verifying` 候选在 Kernel 归约中压过 Host 的 terminal/inventory 证据；现固定回 `none/fresh` 并短路 goal 刷新（[externalGoalEvidence](../../preload/codex/codexhost-discovery.cjs#L513-L525)）。聚焦五套件 `326/326`（含 bridge 回归、discovery 单测），typecheck 绿，入口棘轮 `14158 / 276 / 153`。当前产物 `host-bf4111f68776b3eca478 / renderer-c88a84c43fefbb104b52`，builtAt `2026-09-02T00:19:46.497Z`；2026-09-02 13:03 真实 uTools 重载验收通过：Host 11 行全部进组（completed 14→24），unread 与 Host 逐位一致（0→1）。随后发现快捷键打开已完成未读后约 5s 又回未读（Host 未见 Desktop `thread/read` 时不翻转，而快照层只读 Host 值）：快照层现改为 EyPc 跳转即已读并绑定打开时刻，产物 `host-77e99c6b3bdc24397bf4 / renderer-d0af6b7d4758cc371d06`（builtAt `2026-09-02T05:25:30.857Z`）13:34 真实重载复验通过：快捷键打开后 live 与冷快照均已读，Host 仍报未读时 3 分钟不再翻回。Current authority: [raw-requirement](260901/codexhost-external-completion/raw-requirement.md#L1)。
- RAW-189 状态因果、置顶折叠与当前宿主构建身份 is `implementation-landed / focused-automated-verified / artifact-ready / main-integrated / host-pending`。五批源提交已快进合入主目录 `main`，源 HEAD 为 `129b5f68bb64`；四个聚焦文件 `316/316`，主目录生产构建及 typecheck 通过。最终主目录产物 `host-48db44ec89d5ec2cb1ad / renderer-e0e87654e3915fd64b38`，builtAt `2026-08-30T10:57:06.496Z`（北京时间 `2026/08/30 18:57:06`）；未推送、安装或重载。详细集成回执及验收边界：[spec](260829/companion-pinned-collapse-plan-input/spec.md#L1)。
- RAW-188 未读/置顶互斥与 attention 稳定轮次 is `implementation-landed / focused-automated-verified / main-integrated / host-pending`。与 RAW-189 同批合入主目录并复用 Kernel、构建与集成核验，真实快捷键验收保持独立；状态与恢复入口：[spec](260829/companion-unread-pin-fallback/spec.md#L1)。
- RAW-187 状态未知任务可手动指定状态 is `implementation-landed / focused-automated-verified / host-pending`。`unknown` 行的状态图标 `Cmd/Ctrl+点击`打开状态菜单（选项同时并入既有右键抽屉，不新造浮层）；普通点击与其余行的多选手势逐字不变。跨平台不按系统分支——[commandModifier.ts](../../src/ui/commandModifier.ts#L1) 双键接受，外接键盘/远程会话/平台探测出错三种情况下分支实现都会静默失效。覆盖落在 [finalizeTask](../../preload/companion/task-kernel.cjs#L674)：分组由 Kernel 的 `views.groups` 决定，只改渲染投影会重演「图标变了但仍留在原分组」。退休用 episode 判据 `statusEnteredAt <= manualPhaseSetAt`，热路径无写入也无需清理。**过程中自己制造并修掉一个真缺陷**：最初把覆盖直接写进 `task.phase`，导致 [#L1713](../../preload/companion/task-kernel.cjs#L1713) 的 unknown 宽限窗拿 `previous.phase` 判「之前是否未知」时，被一个用户偏好回答——由新增 `canonicalPhase` 保存证据原值修复，一切关于证据的判断改读它。该缺陷是新增的第 3 条 kernel 用例逼出来的，不是复审发现的。聚焦 `tests/platform/companionTaskKernel.test.ts` `83/83`（新增 4 条）、`tests/domain/codex.test.ts` `45/45`（新增 2 条）。**同轮按棘轮要求抽取治本，并修好一处既有破损**：`preload/index.js` 在 HEAD 就已 14036 行 > 预算 13935（+101），门禁在 `main` 上本就是红的。把 uTools `dbStorage` 侧状态（Plan 暂停回执、交互身份盐、交互墓碑）连同其 5 个存储键整簇迁入新模块 [persisted-side-state.cjs](../../preload/companion/persisted-side-state.cjs#L1)，入口降到 `13916` 行 / `149` 绑定，预算随之下调（收紧而非放宽）。迁移中撞上并修掉**跨 VM realm** 陷阱：模块经沙箱 `require` 在真实 realm 执行，原先的 `globalThis.utools?.dbStorage` 会读到宿主全局而非沙箱，写入全部落空；改为由入口注入 `storage()`。打包清单、构建产物校验器 require 垫片与三处测试桩同轮登记新模块。全量 `1567/1567`，`pnpm run verify` 端到端通过（含 74 对镜像与入口预算）。`pnpm run build` 通过，产物 `host-8e2cddaf6c3eeeda19bf / renderer-3bf001e3b37780a93bcf`，builtAt `2026-08-28T12:03:17.456Z`。需求登记新叶 [shared-raw-187](requirements/shared-raw-187.md#L1)；**PRD L267 可持久化本地配置由四项扩为五项**，属用户明确裁决的条款修订。Current authority: [raw-requirement](260828/companion-manual-phase/raw-requirement.md#L1)。
- RAW-186 水球球心并列 Fable/普通周限额 is `implementation-landed / focused-automated-verified / host-visual-pending`。紧接同日 RAW-184：球心不再只显示一个百分比，Claude 拥有球心且账号同时上报 scoped 周窗口时并列读作 `{scoped}/{plain}`（scoped 优先 Fable，在前）且不带 `%`。新增 [claudeScopedWeeklyQuotaWindow](../../src/domain/claude.ts#L358) 在全部 scoped 周窗口里择一，[resolveCompanionWaterBallPresentation](../../src/domain/companionPresentation.ts#L79) 增出 `scopedPercent / scopedLabel` 并只在 `quota.weekly` 存在时配对——否则会把 scoped 周与 5 小时回退塞进同一个斜杠。组件侧新增 `.codex-water-ball__pair` 分支：**选择器必须写成 `.codex-water-ball__value strong.codex-water-ball__pair`**，先前写作单类时整条字号上限静默输给既有 `.codex-water-ball__value strong`，实测 `--water-percent-size:32px` 下 `100/100` 渲染 119.7px 直接穿出 94px 球体；改后实测 15.51px / 61.2px，两侧各余 16.4px。字号取 `percentSize * .7` 与 `--water-size * .165` 的 `min`，12px 设置仍降到 8.4px。读数同轮改用 `tabular-nums lining-nums`，避免就地刷新时中心左右跳动。聚焦 `tests/domain` + `tests/ui` `49` 文件 `556/556`（新增 3 条 scoped 选取 + 5 条并列/兼容 + 1 条组件渲染与层叠断言），`tests/runtime` + `tests/integration` `18` 文件 `323/323`；`pnpm run build`（含 validate:contracts、vue-tsc、validate:utools）通过，产物 `host-955fcad76c1e47f47039 / renderer-ba50c54f744e6e379bbb`，builtAt `2026-08-28T09:04:19.445Z`。**交付后核验补记**：用户报「没有效果」，逐层实测排除了产物与数据——解包运行中的 asar 确认 `codex-water-ball__pair / scopedPercent` 已在 `assets/companionPresentation-CBq5fMRD.js` 里，`pnpm run probe:claude-quota` 读到真实 `seven_day_fable`（剩 78%）与 `seven_day`（剩 76%），持久化设置 `claudeAppQuotaAccess: true`，真实 payload 走一遍域函数得到 `scopedPercent:78 / percentOverride:76`。真因是**持久化悬浮窗不随插件重载换 bundle**：[handleHostVisibility](../../preload/codex/float-bridge.cjs#L1014) 在 `isKill === false` 且 `codexFloatPersistent === true` 时不 `closeCodexFloat()`，诊断日志 `plugin-out {"isKill":false,"floatPersistent":true}` 正是该分支，于是 `createBrowserWindow('float.html')` 创建的窗口一直跑创建时那份 renderer。同轮另修一处阻断：仓库根 [index.html](../../index.html#L9) 被产物 HTML 覆盖（指向 `./assets/main-BmQt-f76.js` 而非 `/src/main.ts`），`vite build` 因此报 `Failed to resolve`，已从 HEAD 还原并重建通过。需求登记新叶 [claude-raw-186](requirements/claude-raw-186.md#L1)（`refines` RAW-184 与 RAW-007），PRD 球心口径与用户帮助同轮同步；真机水球目视（并列是否真的在 uTools 里居中不溢出、Fable 窗口是否确实存在于该账号）待用户重新接入后确认。Current authority: [raw-requirement](260828/claude-ball-centre-dual-weekly/raw-requirement.md#L1)。
- RAW-185 置顶豁免活动时间窗与单一显示分组 is `implementation-landed / focused-automated-verified / artifact-ready / host-pending`。用户原话要求「如果它属于置顶，也归到置顶里面」；现由 Kernel 把任何相位的本地置顶根任务只显示在顶部置顶分组，不再同时出现在状态分组，取消置顶后才恢复按真实状态分组与活动时间窗。显示位置不改状态入口：待输入、进行中、待继续和已完成未读仍按真实相位计数并可快捷到达；已完成已读与 `unknown` 走置顶兜底。Controller 的 pin 命令改用 Kernel fallback，并在失败时记录和重新挂接，避免静默丢入口。五个影响测试文件合计 `198/198`，77 对 preload 镜像、需求与生产构建均通过；当前产物 `host-c91b3abde05f01acc5fc / renderer-653e13e4127ae403ecaf`，builtAt `2026-09-01T05:54:13.298Z`。真实 uTools 中的分组、快捷键与 pin 命令仍待用户重载后验收。Current authority: [raw-requirement](260828/companion-pin-window-exemption/raw-requirement.md#L1)。
- RAW-184 水球球心改读 Claude 普通周限额 is `implementation-landed / focused-automated-verified / host-visual-pending`。球心那个百分比的唯一来源 [claudePrimaryQuotaWindow](../../src/domain/claude.ts#L354) 由 `short → weekly → windows[0]` 改为 `weekly → short → windows[0]`：取无 scope 的普通周限额（`weekly_all`），5 小时窗口与 `windows[0]` 只作为「账号没有普通周窗口」时不让球心留空的回退。scoped 周限额仍由既有 `plainWindow` 判据挡在球心之外，归属映射、来源标注、`percentOverride` 兼容路径与展开卡逐窗口额度区全部未动。聚焦 `tests/domain/claude.test.ts` + `tests/domain/companionPresentation.test.ts` + companion 域 `136/136`，`tests/runtime` `309/309`；`pnpm run build`（含 validate:contracts、vue-tsc、validate:utools）通过，产物（当轮）`host-a6d9051811ebbbacaf4e / renderer-e15052ba496853e59fc9`，builtAt `2026-08-28T08:17:16.479Z`。需求登记新叶 [claude-raw-184](requirements/claude-raw-184.md#L1)，PRD 球心口径与用户帮助同轮同步；真机水球目视待用户重新接入 uTools 后确认。Current authority: [raw-requirement](260828/claude-ball-centre-weekly/raw-requirement.md#L1)。
- 2026-08-26 Claude V7 state/read correction is `implementation-landed / focused-automated-verified / current-log-replay-verified / artifact-ready / host-reload-pending`。当前 App `1.37937.0` 的隐私安全语法核验恢复 app-log phase/hot-unread 快车道；Claude inventory mutation 只先发布 indexed metadata，不再读取或推进 activity/interaction/unread/topology，独立 state microtask 随后归约新成员，因而真实 state generation 不会被高频 metadata callback 判旧。快捷键与卡片点击保留不同 selector/source，但在同一 Navigation→Action→Provider open/read authority 汇合；两者的 `dispatched` 都是 `confirmsRead=false`，只有精确原生 focus 才清 unread，诊断现记录 `confirmsRead/handoffStage/nativeVisible`。用户指定的「0826 EyPc V8 插件重构设计文档」真机日志另有一个更晚的固定 `CycleHealth api_error` 额度耗尽行；解析后为 `stopped/exact-terminal`，焦点行不再续写 phase revision。影响选择的 `8` 文件 `356/356` 测试、contracts、typecheck、`74` 对 preload 镜像、requirements/error-memory validation 和 production/uTools build 均通过；当前 artifact 为 `host-328ae8b2f2ff25baf18f / renderer-34ee7434ee28243c10e7`。当前日志只读重放已得到 stopped；运行中的 uTools 是否加载该 identity 仍待重载/回读，不由构建结果代替。
- RAW-181 孤儿 SubagentStop 保守收敛未决 active 子代理 is `implementation-landed / focused-automated-verified / real-queue-replay-verified`。真机队列证实错位类载荷双重签名（agent_id 未知且 agent_type 缺失，7/7 在父 `Stop` 后 2–7 秒到达）；[events.cjs](../../preload/claude/events.cjs#L1) 归约器新增完全源结构化的一对一收敛（无 TTL）：类型缺失孤儿 stop 在父 Turn 关闭后关闭最早未决 active 子代理并标 `reconciledAt`，同 id 直接证据复活、SessionEnd 终态化；带类型孤儿/开 Turn/重复 stop/无候选保持占位。修复对既有队列重放即时生效：真机重放仅 53836e7e 幽灵 live 成员收敛，其余 10 会话逐条无差异。聚焦 `tests/platform/claudeBridge.test.ts` `80/80`（新增 5 用例）+ 下游 topology/preload-core `13/13`，`sync:preloads` 后 `73` 对镜像验证通过，需求登记与真值快照已同步。用户报告的「已完成已读却永久进行中 + 归档被 liveCount 抑制」由证据侧收敛解除，Kernel 聚合未动。上游载荷缺陷已报告：anthropics/claude-code#89555。 2026-08-26 已从芯片 worktree 合入 main（合并提交 deb36a0 / 5144386，随行主线三批：接入来源遥测回读 cc0d491、Claude 版本门扩版 5383841、诊断错忆与权威同步 89eb779）；合并树 `pnpm run verify` 全绿（108 文件 1530 用例、typecheck、production build、uTools validator、74 对 preload 镜像、需求/锚点/错忆校验），designSystemV7 弹层断言对齐 RAW-180 不透明契约（b5cd77b 遗留未同步），产物身份回写 `host-c8d259978cc3975e902f / renderer-7c5f2cefc86156e0f41f`。Current authority: [spec](260825/2242-claude-orphan-subagent-reconcile/spec.md#L1).
- RAW-181 Codex Side 子线程重载恢复 is `implementation-landed / focused-automated-verified / host-reload-pending`。裁决采用方向 A：Desktop Side→parent 关系以有界提示持久化（仅 ID+observedAt，上限 200、TTL 48h，新模块 [side-relation-hints.cjs](../../preload/codex/side-relation-hints.cjs#L1)），重载后恢复并复用既有 follow + 定向 latest-Turn 校验；提示不产生状态，法定人数清退原样适用且清退同步删持久化条目。方向 B（Desktop 运行中会话枚举）因协议面无枚举请求证据被否决。C2 一并补齐：库存 Side 行 idle/notLoaded + fresh inProgress turn 判活（缓存 inProgress 强制新鲜重读），诊断记 `recoveredLiveCount`。入口预算棘轮按可见决定回写为 `13,935 / 274 / 151`。聚焦 `tests/platform/codexAppServerBridge.test.ts` `154/154`（含 4 个新用例：持久化往返+重载恢复、提示不虚构、TTL 过期丢弃、idle 行判活+缓存不作数），platform 全量 `36` 文件 `627/627`，preload 镜像 `74` 对、需求登记与真值快照已同步。真实宿主重载验收待用户。 2026-08-26 已从芯片 worktree 合入 main（合并提交 deb36a0 / 5144386，见上一行合流记录）；合并树 `pnpm run verify` 全绿（108 文件 1530 用例、typecheck、production build、uTools validator、74 对 preload 镜像、需求/锚点/错忆校验），designSystemV7 弹层断言对齐 RAW-180 不透明契约（b5cd77b 遗留未同步），产物身份回写 `host-c8d259978cc3975e902f / renderer-7c5f2cefc86156e0f41f`。Current authority: [spec](260825/codex-side-reload-recovery/spec.md#L1).
- RAW-180 Codex 配置页静默刷新与运行区整合 is `implementation-landed / focused-automated-verified / host-visual-pending`. 已有成功快照后，例行额度/环境重读不再把顶部药丸和运行诊断改写成「正在读取 / 正在核查」；运行区改为叠层换行并常显主诊断详情；主窗 Tooltip 改为不透明。聚焦 `tests/domain/codexEnvironmentPresentation.test.ts` + `tests/ui/codexCompanion.test.ts` + `tests/ui/operationTooltip.test.ts` 为 `93/93`，需求登记与来源目录已同步。五 Tab 与快捷键不回读仍属 RAW-087。真实运行页目视仍待宿主。Current authority: [spec](260825/codex-config-silent-integration/spec.md#L1).
- Product: keyboard-first uTools plugin for local PC capability calls.
- Active lines: port management, file favorites, cross-platform window jump, MQTT workbench, Quick Jump, Codex Companion and settings feature help.
- RAW-179 EyPc V7 global state、interaction、Tab and UI refactor is `implementation-landed / automated-verified / artifact-ready / real-host-excluded-by-user`。The current identity chain is `task-state-v12 / registry-v1 / topology-v2 / kernel-v7 / snapshot-v7 / command-v1 / subscribe-v1 / ack-v2`，generated from one V7 schema。Provider adapters submit raw membership、activity、interaction、unread、plan-artifact、metadata and topology evidence；the Host Kernel alone performs canonical reduction。Current unresolved interaction and executable Plan artifact are distinct：reply/cancel/execute atomically advances the Shadow tombstone；artifact-only is `stopped/待继续`；stale initial/refollow/read-state replay cannot restore a cleared instance。Main、Float、Action、badges and cycles consume one presentation revision and the same key-only Command。FeatureModule/RuntimeSlice owns Tab lifecycle；CommandCatalog/ActionMenu/LayerStack/KeybindingIndex owns interaction；FeatureTarget/NavigationIntent/QuickJumpRegistry owns exact navigation；shared semantic tokens own Main/Float/Action theme、density、focus and forced-colors behavior。Independent architecture re-review closed all five material blockers；Provider/Kernel/Bridge focused tests are `228/228`，Kernel lifecycle tests are `72/72`，repository tests are `1494/1494`，and contracts、mirrors、typecheck、production build、uTools runtime、requirements/source anchors and error-memory validation all pass。Current artifact identity is `host-cb0294e803978c67b881 / renderer-6817c1e4fe6fd2808739`。Real uTools、300ms/30s no-rebound、system dark-mode visual and publish/install remain unrun。2026-08-25 该任务线已从 worktree 落地并合入主线：账本、契约修正与 145 文件实现里程碑三笔提交经 `codex/260824-eypc-v7-global-refactor` 合并到 `codex/port-management-redesign`，合并树重跑 `pnpm run verify` 全绿（`106/106` 文件、`1494/1494` 测试、typecheck、production build、uTools validator、`64` 对 preload 镜像）。合入使 `package.json` 进入 Renderer 内容指纹，产物身份的 Renderer 段随之由 `renderer-bf96c22c752c95dd2d6b` 变为 `renderer-6817c1e4fe6fd2808739`，Host 段不变；当前真值快照已按新身份确定性回写。真实宿主门禁不因合入而降低。Current authority: [spec](260824/eypc-v7-global-refactor/spec.md#L1) / [verify](260824/eypc-v7-global-refactor/verify.md#L1).
- RAW-177 Codex Tab boundary optimization is retained as an `integrated-historical-foundation / superseded-current-runtime-by-RAW-179` line。Its deterministic metadata/hash-only Source Anchor catalog now feeds the V7 registry，which records 315 leaves、30 raw documents、207 ordered anchors、105 RAW-parent clauses、288 active、22 superseded、5 proposed and zero semantic conflicts。Every Deep Link still stops at `dispatched/pending` without matching native-visible/read receipt；RAW-179 changes neither that native-read boundary nor the Source Anchor identity model。Its V6 tests/build identities remain retrieval evidence only；current runtime and gates are owned by RAW-179。Evidence: [historical verify](260823/codex-tab-boundary-optimization/verify.md#L1).
- RAW-178 global current truth is `integrated / single-owner / deterministic-freshness-gated`。[PRODUCT_REQUIREMENTS.md](PRODUCT_REQUIREMENTS.md#L1) now exclusively projects current product semantics after original requirements、later additions、changes、optimizations and architecture adjustments are resolved；requirement/source/PRD-body/architecture/runtime digests make any unsynchronized drift fail validation。Historical RAW、task ledgers、architecture and registry remain evidence/lifecycle owners，not parallel current PRDs。
- RAW-169 Codex 入口抽取 remains `proposed / agent-transcribed / re-verified / 三项决策未裁决 / P2 未开工`。2026-08-23 按条款字面逐条实测四项判据：「职责边界而非行数」达成（22 个模块按低耦合簇抽出）；「不与行为修改混提交」达成（16 个 `refactor(codex):` 提交无一触及 `src/`）；「与 Claude 侧同量级规模」未达成（`preload/codex/` 22 模块 2,901 行、均值 132，`preload/claude/` 13 模块 4,664 行、均值 359，差约 2.7 倍）；「Codex 侧离开入口」为净额倒退。入口行数轨迹现为 15,046（提出基线）→ 14,304（十六块最低点）→ 15,186（`765f77c`，再核验落笔）→ 15,388（`6e1d6e3`）→ **15,733**（RAW-179 V7 合入后），相对基线净 **+687**；模块级可变绑定 166 个，仍高于 129 的草案基线。根因是抽取为纯人工推进而并发增量无门禁约束，`765f77c` 一笔即在入口新增 646 行与三个 codex 顶层函数。2026-08-25 三项阻塞决策中的两项已结案，需求叶子亦已更正：**D2 登记为 route 3**——route 1 委托桩抽取于第十七块耗尽后正式转闭包化改写，共交付 float、archive、environment、window-size 四个域；「按簇抽取」与「闭包化改写」是先后阶段而非竞争方案。**D3 结案为不扩**——`preload/cursor/` 经实测在再核验落笔时即已有 7 个模块，§2.4「该目录不存在」当时即不成立，该条前提不存在。**D1 亦已裁决，RAW-169 转 `active`**：2026-08-25 用户确认本条转述忠实于原话（`authority` 转 `user-stated`），并裁定判据 2「与 Claude 侧同量级」为参考描述而非硬验收门槛——条款正文已改写，该半句由并列判据降为引注，规模是职责边界划分的结果而非需要单独对齐的目标，两者冲突时以职责边界为准，31 个模块不需重新归并。同源草案的 RAW-167/168/170/171/172 已于 2026-09-01 由用户确认转述忠实并转 `active`（`authority` 保留 `agent-transcribed`：确认的是忠实度，不是作者身份），草案 §5 三项待确认项同批结清。判据 2 现状：codex 31 模块均值 192 / 中位 117，claude 13 模块均值 360 / 中位 294——均值差距由 2.7 倍收窄到 1.9 倍，中位仍差 2.5 倍，但两侧区间已充分重叠（codex 最大 1,074 已超过 claude 的 883，claude 最小 27 低于 codex 的 41）。判据 1、3、4 均达成，判据 2 自此不参与达成判定。P1 入口预算门禁仍未实施，是唯一不依赖裁决的待办。[再核验计划](260823/2049-codex-entry-extraction-reverify/plan.md#L6) 已标注被 [需求叶子 08-25 复核节](requirements/invariants-raw-169.md#L18) 取代。入口预算门禁已落地：[validate-preload-entry-budget.mjs](../../scripts/validate-preload-entry-budget.mjs#L1) 把入口总行数、顶层 codex 函数数与模块级可变绑定数钉为只降不升的棘轮，基线 `13,811 / 271 / 147`，挂在 `pnpm run verify` 末段并另有 6 项自测覆盖两个方向的越界路径与「前缀过滤会漏计」这一既有错误记忆的形态。**门禁双向失败**：长高需编辑基线，使入口再度增长成为一次可见决定而非无人察觉的漂移；**降到基线之下同样失败**——天花板悬在地板之上等于棘轮失去张力，下一轮增长可以免费涨到那个陈旧数字为止。两个方向的报错都直接给出应写回的确切数值，因此修复都只是一次编辑，且基线恒等于当前实测值——这正是 RAW-169 十六块抽取被并发增量抵消、以及 V7 合入再推高 345 行时缺失的那道防线。承载本轮融合的 `claude/epic-hugle-7e725c` 工作树**标记为已融合、合并阶段完成**：其 route 1/route 3 的十个候选模块与相关测试改动均已进入主线，该工作树自此不再主动扫描或回填状态；若其中新增任务再作变更即可。2026-08-25 首批融合已落地：`claude/epic-hugle-7e725c` 工作树里 route 3 的 `environment-bridge`（705 行 / 24 函数）与 route 1 的 `action-runtime-projection`（99 行）经三方合并**零冲突**移植到 V7 入口，`preload/codex/` 由 22 增至 24 个模块，入口 15,733 → **15,214**（-519），相对 15,046 基线由净 +687 收窄到净 **+168**。移植前逐函数体比对确认这两个域的全部基线函数在 V7 下未被改动，可原样套用；聚焦 `5` 文件 `255/255`、全量 `107/107` 文件 `1510/1510` 测试、contracts、typecheck、production build、uTools validator 与 `64` 对镜像全过，产物身份 `host-7d012e6b577dfe312c55 / renderer-320a5132736d185aacc8`。同日第二批 route 1 委托桩 `desktop-activity-resolution`（183 行）、`desktop-process-probe`（72 行）、`native-state-paths`（81 行）亦已移植，三处冲突全部落在模块装载区、按两侧 loader 并集解开，入口 15,214 → **15,180**，`preload/codex/` 增至 27 个模块；全量 `107/107` 文件 `1510/1510` 测试与 `66` 对镜像通过，产物 `host-87cb3352c1db0564c478 / renderer-762ce50de7a677ef4d42`。`archive-bridge`（588 行）同日完成，六个模块全部落地。逐段归属后确认那处 507 行冲突并非取舍题：其中仅 36 行是 V7 新增的 `readCompanionCodexPreflightSnapshotV7`，其余 471 行分属 13 个已合法迁入模块的归档函数，保留前者、删除后者即可，两侧语义均无损失。真正需要裁决的是 V7 已把 kernel 内联 `adapters` 迁入 `companionHostRegistry`，epic 的注入点不复存在——归档适配器改在 Registry 侧接 `codexArchiveBridge`，箭头函数按调用求值，Registry→Kernel→Bridge 的构造顺序仍成立。实施中另修正一处 off-by-one：装载块曾被插进 `if (companionTaskKernel?.onPackage) {` 块内形成块级作用域，`node --check` 与全量构建均无法发现，仅 vm 沙箱以 `ReferenceError` 暴露；移出该块后八个模块句柄的作用域可见性已逐一探测通过。入口 15,180 → **14,781**，`preload/codex/` 增至 28 个模块，相对 15,046 基线由净 +687 转为净 **-265**——RAW-169 判据 4 的净额倒退至此消除。全量 `107/107` 文件 `1510/1510` 测试、`69` 对镜像通过，产物 `host-c391c69d93688de86535 / renderer-4c2d7cb16c738a29bd02`；2026-08-25 剩余三个模块亦已完成，RAW-169 的十个候选模块全部结案。`float-bridge`（1,068 行）与 `float-window-size`（95 行）因前者直接 `require` 后者而同批移植；被 V7 改写的 7 个函数实测仅 49 行差异，按性质分三类回灌进模块：`taskSnapshot` 字段改名（4 函数 8 行，机械）、V7 的两处行为修正（`armCodexFloatTaskAck` 取消 ACK 缺失时的强制重建——该行为曾把慢渲染变成假崩溃；`installCodexFloatIpc` 的 ACK V2 修订号闸门与内核回执）、以及 `codexFloatExpandedHeight` 改按 `taskSnapshot.views.groups` 五组求和加 `pausedKeys` 计数。`activity-public-projection`（113 行）的 `codexActivityPublicEntry` 由 V7 新增的 `planLifecycleState` / `planClearReason` 派生一并回灌。三个几何函数（`floatDisplayForPoint` / `floatDisplayForPosition` / `validCodexResizeCorner`）按设计留在入口并注入，避免与 Action Runner 域分家；`scripts/validate-utools-runtime.mjs` 的 `task-package-ack` / `stage === 'applied'` / `requestCodexFloatRecreate` 三条断言随之改指 `codex/float-bridge.cjs`。**`plan-wait-promotion` 结案为作废**——V7 已删除其唯一函数 `codexPromoteCompletedPlanWait`，与 V7「artifact-only 归为 stopped」的重新定义一致，无移植目标。入口最终 **13,811** 行、`preload/codex/` **31** 个模块，相对 15,046 基线净 **-1,235**；每一批均以全量 `107/107` 文件 `1510/1510` 测试、typecheck、production build、uTools validator 与 preload 镜像通过为准，最终产物 `host-a882f08c486a075f4f3d / renderer-a200596242f0a9317473`。该工作树的叶子另记有用户在 route 2 与 route 3 之间**选定 route 3**，与本行 D2「未裁决」存在冲突，待用户确认何者为当前真值。Evidence: [re-verification plan](260823/2049-codex-entry-extraction-reverify/plan.md#L1).
- Codex Environment Action Runner: [spec](260729/1435-codex-environment-actions/spec.md#L1) / [verify](260729/1435-codex-environment-actions/verify.md#L1). 2026-08-03 接纳复核现为 `automated-verified / host-pending`：exact structured argv、strict TOML version、Action Host revision、worktree-sensitive target identity、tasks-only cold preflight、Host-persisted Environment 槽位、Runner-first message、一次性 stale-alias retry、`onPluginOut(isKill)` 双生命周期与 Windows 绝对 `taskkill /T` 非强制树停止已闭环。聚焦 7 文件 `95/95`；最终 `pnpm run verify` 为 57 文件 `733/733`，随后 typecheck、production build、runtime prepare 与 uTools validation 全部通过。真实 child/hotkey/plugin-out/Windows/长期 Serve/真实 Action 继续由宿主验收。
- RAW-143 performance increment: ordinary non-kill hide now keeps App Server/project aliases hot; Runner/slots reuse verified inventory and rebuild only on first use or Host stale-alias. Related four files are `149/149` with typecheck and preload syntax/mirror verified; full verify/build was intentionally not repeated, and real shortcut latency remains host-pending.
- RAW-144 incremental-cache/read-recurrence increment is a historical baseline for hot task inventory and per-project Runner invalidation。Its accepted-fallback-open / Turn-bound read acknowledgement is superseded by RAW-177#3；current dispatch changes no unread without native receipt。Historical affected evidence remains `301/301`，while current behavior follows RAW-177。
- RAW-145 persisted-decision increment: current-machine read-only evidence exposed a Preload→Domain provenance break hidden by a duplicated loose preflight predicate. `task-state-v5` now carries only structurally verified ordinary-input/Plan decisions as `persisted-decision`; plain connector hints remain conservative. Focused `192/192`, typecheck and full build passed. The historical v4 installation gate is closed by the 2026-08-07 active-ASAR readback below; RAW-147 has repaired the [real-preflight script](../../scripts/codex-real-preflight.mjs#L1) so its production-Domain gate is reusable again.
- RAW-146 consumer-consistency closeout is historical for the two dedicated attention actions: it originally unified pinned-first “打开第一条” behavior and passed focused `90/90`, full Vitest `752/752`, typecheck and runtime/static gates. RAW-149 now supersedes only those waiting-input/completed-unread action semantics with global newest-first persistent progress; the ordinary display comparator and generic task cycle retain the RAW-146 foundation.
- RAW-147 active→waiting optimization: active-ASAR/readback and current Codex package inspection proved `following=true` is a peer follower-state announcement, not a request. The old branch now represented by the [no-reply boundary](../../preload/index.js#L3519) echoed it, allowing two EyPc followers to create 32,329 messages in 250ms and about 368,000 in 2.5s. The handler now replies only to explicit following-status requests; the new RED/GREEN contract and Bridge `81/81`, exact-change typecheck/production build, current three-way Preload/runtime gates and repaired real preflight pass. A packaged-source probe sends a bounded 24 initial follows and emits no recursive writes after 48 old-client announcements. Error memory is routed as one stream-follow primary plus one pending-request dependency introduction, preventing a second protocol-loop owner. A later concurrent Claude fixture change blocked the whole-tree typecheck/build rerun at that time; RAW-148 current-tree typecheck now passes. Running uTools still uses the pre-RAW-147 ASAR, so reload, owner snapshot and real active→waiting acceptance remain pending.
- RAW-148 recognition/dedup closeout: [codexEnvironmentPresentation.ts](../../src/domain/codexEnvironmentPresentation.ts#L1) now owns the Runtime banner, grid, compatibility-pending predicate and launch help; the Page only renders it. Task/project ownership is resolved once per Float row from the Domain markers, and launch-path mutations publish the Host-returned snapshot without a second inspection. RAW-022 wins over stale Codex-only wording: data/state/quota/empty-state compatibility remains, while every task/project keeps one visible and accessible ownership marker. Focused `188/188` and current-tree typecheck pass; no Bridge/Preload/build/runtime surface was touched.
- RAW-149 permission-attention/latest-first increment is an `automated-verified` foundation：command execution、file change、permissions and MCP elicitation approvals join questions/Plan as input attention；input/approval share one count and are excluded from ongoing。RAW-177#3 refines only progress semantics：accepted dispatch advances the bounded local request sequence，while Provider read progress remains native-authority-only。Its historical `292/292` evidence is retained；current handoff behavior is verified by RAW-177 focused tests。
- RAW-150 waiting-to-continue/archive increment is `automated-verified / host-pending`: Domain keeps `stopped`, while all card/segment copy shows “待继续” without a fifth top Tab, counter or shortcut. Explicit stopped is task-archiveable；Codex rereads exact thread/latest Turn and returns `state-changed` after resume。更新引入：RAW-154 已以 Claude D′ 单目标静默 `isArchived` 事务取代本条历史 Deep Link+AX 路线；App 日志降为增强证据，元数据+私有活动库存双确认即可成功，非成功仍保留卡片。Project batch remains Codex completed-only.
- RAW-151 bidirectional waiting-edge increment is historical `host-exposed-defect / superseded-by-RAW-153` for active-vs-active arbitration. Its reducer、bounded resubscribe and 100-cycle latency intent remain regression foundations；its Renderer phase-only recovery owner is superseded by RAW-160 process-Host native+StatWatcher recovery。The true v7 host proved that current-owner old waiting could survive a newer active/Turn-started and later reappear after shadow replay.
- RAW-152 process-navigation increment is a historical `automated-verified / superseded-by-RAW-155` baseline: it introduced the process-lifetime cursor, one Codex/Claude queue, Preload consumption and concurrency 1. Its fixed 75ms trailing delay is no longer current；RAW-155 keeps the ownership/lifecycle foundation but replaces dispatch timing with leading-immediate plus in-flight final-trailing.
- RAW-153 waiting-clear causal barrier increment is `automated-verified / host-rework`: `task-state-v8` gives private main/Side Chat request instances and runtime waiting flags monotonic observation sequences. Request removal, matching `serverRequest/resolved`, newer active/Turn-started, matching output, user continuation and new `task_started` establish a clear barrier; older snapshot/read-state/refollow/sticky/rollout evidence cannot revive the cleared instance, while a genuinely newer correlation re-enters immediately. Unmatched resolved only bounds a target-task resubscribe and preserves concurrent approvals. Bridge `94/94`, the affected `9/9` files `379/379`, typecheck, production/uTools build, canonical/public syntax+mirror and synchronous-IPC static gates pass；当前源码的真实 Provider→生产 Domain/Presentation 只读预检也返回 v8、connected、completeness verified，匿名瞬时投影为 `waiting=1 / active=2`。运行中 ASAR 仍是 v7，并曾复现 `waiting=1 / active=4` 而同期旧 Provider→Domain 为 `0 / 5`；正常 v8 reconnect 和 1.25 秒/30 秒 no-rebound 门禁仍未完成，所以不得形成完成提交。
- RAW-154 unified companion task kernel is a historical `automated-verified` V9/Actions-v1 baseline. Its Provider-neutral intent、Claude D′ target-only transaction、indexed membership delta and five-second archive confirmation remain foundations；its broad exact-interrupted edge and Actions-v1 identity are superseded by RAW-160 V10/V4/Actions-v2。Historical evidence remains `20/20` files、`550/550` tests plus the separately authorized 2026-08-10 D′ canary；real current-host acceptance follows RAW-160, not the old v9 gate。
- RAW-154 rework is the historical V1 Kernel/package and runtime-identity baseline. Its single intent path、cold fail-closed preflight、silent `onPluginEnter` ownership、Main/Float identity handshake and D′ archive boundary remain current foundations；its shared generation and V1 package identity are superseded by RAW-155 V2 lanes. Historical evidence remains `12/12` files、`485/485` tests plus the older D′/v9 `550/550` baseline and does not replace RAW-155 verification.
- RAW-159 Codex unified-state/archive/diagnostics increment is historical `automated-verified / installed-host-reproduced-failure / superseded-by-RAW-160-v4-rework`. Its `83/83` files、`1272/1272` tests、pagination、minimal membership、Codex archive postconditions、diagnostics and Runtime Identity remain valid regression foundations；the installed-host gate exposed broad interrupted→stopped、unstable Plan candidates and missing consumer applied-state semantics, so V3 is not current acceptance authority.
- RAW-160 Plan lifecycle/change-only-publication increment is a historical `rework-implemented / full-automated-verified / superseded-current-artifact-by-RAW-161` baseline. Earlier green gates were invalidated by 1.5.4 active→待继续, 1.5.5 stale-target/focus publication, Claude hidden-Host timer delay, and finally Codex native completed-unread changes not entering Host at all。That V10/V4 increment kept the private Branch Evidence Store、Host-current same-key opening、focus-private publication and `20×20` counters；Plan pause/execute no longer depended on a dedicated Implement Plan request，while the Host still rechecked real activity/pending on confirmation。Claude Hook/App-log/membership/unread and Codex native unread/rollout decisions processed the first native callback synchronously，used 1-second Node StatWatchers for registered-target recovery，auto-rebuilt directory watchers after errors，and had no Controller phase interval。Codex persisted unread true forced a same-key latest-Turn check even after exact active；Branch Evidence and the matching public draft committed once，so one source event could not create two task revisions。Stopped tasks could archive directly after the existing two-step confirmation and Provider exact reread。The preceding broad matrix remains `20/20` files、`547/547` tests；the latest Codex increment was core `3/3` files、`221/221` tests and expanded `15/15` files、`433/433` tests。The user-required escalation passed full `83/83` files、`1328/1328` tests、typecheck、1871-module build、preload mirrors、Runtime Identity and uTools validator。Its full-gate identity was `host-252d34393f05b238e278 / renderer-ff8fbe75184168a9e150`; RAW-161 below owned the then-current reload target. See [historical Controlled requirement](260810/1155-install-runtime-diagnostics/raw-requirement.md#L1).
- RAW-161 external Codex archive recovery increment is `increment-automated-verified / rebuilt-artifact-ready / dev-plugin-reload-pending`. Real Codex Desktop evidence proved a manual native archive could move the rollout into `archived_sessions` without delivering either compatible archive broadcast to EyPc, leaving Kernel membership unchanged. The process Host now watches exact `sessions/archived_sessions` roots, immediately compares full `thread/list archived:false/true` inventories, recovers dropped directory notification through one-second StatWatchers, rebuilds failed watchers, and forces Codex tasks-only reconciliation on plugin enter/Desktop IPC reconnect/rebuild. A key confirmed only in archived inventory is removed immediately through anonymous `archivedKeys`; dirty recovery subtracts archived IDs before `thread/read`. Local EyPc archive keeps its strict double-inventory/Desktop-ACK transaction and releases its suppression when verify-1 proves the task remains unarchived. Focused recovery `5/5`（4 new + existing local transaction guard）and the affected Bridge `131/131` pass with typecheck、1871-module build、preload mirror/syntax、uTools validator and diff checks. Current identity is `host-78205ae167fc7b27c653 / renderer-9c35abd09a8a390040c5`; the prior `host-252d… / renderer-ff8…` is the RAW-160 full-suite baseline. See [RAW-161](260810/1155-install-runtime-diagnostics/raw-requirement.md#L1).
- RAW-162 Goal-aware completion stability increment is `increment-automated-verified / rebuilt-artifact-ready / dev-plugin-reload-pending`. Codex Turn completion is no longer treated as long-running Goal completion: process-private Goal evidence is cold-read and notification-fed with sequence/`updatedAt` stale-result rejection, then committed atomically with the private Branch Evidence ledger. Active Goals stay running across automatic Turns；paused/blocked/usageLimited/budgetLimited map to stopped/“待继续”；only current Goal `complete` may complete the task，while cleared/unsupported Goal RPC preserves ordinary Turn behavior and a strictly newer Turn opens a new epoch. Transient Goal uncertainty retains the last stable nonterminal in `verifying` instead of publishing a false completion. The affected 7 files pass `333/333` tests, including Bridge `138/138` and Kernel `39/39`, plus syntax、preload mirrors、typecheck、1871-module build、uTools validator and privacy assertions. Current identity is `host-c36f104c3a4cd42e77c2 / renderer-27b635545542097fd7b1`; real uTools Goal canary across two automatic Turns remains user-owned. See [RAW-162](260810/1155-install-runtime-diagnostics/raw-requirement.md#L1).
- RAW-164 Side Chat/Cloud convergence increment is `increment-automated-verified / rebuilt-artifact-ready / superseded-host-gate-by-RAW-165`. Valid same-session inventory forks are now process-private Side Chats rooted through `sessionId/forkedFromId`；nested forks resolve to the root，abnormal relations stay standalone，and public inventory contains roots only。Kernel always aggregates every bead with `running > completed-unread > completed` after existing input/approval/Plan/Goal causality；active and unread groups/counts are mutually exclusive。Goal active/verifying still suppresses intermediate Turn terminal，and Goal complete finalizes once using current unread without stale-snapshot rollback。Three semantic-only anonymous diagnostics cover topology、parent decision and Runtime Identity。Focused Bridge+Kernel+Runtime Diagnostics passed `189/189`，canonical/public mirrors、typecheck、1871-module build and runtime validator passed。Its artifact was `host-251a728efafbf4c7f7d6 / renderer-a671d108ff9d315b7ea4`；the current gate is RAW-165 below。See [RAW-164](260810/1155-install-runtime-diagnostics/raw-requirement.md#L1).
- RAW-165 realtime causality/hot-unread increment is `increment-automated-verified / rebuilt-artifact-ready / dev-plugin-reload-pending`. Inventory success no longer fabricates terminal freshness；stable branch references and per-branch Turn epochs prevent old terminal from replacing realtime running/waiting，attention outranks Side running，and Side authority stays branch-local。Push diagnostics now judge the final canonical package。Claude exact completion/focus supplies process-private hot unread over the LevelDB recovery baseline；persisted catch-up requires an opposite edge plus a post-event matching snapshot，so an equal value from the previous completion cannot release the hot hint。No new visible state、60-second delay or faster polling is added。Affected 8 files pass `364/364`，mirrors/syntax、typecheck、1871-module build and uTools validator pass。Current artifact is `host-649d5936516471adcf60 / renderer-7aa872e3d99f003ac3a0`；real Host reload remains pending。See [RAW-165](260810/1155-install-runtime-diagnostics/raw-requirement.md#L1).
- RAW-166 global error-resolution/bidirectional-causality increment is `increment-automated-verified / rebuilt-artifact-ready / documentation-synchronized / dev-plugin-reload-pending`. Kernel now rejects both stale terminal→new live and stale live→new waiting/terminal reversals，uses one branch admission owner across comparable Turn time or real event sequence，merges phase/unread/Goal as independent evidence lanes，and recomputes derived fields once。Exact Claude unread snapshots establish both known/value；log rotation/truncation cold replay cannot create hot unread。Adapter records are proposals only；Codex/Claude provider outcomes are judged after canonical commit。Error memory routes 99 structured leaves through seven unique-Primary responsibility modules，with two legacy compound records retained only as historical/migration sources；superseded records remain logical history and overdue candidates only warn。The stale RAW-163 main-first current clause was removed in favor of the user-approved RAW-164 all-bead rule，while parent-only opening remains。Affected 11 files pass `457/457`，error-memory/mirror/syntax/typecheck、1871-module build and uTools validator pass。Artifact is `host-6ac8de6597dcf0dd644c / renderer-6e677d084be49c8c7878`；real Host has not loaded RAW-166。See [RAW-166](260810/1155-install-runtime-diagnostics/raw-requirement.md#L1).
- RAW-167 快速任务查看与 Codex 键盘契约修复 is `automated-verified / host-pending`. 核验定位了七个源码级缺陷：`Ctrl+F` 被 Codex 层的 Quick Jump 恒胜抢走、跨进程展开后用单个 `nextTick` 抢 DOM 焦点导致落空、`Math.max(0, findIndex)` 把「无游标」折叠成「游标在第 0 项」、`focusedItem` 的隐式回退让高亮与游标分家、搜索框一刀切吞掉所有按键、`previewBlocked` 把任意输入焦点当阻断、以及命令派发挂在根元素冒泡上导致焦点在 `document.body` 时几乎全部快捷键静默失效。现在派发收敛为单一 `window` capture 入口（仅在事件目标不在根内时补派发，不与子层 `@keydown.stop` 冲突），`focusedKey` 成为唯一键盘游标，焦点落点使用有界渲染帧重试，会话搜索框获得 `codex-search` 输入角色与导航白名单（`codex-composer` 保持完全隔离）。新增 `eypc-companion-quick`（`mainHide`）与 `Ctrl+Alt+K` 进入筛选模式：动态列表前十条编号、`Ctrl+1…9` / `Ctrl+0` 直开并退出模式。同轮追加三项：`Alt` 成为「直接打开」族（`Alt+数字` 不依赖筛选模式、`Alt+F` 是只针对会话行且激活即打开的专项 Quick Jump），编号徽标随之常驻以免形成隐藏快捷键；配置页补上此前只注册动作却无按钮派发的「快速任务查看」「Action 执行工作台」两行，并修复 `completed-unread` 传入非 `cmds` 标签导致「去设置」点了没反应的缺陷。`Ctrl+数字` 由 `codexQuickMode` / `codexDrawerActive` 分流，与 `Alt` 族三者守卫两两互斥。冷启动由 Host 直接激活子窗口，不依赖 Renderer 挂载。配置页另补齐「归档当前任务」行，并把「悬浮球开关」行从 `codex.hotkey.configure`（实际配置的是「直接展开 Codex 卡片」）拆到自己的 `codex.float.toggle.hotkey.configure`，使每行只配置它自己那条全局功能。受影响 9 文件 `288/288` 通过，typecheck、production build、preload 镜像与 uTools validator 全绿。产物身份 `host-663509d5b3e9a508e27d / renderer-4a6e75c942a6c41b7cd8`，状态为 `artifact-ready`；真实全局快捷键、冷启动落点与 Deep Link 打开仍由用户验收。See [RAW-167](260813/1455-companion-quick-task-view/spec.md#L1).
- 2026-08-15 state-source reconciliation is `increment-automated-verified / rebuilt-artifact-ready / documentation-synchronized / rule-evolved / dev-plugin-reload-pending`. Claude `SessionEnd` 现在只有在同一 reducer 已观察到 open Turn 时才形成 stopped；冷启动/生命周期-only 尾事件只记录 lifecycle，不能压制 `completedTurns` 历史。固定语法与 D′ 版本门禁加入当前 Claude App `1.30096.5`。Codex 对 restart 可清除、但运行期长期残留的 Desktop-only Side shadow 增加严格来源法定人数：完整 App Server inventory 明确排除 child，且三次定向 latest-Turn 均为 exact empty，才清理私有 relation/shadow 并恢复 parent terminal；waiting/Plan、App Server live、新证据或不完整库存一律保留。没有引入 TTL、定时完成或正文读取。项目规则 `EYPC-COMPANION-STATE-SOURCE-001` 已锁定同一来源法定人数并把固定语法版本同步为 `1.26832.0 / 1.28929.0 / 1.30096.5`。影响选择的 7 文件 `340/340`、Preload canonical/public 语法与镜像、typecheck、1871-module production/uTools build 通过；artifact 为 `host-931a95f5973c8c7f08e2 / renderer-d238ab7d0c6a67a71a5c`。真实 uTools 仍未加载该 identity。
- 2026-08-16 Codex leftover Plan/question waiting-clear is `increment-automated-verified / documentation-synchronized / rebuild-pending / host-reload-pending`. 用户回复 Plan 或提问后，Desktop runtime 已回到无 waiting flag 的 plain-active，但 `conversationState.requests` 仍可能短暂残留同一请求；旧筛选把任意可见请求并入 waiting，卡片停在「待输入」。现在只在先前已观测到同类等待、且 runtime 从 idle/waiting 转入 plain-active 时清除这些残留；首次 `active + request` 仍待输入，idle 上的 Plan 请求仍覆盖 idle。Canonical/public Preload 已同步；Bridge `146/146`（含该合同）。未跑仓库级 typecheck/build，无新 artifact identity。真实 uTools 重载仍待用户验收。
- RAW-173 expanded-card search chrome is `increment-automated-verified / host-visual-pending`. 额度与列表之间的库存状态整行已取消：过期/失败/`!` 悬停进搜索栏左侧，`最近 N 天的 M 条` 右对齐进搜索框，占位精简为 `别名|任务|项目`。聚焦 Domain+UI `124/124`。真实窄宽度重叠与过期态仍待宿主目视。See [RAW-173](260817/0859-float-search-status-compact/spec.md#L1).
- RAW-175 confirmation speech-bubble is `increment-automated-verified / host-visual-pending`. 点击归档不再插入确认整行；「再次操作确认」立即出现在浮窗自有、带尖头的气泡中，按展开卡夹紧。聚焦 Domain+UI `128/128`。真实上/下与窄卡夹紧仍待宿主目视。See [RAW-175](260817/1618-float-action-hint-popover/spec.md#L1).

## Codex Companion

- Current cross-source runtime authority: [EyPc V7 global refactor](260824/eypc-v7-global-refactor/spec.md#L1). Companion Task Topology V6 and Codex-specific state documents remain historical foundations in [V6 spec](260823/companion-task-topology-v5/spec.md#L1) and [codex-quota-float](260718/1148-codex-quota-float/spec.md#L1).

The V4–V6 requirement、state、verification and host-gate bullets below are retained historical baselines。Where they name an older Kernel/Package/Actions identity、Renderer/provider task owner、Plan-waiting semantic or host gate，RAW-179 V7 and the user-excluded host boundary above supersede them；their causal、privacy、Goal and Provider-specific safety constraints remain foundations。
- Current requirement: RAW-179 plus retained non-conflicting RAW-116–177 foundations。V7 separates current interaction from Plan artifact，adds atomic clear/tombstones and seven evidence lanes，and makes evidence insufficiency neutral instead of guessing waiting。RAW-161 full inventory remains external-archive authority；RAW-162 Goal status remains the completion boundary when Goal evidence exists；RAW-164 all-bead aggregation、RAW-165 branch authority and RAW-166 bidirectional causal admission remain foundations。Automated verification、development-plugin acceptance and separately authorized real Execute/Claude archive are distinct gates.
- Retained causal invariant, now carried by V7: each root/member branch is reduced independently and all exact members aggregate to one root。A later transport batch cannot make older live、terminal or waiting causally newer。Activity、interaction、unread、plan-artifact and Goal merge independently and derived fields are calculated once。Public order is archived/removed → running → completed-unread → waiting-approval → waiting-input → artifact-only stopped → completed-read → unknown。A new activity epoch crosses the older waiting barrier；only a new interaction instance may wait again。Independent lane revisions and Float applied ACK invariants remain unchanged。
- Handoff/read invariant: every task-open dispatch targets only the parent task。OS/Deep Link acceptance creates a bounded opaque `dispatched` receipt with `confirmsRead=false`；it does not create a Turn read hint。Only a future matching native-visible/target-owner receipt plus exact Turn/read evidence may enter `native-confirmed/applied` or change unread。
- Shortcut cache invariant: non-kill `mainHide` preserves the ready process package、Host current targets and cursor. Hot exact-key actions resolve against the Host target immediately and ignore stale Renderer target fields；only missing Host target/private mapping/cold gap may join a Provider-scoped tasks-only resolution. Renderer focus updates private action context only，and `dispose()` releases only its lease；feature disable、Provider change、kill and process exit retain teardown authority.
- Removed paths: completion presentation delay、provider/local cross-clock ordering、fixed 75ms first-dispatch delay、periodic/manual broad task reconciliation、`taskRefreshSeconds` and historical card-color transient overrides. Targeted corroboration、structural coalescing、phase watchdog and fixed missing-key quarantine remain recovery paths.
- Historical V4 verification baseline: the 2026-08-16 source carried `task-state-v10`、V4 Kernel/package/actions、bidirectional per-branch Turn/event-sequence causality、independent phase/unread/Goal lanes、attention-first all-bead aggregation、Goal-aware completion、parent-only opening、Claude hot unread/known with rotation-safe cold replay、lifecycle-only SessionEnd abstention、Desktop-only Side source-quorum retirement、proposal/final canonical push judgment、Float applied ACK、native recovery、strict Provider mutations，and leftover Plan/question waiting-clear after Desktop runtime resumed plain-active。Its Bridge file was `146/146` with evidence in [historical verify](260810/1155-install-runtime-diagnostics/verify.md#L1)；current V6 implementation/verification and reload boundary are owned by [RAW-176](260823/companion-task-topology-v5/verify.md#L1), not this baseline。
- Gate: reload EyPc only under a separately authorized host task，first confirm the Runtime Identity handshake is `host-loaded`，then validate the first trustworthy Cloud/Claude event and existing topology/causality matrix。For Codex opening，the additional unresolved gate is a native receipt proving visible target、control-owner transition and exact read state；Deep Link dispatch or build identity is insufficient。EyPc must not approve/reject/submit requests or write Codex native state。
- Development-Renderer partial: the project `8092` Float currently renders all observed single-digit counters as circles and preserves one authorized-prefix completed row's classification across filter/tab/focus plus an independent quota refresh. No task was opened or mutated. The AX surface did not expose the newly built Preload identity and macOS locked before the final presentation restore, so Host/open/Plan/hotkey acceptance remains pending rather than inferred from this partial observation.
- Reusable failure routing: [seven validated responsibility modules](../knowledge/error-memory/README.md#L1); fixture contract: [float-bridge mock drift](../knowledge/error-memory/codex-float-bridge-mock-contract-drift.md#L1).

## Claude Code Companion

- Claude-specific authority: [spec](260807/claude-code-companion-authority-reset/spec.md#L1) / [updated plan](260807/claude-code-companion-authority-reset/plan.md#L1) / [research and strict local test route](260807/claude-code-companion-authority-reset/research.md#L1) / [verification](260807/claude-code-companion-authority-reset/verify.md#L1). Cross-source topology、Snapshot、Command and identity are now owned by [RAW-179 V7](260824/eypc-v7-global-refactor/spec.md#L1)；V6 remains a historical foundation.
- Current state: `revision-7 implementation-landed / RAW-160 hidden-host automated-verified / 2026-08-15 lifecycle reconciliation automated-verified / RAW-174 increment-automated-verified / interactive-host-partial`. Claude membership/phase/unread use independent lanes；the process Host owns immediate native Hook/App-log、registered membership and unread fingerprint reads plus 1-second StatWatcher recovery across `background-hidden`，while equal state is a zero-revision no-op and partial metadata JSON retains the verified row。App `1.30096.5` joins `1.26832.0 / 1.28929.0` as an explicitly fixed-grammar state/archive gate。SessionEnd closes only a Turn observed open by the same reducer；cold lifecycle-only SessionEnd cannot create stopped or override completed history。Hook `StopFailure` must not keep a still-working parent Turn as「待继续」；App live running outranks Hook stopped unless Hook proves a newer Prompt。Focused Claude Bridge `74/74`。Real development-plugin running→completed、StopFailure restore and lifecycle sweep behavior still require reload acceptance.
- Verified gates: prior affected Claude regressions (`16/16` files、`343/343` tests)、RAW-024 (`4/4` files、`120/120` tests) and historical RAW-150 automation remain retrieval evidence；RAW-154's exact file-transaction、open preflight、mutation-delta and Controller tests are finalized in the Codex Companion [verify](260718/1148-codex-quota-float/verify.md#L1)。Latest historical local projection has 27 Code rows (0 running / 24 completed / 1 stopped / 2 unknown), 25 from exact App logs；actual uTools LevelDB reads previously passed 30/30 with one native unread membership，and live App quota HTTP 200 yielded exactly 5h + weekly_all + Fable scoped windows with reset。The historical 74-file / 1061-test full ladder remains non-repeatable without a new RAW-018 impact trigger。更新引入（2026-08-10）：真实 D′ canary 已在用户显式授权下执行并通过 —— 生产 Bridge 对真实 App 数据完成单目标 `completed` 会话归档，`outcome=archived`、语义 diff 仅 `isArchived`、权限位与其余 21 键不变、活动库存 14→13、重复调用幂等且文件哈希不变，证据收口于 [reset verify](260807/claude-code-companion-authority-reset/verify.md#L94)。更新引入（2026-08-11）：D-1 成功与幂等提示已明确区分 EyPc 移除和 Claude 原生侧栏未确认，聚焦 `3/3` 文件、`100/100` 测试、Preload 语法/镜像、typecheck、1870-module production build、runtime validator 通过，产物身份为 `host-36616822511986c18f2c / renderer-25da7ef64b81aadc76f8`；D-2 只读核验结论为当前 `unsupported`，因为 D′ 绕过原生 session-manager/`archived` 事件且官方公开入口没有本地 Code 归档。Manual App archive delta、real old-task UI sync、controlled permission/input/response/title/restart、mixed-project filters、final rendered/high-contrast matrix 以及 D′ 在真实 uTools 宿主内经 Float 卡片点击的移除路径仍 pending。
- Decision/archive owner: `DEC-20260807-01..13`, `DEC-20260808-14` and `ARCH-20260807-01..09`/`ARCH-20260808-10` in the current spec. Five directly affected older Claude documents are `archived-linked` at their original paths with this Spec as replacement; no physical migration occurred or is authorized.
- 2026-08-25 版本门扩版：用户报 Claude 任务「已完成/未读」消退特别慢；根因为 App 于 2026-08-21 自更新至 `1.34493.1`（不在白名单）令 app-log 快清车道 fail closed，未读回落到 App 约每分钟一次的 LevelDB 刷盘栅格（诊断日志实测推送锁在每分钟 :16、间隔 59.7–60.3s）。对 `1.34493.1` 完成隐私边界内八类行式锚定核验（无漂移；唯一近似行为非 local 的 `session_*` id，按设计拒收）后，`SUPPORTED_APP_VERSIONS` 加入该版本（canonical `preload/claude/app-state.cjs` + 镜像同步）。聚焦 `3` 文件 `88/88` 测试、`validate:mirrors 73 对` 通过；真实宿主 focused 快清 ≤1s 恢复仍需插件重载验收（dev-plugin-reload-pending）。决策边界与退出条件（若官方提供直接通道则整条日志车道退役）记录于 [ARCHITECTURE](../knowledge/ARCHITECTURE.md#L203) 与 [error-memory](../knowledge/error-memory/claude-unread-decay-blocked-by-version-gate-and-minute-flush.md#L1)。

## Other Active Deliveries

- Window Jump: [spec](260724/1527-window-jump-workbench/spec.md#L1) / [verify](260724/1527-window-jump-workbench/verify.md#L1). WJ-22 keeps exact root identity as `PID+CGWindowID/HWND`, treats browser Tab/title/display/Space as mutable state, and clears a locator only after `probeInstance()` returns matching verified-gone evidence. A preload-session per-instance/per-display Space cache switches only the target display and retries one stale target once; same-app roots remain isolated, family projection cannot merge targets/slots, and slot recovery updates one slot. Native window scripts and operations now live exclusively in the platform modules; the main preload only guards, injects and mounts. Bridge `wj22-native-instance-space-cache`; full Vitest `58/58` files and `767/767` tests, typecheck, production/uTools build/package validation and post-extraction direct macOS multi-display/multi-Space exact-root smoke pass. Actual uTools reload/global-hotkey visual acceptance and Windows real-host checks remain pending.
- File Favorites: [current increment spec](260806/1120-favorite-quick-open-runners-slots/spec.md#L1) / [original workbench traceability](260711/1452-file-favorites-workbench/requirements-traceability.md#L1). 2026-08-06 状态 `automated-verified / host-pending`：按平台信任的结构化运行器、10 个 `mainHide` 文件槽、跨分组快速搜索、30 天半衰期学习、`Ctrl+1…0` 结果/抽屉分层和完整页槽位/学习管理已完成。收藏聚焦 `91/91`，新增 Runtime `4/4`，typecheck、production build、preload prepare 与 uTools validation 通过；当前并行工作树完整 Vitest 为 `1124/1127`，两项失败来自非收藏 Claude hook 监听，另一项 MQTT Runtime 超时已在干净 `HEAD 28e5db6` 复现。macOS/Windows/Linux 真实默认打开、后台/终端脚本、全局快捷键、重启与平台绑定仍为宿主门禁；固定角独立窗口是第二阶段，未实现。2026-08-07 独立复核 [review](260806/1120-favorite-quick-open-runners-slots/review.md#L1)：需求↔实现逐条取证，无 P0/P1；复跑 8 个 validator 得 `256/257`，唯一失败的 MQTT 巨型用例以 30s timeout 单跑 passed（5.43s，阈值问题）。三处口径差（同平台槽位互斥、只在当前平台配置、学习以宿主受理计数）已回填合同/PRD/用户说明；MQTT 用例阈值明确不归属本增量、暂停并交 MQTT owner。2026-08-07 后继增量 [收藏运行控制台、逐项同步与动态参数](260807/1740-favorite-runner-console-sync-and-args/spec.md#L1) 已批准但**未实现**：插件内终端 UI 已明确否决。**同日落地**：重命名不掉信任（指纹条件纳入 `name` + 旧指纹原地升级）、后台运行 run log 自动采集（fd 重定向、2MB/40 份/60 条上界、目录不可写时只丢日志不丢运行）、运行器选填声明日志路径（占位符展开 + 绝对路径校验，从不创建/执行）、退出码贯通（`已启动 ≠ 成功`，非 0 退出独立措辞与红色态，系统终端明说退出码不可得）。证据：`tests/domain|ui|integration|platform|unit` `66 files / 818 tests`、`action.test.ts` `167/167`、`pnpm run build` 全链通过（typecheck 0 error + uTools validation）、preload 镜像一致；宿主验收未跑。2026-08-08 追加落地：声明式动态参数 `{ask:名称[=默认值]}`（只允许在参数里、取值不二次展开、信任绑声明不绑取值、取消不运行、槽位触发先显示窗口），证据为 `66 files / 822 tests`、`action.test.ts` `168/168` 与 `pnpm run build` 全链通过。**仍未实现**：逐项同步（信任状态永不同步）、L3 自适应日志推测；搜索与使用学习经裁决维持现状不改，系统终端降为逃生舱使 Linux 适配器风险降为常规验收项。该增量未落地前，用户说明与架构文档一律不得按其宣称行为已交付。
- Cross-tab command panels: [spec](260713/0834-cross-tab-responsive-command-panels/spec.md#L1) / [verify](260713/0834-cross-tab-responsive-command-panels/verify.md#L1). Shared panels/tooltips and responsive browser matrix are accepted; real host file actions remain pending.
- Global operation tooltip: [spec](260730/2050-global-operation-tooltip-polish/spec.md#L1). All 271 current main-app native buttons have a static name source (including MQTT tooltip helpers); the shared layer now owns title-chord normalization, descriptions, disabled reasons, dynamic refresh and lifecycle cleanup, and suppresses the competing Codex main-page pseudo-tip. Float remains child-owned; development acceptance is user-owned.
- Quick Jump: [spec](260718/0947-quick-jump-center-overlay/spec.md#L1). Target-center overlay automated/browser acceptance is retained; real uTools visual smoke remains pending.
- MQTT: [current sync](2606231645-eypc-mqtt-websocket-tab/06-sync-doc.md#L1); multi-select merged JSON export: [spec](260730/1016-mqtt-multi-export/spec.md#L1); tooltip/shortcut identifiability polish: [spec](260730/1044-mqtt-tooltip-shortcut-polish/spec.md#L1) / [verify](260730/1044-mqtt-tooltip-shortcut-polish/verify.md#L1), now wired through product `data-operation-*` tips; development acceptance user-owned after reload hover check.
- Settings help: [raw requirement](260729/1135-settings-feature-help/raw-requirement.md#L1); `EYPC-FEATURE-HELP-001` coverage remains active.
- Cursor Agent Companion (2026-08-18): [raw](260818/1335-cursor-companion-feasibility/raw-requirement.md#L1) / [spec](260818/1335-cursor-companion-feasibility/spec.md#L1). Status `research-verified / phase-3-implemented / shortcut-bridge-automated-verified / host-test-not-requested`. 冷库存 + hook 通道已落地；**2026-08-21**：uTools Node 16 无 `node:sqlite`；库存已改为系统 `sqlite3` 白名单只读（需重载插件）。同日跳转翻为 `live-verified`：官方 `agent?id=<composerId>` deeplink 实测切到目标本地对话（Cursor 3.17.8），`preload/cursor/open.cjs` 按 Claude `dispatched` 合同落地。同日归档统一为状态门禁（进行中阻断、待继续/已完成放行、unknown 暂缓，对所有 Provider 一致）：Claude 拆除版本白名单硬闸（本机已升 1.34493.1，保留结构化重验），Cursor 落地 `preload/cursor/archive.cjs` 单行翻转 App 自己的 `isArchived` 对（桥 v5），见 [error-memory](../knowledge/error-memory/provider-version-whitelist-must-not-gate-generic-capability.md#L1)。`companion-navigation-v4` 首轮虽已补 Kernel/Controller 辅助候选，旧宿主仍因生产 preload 暴露层漏转发而只循环 Codex/Claude；D-1 已补转发、将能力纳入严格兼容门禁，并以生产 preload bridge 覆盖发布→Cursor cycle 派发。聚焦 `311/311`、typecheck、1872-module build、uTools validator 与 58 对镜像通过，产物 `host-90b803b87d0962728d5c / renderer-7fd8a53c95d084b6aa77`。用户明确规定 EyPc 真实插件/宿主测试仅在当前任务主动直接要求时执行；本轮不运行、也不保留默认宿主验收待办，见 [error-memory](../knowledge/error-memory/new-companion-source-must-register-with-navigation-authority.md#L1)。主题层新增 `--codex-quota-cursor` 归属色（紫蓝、实心药丸标记），与 Codex/Claude 三向色距 >60°。Does not change the current Codex/Claude Companion contract.
- Shortcut selector readiness (2026-08-21): [raw](260821/2045-shortcut-selector-readiness/raw-requirement.md#L1) / [spec](260821/2045-shortcut-selector-readiness/spec.md#L1). Status `automated-verified / host-pending`. 运行诊断证实 20:14–20:27 五次静默快捷入口被 Kernel 消费后没有任何导航轨迹：`complete` 但 `verifying` 的进程包把上一/下一与待输入入口推进 5 秒两端冷读，失败只剩系统通知。Kernel 选择器改按完整包直接派发；预检开始/接受/失败、空候选退出、入口 feature code 进诊断；`canonical-mismatch-repair` 的 `warn` 级别修正并加 30 秒每 provider 冷却。聚焦 `66/66`、`45/45` 与 bridge 套件通过，production build 重生成 `dist/`；真实宿主需重载插件后验收。**2026-08-22 再核验**：宿主仍运行 21:41 旧包（`host-2cb135f5…`），`dist/` 新包未装入；今日上一个派发成功但 `cycleCount=1`（仅 running Claude 会话，合同层级行为）；`task-next` 从未到达插件；6 月 Codex 任务 Desktop 提议 waiting-input 702 次被 Kernel 维持 completed，待用户确认实况，见 [raw](260821/2045-shortcut-selector-readiness/raw-requirement.md#L1)。

### Historical Claude-related delivery evidence

- Retrieval index only: [feasibility](260805/1130-claude-companion-feasibility/spec.md#L1), [provider](260805/1150-claude-companion-provider/spec.md#L1), [quota row](260805/2051-float-quota-single-line/spec.md#L1), [status visibility](260806/1046-claude-quota-status-visibility/spec.md#L1), [desktop provider](260806/1130-claude-desktop-provider/spec.md#L1), [registration](260806/2115-claude-registration-visibility/spec.md#L1), [N-window quota](260806/2210-claude-quota-all-windows/spec.md#L1), [old open](260806/2147-claude-open-in-desktop-app/verify.md#L1) and [old unread](260806/2147-claude-open-in-desktop-app/unread-authority.md#L1).
- Reusable Hook registration, presentation and N-window domain evidence may be carried forward only through the new plan. Mixed session-family, `resume/import`, byte-scan unread and latest-event conclusions are `ARCH-20260807-01..04` and are not current requirements or acceptance evidence.

## Active Process Index

Claude 的唯一当前行是下表首行；其余 Claude 行保留为历史检索入口，不是并行当前合同。

| Concern | Authority | Verification |
| --- | --- | --- |
| Codex Tab boundary optimization / global current truth | [spec](260823/codex-tab-boundary-optimization/spec.md#L1) / [global truth](PRODUCT_REQUIREMENTS.md#L1) / [source catalog](source-anchors/README.md#L1) | C-1～C-4 integrated；unique owner and deterministic freshness gate current；native Mirasim/Codex receipt not available |
| Claude Code Companion authority reset | [spec](260807/claude-code-companion-authority-reset/spec.md#L1) / [plan](260807/claude-code-companion-authority-reset/plan.md#L1) / [research](260807/claude-code-companion-authority-reset/research.md#L1) | [Implementation landed; automated + quota host verified; interactive UI matrix partial](260807/claude-code-companion-authority-reset/verify.md#L1) |
| Codex quota/task companion | [spec](260718/1148-codex-quota-float/spec.md#L1) | [verify](260718/1148-codex-quota-float/verify.md#L1) |
| Companion quick task view and Codex keyboard contract | [raw](260813/1455-companion-quick-task-view/raw-requirement.md#L1) / [spec](260813/1455-companion-quick-task-view/spec.md#L1) | Spec-owned; `automated-verified / host-pending` |
| Window jump and stable slots | [spec](260724/1527-window-jump-workbench/spec.md#L1) | [verify](260724/1527-window-jump-workbench/verify.md#L1) |
| Settings feature help | [raw](260729/1135-settings-feature-help/raw-requirement.md#L1) | [verify](260729/1135-settings-feature-help/verify.md#L1) |
| Quick Jump overlay | [spec](260718/0947-quick-jump-center-overlay/spec.md#L1) | Spec-owned |
| Cross-tab panels | [spec](260713/0834-cross-tab-responsive-command-panels/spec.md#L1) | [verify](260713/0834-cross-tab-responsive-command-panels/verify.md#L1) |
| Global operation tooltip | [spec](260730/2050-global-operation-tooltip-polish/spec.md#L1) | User-owned acceptance |
| File favorites | [traceability](260711/1452-file-favorites-workbench/requirements-traceability.md#L1) | Task-owned |
| MQTT workbench | [sync](2606231645-eypc-mqtt-websocket-tab/06-sync-doc.md#L1) / [multi-export](260730/1016-mqtt-multi-export/spec.md#L1) / [tooltip polish](260730/1044-mqtt-tooltip-shortcut-polish/spec.md#L1) | User-owned acceptance |
| Historical Claude deliveries | [retrieval index above](#historical-claude-related-delivery-evidence) | Superseded/partial evidence; not current acceptance |
| Shortcut selector readiness | [spec](260821/2045-shortcut-selector-readiness/spec.md#L1) / [raw](260821/2045-shortcut-selector-readiness/raw-requirement.md#L1) | Complete-package selector dispatch plus silent-exit diagnostics; focused suites and build pass, real-host shortcut acceptance pending after plugin reload |
| Cursor Agent Companion | [spec](260818/1335-cursor-companion-feasibility/spec.md#L1) / [raw](260818/1335-cursor-companion-feasibility/raw-requirement.md#L1) | Phase 3 jump implemented via official `agent?id` deeplink (`dispatched`, verified on 3.17.8); archive is status-gated and mirrors Cursor's own `isArchived`; navigation v4 production bridge forwarding and strict capability gate are automated-verified (`311/311`, build/mirrors pass), real-host Cursor shortcut dispatch pending after reload; dedicated Cursor ownership tone; hook opt-in; Cloud Agent excluded; not current Codex/Claude contract |

### 已关闭工作树：260829-completed-unread-pin-sequence

- 清理结果（2026-08-30 20:29:30 +08:00）：源工作树与临时集成工作树均已移除，两条本地分支保留；原任务远端分支仍为 `129b5f68bb64`。生成身份已独立备份，恢复边界与核验见 [任务收尾记录](260829/companion-pinned-collapse-plan-input/spec.md#L180)。
- 下方受管表是源工作树移除前的最后一次门禁快照，其中 `clean` 和待执行清理动作仅描述该历史时点，不表示目录仍在或需要再次清理；实际清理结果以上述记录及 Git 登记为准。

<!-- worktree-task-index:260829-completed-unread-pin-sequence:start -->
| Task ID | Repository | Child Branch | Detail | Source Head | Target Head | Worktree | Milestone | Merge | Push | Marker | State At | Next Action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `260829-completed-unread-pin-sequence` | `eypc` | `codex/260829-completed-unread-pin-sequence` | [task owner](260829/companion-pinned-collapse-plan-input/spec.md#L1) | `129b5f68bb64` | `main@b560cf52626b` | clean | complete | integrated 5/5 | observed-upstream-contained | `6585517e853c` | `2026-08-30T12:28:50Z` | execute only the separately authorized exact removal commands |
<!-- worktree-task-index:260829-completed-unread-pin-sequence:end -->
