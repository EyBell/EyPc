# EyPc Project Status Hub

Tool: codex
Date: 2026-08-17

## Purpose

This hub routes current implementation, verification gates and durable authorities. Product behavior belongs in [PRODUCT_REQUIREMENTS.md](PRODUCT_REQUIREMENTS.md#L1); architecture and implementation facts belong in [ARCHITECTURE.md](../knowledge/ARCHITECTURE.md#L1) and [technical-details.md](../knowledge/technical-details.md#L1).

## Process Governance Baseline

- Template propagation baseline: project adapters route to the CodeNote process owners and keep only EyPc-specific facts; `v3-route-accepted`.
- Hook Primary Response: `w24-primary-objective-continuity-accepted`; non-blocking evolution advisories cannot replace the task result.
- Documentation Impact: `w28-documentation-impact-accepted`; affected current authorities, verification, memory and user help are synchronized before Root acceptance.
- Standard requirement: `w30-standard-requirement-owner-accepted`; `raw-requirement.md + spec.md` uses the Spec owner, while Controlled work retains its five-file ledger.

## Current Snapshot

- Product: keyboard-first uTools plugin for local PC capability calls.
- Active lines: port management, file favorites, cross-platform window jump, MQTT workbench, Quick Jump, Codex Companion and settings feature help.
- Codex Environment Action Runner: [spec](260729/1435-codex-environment-actions/spec.md#L1) / [verify](260729/1435-codex-environment-actions/verify.md#L1). 2026-08-03 接纳复核现为 `automated-verified / host-pending`：exact structured argv、strict TOML version、Action Host revision、worktree-sensitive target identity、tasks-only cold preflight、Host-persisted Environment 槽位、Runner-first message、一次性 stale-alias retry、`onPluginOut(isKill)` 双生命周期与 Windows 绝对 `taskkill /T` 非强制树停止已闭环。聚焦 7 文件 `95/95`；最终 `pnpm run verify` 为 57 文件 `733/733`，随后 typecheck、production build、runtime prepare 与 uTools validation 全部通过。真实 child/hotkey/plugin-out/Windows/长期 Serve/真实 Action 继续由宿主验收。
- RAW-143 performance increment: ordinary non-kill hide now keeps App Server/project aliases hot; Runner/slots reuse verified inventory and rebuild only on first use or Host stale-alias. Related four files are `149/149` with typecheck and preload syntax/mirror verified; full verify/build was intentionally not repeated, and real shortcut latency remains host-pending.
- RAW-144 incremental-cache/read-recurrence increment: task inventory and Activity remain hot across Tab/Float visibility for the enabled feature; Runner catalogs invalidate per project rather than rebuilding all; accepted uTools fallback open and Turn-ID-bound acknowledgement close same-Turn unread recurrence. Related affected suite is `301/301` with type/preload and focused packaging checks; real-host latency and read recurrence remain pending.
- RAW-145 persisted-decision increment: current-machine read-only evidence exposed a Preload→Domain provenance break hidden by a duplicated loose preflight predicate. `task-state-v5` now carries only structurally verified ordinary-input/Plan decisions as `persisted-decision`; plain connector hints remain conservative. Focused `192/192`, typecheck and full build passed. The historical v4 installation gate is closed by the 2026-08-07 active-ASAR readback below; RAW-147 has repaired the [real-preflight script](../../scripts/codex-real-preflight.mjs#L1) so its production-Domain gate is reusable again.
- RAW-146 consumer-consistency closeout is historical for the two dedicated attention actions: it originally unified pinned-first “打开第一条” behavior and passed focused `90/90`, full Vitest `752/752`, typecheck and runtime/static gates. RAW-149 now supersedes only those waiting-input/completed-unread action semantics with global newest-first persistent progress; the ordinary display comparator and generic task cycle retain the RAW-146 foundation.
- RAW-147 active→waiting optimization: active-ASAR/readback and current Codex package inspection proved `following=true` is a peer follower-state announcement, not a request. The old branch now represented by the [no-reply boundary](../../preload/index.js#L3519) echoed it, allowing two EyPc followers to create 32,329 messages in 250ms and about 368,000 in 2.5s. The handler now replies only to explicit following-status requests; the new RED/GREEN contract and Bridge `81/81`, exact-change typecheck/production build, current three-way Preload/runtime gates and repaired real preflight pass. A packaged-source probe sends a bounded 24 initial follows and emits no recursive writes after 48 old-client announcements. Error memory is routed as one stream-follow primary plus one pending-request dependency introduction, preventing a second protocol-loop owner. A later concurrent Claude fixture change blocked the whole-tree typecheck/build rerun at that time; RAW-148 current-tree typecheck now passes. Running uTools still uses the pre-RAW-147 ASAR, so reload, owner snapshot and real active→waiting acceptance remain pending.
- RAW-148 recognition/dedup closeout: [codexEnvironmentPresentation.ts](../../src/domain/codexEnvironmentPresentation.ts#L1) now owns the Runtime banner, grid, compatibility-pending predicate and launch help; the Page only renders it. Task/project ownership is resolved once per Float row from the Domain markers, and launch-path mutations publish the Host-returned snapshot without a second inspection. RAW-022 wins over stale Codex-only wording: data/state/quota/empty-state compatibility remains, while every task/project keeps one visible and accessible ownership marker. Focused `188/188` and current-tree typecheck pass; no Bridge/Preload/build/runtime surface was touched.
- RAW-149 permission-attention/latest-first increment is `automated-verified / host-pending`: the Desktop follower now treats command execution, file change, permissions and MCP elicitation approvals as input attention alongside questions and Plan decisions. `task-state-v6` carries only anonymous state-entry time; input/approval share one count, approval is excluded from ongoing, and input/completed-unread are globally latest-first across providers. Their dedicated actions persist a bounded anonymous unseen-instance progress and advance only after confirmed Host open, including manual row opening. Generic previous/next, projects/pins and local-pin fallback remain unchanged. Affected `8/8` files and `292/292` tests, typecheck, production build, Preload sync/syntax, runtime validation and documentation audit pass. Real Provider→production Domain/Presentation v6 preflight is connected/verified and asserts input/active disjointness; running uTools ASARs remain v5, and the first UI reconnect observation stopped at the locked-Mac gate, so real non-Full-Access approval acceptance remains pending.
- RAW-150 waiting-to-continue/archive increment is `automated-verified / host-pending`: Domain keeps `stopped`, while all card/segment copy shows “待继续” without a fifth top Tab, counter or shortcut. Explicit stopped is task-archiveable；Codex rereads exact thread/latest Turn and returns `state-changed` after resume。更新引入：RAW-154 已以 Claude D′ 单目标静默 `isArchived` 事务取代本条历史 Deep Link+AX 路线；App 日志降为增强证据，元数据+私有活动库存双确认即可成功，非成功仍保留卡片。Project batch remains Codex completed-only.
- RAW-151 bidirectional waiting-edge increment is historical `host-exposed-defect / superseded-by-RAW-153` for active-vs-active arbitration. Its reducer、bounded resubscribe and 100-cycle latency intent remain regression foundations；its Renderer phase-only recovery owner is superseded by RAW-160 process-Host native+StatWatcher recovery。The true v7 host proved that current-owner old waiting could survive a newer active/Turn-started and later reappear after shadow replay.
- RAW-152 process-navigation increment is a historical `automated-verified / superseded-by-RAW-155` baseline: it introduced the process-lifetime cursor, one Codex/Claude queue, Preload consumption and concurrency 1. Its fixed 75ms trailing delay is no longer current；RAW-155 keeps the ownership/lifecycle foundation but replaces dispatch timing with leading-immediate plus in-flight final-trailing.
- RAW-153 waiting-clear causal barrier increment is `automated-verified / host-rework`: `task-state-v8` gives private main/Side Chat request instances and runtime waiting flags monotonic observation sequences. Request removal, matching `serverRequest/resolved`, newer active/Turn-started, matching output, user continuation and new `task_started` establish a clear barrier; older snapshot/read-state/refollow/sticky/rollout evidence cannot revive the cleared instance, while a genuinely newer correlation re-enters immediately. Unmatched resolved only bounds a target-task resubscribe and preserves concurrent approvals. Bridge `94/94`, the affected `9/9` files `379/379`, typecheck, production/uTools build, canonical/public syntax+mirror and synchronous-IPC static gates pass；当前源码的真实 Provider→生产 Domain/Presentation 只读预检也返回 v8、connected、completeness verified，匿名瞬时投影为 `waiting=1 / active=2`。运行中 ASAR 仍是 v7，并曾复现 `waiting=1 / active=4` 而同期旧 Provider→Domain 为 `0 / 5`；正常 v8 reconnect 和 1.25 秒/30 秒 no-rebound 门禁仍未完成，所以不得形成完成提交。
- RAW-154 unified companion task kernel is a historical `automated-verified` V9/Actions-v1 baseline. Its Provider-neutral intent、Claude D′ target-only transaction、indexed membership delta and five-second archive confirmation remain foundations；its broad exact-interrupted edge and Actions-v1 identity are superseded by RAW-160 V10/V4/Actions-v2。Historical evidence remains `20/20` files、`550/550` tests plus the separately authorized 2026-08-10 D′ canary；real current-host acceptance follows RAW-160, not the old v9 gate。
- RAW-154 rework is the historical V1 Kernel/package and runtime-identity baseline. Its single intent path、cold fail-closed preflight、silent `onPluginEnter` ownership、Main/Float identity handshake and D′ archive boundary remain current foundations；its shared generation and V1 package identity are superseded by RAW-155 V2 lanes. Historical evidence remains `12/12` files、`485/485` tests plus the older D′/v9 `550/550` baseline and does not replace RAW-155 verification.
- RAW-159 Codex unified-state/archive/diagnostics increment is historical `automated-verified / installed-host-reproduced-failure / superseded-by-RAW-160-v4-rework`. Its `83/83` files、`1272/1272` tests、pagination、minimal membership、Codex archive postconditions、diagnostics and Runtime Identity remain valid regression foundations；the installed-host gate exposed broad interrupted→stopped、unstable Plan candidates and missing consumer applied-state semantics, so V3 is not current acceptance authority.
- RAW-160 Plan lifecycle/change-only-publication increment is `rework-implemented / full-automated-verified / superseded-current-artifact-by-RAW-161`. Earlier green gates were invalidated by 1.5.4 active→待继续, 1.5.5 stale-target/focus publication, Claude hidden-Host timer delay, and finally Codex native completed-unread changes not entering Host at all。Current V10/V4 authority keeps the private Branch Evidence Store、Host-current same-key opening、focus-private publication and `20×20` counters；Plan pause/execute no longer depends on a dedicated Implement Plan request，while the Host still rechecks real activity/pending on confirmation。Claude Hook/App-log/membership/unread and Codex native unread/rollout decisions now process the first native callback synchronously，use 1-second Node StatWatchers for registered-target recovery，auto-rebuild directory watchers after errors，and have no Controller phase interval。Codex persisted unread true forces a same-key latest-Turn check even after exact active；Branch Evidence and the matching public draft commit once，so one source event cannot create two task revisions。Stopped tasks can archive directly after the existing two-step confirmation and Provider exact reread。The preceding broad matrix remains `20/20` files、`547/547` tests；the latest Codex increment is core `3/3` files、`221/221` tests and expanded `15/15` files、`433/433` tests。The user-required escalation passes full `83/83` files、`1328/1328` tests、typecheck、1871-module build、preload mirrors、Runtime Identity and uTools validator。Its full-gate identity was `host-252d34393f05b238e278 / renderer-ff8fbe75184168a9e150`; RAW-161 below owns the current reload target. See [current Controlled requirement](260810/1155-install-runtime-diagnostics/raw-requirement.md#L1).
- RAW-161 external Codex archive recovery increment is `increment-automated-verified / rebuilt-artifact-ready / dev-plugin-reload-pending`. Real Codex Desktop evidence proved a manual native archive could move the rollout into `archived_sessions` without delivering either compatible archive broadcast to EyPc, leaving Kernel membership unchanged. The process Host now watches exact `sessions/archived_sessions` roots, immediately compares full `thread/list archived:false/true` inventories, recovers dropped directory notification through one-second StatWatchers, rebuilds failed watchers, and forces Codex tasks-only reconciliation on plugin enter/Desktop IPC reconnect/rebuild. A key confirmed only in archived inventory is removed immediately through anonymous `archivedKeys`; dirty recovery subtracts archived IDs before `thread/read`. Local EyPc archive keeps its strict double-inventory/Desktop-ACK transaction and releases its suppression when verify-1 proves the task remains unarchived. Focused recovery `5/5`（4 new + existing local transaction guard）and the affected Bridge `131/131` pass with typecheck、1871-module build、preload mirror/syntax、uTools validator and diff checks. Current identity is `host-78205ae167fc7b27c653 / renderer-9c35abd09a8a390040c5`; the prior `host-252d… / renderer-ff8…` is the RAW-160 full-suite baseline. See [RAW-161](260810/1155-install-runtime-diagnostics/raw-requirement.md#L1).
- RAW-162 Goal-aware completion stability increment is `increment-automated-verified / rebuilt-artifact-ready / dev-plugin-reload-pending`. Codex Turn completion is no longer treated as long-running Goal completion: process-private Goal evidence is cold-read and notification-fed with sequence/`updatedAt` stale-result rejection, then committed atomically with the private Branch Evidence ledger. Active Goals stay running across automatic Turns；paused/blocked/usageLimited/budgetLimited map to stopped/“待继续”；only current Goal `complete` may complete the task，while cleared/unsupported Goal RPC preserves ordinary Turn behavior and a strictly newer Turn opens a new epoch. Transient Goal uncertainty retains the last stable nonterminal in `verifying` instead of publishing a false completion. The affected 7 files pass `333/333` tests, including Bridge `138/138` and Kernel `39/39`, plus syntax、preload mirrors、typecheck、1871-module build、uTools validator and privacy assertions. Current identity is `host-c36f104c3a4cd42e77c2 / renderer-27b635545542097fd7b1`; real uTools Goal canary across two automatic Turns remains user-owned. See [RAW-162](260810/1155-install-runtime-diagnostics/raw-requirement.md#L1).
- RAW-164 Side Chat/Cloud convergence increment is `increment-automated-verified / rebuilt-artifact-ready / superseded-host-gate-by-RAW-165`. Valid same-session inventory forks are now process-private Side Chats rooted through `sessionId/forkedFromId`；nested forks resolve to the root，abnormal relations stay standalone，and public inventory contains roots only。Kernel always aggregates every bead with `running > completed-unread > completed` after existing input/approval/Plan/Goal causality；active and unread groups/counts are mutually exclusive。Goal active/verifying still suppresses intermediate Turn terminal，and Goal complete finalizes once using current unread without stale-snapshot rollback。Three semantic-only anonymous diagnostics cover topology、parent decision and Runtime Identity。Focused Bridge+Kernel+Runtime Diagnostics passed `189/189`，canonical/public mirrors、typecheck、1871-module build and runtime validator passed。Its artifact was `host-251a728efafbf4c7f7d6 / renderer-a671d108ff9d315b7ea4`；the current gate is RAW-165 below。See [RAW-164](260810/1155-install-runtime-diagnostics/raw-requirement.md#L1).
- RAW-165 realtime causality/hot-unread increment is `increment-automated-verified / rebuilt-artifact-ready / dev-plugin-reload-pending`. Inventory success no longer fabricates terminal freshness；stable branch references and per-branch Turn epochs prevent old terminal from replacing realtime running/waiting，attention outranks Side running，and Side authority stays branch-local。Push diagnostics now judge the final canonical package。Claude exact completion/focus supplies process-private hot unread over the LevelDB recovery baseline；persisted catch-up requires an opposite edge plus a post-event matching snapshot，so an equal value from the previous completion cannot release the hot hint。No new visible state、60-second delay or faster polling is added。Affected 8 files pass `364/364`，mirrors/syntax、typecheck、1871-module build and uTools validator pass。Current artifact is `host-649d5936516471adcf60 / renderer-7aa872e3d99f003ac3a0`；real Host reload remains pending。See [RAW-165](260810/1155-install-runtime-diagnostics/raw-requirement.md#L1).
- RAW-166 global error-resolution/bidirectional-causality increment is `increment-automated-verified / rebuilt-artifact-ready / documentation-synchronized / dev-plugin-reload-pending`. Kernel now rejects both stale terminal→new live and stale live→new waiting/terminal reversals，uses one branch admission owner across comparable Turn time or real event sequence，merges phase/unread/Goal as independent evidence lanes，and recomputes derived fields once。Exact Claude unread snapshots establish both known/value；log rotation/truncation cold replay cannot create hot unread。Adapter records are proposals only；Codex/Claude provider outcomes are judged after canonical commit。Error memory routes 99 structured leaves through seven unique-Primary responsibility modules，with two legacy compound records retained only as historical/migration sources；superseded records remain logical history and overdue candidates only warn。The stale RAW-163 main-first current clause was removed in favor of the user-approved RAW-164 all-bead rule，while parent-only opening remains。Affected 11 files pass `457/457`，error-memory/mirror/syntax/typecheck、1871-module build and uTools validator pass。Artifact is `host-6ac8de6597dcf0dd644c / renderer-6e677d084be49c8c7878`；real Host has not loaded RAW-166。See [RAW-166](260810/1155-install-runtime-diagnostics/raw-requirement.md#L1).
- RAW-167 快速任务查看与 Codex 键盘契约修复 is `automated-verified / host-pending`. 核验定位了七个源码级缺陷：`Ctrl+F` 被 Codex 层的 Quick Jump 恒胜抢走、跨进程展开后用单个 `nextTick` 抢 DOM 焦点导致落空、`Math.max(0, findIndex)` 把「无游标」折叠成「游标在第 0 项」、`focusedItem` 的隐式回退让高亮与游标分家、搜索框一刀切吞掉所有按键、`previewBlocked` 把任意输入焦点当阻断、以及命令派发挂在根元素冒泡上导致焦点在 `document.body` 时几乎全部快捷键静默失效。现在派发收敛为单一 `window` capture 入口（仅在事件目标不在根内时补派发，不与子层 `@keydown.stop` 冲突），`focusedKey` 成为唯一键盘游标，焦点落点使用有界渲染帧重试，会话搜索框获得 `codex-search` 输入角色与导航白名单（`codex-composer` 保持完全隔离）。新增 `eypc-companion-quick`（`mainHide`）与 `Ctrl+Alt+K` 进入筛选模式：动态列表前十条编号、`Ctrl+1…9` / `Ctrl+0` 直开并退出模式。同轮追加三项：`Alt` 成为「直接打开」族（`Alt+数字` 不依赖筛选模式、`Alt+F` 是只针对会话行且激活即打开的专项 Quick Jump），编号徽标随之常驻以免形成隐藏快捷键；配置页补上此前只注册动作却无按钮派发的「快速任务查看」「Action 执行工作台」两行，并修复 `completed-unread` 传入非 `cmds` 标签导致「去设置」点了没反应的缺陷。`Ctrl+数字` 由 `codexQuickMode` / `codexDrawerActive` 分流，与 `Alt` 族三者守卫两两互斥。冷启动由 Host 直接激活子窗口，不依赖 Renderer 挂载。配置页另补齐「归档当前任务」行，并把「悬浮球开关」行从 `codex.hotkey.configure`（实际配置的是「直接展开 Codex 卡片」）拆到自己的 `codex.float.toggle.hotkey.configure`，使每行只配置它自己那条全局功能。受影响 9 文件 `288/288` 通过，typecheck、production build、preload 镜像与 uTools validator 全绿。产物身份 `host-663509d5b3e9a508e27d / renderer-4a6e75c942a6c41b7cd8`，状态为 `artifact-ready`；真实全局快捷键、冷启动落点与 Deep Link 打开仍由用户验收。See [RAW-167](260813/1455-companion-quick-task-view/spec.md#L1).
- 2026-08-15 state-source reconciliation is `increment-automated-verified / rebuilt-artifact-ready / documentation-synchronized / rule-evolved / dev-plugin-reload-pending`. Claude `SessionEnd` 现在只有在同一 reducer 已观察到 open Turn 时才形成 stopped；冷启动/生命周期-only 尾事件只记录 lifecycle，不能压制 `completedTurns` 历史。固定语法与 D′ 版本门禁加入当前 Claude App `1.30096.5`。Codex 对 restart 可清除、但运行期长期残留的 Desktop-only Side shadow 增加严格来源法定人数：完整 App Server inventory 明确排除 child，且三次定向 latest-Turn 均为 exact empty，才清理私有 relation/shadow 并恢复 parent terminal；waiting/Plan、App Server live、新证据或不完整库存一律保留。没有引入 TTL、定时完成或正文读取。项目规则 `EYPC-COMPANION-STATE-SOURCE-001` 已锁定同一来源法定人数并把固定语法版本同步为 `1.26832.0 / 1.28929.0 / 1.30096.5`。影响选择的 7 文件 `340/340`、Preload canonical/public 语法与镜像、typecheck、1871-module production/uTools build 通过；artifact 为 `host-931a95f5973c8c7f08e2 / renderer-d238ab7d0c6a67a71a5c`。真实 uTools 仍未加载该 identity。
- RAW-173 expanded-card search chrome is `increment-automated-verified / host-visual-pending`. 额度与列表之间的库存状态整行已取消：过期/失败/`!` 悬停进搜索栏左侧，`最近 N 天的 M 条` 右对齐进搜索框，占位精简为 `别名|任务|项目`。聚焦 Domain+UI `124/124`。真实窄宽度重叠与过期态仍待宿主目视。See [RAW-173](260817/0859-float-search-status-compact/spec.md#L1).
- RAW-175 confirmation speech-bubble is `increment-automated-verified / host-visual-pending`. 点击归档不再插入确认整行；「再次操作确认」立即出现在浮窗自有、带尖头的气泡中，按展开卡夹紧。聚焦 Domain+UI `128/128`。真实上/下与窄卡夹紧仍待宿主目视。See [RAW-175](260817/1618-float-action-hint-popover/spec.md#L1).

## Codex Companion

- Authority: [current spec](260718/1148-codex-quota-float/spec.md#L1), [verification](260718/1148-codex-quota-float/verify.md#L1), [handoff](260718/1148-codex-quota-float/handoff.md#L1).
- Current requirement: RAW-116–166 plus RAW-173 search-chrome compaction, RAW-175 confirmation speech-bubble, and the 2026-08-15 state-source reconciliation. RAW-160 supersedes arbitrary Plan clearing、broad interrupted stop、V3 ownership、Kernel-only no-op、hard Renderer-target equality、focus-as-package-semantics、`26px` single-digit geometry and hidden-Renderer timer ownership。RAW-161 makes full unarchived/archived inventory the external-archive recovery authority。RAW-162 makes Goal status the completion boundary when Goal evidence exists。RAW-163 retains parent-only opening；RAW-164 adds inventory-rooted all-bead aggregation；RAW-165 separates inventory observation from terminal causality，makes attention outrank ordinary running，keeps authority branch-local and adds Claude completion/focus hot unread；RAW-166 makes phase admission bidirectional、separates phase/unread/Goal lanes、unifies proposal/final diagnostics and gives every error record one validated responsibility route。The 2026-08-15 refinement permits only complete-inventory + bounded exact-empty targeted reads to retire a stale Desktop-only Side shadow；it adds no elapsed-time terminal path。The line preserves RAW-159 pagination/archive/diagnostics foundations and RAW-154's version-gated Claude D′ boundary。Automated verification、development-plugin acceptance and separately authorized real Execute/Claude archive are distinct gates.
- State invariant: V4 reduces each root/Side Chat branch independently and always aggregates all confirmed beads。Per-branch phase admission compares Provider Turn epoch/event sequence in both directions；a later transport batch cannot make older live or terminal causally newer。Phase、unread and Goal merge independently and derived fields are recalculated after the merge。After retained approval → input/Plan → Goal causality，running outranks completed-unread and completed；while any bead is active，latent unread stays private and the public unread count remains zero。When private Goal evidence exists，active Goal suppresses intermediate Turn completion；paused/blocked/usageLimited/budgetLimited is stopped；only current Goal complete is terminal，with causal sequence/`updatedAt` guards and a strictly newer Turn opening a new epoch。Ordinary interrupted and Plan stop retain their existing branch-idle/targeted proof across the aggregated scope。Plan readiness、independent lane generations and Float applied ACK invariants remain unchanged。
- Read acknowledgement invariant: every task-open dispatch targets only the parent task。A confirmed parent open is held in bounded preload-process memory for a concrete Turn and its known Side Chats；transport teardown/rebuild、stale native true、same-Turn `completedAt` enrichment and older full snapshots cannot revoke it，while exact active/Turn-started、a different Turn ID/startedAt、explicit removal or process end can。
- Shortcut cache invariant: non-kill `mainHide` preserves the ready process package、Host current targets and cursor. Hot exact-key actions resolve against the Host target immediately and ignore stale Renderer target fields；only missing Host target/private mapping/cold gap may join a Provider-scoped tasks-only resolution. Renderer focus updates private action context only，and `dispose()` releases only its lease；feature disable、Provider change、kill and process exit retain teardown authority.
- Removed paths: completion presentation delay、provider/local cross-clock ordering、fixed 75ms first-dispatch delay、periodic/manual broad task reconciliation、`taskRefreshSeconds` and historical card-color transient overrides. Targeted corroboration、structural coalescing、phase watchdog and fixed missing-key quarantine remain recovery paths.
- Verification: current source carries `task-state-v10`、V4 Kernel/package/actions、bidirectional per-branch Turn/event-sequence causality、independent phase/unread/Goal lanes、attention-first all-bead aggregation、Goal-aware completion、parent-only opening、Claude hot unread/known with rotation-safe cold replay、lifecycle-only SessionEnd abstention、Desktop-only Side source-quorum retirement、proposal/final canonical push judgment、Float applied ACK、native recovery and strict Provider mutations。RAW-160–166 foundations remain recorded；the 2026-08-15 affected 7-file `340/340`、error-memory/requirements validators、canonical/public syntax+mirror、typecheck、1871-module build and uTools validator evidence is in [current verify](260810/1155-install-runtime-diagnostics/verify.md#L1)。State remains `dev-plugin-reload-pending` until uTools development mode reports `host-931a95f5973c8c7f08e2 / renderer-d238ab7d0c6a67a71a5c` as `host-loaded` and passes the realtime Cloud/Claude→Float applied matrix。
- Gate: reload EyPc，first confirm the Runtime Identity handshake is `host-loaded`，then validate the first trustworthy Cloud/Claude event is published immediately：main completed read/unread + Side running → running；no active bead + any unread → completed-unread；all read → completed；waiting/approval cannot be cleared by older live；terminal cannot be reopened by older live；Cloud active Goal across at least two Turns has no intermediate terminal；no child top-level row or rebound；every card/counter/shortcut still opens only the parent。A later anonymous sample confirms no rollback but is not a 20/60-second display hold。Then retain the existing Claude、mainHide、Float stall and sanitized diagnostics gates。EyPc must not approve/reject/submit requests or write Codex native state。
- Development-Renderer partial: the project `8092` Float currently renders all observed single-digit counters as circles and preserves one authorized-prefix completed row's classification across filter/tab/focus plus an independent quota refresh. No task was opened or mutated. The AX surface did not expose the newly built Preload identity and macOS locked before the final presentation restore, so Host/open/Plan/hotkey acceptance remains pending rather than inferred from this partial observation.
- Reusable failure routing: [seven validated responsibility modules](../knowledge/error-memory/README.md#L1); fixture contract: [float-bridge mock drift](../knowledge/error-memory/codex-float-bridge-mock-contract-drift.md#L1).

## Claude Code Companion

- Current authority: [spec](260807/claude-code-companion-authority-reset/spec.md#L1) / [updated plan](260807/claude-code-companion-authority-reset/plan.md#L1) / [research and strict local test route](260807/claude-code-companion-authority-reset/research.md#L1) / [verification](260807/claude-code-companion-authority-reset/verify.md#L1).
- Current state: `revision-7 implementation-landed / RAW-160 hidden-host automated-verified / 2026-08-15 lifecycle reconciliation automated-verified / interactive-host-partial`. Claude membership/phase/unread use independent lanes；the process Host owns immediate native Hook/App-log、registered membership and unread fingerprint reads plus 1-second StatWatcher recovery across `background-hidden`，while equal state is a zero-revision no-op and partial metadata JSON retains the verified row。App `1.30096.5` joins `1.26832.0 / 1.28929.0` as an explicitly fixed-grammar state/archive gate。SessionEnd closes only a Turn observed open by the same reducer；cold lifecycle-only SessionEnd cannot create stopped or override completed history。Real development-plugin running→completed and lifecycle sweep behavior still require reload acceptance.
- Verified gates: prior affected Claude regressions (`16/16` files、`343/343` tests)、RAW-024 (`4/4` files、`120/120` tests) and historical RAW-150 automation remain retrieval evidence；RAW-154's exact file-transaction、open preflight、mutation-delta and Controller tests are finalized in the Codex Companion [verify](260718/1148-codex-quota-float/verify.md#L1)。Latest historical local projection has 27 Code rows (0 running / 24 completed / 1 stopped / 2 unknown), 25 from exact App logs；actual uTools LevelDB reads previously passed 30/30 with one native unread membership，and live App quota HTTP 200 yielded exactly 5h + weekly_all + Fable scoped windows with reset。The historical 74-file / 1061-test full ladder remains non-repeatable without a new RAW-018 impact trigger。更新引入（2026-08-10）：真实 D′ canary 已在用户显式授权下执行并通过 —— 生产 Bridge 对真实 App 数据完成单目标 `completed` 会话归档，`outcome=archived`、语义 diff 仅 `isArchived`、权限位与其余 21 键不变、活动库存 14→13、重复调用幂等且文件哈希不变，证据收口于 [reset verify](260807/claude-code-companion-authority-reset/verify.md#L94)。更新引入（2026-08-11）：D-1 成功与幂等提示已明确区分 EyPc 移除和 Claude 原生侧栏未确认，聚焦 `3/3` 文件、`100/100` 测试、Preload 语法/镜像、typecheck、1870-module production build、runtime validator 通过，产物身份为 `host-36616822511986c18f2c / renderer-25da7ef64b81aadc76f8`；D-2 只读核验结论为当前 `unsupported`，因为 D′ 绕过原生 session-manager/`archived` 事件且官方公开入口没有本地 Code 归档。Manual App archive delta、real old-task UI sync、controlled permission/input/response/title/restart、mixed-project filters、final rendered/high-contrast matrix 以及 D′ 在真实 uTools 宿主内经 Float 卡片点击的移除路径仍 pending。
- Decision/archive owner: `DEC-20260807-01..13`, `DEC-20260808-14` and `ARCH-20260807-01..09`/`ARCH-20260808-10` in the current spec. Five directly affected older Claude documents are `archived-linked` at their original paths with this Spec as replacement; no physical migration occurred or is authorized.

## Other Active Deliveries

- Window Jump: [spec](260724/1527-window-jump-workbench/spec.md#L1) / [verify](260724/1527-window-jump-workbench/verify.md#L1). WJ-22 keeps exact root identity as `PID+CGWindowID/HWND`, treats browser Tab/title/display/Space as mutable state, and clears a locator only after `probeInstance()` returns matching verified-gone evidence. A preload-session per-instance/per-display Space cache switches only the target display and retries one stale target once; same-app roots remain isolated, family projection cannot merge targets/slots, and slot recovery updates one slot. Native window scripts and operations now live exclusively in the platform modules; the main preload only guards, injects and mounts. Bridge `wj22-native-instance-space-cache`; full Vitest `58/58` files and `767/767` tests, typecheck, production/uTools build/package validation and post-extraction direct macOS multi-display/multi-Space exact-root smoke pass. Actual uTools reload/global-hotkey visual acceptance and Windows real-host checks remain pending.
- File Favorites: [current increment spec](260806/1120-favorite-quick-open-runners-slots/spec.md#L1) / [original workbench traceability](260711/1452-file-favorites-workbench/requirements-traceability.md#L1). 2026-08-06 状态 `automated-verified / host-pending`：按平台信任的结构化运行器、10 个 `mainHide` 文件槽、跨分组快速搜索、30 天半衰期学习、`Ctrl+1…0` 结果/抽屉分层和完整页槽位/学习管理已完成。收藏聚焦 `91/91`，新增 Runtime `4/4`，typecheck、production build、preload prepare 与 uTools validation 通过；当前并行工作树完整 Vitest 为 `1124/1127`，两项失败来自非收藏 Claude hook 监听，另一项 MQTT Runtime 超时已在干净 `HEAD 28e5db6` 复现。macOS/Windows/Linux 真实默认打开、后台/终端脚本、全局快捷键、重启与平台绑定仍为宿主门禁；固定角独立窗口是第二阶段，未实现。2026-08-07 独立复核 [review](260806/1120-favorite-quick-open-runners-slots/review.md#L1)：需求↔实现逐条取证，无 P0/P1；复跑 8 个 validator 得 `256/257`，唯一失败的 MQTT 巨型用例以 30s timeout 单跑 passed（5.43s，阈值问题）。三处口径差（同平台槽位互斥、只在当前平台配置、学习以宿主受理计数）已回填合同/PRD/用户说明；MQTT 用例阈值明确不归属本增量、暂停并交 MQTT owner。2026-08-07 后继增量 [收藏运行控制台、逐项同步与动态参数](260807/1740-favorite-runner-console-sync-and-args/spec.md#L1) 已批准但**未实现**：插件内终端 UI 已明确否决。**同日落地**：重命名不掉信任（指纹条件纳入 `name` + 旧指纹原地升级）、后台运行 run log 自动采集（fd 重定向、2MB/40 份/60 条上界、目录不可写时只丢日志不丢运行）、运行器选填声明日志路径（占位符展开 + 绝对路径校验，从不创建/执行）、退出码贯通（`已启动 ≠ 成功`，非 0 退出独立措辞与红色态，系统终端明说退出码不可得）。证据：`tests/domain|ui|integration|platform|unit` `66 files / 818 tests`、`action.test.ts` `167/167`、`pnpm run build` 全链通过（typecheck 0 error + uTools validation）、preload 镜像一致；宿主验收未跑。2026-08-08 追加落地：声明式动态参数 `{ask:名称[=默认值]}`（只允许在参数里、取值不二次展开、信任绑声明不绑取值、取消不运行、槽位触发先显示窗口），证据为 `66 files / 822 tests`、`action.test.ts` `168/168` 与 `pnpm run build` 全链通过。**仍未实现**：逐项同步（信任状态永不同步）、L3 自适应日志推测；搜索与使用学习经裁决维持现状不改，系统终端降为逃生舱使 Linux 适配器风险降为常规验收项。该增量未落地前，用户说明与架构文档一律不得按其宣称行为已交付。
- Cross-tab command panels: [spec](260713/0834-cross-tab-responsive-command-panels/spec.md#L1) / [verify](260713/0834-cross-tab-responsive-command-panels/verify.md#L1). Shared panels/tooltips and responsive browser matrix are accepted; real host file actions remain pending.
- Global operation tooltip: [spec](260730/2050-global-operation-tooltip-polish/spec.md#L1). All 271 current main-app native buttons have a static name source (including MQTT tooltip helpers); the shared layer now owns title-chord normalization, descriptions, disabled reasons, dynamic refresh and lifecycle cleanup, and suppresses the competing Codex main-page pseudo-tip. Float remains child-owned; development acceptance is user-owned.
- Quick Jump: [spec](260718/0947-quick-jump-center-overlay/spec.md#L1). Target-center overlay automated/browser acceptance is retained; real uTools visual smoke remains pending.
- MQTT: [current sync](2606231645-eypc-mqtt-websocket-tab/06-sync-doc.md#L1); multi-select merged JSON export: [spec](260730/1016-mqtt-multi-export/spec.md#L1); tooltip/shortcut identifiability polish: [spec](260730/1044-mqtt-tooltip-shortcut-polish/spec.md#L1) / [verify](260730/1044-mqtt-tooltip-shortcut-polish/verify.md#L1), now wired through product `data-operation-*` tips; development acceptance user-owned after reload hover check.
- Settings help: [raw requirement](260729/1135-settings-feature-help/raw-requirement.md#L1); `EYPC-FEATURE-HELP-001` coverage remains active.

### Historical Claude-related delivery evidence

- Retrieval index only: [feasibility](260805/1130-claude-companion-feasibility/spec.md#L1), [provider](260805/1150-claude-companion-provider/spec.md#L1), [quota row](260805/2051-float-quota-single-line/spec.md#L1), [status visibility](260806/1046-claude-quota-status-visibility/spec.md#L1), [desktop provider](260806/1130-claude-desktop-provider/spec.md#L1), [registration](260806/2115-claude-registration-visibility/spec.md#L1), [N-window quota](260806/2210-claude-quota-all-windows/spec.md#L1), [old open](260806/2147-claude-open-in-desktop-app/verify.md#L1) and [old unread](260806/2147-claude-open-in-desktop-app/unread-authority.md#L1).
- Reusable Hook registration, presentation and N-window domain evidence may be carried forward only through the new plan. Mixed session-family, `resume/import`, byte-scan unread and latest-event conclusions are `ARCH-20260807-01..04` and are not current requirements or acceptance evidence.

## Active Process Index

Claude 的唯一当前行是下表首行；其余 Claude 行保留为历史检索入口，不是并行当前合同。

| Concern | Authority | Verification |
| --- | --- | --- |
| Claude Code Companion authority reset | [spec](260807/claude-code-companion-authority-reset/spec.md#L1) / [plan](260807/claude-code-companion-authority-reset/plan.md#L1) / [research](260807/claude-code-companion-authority-reset/research.md#L1) | [Implementation landed; automated + quota host verified; interactive UI matrix partial](260807/claude-code-companion-authority-reset/verify.md#L1) |
| Codex quota/task companion | [spec](260718/1148-codex-quota-float/spec.md#L1) | [verify](260718/1148-codex-quota-float/verify.md#L1) |
| Companion quick task view and Codex keyboard contract | [raw](260813/1455-companion-quick-task-view/raw-requirement.md#L1) / [spec](260813/1455-companion-quick-task-view/spec.md#L1) | Spec-owned; `automated-verified / host-pending` |
| Window jump and stable slots | [spec](260724/1527-window-jump-workbench/spec.md#L1) | [verify](260724/1527-window-jump-workbench/verify.md#L1) |
| Settings feature help | [raw](260729/1135-settings-feature-help/raw-requirement.md#L1) | [verify](260729/1135-settings-feature-help/verify.md#L1) |
| Quick Jump overlay | [spec](260718/0947-quick-jump-center-overlay/spec.md#L1) | Spec-owned |
| Cross-tab panels | [spec](260713/0834-cross-tab-responsive-command-panels/spec.md#L1) | [verify](260713/0834-cross-tab-responsive-command-panels/verify.md#L1) |
| Global operation tooltip | [spec](260730/2050-global-operation-tooltip-polish/spec.md#L1) | User-owned acceptance |
| File favorites | [traceability](260711/1452-file-favorites-workbench/requirements-traceability.md#L1) | Task-owned |
| MQTT workbench | [sync](2606231645-eypc-mqtt-websocket-tab/06-sync-doc.md#L1) / [multi-export](260730/1016-mqtt-multi-export/spec.md#L1) / [tooltip polish](260730/1044-mqtt-tooltip-shortcut-polish/spec.md#L1) | User-owned acceptance |
| Historical Claude deliveries | [retrieval index above](#historical-claude-related-delivery-evidence) | Superseded/partial evidence; not current acceptance |
